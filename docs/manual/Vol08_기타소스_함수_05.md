# Vol.08 — 기타 함수 (5)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

com_tml 등.

---

### Check_Cell_Offset_Config

- **종류**: 함수
- **시그니처**: `INT08U Check_Cell_Offset_Config() { if (checkFusingString((INT08U*)m_ExtSEnv0.InitCellOffsetCode, (INT08U*)CELL_OFFSET_INIT_CODE, 4))`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1460: INT08U Check_Cell_Offset_Config()`
  - `Core/Src/com_tml.c:4290: if (Check_Cell_Offset_Config())`
  - `Core/Src/com_tml.c:4631: if (Check_Cell_Offset_Config())`
  - `Core/Inc/SRM_Parameter.h:57: extern INT08U Check_Cell_Offset_Config();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Clear_CellOffset_All

- **종류**: 함수
- **시그니처**: `void Clear_CellOffset_All()`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1477: void Clear_CellOffset_All()`
  - `Core/Src/SRM_Parameter.c:1587: Clear_CellOffset_All();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Write_CellOffset_InitCode

- **종류**: 함수
- **시그니처**: `void Write_CellOffset_InitCode()`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1534: Write_CellOffset_InitCode();`
  - `Core/Src/SRM_Parameter.c:1542: void Write_CellOffset_InitCode()`
  - `Core/Src/com_tml.c:4659: Write_CellOffset_InitCode();`
  - `Core/Inc/SRM_Parameter.h:56: extern void Write_CellOffset_InitCode();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Clear_Cell_Offset_UseFlag

- **종류**: 함수
- **시그니처**: `void Clear_Cell_Offset_UseFlag(INT16U nStart)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1553: void Clear_Cell_Offset_UseFlag(INT16U nStart)`
  - `Core/Inc/SRM_Parameter.h:58: extern void Clear_Cell_Offset_UseFlag(INT16U nStart);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Clear_Cell_Offset_UseFlagAll

- **종류**: 함수
- **시그니처**: `void Clear_Cell_Offset_UseFlagAll()`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1576: void Clear_Cell_Offset_UseFlagAll()`
  - `Core/Src/SRM_Parameter.c:1589: Clear_Cell_Offset_UseFlagAll();`
  - `Core/Inc/SRM_Parameter.h:59: extern void Clear_Cell_Offset_UseFlagAll();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Clear_CellOffset_Config

- **종류**: 함수
- **시그니처**: `void Clear_CellOffset_Config()`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1585: void Clear_CellOffset_Config()`
  - `Core/Src/SRM_Parameter.c:1608: Clear_CellOffset_Config();`
  - `Core/Src/com_tml.c:4288: Clear_CellOffset_Config();`
  - `Core/Src/com_tml.c:4292: Clear_CellOffset_Config();`
  - `Core/Src/com_tml.c:4635: Clear_CellOffset_Config();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### CellOffset_Config_Init

- **종류**: 함수
- **시그니처**: `void CellOffset_Config_Init()`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1597: void CellOffset_Config_Init()`
  - `Core/Src/main.c:370: CellOffset_Config_Init();`
  - `Core/Inc/SRM_Parameter.h:61: extern void CellOffset_Config_Init();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Read_CellOffset_Bay_Lv

- **종류**: 함수
- **시그니처**: `INT08U Read_CellOffset_Bay_Lv(INT08U nBay, INT08U nLv, CellOffsetSTR* pReadBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1644: INT08U Read_CellOffset_Bay_Lv(INT08U nBay, INT08U nLv, CellOffsetSTR* pReadBuf)`
  - `Core/Src/dev_SRM.c:15139: Read_CellOffset_Bay_Lv(m_St.PosFork1.Bay_No, m_St.PosFork1.Level_No, &nReadOffset);`
  - `Core/Src/dev_SRM.c:16270: Read_CellOffset_Bay_Lv(m_St.PosFork1.Bay_No, m_St.PosFork1.Level_No, &nReadOffset);`
  - `Core/Src/dev_SRM.c:22497: Read_CellOffset_Bay_Lv(m_St.PosFork1.Bay_No, m_St.PosFork1.Level_No, &nReadOffset);`
  - `Core/Src/dev_SRM.c:23017: Read_CellOffset_Bay_Lv(nTo_Bay_No, m_St.PosFork1.Level_No, &nReadOffset);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Read_CellOffset_Addr

- **종류**: 함수
- **시그니처**: `void Read_CellOffset_Addr(INT16U nIndex, CellOffsetSTR* pReadBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1692: void Read_CellOffset_Addr(INT16U nIndex, CellOffsetSTR* pReadBuf)`
  - `Core/Src/com_tml.c:4425: Read_CellOffset_Addr(s_Process_Idx, &nReadCellOffset);`
  - `Core/Inc/SRM_Parameter.h:66: extern void Read_CellOffset_Addr(INT16U nIndex, CellOffsetSTR* pReadBuf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Write_CellOffset

- **종류**: 함수
- **시그니처**: `INT08U Write_CellOffset(CellOffsetSTR* pBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1723: INT08U Write_CellOffset(CellOffsetSTR* pBuf)`
  - `Core/Inc/SRM_Parameter.h:68: extern INT08U Write_CellOffset(CellOffsetSTR* pBuf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Read_CellOffset_Block

- **종류**: 함수
- **시그니처**: `INT08U Read_CellOffset_Block(INT16U nBlockNo, INT08U* pBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1808: INT08U Read_CellOffset_Block(INT16U nBlockNo, INT08U* pBuf)`
  - `Core/Src/com_tml.c:4566: Read_CellOffset_Block(nCurBlockNo, (INT08U*)nWriteBuf);`
  - `Core/Src/com_tml.c:4591: Read_CellOffset_Block(nCurBlockNo, (INT08U*)nWriteBuf);`
  - `Core/Inc/SRM_Parameter.h:71: extern INT08U Read_CellOffset_Block(INT16U nBlockNo, INT08U* pBuf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Write_CellOffset_Block

- **종류**: 함수
- **시그니처**: `INT08U Write_CellOffset_Block(INT16U nBlockNo, INT08U* pBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1836: INT08U Write_CellOffset_Block(INT16U nBlockNo, INT08U* pBuf)`
  - `Core/Src/com_tml.c:4585: Write_CellOffset_Block(nPreBlockNo, (INT08U*)nWriteBuf);`
  - `Core/Src/com_tml.c:4613: Write_CellOffset_Block(nPreBlockNo, (INT08U*)nWriteBuf);`
  - `Core/Inc/SRM_Parameter.h:72: extern INT08U Write_CellOffset_Block(INT16U nBlockNo, INT08U* pBuf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Check_CellOffset_BlockNo

- **종류**: 함수
- **시그니처**: `INT08U Check_CellOffset_BlockNo(CellOffsetSTR* pBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1877: INT08U Check_CellOffset_BlockNo(CellOffsetSTR* pBuf)`
  - `Core/Src/com_tml.c:4563: nCurBlockNo = Check_CellOffset_BlockNo(&nRecvCellOffset[0]);`
  - `Core/Src/com_tml.c:4580: nCurBlockNo = Check_CellOffset_BlockNo(&nRecvCellOffset[nCheckCount]);`
  - `Core/Inc/SRM_Parameter.h:74: extern INT08U Check_CellOffset_BlockNo(CellOffsetSTR* pBuf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Check_CellOffset_UseFlag

- **종류**: 함수
- **시그니처**: `void Check_CellOffset_UseFlag(CellOffsetSTR* pBuf, INT08U* pUseFlag)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **인근 주석**: INT32U nBayoffset = 1024;
- **용도(읽는 법)**: 헤더 주석: INT32U nBayoffset = 1024;
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1897: void Check_CellOffset_UseFlag(CellOffsetSTR* pBuf, INT08U* pUseFlag)`
  - `Core/Src/com_tml.c:4605: Check_CellOffset_UseFlag(&nWriteBuf[nCell_Idx], s_TmpUseFlag);`
  - `Core/Inc/SRM_Parameter.h:75: extern void Check_CellOffset_UseFlag(CellOffsetSTR* pBuf, INT08U* pUseFlag);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Scan_CellOffset_UseCount

- **종류**: 함수
- **시그니처**: `INT16U Scan_CellOffset_UseCount()`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1632: m_pgmEnv.CellOffsetUseCount = Scan_CellOffset_UseCount();`
  - `Core/Src/SRM_Parameter.c:1929: INT16U Scan_CellOffset_UseCount()`
  - `Core/Src/com_tml.c:4664: m_pgmEnv.CellOffsetUseCount = Scan_CellOffset_UseCount();`
  - `Core/Inc/SRM_Parameter.h:76: extern INT16U Scan_CellOffset_UseCount();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Read_CellOffset_Data

- **종류**: 함수
- **시그니처**: `void Read_CellOffset_Data(INT16U nPage)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1971: void Read_CellOffset_Data(INT16U nPage)`
  - `Core/Inc/SRM_Parameter.h:77: extern void Read_CellOffset_Data(INT16U nPage);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Read_CellOffset_Data_1

- **종류**: 함수
- **시그니처**: `void Read_CellOffset_Data_1(INT16U nReadPage)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2006: void Read_CellOffset_Data_1(INT16U nReadPage)`
  - `Core/Inc/SRM_Parameter.h:78: extern void Read_CellOffset_Data_1(INT16U nReadPage);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_SafetyPlug_Type

- **종류**: 함수
- **시그니처**: `INT08U Set_SafetyPlug_Type(ParamControlSTR* pSetting) { if (pSetting->SafetyPlug_Type < 2)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **인근 주석**: 0 : Start Off 1 : Emergency Stop
- **용도(읽는 법)**: 헤더 주석: 0 : Start Off 1 : Emergency Stop
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2048: INT08U Set_SafetyPlug_Type(ParamControlSTR* pSetting)`
  - `Core/Src/com_tml.c:4751: if (pCom->Data[1] & cbits[0])	is_save = Set_SafetyPlug_Type(pSetParam);//안전플러그 타입`
  - `Core/Inc/SRM_Parameter.h:80: extern INT08U Set_SafetyPlug_Type(ParamControlSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_DoorSensor_UseFlag

- **종류**: 함수
- **시그니처**: `INT08U Set_DoorSensor_UseFlag(ParamControlSTR* pSetting) { if (pSetting->DoorSensor_UseFlag < 2)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **인근 주석**: 0 : not use, 1 : use
- **용도(읽는 법)**: 헤더 주석: 0 : not use, 1 : use
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2068: INT08U Set_DoorSensor_UseFlag(ParamControlSTR* pSetting)`
  - `Core/Src/com_tml.c:4752: if (pCom->Data[1] & cbits[1])	is_save = Set_DoorSensor_UseFlag(pSetParam);//도어센서 사용여부`
  - `Core/Inc/SRM_Parameter.h:81: extern INT08U Set_DoorSensor_UseFlag(ParamControlSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_DoorSensor_AlarmFlag

- **종류**: 함수
- **시그니처**: `INT08U Set_DoorSensor_AlarmFlag(ParamControlSTR* pSetting) { if (pSetting->DoorSensor_AlarmFlag < 2)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **인근 주석**: 0 : not use, 1 : use
- **용도(읽는 법)**: 헤더 주석: 0 : not use, 1 : use
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2086: INT08U Set_DoorSensor_AlarmFlag(ParamControlSTR* pSetting)`
  - `Core/Src/com_tml.c:4753: if (pCom->Data[1] & cbits[2])	is_save = Set_DoorSensor_AlarmFlag(pSetParam);//도어알람 사용여부`
  - `Core/Inc/SRM_Parameter.h:82: extern INT08U Set_DoorSensor_AlarmFlag(ParamControlSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_LaserModem_AlarmFlag

- **종류**: 함수
- **시그니처**: `INT08U Set_LaserModem_AlarmFlag(ParamControlSTR* pSetting) { if (pSetting->LaserModem_AlarmFlag < 2)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **인근 주석**: 0 : not use, 1 : use
- **용도(읽는 법)**: 헤더 주석: 0 : not use, 1 : use
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2105: INT08U Set_LaserModem_AlarmFlag(ParamControlSTR* pSetting)`
  - `Core/Src/com_tml.c:4754: if (pCom->Data[1] & cbits[3])	is_save = Set_LaserModem_AlarmFlag(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:83: extern INT08U Set_LaserModem_AlarmFlag(ParamControlSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_AutoChange_Mode

- **종류**: 함수
- **시그니처**: `INT08U Set_AutoChange_Mode(ParamControlSTR* pSetting) { if (pSetting->AutoChange_Mode < 2)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **인근 주석**: 0 : not use, 1 : use
- **용도(읽는 법)**: 헤더 주석: 0 : not use, 1 : use
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2123: INT08U Set_AutoChange_Mode(ParamControlSTR* pSetting)`
  - `Core/Src/com_tml.c:4755: if (pCom->Data[1] & cbits[4])	is_save = Set_AutoChange_Mode(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:84: extern INT08U Set_AutoChange_Mode(ParamControlSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_TravLift_OperTime_SyncFlag

- **종류**: 함수
- **시그니처**: `INT08U Set_TravLift_OperTime_SyncFlag(ParamControlSTR* pSetting) { if (pSetting->TravLift_OperTime_SyncFlag < 3)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **인근 주석**: 0 : 주행, 승강 동시 출발 1 : 승강 하강 시 동시 출발, 상승 시 동시 도착 2 : 주행, 승강 동시 출발, 동시 도착
- **용도(읽는 법)**: 헤더 주석: 0 : 주행, 승강 동시 출발 1 : 승강 하강 시 동시 출발, 상승 시 동시 도착 2 : 주행, 승강 동시 출발, 동시 도착
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2141: INT08U Set_TravLift_OperTime_SyncFlag(ParamControlSTR* pSetting)`
  - `Core/Src/com_tml.c:4756: if (pCom->Data[1] & cbits[5])	is_save = Set_TravLift_OperTime_SyncFlag(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:85: extern INT08U Set_TravLift_OperTime_SyncFlag(ParamControlSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Cargo_Adnormal_HomeReturn_UseFlag

- **종류**: 함수
- **시그니처**: `INT08U Set_Cargo_Adnormal_HomeReturn_UseFlag(ParamControlSTR* pSetting) { if (pSetting->HomeReturn_Fault_UseFlag < 2)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **인근 주석**: 0 : not use, 1 : use
- **용도(읽는 법)**: HOME/RETURN: 원점복귀 vs 에러복귀 RunMode 혼재. grep으로 확인.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2161: INT08U Set_Cargo_Adnormal_HomeReturn_UseFlag(ParamControlSTR* pSetting)`
  - `Core/Src/com_tml.c:4757: if (pCom->Data[1] & cbits[6])	is_save = Set_Cargo_Adnormal_HomeReturn_UseFlag(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:86: extern INT08U Set_Cargo_Adnormal_HomeReturn_UseFlag(ParamControlSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Forking_Sensor_AlarmFlag

- **종류**: 함수
- **시그니처**: `INT08U Set_Forking_Sensor_AlarmFlag(ParamControlSTR* pSetting) { if (pSetting->ForkOut_cargo_misalignment_alarm < 2)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **인근 주석**: 0 : not use, 1 : use
- **용도(읽는 법)**: 헤더 주석: 0 : not use, 1 : use
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2179: INT08U Set_Forking_Sensor_AlarmFlag(ParamControlSTR* pSetting)`
  - `Core/Src/com_tml.c:4758: if (pCom->Data[1] & cbits[7])	is_save = Set_Forking_Sensor_AlarmFlag(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:87: extern INT08U Set_Forking_Sensor_AlarmFlag(ParamControlSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

