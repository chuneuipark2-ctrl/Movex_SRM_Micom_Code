# Vol.08 — 기타 함수 (22)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

com_tml 등.

---

### EC_Mbx_Empty

- **종류**: 함수
- **시그니처**: `int EC_Mbx_Empty(uint16 slave, int timeout)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:1391: int EC_Mbx_Empty(uint16 slave, int timeout)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ec_mbxsend

- **종류**: 함수
- **시그니처**: `int ec_mbxsend(uint16 slave, EC_Mbx_buffer_t* mbx, int timeout)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:1398: int ec_mbxsend(uint16 slave, EC_Mbx_buffer_t* mbx, int timeout)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ec_mbxreceive

- **종류**: 함수
- **시그니처**: `int ec_mbxreceive(uint16 slave, EC_Mbx_buffer_t* mbx, int timeout)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:1405: int ec_mbxreceive(uint16 slave, EC_Mbx_buffer_t* mbx, int timeout)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ec_esidump

- **종류**: 함수
- **시그니처**: `void ec_esidump(uint16 slave, uint8* esibuf)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:1412: void ec_esidump(uint16 slave, uint8* esibuf)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ec_eeprom2master

- **종류**: 함수
- **시그니처**: `int ec_eeprom2master(uint16 slave)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:1426: int ec_eeprom2master(uint16 slave)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ec_eeprom2pdi

- **종류**: 함수
- **시그니처**: `int ec_eeprom2pdi(uint16 slave)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:1432: int ec_eeprom2pdi(uint16 slave)`
  - `Core/Src/ecat_main.c:1883: if (eectl) ec_eeprom2pdi(slave);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ec_readeeprom1

- **종류**: 함수
- **시그니처**: `void ec_readeeprom1(uint16 slave, uint16 eeproma)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:1452: void ec_readeeprom1(uint16 slave, uint16 eeproma)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_Send_ProcessData

- **종류**: 함수
- **시그니처**: `int EC_Send_ProcessData(uint8 group)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:1466: int EC_Send_ProcessData(uint8 group)`
  - `Core/Src/ecat_main.c:2821: EC_Send_ProcessData(0);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ec_send_processdata_group_1

- **종류**: 함수
- **시그니처**: `int ec_send_processdata_group_1(uint8 group)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:1482: int ec_send_processdata_group_1(uint8 group)`
  - `Core/Src/ecat_main.c:5134: ec_send_processdata_group_1(0);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### si_PDOassign

- **종류**: 함수
- **시그니처**: `int si_PDOassign(uint16 slave, uint16 PDOassign, int mapoffset, int bitoffset)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:1670: int si_PDOassign(uint16 slave, uint16 PDOassign, int mapoffset, int bitoffset)`
  - `Core/Src/ecat_main.c:1768: Tsize = si_PDOassign(slave, ECT_SDO_PDO_ASSIGN + iSM, (int)(m_EC_Slave[slave].outputs - (uint8*)&m_I`
  - `Core/Src/ecat_main.c:1774: Tsize = si_PDOassign(slave, ECT_SDO_PDO_ASSIGN + iSM, (int)(m_EC_Slave[slave].inputs - (uint8*)&m_IO`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### si_map_sdo

- **종류**: 함수
- **시그니처**: `int si_map_sdo(int slave)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:1732: int si_map_sdo(int slave)`
  - `Core/Src/ecat_main.c:2015: si_map_sdo(cnt);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### si_siiPDO

- **종류**: 함수
- **시그니처**: `int si_siiPDO(uint16 slave, uint8 t, int mapoffset, int bitoffset)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:1787: int si_siiPDO(uint16 slave, uint8 t, int mapoffset, int bitoffset)`
  - `Core/Src/ecat_main.c:1898: Tsize = si_siiPDO(slave, 1, (int)(m_EC_Slave[slave].outputs - (uint8*)&m_IOmap), outputs_bo);`
  - `Core/Src/ecat_main.c:1900: Tsize = si_siiPDO(slave, 0, (int)(m_EC_Slave[slave].inputs - (uint8*)&m_IOmap), inputs_bo);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### si_map_sii

- **종류**: 함수
- **시그니처**: `int si_map_sii(int slave)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:1889: int si_map_sii(int slave)`
  - `Core/Src/ecat_main.c:2017: si_map_sii(cnt);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### si_sdo

- **종류**: 함수
- **시그니처**: `void si_sdo(int cnt)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:1909: void si_sdo(int cnt)`
  - `Core/Src/ecat_main.c:2010: si_sdo(cnt);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### slaveinfo

- **종류**: 함수
- **시그니처**: `void slaveinfo()`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:1947: void slaveinfo()`
  - `Core/Src/ecat_main.c:4068: slaveinfo();`
  - `Core/Src/ecat_main.c:4864: slaveinfo();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Check_SlaveState

- **종류**: 함수
- **시그니처**: `INT08U Check_SlaveState(uint16 reqstate)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:2147: INT08U Check_SlaveState(uint16 reqstate)`
  - `Core/Src/ecat_main.c:4073: nCheckFlag = Check_SlaveState(EC_STATE_PRE_OP);`
  - `Core/Src/ecat_main.c:4664: nCheckFlag = Check_SlaveState(EC_STATE_PRE_OP);`
  - `Core/Src/ecat_main.c:4867: nCheckFlag = Check_SlaveState(EC_STATE_PRE_OP);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_FPWR_SM_Set

- **종류**: 함수
- **시그니처**: `void EC_FPWR_SM_Set(INT08U nSM_No)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:2196: void EC_FPWR_SM_Set(INT08U nSM_No)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ABB_ACS380_setup

- **종류**: 함수
- **시그니처**: `int ABB_ACS380_setup(uint16 slave)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **인근 주석**: RxPDO Map Index,	Name,						Type 6040		Controlword					U16 6060		Modes of operation			I8 6042		vl target velocity			I16
- **용도(읽는 법)**: 헤더 주석: RxPDO Map Index,	Name,						Type 6040		Controlword					U16 6060		Modes of operation			I8 6042		vl target velocity			I16
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:2213: int ABB_ACS380_setup(uint16 slave)`
  - `Core/Src/ecat_main.c:2761: m_EC_Slave[i].PO2SOconfig = &ABB_ACS380_setup;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### GX_EC0211_setup

- **종류**: 함수
- **시그니처**: `int GX_EC0211_setup(uint16 slave)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:2340: int GX_EC0211_setup(uint16 slave)`
  - `Core/Src/ecat_main.c:2768: m_EC_Slave[i].PO2SOconfig = &GX_EC0211_setup;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### MX_DIO_10x_setup

- **종류**: 함수
- **시그니처**: `int MX_DIO_10x_setup(uint16 slave)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:2415: int MX_DIO_10x_setup(uint16 slave)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### MX_DIO_20x_setup

- **종류**: 함수
- **시그니처**: `int MX_DIO_20x_setup(uint16 slave)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:2459: int MX_DIO_20x_setup(uint16 slave)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### MX_EXT_setup

- **종류**: 함수
- **시그니처**: `int MX_EXT_setup(uint16 slave)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:2503: int MX_EXT_setup(uint16 slave)`
  - `Core/Src/ecat_main.c:2775: m_EC_Slave[i].PO2SOconfig = &MX_EXT_setup;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### MX_EXT_setup_1

- **종류**: 함수
- **시그니처**: `int MX_EXT_setup_1(uint16 slave)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **인근 주석**: RxPDO Map Index,		Name,						Type 6200.02		Output 1st byte				U8 3020.00		Encoder Instruction Bits	U8 3011.00		Encoder Preset Valu			U32
- **용도(읽는 법)**: 헤더 주석: RxPDO Map Index,		Name,						Type 6200.02		Output 1st byte				U8 3020.00		Encoder Instruction Bits	U8 3011.00		Encoder Preset Valu			U32
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:2547: int MX_EXT_setup_1(uint16 slave)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### MX_Slave_setup

- **종류**: 함수
- **시그니처**: `int MX_Slave_setup(uint16 slave)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:2688: int MX_Slave_setup(uint16 slave)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### GX_EX_GetErrorCode

- **종류**: 함수
- **시그니처**: `int GX_EX_GetErrorCode(uint16 slave)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:2718: int GX_EX_GetErrorCode(uint16 slave)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

