# Vol.08 — 기타 함수 (20)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

com_tml 등.

---

### ecx_FPWRw

- **종류**: 함수
- **시그니처**: `int ecx_FPWRw(uint16 ADP, uint16 ADO, uint16 data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:927: int ecx_FPWRw(uint16 ADP, uint16 ADO, uint16 data)`
  - `Core/Src/ecat_config.c:698: ecx_FPWRw(nConfigadr, ECT_REG_ALCTL, htoes(EC_STATE_PRE_OP));`
  - `Core/Src/ecat_config.c:906: ecx_FPWRw(configadr, ECT_REG_WD_DIVIDER, val16);`
  - `Core/Src/ecat_config.c:909: ecx_FPWRw(configadr, ECT_REG_WD_PDI, val16);`
  - `Core/Src/ecat_config.c:912: ecx_FPWRw(configadr, ECT_REG_WD_PD, val16);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ecx_FPWR

- **종류**: 함수
- **시그니처**: `int ecx_FPWR(uint16 ADP, uint16 ADO, uint16 length, void *data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:929: return ecx_FPWR(ADP, ADO, sizeof(data), &data);`
  - `Core/Src/ecat_combase.c:943: int ecx_FPWR(uint16 ADP, uint16 ADO, uint16 length, void *data)`
  - `Core/Src/ecat_config.c:266: ecx_FPWR(configadr, ECT_REG_FMMU0 + (sizeof(ec_fmmu_t) * FMMUc),`
  - `Core/Src/ecat_config.c:314: ecx_FPWR(configadr, ECT_REG_SM0,`
  - `Core/Src/ecat_config.c:326: ecx_FPWR(configadr, ECT_REG_SM1,`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ecx_LRW

- **종류**: 함수
- **시그니처**: `int ecx_LRW(uint32 LogAdr, uint16 length, void *data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:975: int ecx_LRW(uint32 LogAdr, uint16 length, void *data)`
  - `Core/Src/ecat_main.c:1469: ecx_LRW(ec_group[group].logstartaddr, ec_group[group].IOsegment[0], IOmap);`
  - `Core/Src/ecat_main.c:1484: ecx_LRW(m_EC_Group[group].logstartaddr, m_EC_Group[group].IOsegment[0], m_IOmap);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ecx_LRW_OverLoad

- **종류**: 함수
- **시그니처**: `int ecx_LRW_OverLoad(uint32 LogAdr, uint16 length, void* DataOut, void* DataIn)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:1019: int ecx_LRW_OverLoad(uint32 LogAdr, uint16 length, void* DataOut, void* DataIn)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ecx_LRD

- **종류**: 함수
- **시그니처**: `int ecx_LRD(uint32 LogAdr, uint16 length, void *data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:1051: int ecx_LRD(uint32 LogAdr, uint16 length, void *data)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ecx_LWR

- **종류**: 함수
- **시그니처**: `int ecx_LWR(uint32 LogAdr, uint16 length, void *data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:1083: int ecx_LWR(uint32 LogAdr, uint16 length, void *data)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ecx_LRWDC

- **종류**: 함수
- **시그니처**: `int ecx_LRWDC(uint32 LogAdr, uint16 length, void *data, uint16 DCrs, int64 *DCtime)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:1129: int ecx_LRWDC(uint32 LogAdr, uint16 length, void *data, uint16 DCrs, int64 *DCtime)`
  - `Core/Src/ecat_main.c:1475: ecx_LRWDC(m_EC_Group[group].logstartaddr, m_EC_Group[group].IOsegment[0], m_IOmap, \`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_Map_COE_SOE

- **종류**: 함수
- **시그니처**: `static int EC_Map_COE_SOE(EC_Main_t* EC_Main, uint16 slave, int thread_n)`
- **정의 위치**: `Core/Src/ecat_config.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_config.c:16: static int EC_Map_COE_SOE(EC_Main_t* EC_Main, uint16 slave, int thread_n)`
  - `Core/Src/ecat_config.c:441: EC_Map_COE_SOE(nEC_Main, slave, 0);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_lookup_mapping

- **종류**: 함수
- **시그니처**: `static int EC_lookup_mapping(EC_Main_t* pEC_Main, uint16 slave, int* Osize, int* Isize)`
- **정의 위치**: `Core/Src/ecat_config.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_config.c:67: static int EC_lookup_mapping(EC_Main_t* pEC_Main, uint16 slave, int* Osize, int* Isize)`
  - `Core/Src/ecat_config.c:386: (void)EC_lookup_mapping(pEC_Main, slave, &Osize, &Isize);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_lookup_prev_sii

- **종류**: 함수
- **시그니처**: `static int EC_lookup_prev_sii(EC_Main_t* EC_Main, uint16 slave)`
- **정의 위치**: `Core/Src/ecat_config.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_config.c:102: static int EC_lookup_prev_sii(EC_Main_t* EC_Main, uint16 slave)`
  - `Core/Src/ecat_config.c:1052: if (!cindex && !EC_lookup_prev_sii(pEC_Main, slave))`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_Config_Create_Input_Mappings

- **종류**: 함수
- **시그니처**: `static void EC_Config_Create_Input_Mappings(EC_Main_t* pEC_Main, void* pIOmap, uint8 group, int16 slave, uint32* LogAddr, uint8* BitPos)`
- **정의 위치**: `Core/Src/ecat_config.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_config.c:152: static void EC_Config_Create_Input_Mappings(EC_Main_t* pEC_Main, void* pIOmap, uint8 group, int16 sl`
  - `Core/Src/ecat_config.c:677: EC_Config_Create_Input_Mappings(pEC_Main, pIOmap, group, nSlave, &nLogAddr, &nBitPos);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_Map_SM

- **종류**: 함수
- **시그니처**: `static int EC_Map_SM(EC_Main_t* pEC_Main, uint16 slave)`
- **정의 위치**: `Core/Src/ecat_config.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_config.c:301: static int EC_Map_SM(EC_Main_t* pEC_Main, uint16 slave)`
  - `Core/Src/ecat_config.c:450: EC_Map_SM(nEC_Main, slave);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_Map_Sii

- **종류**: 함수
- **시그니처**: `static int EC_Map_Sii(EC_Main_t* pEC_Main, uint16 slave)`
- **정의 위치**: `Core/Src/ecat_config.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_config.c:375: static int EC_Map_Sii(EC_Main_t* pEC_Main, uint16 slave)`
  - `Core/Src/ecat_config.c:449: EC_Map_Sii(nEC_Main, slave);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_Config_Find_Map

- **종류**: 함수
- **시그니처**: `static void EC_Config_Find_Map(EC_Main_t* nEC_Main, uint8 group)`
- **정의 위치**: `Core/Src/ecat_config.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_config.c:433: static void EC_Config_Find_Map(EC_Main_t* nEC_Main, uint8 group)`
  - `Core/Src/ecat_config.c:610: EC_Config_Find_Map(pEC_Main, group);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_Config_Create_Output_Mappings

- **종류**: 함수
- **시그니처**: `static void EC_Config_Create_Output_Mappings(EC_Main_t* pEC_Main, void* pIOmap, uint8 group, int16 slave, uint32* LogAddr, uint8* BitPos)`
- **정의 위치**: `Core/Src/ecat_config.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_config.c:455: static void EC_Config_Create_Output_Mappings(EC_Main_t* pEC_Main, void* pIOmap, uint8 group, int16 s`
  - `Core/Src/ecat_config.c:620: EC_Config_Create_Output_Mappings(pEC_Main, pIOmap, group, nSlave, &nLogAddr, &nBitPos);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_Config_Map_Group

- **종류**: 함수
- **시그니처**: `int EC_Config_Map_Group(EC_Main_t* pEC_Main, void* pIOmap, uint8 group)`
- **정의 위치**: `Core/Src/ecat_config.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_config.c:588: int EC_Config_Map_Group(EC_Main_t* pEC_Main, void* pIOmap, uint8 group)`
  - `Core/Src/ecat_config.c:1164: return EC_Config_Map_Group(&m_EC_Main, pIOmap, 0);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_init_pEC_Main

- **종류**: 함수
- **시그니처**: `void EC_init_pEC_Main(EC_Main_t* pEC_Main)`
- **정의 위치**: `Core/Src/ecat_config.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_config.c:750: void EC_init_pEC_Main(EC_Main_t* pEC_Main)`
  - `Core/Src/ecat_config.c:872: EC_init_pEC_Main(pEC_Main);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_Detect_Slaves

- **종류**: 함수
- **시그니처**: `int EC_Detect_Slaves(EC_Main_t* pEC_Main)`
- **정의 위치**: `Core/Src/ecat_config.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_config.c:771: int EC_Detect_Slaves(EC_Main_t* pEC_Main)`
  - `Core/Src/ecat_config.c:873: wkc = EC_Detect_Slaves(pEC_Main);`
  - `Core/Src/ecat_main.c:3990: expectedWKC = EC_Detect_Slaves(&m_EC_Main);`
  - `Core/Src/ecat_main.c:4017: expectedWKC = EC_Detect_Slaves(&m_EC_Main);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_set_slaves_to_default

- **종류**: 함수
- **시그니처**: `static void EC_set_slaves_to_default(EC_Main_t* pEC_Main)`
- **정의 위치**: `Core/Src/ecat_config.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_config.c:798: static void EC_set_slaves_to_default(EC_Main_t* pEC_Main)`
  - `Core/Src/ecat_config.c:876: EC_set_slaves_to_default(pEC_Main);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_Config_Init

- **종류**: 함수
- **시그니처**: `int EC_Config_Init(EC_Main_t* pEC_Main)`
- **정의 위치**: `Core/Src/ecat_config.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_config.c:861: int EC_Config_Init(EC_Main_t* pEC_Main)`
  - `Core/Src/ecat_config.c:1172: wkc = EC_Config_Init(&m_EC_Main);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ECAT_Config_Map

- **종류**: 함수
- **시그니처**: `int ECAT_Config_Map(void* pIOmap)`
- **정의 위치**: `Core/Src/ecat_config.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_config.c:1162: int ECAT_Config_Map(void* pIOmap)`
  - `Core/Src/ecat_main.c:2793: ECAT_Config_Map(&m_IOmap);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ECAT_Config

- **종류**: 함수
- **시그니처**: `int ECAT_Config()`
- **정의 위치**: `Core/Src/ecat_config.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_config.c:1168: int ECAT_Config()`
  - `Core/Src/ecat_main.c:2749: wkc = ECAT_Config();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_DC_Sync0

- **종류**: 함수
- **시그니처**: `void EC_DC_Sync0(EC_Main_t* pEC_Main, uint16 slave, boolean act, uint32 CyclTime, int32 CyclShift)`
- **정의 위치**: `Core/Src/ecat_dc.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_dc.c:20: void EC_DC_Sync0(EC_Main_t* pEC_Main, uint16 slave, boolean act, uint32 CyclTime, int32 CyclShift)`
  - `Core/Src/ecat_dc.c:395: EC_DC_Sync0(&m_EC_Main, slave, act, CyclTime, CyclShift);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_DC_Sync01

- **종류**: 함수
- **시그니처**: `void EC_DC_Sync01(EC_Main_t* pEC_Main, uint16 slave, boolean act, uint32 CyclTime0, uint32 CyclTime1, int32 CyclShift)`
- **정의 위치**: `Core/Src/ecat_dc.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_dc.c:61: void EC_DC_Sync01(EC_Main_t* pEC_Main, uint16 slave, boolean act, uint32 CyclTime0, uint32 CyclTime1`
  - `Core/Src/ecat_dc.c:401: EC_DC_Sync01(&m_EC_Main, slave, act, CyclTime0, CyclTime1, CyclShift);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ECAT_Dcsync0

- **종류**: 함수
- **시그니처**: `void ECAT_Dcsync0(uint16 slave, boolean act, uint32 CyclTime, int32 CyclShift)`
- **정의 위치**: `Core/Src/ecat_dc.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_dc.c:393: void ECAT_Dcsync0(uint16 slave, boolean act, uint32 CyclTime, int32 CyclShift)`
  - `Core/Src/ecat_main.c:3641: ECAT_Dcsync0(i, TRUE, SYNC0TIME, 0); // SYNC0 on slave`
  - `Core/Src/ecat_main.c:3721: ECAT_Dcsync0(i, TRUE, SYNC0TIME, 0); // SYNC0 on slave`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

