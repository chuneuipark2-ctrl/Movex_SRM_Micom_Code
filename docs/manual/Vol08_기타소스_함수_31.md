# Vol.08 — 기타 함수 (31)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

com_tml 등.

---

### clsUartRxData

- **종류**: 함수
- **시그니처**: `void clsUartRxData(INT08U port) { if (port < MAX_UART_PORT)`
- **정의 위치**: `Core/Src/uart.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/uart.c:175: void clsUartRxData(INT08U port)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Send_Uart_Polling

- **종류**: 함수
- **시그니처**: `void Send_Uart_Polling(INT08U port) { if (port < MAX_UART_PORT) { if (port == rsTMLPort)`
- **정의 위치**: `Core/Src/uart.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:254: Send_Uart_Polling(port);`
  - `Core/Src/com_tml.c:259: Send_Uart_Polling(port);`
  - `Core/Src/com_tml.c:265: Send_Uart_Polling(port);`
  - `Core/Src/uart.c:184: void Send_Uart_Polling(INT08U port)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Send_Uart_DMA

- **종류**: 함수
- **시그니처**: `void Send_Uart_DMA(INT08U port) { if (port < MAX_UART_PORT) { if (port == rsTMLPort)`
- **정의 위치**: `Core/Src/uart.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/uart.c:228: void Send_Uart_DMA(INT08U port)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsPutcPort

- **종류**: 함수
- **시그니처**: `void rsPutcPort(INT08U port, INT08U data) { switch (port)`
- **정의 위치**: `Core/Src/uart.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/uart.c:240: void rsPutcPort(INT08U port, INT08U data)`
  - `Core/Src/uart.c:289: while (*txStr) rsPutcPort(port, *txStr++);`
  - `Core/Src/uart.c:294: while (txLen--) rsPutcPort(port, *txStr++);`
  - `Core/Src/uart.c:299: while (txLen--) rsPutcPort(port, txData);`
  - `Core/Src/uart.c:304: rsPutcPort(port, (Value) & 0xFF);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsPutsPort

- **종류**: 함수
- **시그니처**: `void rsPutsPort(INT08U port, INT08U* txStr)`
- **정의 위치**: `Core/Src/uart.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/uart.c:287: void rsPutsPort(INT08U port, INT08U* txStr)`
  - `Core/Src/uart.c:432: rsPutsPort(port, (INT08U*)buf);`
  - `Core/Src/uart.c:446: rsPutsPort(port, (INT08U*)buf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsPutnPort

- **종류**: 함수
- **시그니처**: `void rsPutnPort(INT08U port, INT08U* txStr, INT16U txLen)`
- **정의 위치**: `Core/Src/uart.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/uart.c:292: void rsPutnPort(INT08U port, INT08U* txStr, INT16U txLen)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsPutmPort

- **종류**: 함수
- **시그니처**: `void rsPutmPort(INT08U port, INT08U txData, INT16U txLen)`
- **정의 위치**: `Core/Src/uart.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_dsp.c:61: rsPutmPort(port, FRAME_STX, 4);										// STX : 0x16 0x16 0x16 0x16`
  - `Core/Src/com_dsp.c:66: rsPutmPort(port, FRAME_ETX, 1);										// ETX : 0xF5`
  - `Core/Src/com_dsp.c:102: rsPutmPort(port, FRAME_STX, 4);										// STX : 0x16 0x16 0x16 0x16`
  - `Core/Src/com_dsp.c:107: rsPutmPort(port, FRAME_ETX, 1);										// ETX : 0xF5`
  - `Core/Src/com_tml.c:379: rsPutmPort(port, FRAME_STX, 4);                                        // STX : 0x16 0x16 0x16 0x16`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsPutfPort

- **종류**: 함수
- **시그니처**: `void rsPutfPort(INT08U port, float val)`
- **정의 위치**: `Core/Src/uart.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/uart.c:322: void rsPutfPort(INT08U port, float val)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### crcClear

- **종류**: 함수
- **시그니처**: `void crcClear(INT08U port)`
- **정의 위치**: `Core/Src/uart.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_dsp.c:62: crcClear(port);														// Clear CRC`
  - `Core/Src/com_dsp.c:103: crcClear(port);														// Clear CRC`
  - `Core/Src/com_tml.c:380: crcClear(port);                                                      // Clear CRC`
  - `Core/Src/com_tml.c:532: crcClear(port);		// Clear CRC`
  - `Core/Src/com_tml.c:564: crcClear(port);		// Clear CRC`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### calcCrc

- **종류**: 함수
- **시그니처**: `void calcCrc(INT08U port, INT08U crcData)`
- **정의 위치**: `Core/Src/uart.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/uart.c:338: void calcCrc(INT08U port, INT08U crcData)`
  - `Core/Src/uart.c:345: calcCrc(port, txData);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### txPutcPort

- **종류**: 함수
- **시그니처**: `void txPutcPort(INT08U port, INT08U txData)`
- **정의 위치**: `Core/Src/uart.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:326: txPutcPort(port, m_pgmEnv.DeviceType);		// Src ID - Type`
  - `Core/Src/com_tml.c:327: txPutcPort(port, m_pgmEnv.DeviceID);		// Src ID - Index`
  - `Core/Src/com_tml.c:328: txPutcPort(port, pRxHeader->SrcID.Type);	// Dst ID - Type`
  - `Core/Src/com_tml.c:329: txPutcPort(port, pRxHeader->SrcID.Index);	// Dst ID - Index`
  - `Core/Src/com_tml.c:330: txPutcPort(port, pRxHeader->Seq);			// Seq`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### txPutsPort

- **종류**: 함수
- **시그니처**: `void txPutsPort(INT08U port, INT08U* txStr)`
- **정의 위치**: `Core/Src/uart.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/uart.c:377: void txPutsPort(INT08U port, INT08U* txStr)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### txPutnPort

- **종류**: 함수
- **시그니처**: `void txPutnPort(INT08U port, INT08U* txStr, INT16U txLen)`
- **정의 위치**: `Core/Src/uart.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_dsp.c:64: txPutnPort(port, (INT08U*)&nComData.Data, sizeof(INT08U) * 20);		// Data`
  - `Core/Src/com_dsp.c:105: txPutnPort(port, (INT08U*)&m_DspData, sizeof(DspDataSTR));		// Data`
  - `Core/Src/com_tml.c:382: txPutnPort(port, (INT08U*)&m_DevStatus, sizeof(DevStatusSTR));                  // Data`
  - `Core/Src/com_tml.c:566: txPutnPort(port, (INT08U*)&m_ExtSEnv.Machine, sizeof(SRM_Machine_TypeCom));	// Data`
  - `Core/Src/com_tml.c:647: txPutnPort(port, (INT08U*)&m_TestStatus, sizeof(TestStatusSTR));      // Data`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### txCRCPort

- **종류**: 함수
- **시그니처**: `void txCRCPort(INT08U port)`
- **정의 위치**: `Core/Src/uart.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_dsp.c:65: txCRCPort(port);													// Transmit CRC`
  - `Core/Src/com_dsp.c:106: txCRCPort(port);													// Transmit CRC`
  - `Core/Src/com_tml.c:383: txCRCPort(port);                                                      // Transmit CRC`
  - `Core/Src/com_tml.c:534: txCRCPort(port);	// Transmit CRC`
  - `Core/Src/com_tml.c:567: txCRCPort(port);	// Transmit CRC`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### printfPort

- **종류**: 함수
- **시그니처**: `void printfPort(INT08U port, char* format, ...)`
- **정의 위치**: `Core/Src/uart.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/uart.c:424: void printfPort(INT08U port, char* format, ...)`
  - `Core/Src/uart.c:457: printfPort(port, "%s(%d.%03d)\n\r", str, m_pgmEnv.timer1ms / 1000, m_pgmEnv.timer1ms % 1000);`
  - `Core/Src/uart.c:460: printfPort(port, "%02X", data[i]);`
  - `Core/Src/uart.c:463: printfPort(port, "\n\r");`
  - `Core/Src/uart.c:468: printfPort(port, " ");`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### printfPort2

- **종류**: 함수
- **시그니처**: `void printfPort2(INT08U port, char* format, ...)`
- **정의 위치**: `Core/Src/uart.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/uart.c:439: void printfPort2(INT08U port, char* format, ...)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### printfPort_Data

- **종류**: 함수
- **시그니처**: `void printfPort_Data(INT08U port, INT08U* str, INT08U* data, INT16U len)`
- **정의 위치**: `Core/Src/uart.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/uart.c:452: void printfPort_Data(INT08U port, INT08U* str, INT08U* data, INT16U len)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### printfPort_Data2

- **종류**: 함수
- **시그니처**: `void printfPort_Data2(INT08U port, INT08U* str, INT08U* data, INT16U len)`
- **정의 위치**: `Core/Src/uart.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/uart.c:479: void printfPort_Data2(INT08U port, INT08U* str, INT08U* data, INT16U len)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### printfPort_Time

- **종류**: 함수
- **시그니처**: `void printfPort_Time(INT08U port, INT08U* str)`
- **정의 위치**: `Core/Src/uart.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/uart.c:492: void printfPort_Time(INT08U port, INT08U* str)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### jumpApplication

- **종류**: 함수
- **시그니처**: `void jumpApplication(void) // upgradeJumpAppl { // Test if user code is programmed starting from address "APPLICATION_ADDRESS" if (((*(__IO uint32_t*)APPLICATION_ADDRESS) & 0x2FFE0000) == 0x20000000)`
- **정의 위치**: `Core/Src/upgrade.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/upgrade.c:59: void jumpApplication(void) // upgradeJumpAppl`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### checkFusingString

- **종류**: 함수
- **시그니처**: `INT08U checkFusingString(INT08U* s, INT08U* d, INT08U len)`
- **정의 위치**: `Core/Src/upgrade.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:174: if (checkFusingString((INT08U*)m_ExtSEnv.InitDeviceCode, (INT08U*)DEVICE_INIT_CODE, 4))`
  - `Core/Src/SRM_Parameter.c:177: if (checkFusingString((INT08U*)m_BKSram.InitDeviceCode, (INT08U*)DEVICE_INIT_CODE, 4))`
  - `Core/Src/SRM_Parameter.c:204: if (checkFusingString((INT08U*)m_BKSram.InitDeviceCode, (INT08U*)DEVICE_INIT_CODE, 4))`
  - `Core/Src/SRM_Parameter.c:231: if (checkFusingString((INT08U*)m_BKSram.InitDeviceCode, (INT08U*)DEVICE_INIT_CODE, 4))`
  - `Core/Src/SRM_Parameter.c:428: if (checkFusingString((INT08U*)m_ExtSEnv.InitMachineCode, (INT08U*)MACHINE_TYPE_INIT_CODE, 4))`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### getStr2ShortValue

- **종류**: 함수
- **시그니처**: `INT16U getStr2ShortValue(INT08U* str)`
- **정의 위치**: `Core/Src/upgrade.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/upgrade.c:105: INT16U getStr2ShortValue(INT08U* str)`
  - `Core/Src/upgrade.c:654: download_size = getStr2ShortValue(&pCom->Data[4]);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### getStr2LongValue

- **종류**: 함수
- **시그니처**: `INT32U getStr2LongValue(INT08U* str)`
- **정의 위치**: `Core/Src/upgrade.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/upgrade.c:115: INT32U getStr2LongValue(INT08U* str)`
  - `Core/Src/upgrade.c:141: chk_mem[0] = getStr2LongValue(&d[0x00]);`
  - `Core/Src/upgrade.c:142: chk_mem[1] = getStr2LongValue(&d[0x04]);`
  - `Core/Src/upgrade.c:143: chk_mem[2] = getStr2LongValue(&d[0x08]);`
  - `Core/Src/upgrade.c:144: chk_mem[3] = getStr2LongValue(&d[0x0C]);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### checkDownloadCode

- **종류**: 함수
- **시그니처**: `INT08U checkDownloadCode(INT08U* d, INT08U mode)`
- **정의 위치**: `Core/Src/upgrade.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/upgrade.c:134: INT08U checkDownloadCode(INT08U* d, INT08U mode)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### checkDownloadCode_1

- **종류**: 함수
- **시그니처**: `INT08U checkDownloadCode_1(INT32U nRead_Addr, INT08U mode)`
- **정의 위치**: `Core/Src/upgrade.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/upgrade.c:209: INT08U checkDownloadCode_1(INT32U nRead_Addr, INT08U mode)`
  - `Core/Src/upgrade.c:708: if ((checkDownloadCode_1(m_pgmEnv.Fw_Info.Buf_ExecFile.DownloadBufPos, \`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

