# Vol.08 — 기타 함수 (21)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

com_tml 등.

---

### ECAT_Dcsync01

- **종류**: 함수
- **시그니처**: `void ECAT_Dcsync01(uint16 slave, boolean act, uint32 CyclTime0, uint32 CyclTime1, int32 CyclShift)`
- **정의 위치**: `Core/Src/ecat_dc.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_dc.c:399: void ECAT_Dcsync01(uint16 slave, boolean act, uint32 CyclTime0, uint32 CyclTime1, int32 CyclShift)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### InitFPGAEtherCat_1

- **종류**: 함수
- **시그니처**: `static void InitFPGAEtherCat_1(void)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:240: static void InitFPGAEtherCat_1(void)`
  - `Core/Src/ecat_main.c:3948: InitFPGAEtherCat_1();`
  - `Core/Src/ecat_main.c:4618: InitFPGAEtherCat_1();`
  - `Core/Src/ecat_main.c:4819: InitFPGAEtherCat_1();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_Push_Error

- **종류**: 함수
- **시그니처**: `void EC_Push_Error(EC_Main_t* pEC_Main, const EC_Error_t* Ec)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_coe.c:30: EC_Push_Error(pEC_Main, &Ec);`
  - `Core/Src/ecat_coe.c:44: EC_Push_Error(pEC_Main, &Ec_Err);`
  - `Core/Src/ecat_main.c:250: void EC_Push_Error(EC_Main_t* pEC_Main, const EC_Error_t* Ec)`
  - `Core/Src/ecat_main.c:317: EC_Push_Error(pEC_Main, &nEc_Error);`
  - `Core/Src/ecat_main.c:341: EC_Push_Error(pEC_Main, &nEc_Error);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_Packet_Error

- **종류**: 함수
- **시그니처**: `void EC_Packet_Error(EC_Main_t* pEC_Main, uint16 Slave, uint16 Index, uint8 SubIdx, uint16 ErrorCode)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_coe.c:114: EC_Packet_Error(pEC_Main, slave, index, subindex, 3);`
  - `Core/Src/ecat_coe.c:183: EC_Packet_Error(pEC_Main, slave, index, subindex, 1);`
  - `Core/Src/ecat_coe.c:201: EC_Packet_Error(pEC_Main, slave, index, subindex, 3);`
  - `Core/Src/ecat_coe.c:213: EC_Packet_Error(pEC_Main, slave, index, subindex, 1);`
  - `Core/Src/ecat_coe.c:280: EC_Packet_Error(pEC_Main, Slave, Index, SubIndex, 1);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_mbx_Error

- **종류**: 함수
- **시그니처**: `static void EC_mbx_Error(EC_Main_t* pEC_Main, uint16 Slave, uint16 Detail)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:320: static void EC_mbx_Error(EC_Main_t* pEC_Main, uint16 Slave, uint16 Detail)`
  - `Core/Src/ecat_main.c:1003: EC_mbx_Error(pEC_Main, slave, etohs(MBXEp->Detail));`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_mbx_emergency_error

- **종류**: 함수
- **시그니처**: `static void EC_mbx_emergency_error(EC_Main_t* pEC_Main, uint16 Slave, uint16 ErrorCode, uint16 ErrorReg, uint8 b1, uint16 w1, uint16 w2)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:345: static void EC_mbx_emergency_error(EC_Main_t* pEC_Main, uint16 Slave, uint16 ErrorCode, uint16 Error`
  - `Core/Src/ecat_main.c:1012: EC_mbx_emergency_error(pEC_Main, slave, etohs(EMp->ErrorCode), EMp->ErrorReg,`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_SII_String

- **종류**: 함수
- **시그니처**: `void EC_SII_String(EC_Main_t* pEC_Main, char* str, uint16 slave, uint16 Sn)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_config.c:1073: EC_SII_String(pEC_Main, pEC_Main->slave_list[slave].name, slave, 1);`
  - `Core/Src/ecat_main.c:465: void EC_SII_String(EC_Main_t* pEC_Main, char* str, uint16 slave, uint16 Sn)`
  - `Core/Src/ecat_main.c:1328: EC_SII_String(&m_EC_Main, str, slave, Sn);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_SII_PDO

- **종류**: 함수
- **시그니처**: `int EC_SII_PDO(EC_Main_t* pEC_Main, uint16 slave, EC_eepromPDO_t* PDO, uint8 t)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_config.c:391: Isize = (int)EC_SII_PDO(pEC_Main, slave, &eepPDO, 0);`
  - `Core/Src/ecat_config.c:406: Osize = (int)EC_SII_PDO(pEC_Main, slave, &eepPDO, 1);`
  - `Core/Src/ecat_main.c:612: int EC_SII_PDO(EC_Main_t* pEC_Main, uint16 slave, EC_eepromPDO_t* PDO, uint8 t)`
  - `Core/Src/ecat_main.c:1355: return EC_SII_PDO(&m_EC_pEC_Main, slave, PDO, t);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_FPRD_multi

- **종류**: 함수
- **시그니처**: `int EC_FPRD_multi(int n, uint16* configlst, EC_Alstatus_t* slstatlst, int timeout)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:681: int EC_FPRD_multi(int n, uint16* configlst, EC_Alstatus_t* slstatlst, int timeout)`
  - `Core/Src/ecat_main.c:803: wkc = EC_FPRD_multi((lslave - fslave) + 1, &(slca[0]), &(sl[0]), EC_TIMEOUT_RETRY3);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_Write_State

- **종류**: 함수
- **시그니처**: `int EC_Write_State(EC_pEC_Main_t* pEC_Main, uint16 slave)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:702: int EC_Write_State(EC_pEC_Main_t* pEC_Main, uint16 slave)`
  - `Core/Src/ecat_main.c:1380: return EC_Write_State(&m_EC_pEC_Main, slave);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_Read_State_1

- **종류**: 함수
- **시그니처**: `int EC_Read_State_1(EC_Main_t* pEC_Main)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:722: int EC_Read_State_1(EC_Main_t* pEC_Main)`
  - `Core/Src/ecat_main.c:1368: nReturn = EC_Read_State_1(&m_EC_Main);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_Clear_MailBox

- **종류**: 함수
- **시그니처**: `void EC_Clear_MailBox(EC_Mbx_buffer_t* Mbx)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_coe.c:59: EC_Clear_MailBox(&MbxIn);`
  - `Core/Src/ecat_coe.c:64: EC_Clear_MailBox(&MbxOut);`
  - `Core/Src/ecat_coe.c:94: EC_Clear_MailBox(&MbxIn);`
  - `Core/Src/ecat_coe.c:152: EC_Clear_MailBox(&MbxIn);`
  - `Core/Src/ecat_coe.c:232: EC_Clear_MailBox(&MbxIn);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_MailBox_Empty

- **종류**: 함수
- **시그니처**: `int EC_MailBox_Empty(EC_Main_t* pEC_Main, uint16 slave, int timeout)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:911: int EC_MailBox_Empty(EC_Main_t* pEC_Main, uint16 slave, int timeout)`
  - `Core/Src/ecat_main.c:949: if (EC_MailBox_Empty(pEC_Main, slave, timeout))`
  - `Core/Src/ecat_main.c:1393: return EC_MailBox_Empty(&m_EC_pEC_Main, slave, timeout);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_MailBox_Send

- **종류**: 함수
- **시그니처**: `int EC_MailBox_Send(EC_Main_t* pEC_Main, uint16 slave, EC_Mbx_buffer_t* mbx, int timeout)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_coe.c:91: wkc = EC_MailBox_Send(pEC_Main, slave, (EC_Mbx_buffer_t*)&MbxOut, EC_TIMEOUT_TXM);`
  - `Core/Src/ecat_coe.c:149: wkc = EC_MailBox_Send(pEC_Main, slave, (EC_Mbx_buffer_t*)&MbxOut, EC_TIMEOUT_TXM);`
  - `Core/Src/ecat_coe.c:258: wkc = EC_MailBox_Send(pEC_Main, Slave, (EC_Mbx_buffer_t*)&MbxOut, EC_TIMEOUT_TXM);`
  - `Core/Src/ecat_coe.c:324: wkc = EC_MailBox_Send(pEC_Main, Slave, (EC_Mbx_buffer_t*)&MbxOut, EC_TIMEOUT_TXM);`
  - `Core/Src/ecat_coe.c:375: wkc = EC_MailBox_Send(pEC_Main, Slave, (EC_Mbx_buffer_t*)&MbxOut, EC_TIMEOUT_TXM);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_MailBox_Receive

- **종류**: 함수
- **시그니처**: `int EC_MailBox_Receive(EC_Main_t* pEC_Main, uint16 slave, EC_Mbx_buffer_t* mbx, int timeout)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_coe.c:62: wkc = EC_MailBox_Receive(pEC_Main, slave, (EC_Mbx_buffer_t*)&MbxIn, 10);`
  - `Core/Src/ecat_coe.c:96: wkc = EC_MailBox_Receive(pEC_Main, slave, (EC_Mbx_buffer_t*)&MbxIn, timeout);`
  - `Core/Src/ecat_coe.c:154: wkc = EC_MailBox_Receive(pEC_Main, slave, (EC_Mbx_buffer_t*)&MbxIn, timeout);`
  - `Core/Src/ecat_coe.c:235: wkc = EC_MailBox_Receive(pEC_Main, Slave, (EC_Mbx_buffer_t*)&MbxIn, 10);`
  - `Core/Src/ecat_coe.c:262: wkc = EC_MailBox_Receive(pEC_Main, Slave, (EC_Mbx_buffer_t*)&MbxIn, Timeout);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_Esi_dump

- **종류**: 함수
- **시그니처**: `void EC_Esi_dump(EC_Main_t* pEC_Main, uint16 slave, uint8* esibuf)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:1057: void EC_Esi_dump(EC_Main_t* pEC_Main, uint16 slave, uint8* esibuf)`
  - `Core/Src/ecat_main.c:1414: EC_Esi_dump(&m_EC_pEC_Main, slave, esibuf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_EEpromToMaster

- **종류**: 함수
- **시그니처**: `int EC_EEpromToMaster(EC_Main_t* pEC_Main, uint16 slave)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:400: EC_EEpromToMaster(pEC_Main, slave);`
  - `Core/Src/ecat_main.c:1066: EC_EEpromToMaster(pEC_Main, slave);`
  - `Core/Src/ecat_main.c:1097: EC_EEpromToMaster(pEC_Main, slave);`
  - `Core/Src/ecat_main.c:1103: int EC_EEpromToMaster(EC_Main_t* pEC_Main, uint16 slave)`
  - `Core/Src/ecat_main.c:1246: EC_EEpromToMaster(pEC_Main, slave);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_EEpromToPDI

- **종류**: 함수
- **시그니처**: `int EC_EEpromToPDI(EC_Main_t* pEC_Main, uint16 slave)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_config.c:695: EC_EEpromToPDI(pEC_Main, nSlave);`
  - `Core/Src/ecat_config.c:1151: EC_EEpromToPDI(pEC_Main, slave);`
  - `Core/Src/ecat_main.c:459: EC_EEpromToPDI(pEC_Main, slave);`
  - `Core/Src/ecat_main.c:514: EC_EEpromToPDI(pEC_Main, slave);`
  - `Core/Src/ecat_main.c:547: EC_EEpromToPDI(pEC_Main, slave);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_Read_EEprom1

- **종류**: 함수
- **시그니처**: `void EC_Read_EEprom1(EC_Main_t* pEC_Main, uint16 slave, uint16 eeproma)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_config.c:914: EC_Read_EEprom1(pEC_Main, slave, ECT_SII_MANUF);`
  - `Core/Src/ecat_config.c:921: EC_Read_EEprom1(pEC_Main, slave, ECT_SII_ID);`
  - `Core/Src/ecat_config.c:927: EC_Read_EEprom1(pEC_Main, slave, ECT_SII_REV);`
  - `Core/Src/ecat_config.c:933: EC_Read_EEprom1(pEC_Main, slave, ECT_SII_RXMBXADR);`
  - `Core/Src/ecat_config.c:942: EC_Read_EEprom1(pEC_Main, slave, ECT_SII_TXMBXADR);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ec_pusherror

- **종류**: 함수
- **시그니처**: `void ec_pusherror(const ec_errort* Ec)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:1288: void ec_pusherror(const ec_errort* Ec)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ec_packeterror

- **종류**: 함수
- **시그니처**: `void ec_packeterror(uint16 Slave, uint16 Index, uint8 SubIdx, uint16 ErrorCode)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:1309: void ec_packeterror(uint16 Slave, uint16 Index, uint8 SubIdx, uint16 ErrorCode)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_Sii_string

- **종류**: 함수
- **시그니처**: `void EC_Sii_string(char* str, uint16 slave, uint16 Sn)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:1326: void EC_Sii_string(char* str, uint16 slave, uint16 Sn)`
  - `Core/Src/ecat_main.c:1837: EC_Sii_string(str_name, slave, obj_name);`
  - `Core/Src/ecat_main.c:1861: EC_Sii_string(str_name, slave, obj_name);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ec_siiPDO

- **종류**: 함수
- **시그니처**: `int ec_siiPDO(uint16 slave, ec_eepromPDOt* PDO, uint8 t)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:1353: int ec_siiPDO(uint16 slave, ec_eepromPDOt* PDO, uint8 t)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_Read_State

- **종류**: 함수
- **시그니처**: `int EC_Read_State(void)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:1359: int EC_Read_State(void)`
  - `Core/Src/ecat_main.c:1956: EC_Read_State();`
  - `Core/Src/ecat_main.c:2173: EC_Read_State();`
  - `Core/Src/ecat_main.c:4188: EC_Read_State();`
  - `Core/Src/ecat_main.c:4267: EC_Read_State();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ec_writestate

- **종류**: 함수
- **시그니처**: `int ec_writestate(uint16 slave)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:1378: int ec_writestate(uint16 slave)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

