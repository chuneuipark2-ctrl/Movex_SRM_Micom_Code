# Vol.08 — 기타 함수 (19)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

com_tml 등.

---

### EC_APWR_SetDatagram

- **종류**: 함수
- **시그니처**: `void EC_APWR_SetDatagram(INT16U Adp, INT16U Ado, INT16U Len, void *Data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **인근 주석**: ecx_adddatagram(m_Datagram_Tx_Cnt++, EC_CMD_APWR, Adp, Ado, Len, Data);
- **용도(읽는 법)**: 헤더 주석: ecx_adddatagram(m_Datagram_Tx_Cnt++, EC_CMD_APWR, Adp, Ado, Len, Data);
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:488: void EC_APWR_SetDatagram(INT16U Adp, INT16U Ado, INT16U Len, void *Data)`
  - `Core/Src/ecat_combase.c:899: EC_APWR_SetDatagram(ADP, ADO, length, data);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_APRW_SetDatagram

- **종류**: 함수
- **시그니처**: `void EC_APRW_SetDatagram(INT16U Adp, INT16U Ado, INT16U Len, void *Data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **인근 주석**: ecx_adddatagram(m_Datagram_Tx_Cnt++, EC_CMD_APRW, Adp, Ado, Len, Data);
- **용도(읽는 법)**: 헤더 주석: ecx_adddatagram(m_Datagram_Tx_Cnt++, EC_CMD_APRW, Adp, Ado, Len, Data);
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:495: void EC_APRW_SetDatagram(INT16U Adp, INT16U Ado, INT16U Len, void *Data)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_ARMW_SetDatagram

- **종류**: 함수
- **시그니처**: `void EC_ARMW_SetDatagram(INT16U Adp, INT16U Ado, INT16U Len, void *Data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **인근 주석**: ecx_adddatagram(0, EC_CMD_ARMW, Adp, Ado, Len, Data);
- **용도(읽는 법)**: 헤더 주석: ecx_adddatagram(0, EC_CMD_ARMW, Adp, Ado, Len, Data);
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:502: void EC_ARMW_SetDatagram(INT16U Adp, INT16U Ado, INT16U Len, void *Data)`
  - `Core/Src/ecat_combase.c:747: EC_ARMW_SetDatagram(ADP, ADO, length, data);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_FPRD_SetDatagram

- **종류**: 함수
- **시그니처**: `void EC_FPRD_SetDatagram(INT16U Adp, INT16U Ado, INT16U Len, void *Data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **인근 주석**: ecx_adddatagram(m_Datagram_Tx_Cnt++, EC_CMD_FPRD, Adp, Ado, Len, Data);
- **용도(읽는 법)**: 헤더 주석: ecx_adddatagram(m_Datagram_Tx_Cnt++, EC_CMD_FPRD, Adp, Ado, Len, Data);
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:509: void EC_FPRD_SetDatagram(INT16U Adp, INT16U Ado, INT16U Len, void *Data)`
  - `Core/Src/ecat_combase.c:831: EC_FPRD_SetDatagram(ADP, ADO, length, data);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_FPWR_SetDatagram

- **종류**: 함수
- **시그니처**: `void EC_FPWR_SetDatagram(INT16U Adp, INT16U Ado, INT16U Len, void *Data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **인근 주석**: ecx_adddatagram(m_Datagram_Tx_Cnt++, EC_CMD_FPWR, Adp, Ado, Len, Data);
- **용도(읽는 법)**: 헤더 주석: ecx_adddatagram(m_Datagram_Tx_Cnt++, EC_CMD_FPWR, Adp, Ado, Len, Data);
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:516: void EC_FPWR_SetDatagram(INT16U Adp, INT16U Ado, INT16U Len, void *Data)`
  - `Core/Src/ecat_combase.c:947: EC_FPWR_SetDatagram(ADP, ADO, length, data);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_FPRW_SetDatagram

- **종류**: 함수
- **시그니처**: `void EC_FPRW_SetDatagram(INT16U Adp, INT16U Ado, INT16U Len, void *Data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **인근 주석**: ecx_adddatagram(m_Datagram_Tx_Cnt++, EC_CMD_FPRW, Adp, Ado, Len, Data);
- **용도(읽는 법)**: 헤더 주석: ecx_adddatagram(m_Datagram_Tx_Cnt++, EC_CMD_FPRW, Adp, Ado, Len, Data);
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:524: void EC_FPRW_SetDatagram(INT16U Adp, INT16U Ado, INT16U Len, void *Data)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_FRMW_SetDatagram

- **종류**: 함수
- **시그니처**: `void EC_FRMW_SetDatagram(INT16U Adp, INT16U Ado, INT16U Len, void *Data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **인근 주석**: ecx_adddatagram(m_Datagram_Tx_Cnt++, EC_CMD_FRMW, Adp, Ado, Len, Data);
- **용도(읽는 법)**: 헤더 주석: ecx_adddatagram(m_Datagram_Tx_Cnt++, EC_CMD_FRMW, Adp, Ado, Len, Data);
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:531: void EC_FRMW_SetDatagram(INT16U Adp, INT16U Ado, INT16U Len, void *Data)`
  - `Core/Src/ecat_combase.c:780: EC_FRMW_SetDatagram(ADP, ADO, length, data);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_BRD_SetDatagram

- **종류**: 함수
- **시그니처**: `void EC_BRD_SetDatagram(INT16U Adp, INT16U Ado, INT16U Len, void *Data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **인근 주석**: ecx_adddatagram(m_Datagram_Tx_Cnt++, EC_CMD_BRD, Adp, Ado, Len, Data);
- **용도(읽는 법)**: 헤더 주석: ecx_adddatagram(m_Datagram_Tx_Cnt++, EC_CMD_BRD, Adp, Ado, Len, Data);
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:538: void EC_BRD_SetDatagram(INT16U Adp, INT16U Ado, INT16U Len, void *Data)`
  - `Core/Src/ecat_combase.c:681: EC_BRD_SetDatagram(ADP, ADO, length, data);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_BWR_SetDatagram

- **종류**: 함수
- **시그니처**: `void EC_BWR_SetDatagram(INT16U Adp, INT16U Ado, INT16U Len, void *Data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **인근 주석**: ecx_adddatagram(m_Datagram_Tx_Cnt++, EC_CMD_BWR, Adp, Ado, Len, Data);
- **용도(읽는 법)**: 헤더 주석: ecx_adddatagram(m_Datagram_Tx_Cnt++, EC_CMD_BWR, Adp, Ado, Len, Data);
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:545: void EC_BWR_SetDatagram(INT16U Adp, INT16U Ado, INT16U Len, void *Data)`
  - `Core/Src/ecat_combase.c:611: EC_BWR_SetDatagram(ADP, ADO, length, data);`
  - `Core/Src/ecat_combase.c:648: EC_BWR_SetDatagram(ADP, ADO, length, data);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_BRW_SetDatagram

- **종류**: 함수
- **시그니처**: `void EC_BRW_SetDatagram(INT16U Adp, INT16U Ado, INT16U Len, void *Data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **인근 주석**: ecx_adddatagram(m_Datagram_Tx_Cnt++, EC_CMD_BRW, Adp, Ado, Len, Data);
- **용도(읽는 법)**: 헤더 주석: ecx_adddatagram(m_Datagram_Tx_Cnt++, EC_CMD_BRW, Adp, Ado, Len, Data);
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:552: void EC_BRW_SetDatagram(INT16U Adp, INT16U Ado, INT16U Len, void *Data)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_NOP_SetDatagram

- **종류**: 함수
- **시그니처**: `void EC_NOP_SetDatagram(INT16U Adp, INT16U Ado, INT16U Len, void* Data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **인근 주석**: ecx_adddatagram(m_Datagram_Tx_Cnt++, EC_CMD_BRW, Adp, Ado, Len, Data);
- **용도(읽는 법)**: 헤더 주석: ecx_adddatagram(m_Datagram_Tx_Cnt++, EC_CMD_BRW, Adp, Ado, Len, Data);
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:558: void EC_NOP_SetDatagram(INT16U Adp, INT16U Ado, INT16U Len, void* Data)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_LRD_SetDatagram

- **종류**: 함수
- **시그니처**: `void EC_LRD_SetDatagram(INT32U LAddr, INT16U Len, void *Data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **인근 주석**: ecx_adddatagram(0, EC_CMD_LRD, LO_WORD(LAddr), HI_WORD(LAddr), Len, Data);
- **용도(읽는 법)**: 헤더 주석: ecx_adddatagram(0, EC_CMD_LRD, LO_WORD(LAddr), HI_WORD(LAddr), Len, Data);
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:565: void EC_LRD_SetDatagram(INT32U LAddr, INT16U Len, void *Data)`
  - `Core/Src/ecat_combase.c:1055: EC_LRD_SetDatagram(LogAdr, length, data);`
  - `Core/Src/ecat_combase.c:1143: EC_LRD_SetDatagram(LogAdr, 0, nTemp);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_LRD_AddDatagram

- **종류**: 함수
- **시그니처**: `void EC_LRD_AddDatagram(INT32U LAddr, INT16U Len, void* Data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:571: void EC_LRD_AddDatagram(INT32U LAddr, INT16U Len, void* Data)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_LWR_SetDatagram

- **종류**: 함수
- **시그니처**: `void EC_LWR_SetDatagram(INT32U LAddr, INT16U Len, void *Data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **인근 주석**: ecx_adddatagram(0, EC_CMD_LWR, LO_WORD(LAddr), HI_WORD(LAddr), Len, Data);
- **용도(읽는 법)**: 헤더 주석: ecx_adddatagram(0, EC_CMD_LWR, LO_WORD(LAddr), HI_WORD(LAddr), Len, Data);
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:578: void EC_LWR_SetDatagram(INT32U LAddr, INT16U Len, void *Data)`
  - `Core/Src/ecat_combase.c:1087: EC_LWR_SetDatagram(LogAdr, length, data);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_LRW_SetDatagram

- **종류**: 함수
- **시그니처**: `void EC_LRW_SetDatagram(INT32U LAddr, INT16U Len, void *Data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:585: void EC_LRW_SetDatagram(INT32U LAddr, INT16U Len, void *Data)`
  - `Core/Src/ecat_combase.c:979: EC_LRW_SetDatagram(LogAdr, length, data);`
  - `Core/Src/ecat_combase.c:1023: EC_LRW_SetDatagram(LogAdr, length, DataOut);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_LRW_AddDatagram

- **종류**: 함수
- **시그니처**: `void EC_LRW_AddDatagram(INT32U LAddr, INT16U Len, void* Data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:590: void EC_LRW_AddDatagram(INT32U LAddr, INT16U Len, void* Data)`
  - `Core/Src/ecat_combase.c:1144: EC_LRW_AddDatagram(LogAdr, length, data);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ecx_NOP

- **종류**: 함수
- **시그니처**: `int ecx_NOP(INT16U ADP, INT16U ADO, uint16 length, void* data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:607: int ecx_NOP(INT16U ADP, INT16U ADO, uint16 length, void* data)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ecx_BWR

- **종류**: 함수
- **시그니처**: `int ecx_BWR(INT16U ADP, INT16U ADO, uint16 length, void *data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:644: int ecx_BWR(INT16U ADP, INT16U ADO, uint16 length, void *data)`
  - `Core/Src/ecat_config.c:778: ecx_BWR(0x0000, ECT_REG_DLALIAS, sizeof(b), &b);`
  - `Core/Src/ecat_config.c:780: ecx_BWR(0x0000, ECT_REG_ALCTL, sizeof(b), &b);`
  - `Core/Src/ecat_config.c:782: ecx_BWR(0x0000, ECT_REG_ALCTL, sizeof(b), &b);`
  - `Core/Src/ecat_config.c:807: ecx_BWR(0x0000, ECT_REG_RESET_ECAT, sizeof(nBuf), &nBuf);     // EtherCAT Reset 'R'`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ecx_BRD

- **종류**: 함수
- **시그니처**: `int ecx_BRD(INT16U ADP, INT16U ADO, uint16 length, void *data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:677: int ecx_BRD(INT16U ADP, INT16U ADO, uint16 length, void *data)`
  - `Core/Src/ecat_config.c:783: wkc = ecx_BRD(0x0000, ECT_REG_TYPE, sizeof(w), &w);`
  - `Core/Src/ecat_config.c:808: ecx_BRD(0x0000, ECT_REG_RESET_ECAT, sizeof(nBuf), &nBuf);`
  - `Core/Src/ecat_config.c:812: ecx_BRD(0x0000, ECT_REG_RESET_ECAT, sizeof(nBuf), &nBuf);`
  - `Core/Src/ecat_config.c:816: ecx_BRD(0x0000, ECT_REG_RESET_ECAT, sizeof(nBuf), &nBuf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ecx_APRD

- **종류**: 함수
- **시그니처**: `int ecx_APRD(uint16 ADP, uint16 ADO, uint16 length, void *data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:710: int ecx_APRD(uint16 ADP, uint16 ADO, uint16 length, void *data)`
  - `Core/Src/ecat_combase.c:811: ecx_APRD(ADP, ADO, sizeof(w), &w);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ecx_ARMW

- **종류**: 함수
- **시그니처**: `int ecx_ARMW(uint16 ADP, uint16 ADO, uint16 length, void *data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:743: int ecx_ARMW(uint16 ADP, uint16 ADO, uint16 length, void *data)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ecx_FRMW

- **종류**: 함수
- **시그니처**: `int ecx_FRMW(uint16 ADP, uint16 ADO, uint16 length, void *data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:776: int ecx_FRMW(uint16 ADP, uint16 ADO, uint16 length, void *data)`
  - `Core/Src/ecat_main.c:1471: ecx_FRMW(m_ecx_pEC_Main.slavelist[m_ecx_pEC_Main.grouplist[group].DCnext].configadr,`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ecx_FPRD

- **종류**: 함수
- **시그니처**: `int ecx_FPRD(uint16 ADP, uint16 ADO, uint16 length, void *data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:827: int ecx_FPRD(uint16 ADP, uint16 ADO, uint16 length, void *data)`
  - `Core/Src/ecat_combase.c:864: ecx_FPRD(ADP, ADO, sizeof(w), &w);`
  - `Core/Src/ecat_config.c:896: ecx_FPRD(configadr, ECT_REG_ALIAS, sizeof(aliasadr), &aliasadr);`
  - `Core/Src/ecat_config.c:898: ecx_FPRD(configadr, ECT_REG_EEPSTAT, sizeof(estat), &estat);`
  - `Core/Src/ecat_dc.c:38: (void)ecx_FPRD(slaveh, ECT_REG_DCSYSTIME, sizeof(t1), &t1);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ecx_APWRw

- **종류**: 함수
- **시그니처**: `int ecx_APWRw(uint16 ADP, uint16 ADO, uint16 data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:879: int ecx_APWRw(uint16 ADP, uint16 ADO, uint16 data)`
  - `Core/Src/ecat_config.c:883: ecx_APWRw(ADPh, ECT_REG_STADR, htoes(slave + EC_NODEOFFSET));`
  - `Core/Src/ecat_config.c:892: ecx_APWRw(ADPh, ECT_REG_DLCTL, htoes(b));`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ecx_APWR

- **종류**: 함수
- **시그니처**: `int ecx_APWR(uint16 ADP, uint16 ADO, uint16 length, void *data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:881: return ecx_APWR(ADP, ADO, sizeof(data), &data);`
  - `Core/Src/ecat_combase.c:895: int ecx_APWR(uint16 ADP, uint16 ADO, uint16 length, void *data)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

