# Vol.08 — 기타 함수 (18)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

com_tml 등.

---

### ecx_RxPDO

- **종류**: 함수
- **시그니처**: `int ecx_RxPDO(EC_pEC_Main_t* pEC_Main, uint16 Slave, uint16 RxPDOnumber, int psize, void* p)`
- **정의 위치**: `Core/Src/ecat_coe.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_coe.c:426: int ecx_RxPDO(EC_pEC_Main_t* pEC_Main, uint16 Slave, uint16 RxPDOnumber, int psize, void* p)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ecx_TxPDO

- **종류**: 함수
- **시그니처**: `int ecx_TxPDO(EC_pEC_Main_t* pEC_Main, uint16 slave, uint16 TxPDOnumber, int* psize, void* p, int timeout)`
- **정의 위치**: `Core/Src/ecat_coe.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_coe.c:461: int ecx_TxPDO(EC_pEC_Main_t* pEC_Main, uint16 slave, uint16 TxPDOnumber, int* psize, void* p, int ti`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_Read_PDOassign

- **종류**: 함수
- **시그니처**: `int EC_Read_PDOassign(EC_Main_t* pEC_Main, uint16 Slave, uint16 PDOassign)`
- **정의 위치**: `Core/Src/ecat_coe.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_coe.c:522: int EC_Read_PDOassign(EC_Main_t* pEC_Main, uint16 Slave, uint16 PDOassign)`
  - `Core/Src/ecat_coe.c:702: Tsize = EC_Read_PDOassign(pEC_Main, Slave, ECT_SDO_PDO_ASSIGN + iSM);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_Read_PDOassignCA

- **종류**: 함수
- **시그니처**: `int EC_Read_PDOassignCA(EC_Main_t* pEC_Main, uint16 Slave, int Thread_n, uint16 PDOassign)`
- **정의 위치**: `Core/Src/ecat_coe.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_coe.c:572: int EC_Read_PDOassignCA(EC_Main_t* pEC_Main, uint16 Slave, int Thread_n, uint16 PDOassign)`
  - `Core/Src/ecat_coe.c:776: Tsize = EC_Read_PDOassignCA(pEC_Main, Slave, Thread_n,`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_Read_PDOassignCA_1

- **종류**: 함수
- **시그니처**: `int EC_Read_PDOassignCA_1(EC_Main_t* pEC_Main, uint16 Slave, int Thread_n, uint16 PDOassign, uint8 iSm)`
- **정의 위치**: `Core/Src/ecat_coe.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_coe.c:609: int EC_Read_PDOassignCA_1(EC_Main_t* pEC_Main, uint16 Slave, int Thread_n, uint16 PDOassign, uint8 i`
  - `Core/Src/ecat_coe.c:781: Tsize = EC_Read_PDOassignCA_1(pEC_Main, Slave, Thread_n,`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_Read_PDOmap

- **종류**: 함수
- **시그니처**: `int EC_Read_PDOmap(EC_Main_t* pEC_Main, uint16 Slave, int* Osize, int* Isize)`
- **정의 위치**: `Core/Src/ecat_coe.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_coe.c:652: int EC_Read_PDOmap(EC_Main_t* pEC_Main, uint16 Slave, int* Osize, int* Isize)`
  - `Core/Src/ecat_config.c:52: nReturn_val = EC_Read_PDOmap(EC_Main, slave, &Osize, &Isize);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_Read_PDOmap_CA

- **종류**: 함수
- **시그니처**: `int EC_Read_PDOmap_CA(EC_Main_t* pEC_Main, uint16 Slave, int Thread_n, int* Osize, int* Isize)`
- **정의 위치**: `Core/Src/ecat_coe.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_coe.c:728: int EC_Read_PDOmap_CA(EC_Main_t* pEC_Main, uint16 Slave, int Thread_n, int* Osize, int* Isize)`
  - `Core/Src/ecat_config.c:47: nReturn_val = EC_Read_PDOmap_CA(EC_Main, slave, thread_n, &Osize, &Isize);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_Read_ODlist

- **종류**: 함수
- **시그니처**: `int EC_Read_ODlist(EC_Main_t* pEC_Main, uint16 Slave, EC_ODlist_t* pODlist)`
- **정의 위치**: `Core/Src/ecat_coe.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_coe.c:807: int EC_Read_ODlist(EC_Main_t* pEC_Main, uint16 Slave, EC_ODlist_t* pODlist)`
  - `Core/Src/ecat_coe.c:1108: return EC_Read_ODlist(&m_EC_Main, Slave, pODlist);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_Read_ODdescription

- **종류**: 함수
- **시그니처**: `int EC_Read_ODdescription(EC_Main_t* pEC_Main, uint16 Item, EC_ODlist_t* pODlist)`
- **정의 위치**: `Core/Src/ecat_coe.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_coe.c:913: int EC_Read_ODdescription(EC_Main_t* pEC_Main, uint16 Item, EC_ODlist_t* pODlist)`
  - `Core/Src/ecat_coe.c:1113: return EC_Read_ODdescription(&m_EC_Main, Item, pODlist);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_Read_OEsingle

- **종류**: 함수
- **시그니처**: `int EC_Read_OEsingle(EC_Main_t* pEC_Main, uint16 Item, uint8 SubI, EC_ODlist_t* pODlist, EC_OElist_t* pOElist)`
- **정의 위치**: `Core/Src/ecat_coe.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_coe.c:986: int EC_Read_OEsingle(EC_Main_t* pEC_Main, uint16 Item, uint8 SubI, EC_ODlist_t* pODlist, EC_OElist_t`
  - `Core/Src/ecat_coe.c:1075: wkc = EC_Read_OEsingle(pEC_Main, Item, (uint8)SubCount, pODlist, pOElist);`
  - `Core/Src/ecat_coe.c:1118: return EC_Read_OEsingle(&m_EC_Main, Item, SubI, pODlist, pOElist);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_ReadOE_1

- **종류**: 함수
- **시그니처**: `int EC_ReadOE_1(EC_Main_t* pEC_Main, uint16 Item, EC_ODlist_t* pODlist, EC_OElist_t* pOElist)`
- **정의 위치**: `Core/Src/ecat_coe.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_coe.c:1063: int EC_ReadOE_1(EC_Main_t* pEC_Main, uint16 Item, EC_ODlist_t* pODlist, EC_OElist_t* pOElist)`
  - `Core/Src/ecat_coe.c:1123: return EC_ReadOE_1(&m_EC_Main, Item, pODlist, pOElist);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_SDOread

- **종류**: 함수
- **시그니처**: `int EC_SDOread(uint16 slave, uint16 index, uint8 subindex, boolean CA, int* psize, void* p, int timeout)`
- **정의 위치**: `Core/Src/ecat_coe.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_coe.c:1081: int EC_SDOread(uint16 slave, uint16 index, uint8 subindex,`
  - `Core/Src/ecat_main.c:1585: EC_SDOread(slave, index, subidx, FALSE, &l, &m_usdo, EC_TIMEOUT_RXM);`
  - `Core/Src/ecat_main.c:1681: wkc = EC_SDOread(slave, PDOassign, 0x00, FALSE, &rdl, &rdat, EC_TIMEOUT_RXM);`
  - `Core/Src/ecat_main.c:1692: wkc = EC_SDOread(slave, PDOassign, (uint8)idxloop, FALSE, &rdl, &rdat, EC_TIMEOUT_RXM);`
  - `Core/Src/ecat_main.c:1697: wkc = EC_SDOread(slave, idx, 0x00, FALSE, &rdl, &subcnt, EC_TIMEOUT_RXM);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_SDOwrite

- **종류**: 함수
- **시그니처**: `int EC_SDOwrite(uint16 Slave, uint16 Index, uint8 SubIndex, boolean CA, int psize, void* p, int Timeout)`
- **정의 위치**: `Core/Src/ecat_coe.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_coe.c:1087: int EC_SDOwrite(uint16 Slave, uint16 Index, uint8 SubIndex, boolean CA, int psize, void* p, int Time`
  - `Core/Src/ecat_main.c:2253: retval += EC_SDOwrite(1, 0x1C12, 0, FALSE, rdl, pRdat, EC_TIMEOUT_TXM);`
  - `Core/Src/ecat_main.c:2257: retval += EC_SDOwrite(1, 0x1C13, 0, FALSE, rdl, pRdat, EC_TIMEOUT_TXM);`
  - `Core/Src/ecat_main.c:2261: retval += EC_SDOwrite(1, 0x1600, 0, FALSE, rdl, pRdat, EC_TIMEOUT_TXM);`
  - `Core/Src/ecat_main.c:2265: retval += EC_SDOwrite(slave, 0x1600, i, FALSE, sizeof(INT32U), &RxPDOmap_1600[i - 1], EC_TIMEOUT_RXM`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_ReadODlist

- **종류**: 함수
- **시그니처**: `int EC_ReadODlist(uint16 Slave, EC_ODlist_t* pODlist)`
- **정의 위치**: `Core/Src/ecat_coe.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_coe.c:1106: int EC_ReadODlist(uint16 Slave, EC_ODlist_t* pODlist)`
  - `Core/Src/ecat_main.c:1915: if (EC_ReadODlist(cnt, &m_ODlist))`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_ReadODdescription

- **종류**: 함수
- **시그니처**: `int EC_ReadODdescription(uint16 Item, EC_ODlist_t* pODlist)`
- **정의 위치**: `Core/Src/ecat_coe.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_coe.c:1111: int EC_ReadODdescription(uint16 Item, EC_ODlist_t* pODlist)`
  - `Core/Src/ecat_main.c:1920: EC_ReadODdescription(i, &m_ODlist);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_ReadOEsingle

- **종류**: 함수
- **시그니처**: `int EC_ReadOEsingle(uint16 Item, uint8 SubI, EC_ODlist_t* pODlist, EC_OElist_t* pOElist)`
- **정의 위치**: `Core/Src/ecat_coe.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_coe.c:1116: int EC_ReadOEsingle(uint16 Item, uint8 SubI, EC_ODlist_t* pODlist, EC_OElist_t* pOElist)`
  - `Core/Src/ecat_main.c:1715: wkc = EC_ReadOEsingle(0, obj_subidx, &m_ODlist, &m_OElist);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_ReadOE

- **종류**: 함수
- **시그니처**: `int EC_ReadOE(uint16 Item, EC_ODlist_t* pODlist, EC_OElist_t* pOElist)`
- **정의 위치**: `Core/Src/ecat_coe.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_coe.c:1121: int EC_ReadOE(uint16 Item, EC_ODlist_t* pODlist, EC_OElist_t* pOElist)`
  - `Core/Src/ecat_main.c:1925: EC_ReadOE(i, &m_ODlist, &m_OElist);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ecx_setupdatagram

- **종류**: 함수
- **시그니처**: `INT08U ecx_setupdatagram(INT08U com, INT16U ADP, INT16U ADO, INT16U length, INT08U *data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:90: INT08U ecx_setupdatagram(INT08U com, INT16U ADP, INT16U ADO, INT16U length, INT08U *data)`
  - `Core/Src/ecat_combase.c:484: ecx_setupdatagram(EC_CMD_APRD, Adp, Ado, Len, Data);`
  - `Core/Src/ecat_combase.c:491: ecx_setupdatagram(EC_CMD_APWR, Adp, Ado, Len, Data);`
  - `Core/Src/ecat_combase.c:498: ecx_setupdatagram(EC_CMD_APWR, Adp, Ado, Len, Data);`
  - `Core/Src/ecat_combase.c:505: ecx_setupdatagram(EC_CMD_ARMW, Adp, Ado, Len, Data);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ecx_adddatagram

- **종류**: 함수
- **시그니처**: `void ecx_adddatagram(INT08U Cmd, INT16U Adp, INT16U Ado, INT16U Len, INT08U* Data) { if (m_Datagram_Tx_Cnt <= MAX_EC_DATAGRAM_COUNT)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:136: void ecx_adddatagram(INT08U Cmd, INT16U Adp, INT16U Ado, INT16U Len, INT08U* Data)`
  - `Core/Src/ecat_combase.c:573: ecx_adddatagram(EC_CMD_LRD, LO_WORD(LAddr), HI_WORD(LAddr), Len, Data);`
  - `Core/Src/ecat_combase.c:592: ecx_adddatagram(EC_CMD_LRW, LO_WORD(LAddr), HI_WORD(LAddr), Len, Data);`
  - `Core/Src/ecat_combase.c:1147: ecx_adddatagram(EC_CMD_FRMW, DCrs, ECT_REG_DCSYSTIME, sizeof(DCtime), (void*)&DCtE);`
  - `Core/Src/ecat_main.c:691: ecx_adddatagram(EC_CMD_FPRD, *(configlst + slcnt), ECT_REG_ALSTAT,`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_Transmit_Data

- **종류**: 함수
- **시그니처**: `INT08U EC_Transmit_Data(void)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:180: INT08U EC_Transmit_Data(void)`
  - `Core/Src/ecat_combase.c:302: ret = EC_Transmit_Data();`
  - `Core/Src/ecat_combase.c:355: ret = EC_Transmit_Data();`
  - `Core/Src/ecat_combase.c:408: ret = EC_Transmit_Data();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_Receive_Data_Process

- **종류**: 함수
- **시그니처**: `INT08U EC_Receive_Data_Process(void)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:240: INT08U EC_Receive_Data_Process(void)`
  - `Core/Src/ecat_combase.c:313: EC_Receive_Data_Process();`
  - `Core/Src/ecat_combase.c:366: EC_Receive_Data_Process();`
  - `Core/Src/ecat_combase.c:418: EC_Receive_Data_Process();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_Data_Process

- **종류**: 함수
- **시그니처**: `INT08U EC_Data_Process(void)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:291: INT08U EC_Data_Process(void)`
  - `Core/Src/ecat_combase.c:346: INT08U EC_Data_Process(INT32U nTimeOut)`
  - `Core/Src/ecat_combase.c:460: nEC_Recieve_Flag = EC_Data_Process(1000);`
  - `Core/Src/ecat_combase.c:613: if (EC_Data_Process(100))`
  - `Core/Src/ecat_combase.c:650: if (EC_Data_Process(100))`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_Data_Process_1

- **종류**: 함수
- **시그니처**: `INT08U EC_Data_Process_1(INT32U nTimeOut)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:398: INT08U EC_Data_Process_1(INT32U nTimeOut)`
  - `Core/Src/ecat_combase.c:981: if (EC_Data_Process_1(100))`
  - `Core/Src/ecat_combase.c:1089: if (EC_Data_Process_1(500))`
  - `Core/Src/ecat_combase.c:1149: if (EC_Data_Process_1(1000))		// MX-EXT Test ï¿½ï¿½ Disconnect ï¿½ß»ï¿½`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_MultiData_Process

- **종류**: 함수
- **시그니처**: `INT16U EC_MultiData_Process(int n, INT08U* pReadData, INT16U nDataSize, int timeout)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:453: INT16U EC_MultiData_Process(int n, INT08U* pReadData, INT16U nDataSize, int timeout)`
  - `Core/Src/ecat_main.c:695: wkc = EC_MultiData_Process(n, (INT08U*)slstatlst, sizeof(EC_Alstatus_t), timeout);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_APRD_SetDatagram

- **종류**: 함수
- **시그니처**: `void EC_APRD_SetDatagram(INT16U Adp, INT16U Ado, INT16U Len, void *Data)`
- **정의 위치**: `Core/Src/ecat_combase.c`
- **인근 주석**: ecx_adddatagram(m_Datagram_Tx_Cnt++, EC_CMD_APRD, Adp, Ado, Len, Data);
- **용도(읽는 법)**: 헤더 주석: ecx_adddatagram(m_Datagram_Tx_Cnt++, EC_CMD_APRD, Adp, Ado, Len, Data);
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:481: void EC_APRD_SetDatagram(INT16U Adp, INT16U Ado, INT16U Len, void *Data)`
  - `Core/Src/ecat_combase.c:714: EC_APRD_SetDatagram(ADP, ADO, length, data);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

