# Vol.08 — 기타 함수 (26)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

com_tml 등.

---

### arp_update

- **종류**: 함수
- **시그니처**: `static void arp_update(INT32U IPAddr, INT08U* MacAddr)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpgatcpipstack.c:138: static void arp_update(INT32U IPAddr, INT08U* MacAddr)`
  - `Core/Src/fpgatcpipstack.c:181: arp_update(IPBUF->SrcIPAddr, (INT08U*)ETHBUF->SrcMacAddr);`
  - `Core/Src/fpgatcpipstack.c:200: arp_update(ARPBUF->SenderIPAddr, (INT08U*)ARPBUF->EthHdr.SrcMacAddr);`
  - `Core/Src/fpgatcpipstack.c:219: arp_update(ARPBUF->SenderIPAddr, (INT08U*)ARPBUF->SenderMacAddr);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### arp_ipin

- **종류**: 함수
- **시그니처**: `static void arp_ipin(void) { // if( (IPBUF->SrcIPAddr & SubnetMask) == (HostIPAddr & SubnetMask) )`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpgatcpipstack.c:177: static void arp_ipin(void)`
  - `Core/Src/fpgatcpipstack.c:874: arp_ipin();     // add the source to the ARP cache`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### arp_arpin

- **종류**: 함수
- **시그니처**: `static void arp_arpin(void) { if (EthLen < sizeof(arp_hdrSTR))`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpgatcpipstack.c:185: static void arp_arpin(void)`
  - `Core/Src/fpgatcpipstack.c:865: arp_arpin();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### makeArpPacket

- **종류**: 함수
- **시그니처**: `static void makeArpPacket(INT32U dstIpAddr)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpgatcpipstack.c:225: static void makeArpPacket(INT32U dstIpAddr)`
  - `Core/Src/fpgatcpipstack.c:282: makeArpPacket(ipaddr);`
  - `Core/Src/fpgatcpipstack.c:737: makeArpPacket(GatewayIPAddr);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### arp_out

- **종류**: 함수
- **시그니처**: `static void arp_out(void)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpgatcpipstack.c:242: static void arp_out(void)`
  - `Core/Src/fpgatcpipstack.c:485: arp_out();`
  - `Core/Src/fpgatcpipstack.c:880: arp_out();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ip_sum_calc

- **종류**: 함수
- **시그니처**: `static INT16U ip_sum_calc(INT08U* buff, INT16U len)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpgatcpipstack.c:298: static INT16U ip_sum_calc(INT08U* buff, INT16U len)`
  - `Core/Src/fpgatcpipstack.c:468: IPBUF->CheckSum = HTONS(ip_sum_calc((INT08U*)&IPBUF->VHL, 20));`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### tcpudpicmp_sum_calc

- **종류**: 함수
- **시그니처**: `static INT16U tcpudpicmp_sum_calc(INT08U* buff, INT16U len)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpgatcpipstack.c:316: static INT16U tcpudpicmp_sum_calc(INT08U* buff, INT16U len)`
  - `Core/Src/fpgatcpipstack.c:516: chksum = tcpudpicmp_sum_calc((INT08U*)&TCPBUF->SrcPort, EthLen - 20);`
  - `Core/Src/fpgatcpipstack.c:549: chksum = tcpudpicmp_sum_calc((INT08U*)&TCPBUF->SrcPort, EthLen - 20);`
  - `Core/Src/fpgatcpipstack.c:583: chksum = tcpudpicmp_sum_calc((INT08U*)&UDPBUF->SrcPort, EthLen - 20);`
  - `Core/Src/fpgatcpipstack.c:790: chksum = tcpudpicmp_sum_calc((INT08U*)&TCPBUF->SrcPort, EthLen - 20);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### icmp_process

- **종류**: 함수
- **시그니처**: `static void icmp_process(void)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpgatcpipstack.c:353: static void icmp_process(void)`
  - `Core/Src/fpgatcpipstack.c:711: case IP_PROTO_ICMP: icmp_process();   break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### tcp_process

- **종류**: 함수
- **시그니처**: `static void tcp_process(void)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpgatcpipstack.c:377: static void tcp_process(void)`
  - `Core/Src/fpgatcpipstack.c:712: case IP_PROTO_TCP: tcp_process();    break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ip_send

- **종류**: 함수
- **시그니처**: `static void ip_send(void)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpgatcpipstack.c:12: static void ip_send(void);`
  - `Core/Src/fpgatcpipstack.c:457: static void ip_send(void)`
  - `Core/Src/fpgatcpipstack.c:520: ip_send();`
  - `Core/Src/fpgatcpipstack.c:553: ip_send();`
  - `Core/Src/fpgatcpipstack.c:587: ip_send();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### put_tcpip_ethernet_packet

- **종류**: 함수
- **시그니처**: `static void put_tcpip_ethernet_packet(INT08U* buf, INT16S len)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpgatcpipstack.c:471: static void put_tcpip_ethernet_packet(INT08U* buf, INT16S len)`
  - `Core/Src/fpgatcpipstack.c:486: put_tcpip_ethernet_packet((INT08U*)&EthData, EthLen);`
  - `Core/Src/fpgatcpipstack.c:738: put_tcpip_ethernet_packet((INT08U*)&EthData, sizeof(arp_hdrSTR));`
  - `Core/Src/fpgatcpipstack.c:868: put_tcpip_ethernet_packet((INT08U*)&EthData, EthLen);`
  - `Core/Src/fpgatcpipstack.c:881: put_tcpip_ethernet_packet((INT08U*)&EthData, EthLen);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### get_tcpip_ethernet_packet

- **종류**: 함수
- **시그니처**: `static INT16U get_tcpip_ethernet_packet(INT08U* buf, INT16S len)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpgatcpipstack.c:476: static INT16U get_tcpip_ethernet_packet(INT08U* buf, INT16S len)`
  - `Core/Src/fpgatcpipstack.c:859: EthLen = get_tcpip_ethernet_packet((INT08U*)EthData, MAX_ETH_DATA_SIZE);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### FpgatransferEthData

- **종류**: 함수
- **시그니처**: `static void FpgatransferEthData(void) { if (EthLen > 0)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpgatcpipstack.c:481: static void FpgatransferEthData(void)`
  - `Core/Src/fpgatcpipstack.c:521: FpgatransferEthData();`
  - `Core/Src/fpgatcpipstack.c:554: FpgatransferEthData();`
  - `Core/Src/fpgatcpipstack.c:588: FpgatransferEthData();`
  - `Core/Src/fpgatcpipstack.c:795: FpgatransferEthData();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Fpga_tcp_send

- **종류**: 함수
- **시그니처**: `void Fpga_tcp_send(INT08U s, INT08U* data, INT16U length)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/dev_SRM.c:202: Fpga_tcp_send(nSocket, (INT08U*)&ModbusTxPkt.TransactionID, len + 6);`
  - `Core/Src/dev_SRM.c:245: Fpga_tcp_send(nSocket, (INT08U*)&ModbusTxPkt.TransactionID, len + 6);`
  - `Core/Src/dev_SRM.c:309: Fpga_tcp_send(nSocket, (INT08U*)&ModbusTxPkt.TransactionID, len + 6);`
  - `Core/Src/fpgatcpipstack.c:491: void Fpga_tcp_send(INT08U s, INT08U* data, INT16U length)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### send_tcp_ack

- **종류**: 함수
- **시그니처**: `void send_tcp_ack(INT08U s)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpgatcpipstack.c:14: void send_tcp_ack(INT08U s);`
  - `Core/Src/fpgatcpipstack.c:407: send_tcp_ack(i);`
  - `Core/Src/fpgatcpipstack.c:440: send_tcp_ack(i);`
  - `Core/Src/fpgatcpipstack.c:527: void send_tcp_ack(INT08U s)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Fpga_udp_send

- **종류**: 함수
- **시그니처**: `void Fpga_udp_send(INT32U ipAddr, INT16U rport, INT16U lport)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpgatcpipstack.c:557: void Fpga_udp_send(INT32U ipAddr, INT16U rport, INT16U lport)`
  - `Core/Src/fpgatcpipstack.c:601: Fpga_udp_send(IPAddr, RemotePort, LocalPort);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### sendUdpTxBuf_Downlink

- **종류**: 함수
- **시그니처**: `void sendUdpTxBuf_Downlink() { if (IPAddr && RemotePort && LocalPort)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:297: sendUdpTxBuf_Downlink();`
  - `Core/Src/fpgatcpipstack.c:597: void sendUdpTxBuf_Downlink()`
  - `Core/Inc/com_tml.h:135: extern void sendUdpTxBuf_Downlink();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### save_setup_func_ip_port

- **종류**: 함수
- **시그니처**: `void save_setup_func_ip_port(void)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: Save_*: RAM(m_St) + BKPSRAM 기록. UART 즉시 송신 아님. 지상반 0x30 폴링.
- **코드 참조 예**:
  - `Core/Src/fpgatcpipstack.c:606: void save_setup_func_ip_port(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### AnalysisUDP

- **종류**: 함수
- **시그니처**: `INT08U AnalysisUDP(INT08U* data, INT16U len, INT32U ipAddr, INT16U lport, INT16U rport)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpgatcpipstack.c:619: INT08U AnalysisUDP(INT08U* data, INT16U len, INT32U ipAddr, INT16U lport, INT16U rport)`
  - `Core/Src/fpgatcpipstack.c:683: AnalysisUDP((INT08U*)&EthData[EthHdrLen + 28], EthLen, UDPBUF->SrcIPAddr, HTONS(UDPBUF->DstPort), HT`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### udp_process

- **종류**: 함수
- **시그니처**: `static void udp_process(void)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpgatcpipstack.c:662: static void udp_process(void)`
  - `Core/Src/fpgatcpipstack.c:713: case IP_PROTO_UDP: udp_process();    break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ip_process

- **종류**: 함수
- **시그니처**: `static void ip_process(void)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpgatcpipstack.c:694: static void ip_process(void)`
  - `Core/Src/fpgatcpipstack.c:877: ip_process();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### checkGatewayMAC

- **종류**: 함수
- **시그니처**: `static void checkGatewayMAC(void)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpgatcpipstack.c:720: static void checkGatewayMAC(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### getFpgaTcpSocketState

- **종류**: 함수
- **시그니처**: `INT08U getFpgaTcpSocketState(INT08U s)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_udp.c:814: * @brief  getFpgaTcpSocketState() 대체 함수`
  - `Core/Src/dev_SRM.c:66077: s_now_SSR = getFpgaTcpSocketState(1);`
  - `Core/Src/dev_SRM.c:67992: s_now_SSR = getFpgaTcpSocketState(0);`
  - `Core/Src/dev_SRM.c:68057: s_now_SSR = getFpgaTcpSocketState(0);`
  - `Core/Src/fpgatcpipstack.c:747: INT08U getFpgaTcpSocketState(INT08U s)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### getFpgaTcpSocketState_1

- **종류**: 함수
- **시그니처**: `INT08U getFpgaTcpSocketState_1(INT08U s) { if (s < MAX_TCP_SOCKET_COUNT)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/dev_SRM.c:66041: s_now_SSR = getFpgaTcpSocketState_1(1);`
  - `Core/Src/fpgatcpipstack.c:752: INT08U getFpgaTcpSocketState_1(INT08U s)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### send_tcp_connect_request

- **종류**: 함수
- **시그니처**: `void send_tcp_connect_request(INT08U s)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpgatcpipstack.c:764: void send_tcp_connect_request(INT08U s)`
  - `Core/Src/fpgatcpipstack.c:811: send_tcp_connect_request(0);`
  - `Core/Src/fpgatcpipstack.c:825: send_tcp_connect_request(nSocket);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

