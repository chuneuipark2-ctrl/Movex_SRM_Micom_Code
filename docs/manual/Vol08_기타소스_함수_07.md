# Vol.08 — 기타 함수 (7)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

com_tml 등.

---

### Set_Trav_InPostion_Hysteresis

- **종류**: 함수
- **시그니처**: `void Set_Trav_InPostion_Hysteresis(ParamTravelDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2946: void Set_Trav_InPostion_Hysteresis(ParamTravelDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:4893: Set_Trav_InPostion_Hysteresis(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:114: extern void Set_Trav_InPostion_Hysteresis(ParamTravelDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Trav_InPostion_Hysteresis_1

- **종류**: 함수
- **시그니처**: `void Set_Trav_InPostion_Hysteresis_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pWrBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2960: void Set_Trav_InPostion_Hysteresis_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:4937: Set_Trav_InPostion_Hysteresis_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:115: extern void Set_Trav_InPostion_Hysteresis_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pWrB`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Trav_Manual_Drive

- **종류**: 함수
- **시그니처**: `void Set_Trav_Manual_Drive(ParamTravelDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2977: void Set_Trav_Manual_Drive(ParamTravelDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:4894: Set_Trav_Manual_Drive(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:116: extern void Set_Trav_Manual_Drive(ParamTravelDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Trav_Manual_Drive_1

- **종류**: 함수
- **시그니처**: `void Set_Trav_Manual_Drive_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pWrBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2997: void Set_Trav_Manual_Drive_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:4940: if (pCom->Data[3] & cbits[1]) Set_Trav_Manual_Drive_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:117: extern void Set_Trav_Manual_Drive_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pWrBuf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Trav_BrakeOn_DelayTime

- **종류**: 함수
- **시그니처**: `void Set_Trav_BrakeOn_DelayTime(ParamTravelDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:3019: void Set_Trav_BrakeOn_DelayTime(ParamTravelDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:4895: Set_Trav_BrakeOn_DelayTime(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:118: extern void Set_Trav_BrakeOn_DelayTime(ParamTravelDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Trav_BrakeOn_DelayTime_1

- **종류**: 함수
- **시그니처**: `void Set_Trav_BrakeOn_DelayTime_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pWrBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:3034: void Set_Trav_BrakeOn_DelayTime_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:4941: if (pCom->Data[3] & cbits[2]) Set_Trav_BrakeOn_DelayTime_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:119: extern void Set_Trav_BrakeOn_DelayTime_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pWrBuf)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Trav_Origin_Position

- **종류**: 함수
- **시그니처**: `void Set_Trav_Origin_Position(ParamTravelDriveSTR* pSetting) { } INT08U Set_Trav_Home_Speed_Mode(INT08U nSetValue) { if (nSetValue > 6)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:3051: void Set_Trav_Origin_Position(ParamTravelDriveSTR* pSetting)`
  - `Core/Src/dev_SRM.c:52721: Set_Trav_Origin_Position(&nTravParam);`
  - `Core/Inc/SRM_Parameter.h:120: extern void Set_Trav_Origin_Position(ParamTravelDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Trav_Home_Position

- **종류**: 함수
- **시그니처**: `INT08U Set_Trav_Home_Position(ParamTravelDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: HOME/RETURN: 원점복귀 vs 에러복귀 RunMode 혼재. grep으로 확인.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:3100: INT08U Set_Trav_Home_Position(ParamTravelDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:4897: Set_Trav_Home_Position(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:125: extern INT08U Set_Trav_Home_Position(ParamTravelDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Trav_Home_Position_1

- **종류**: 함수
- **시그니처**: `INT08U Set_Trav_Home_Position_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pWrBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: HOME/RETURN: 원점복귀 vs 에러복귀 RunMode 혼재. grep으로 확인.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:3114: INT08U Set_Trav_Home_Position_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:4943: if (pCom->Data[3] & cbits[4]) is_save = Set_Trav_Home_Position_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:126: extern INT08U Set_Trav_Home_Position_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pWrBuf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Trav_Maintenance_Speed_Mode

- **종류**: 함수
- **시그니처**: `INT08U Set_Trav_Maintenance_Speed_Mode(INT08U nSetValue) { if (nSetValue > 6)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:3127: INT08U Set_Trav_Maintenance_Speed_Mode(INT08U nSetValue)`
  - `Core/Src/SRM_Parameter.c:3190: pSetting->Maintenance_Spd_Mode = Set_Trav_Maintenance_Speed_Mode(pSetting->Maintenance_Spd_Mode);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Trav_Maintenance_Position

- **종류**: 함수
- **시그니처**: `INT08U Set_Trav_Maintenance_Position(ParamTravelDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **인근 주석**: INT08U nSaveFlag = 0; INT32S nTmpPostion = 0;
- **용도(읽는 법)**: 헤더 주석: INT08U nSaveFlag = 0; INT32S nTmpPostion = 0;
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:3185: INT08U Set_Trav_Maintenance_Position(ParamTravelDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:4898: Set_Trav_Maintenance_Position(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:129: extern INT08U Set_Trav_Maintenance_Position(ParamTravelDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Trav_Maintenance_Position_1

- **종류**: 함수
- **시그니처**: `INT08U Set_Trav_Maintenance_Position_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pWrBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **인근 주석**: INT08U nSaveFlag = 0; INT32S nTmpPostion = 0;
- **용도(읽는 법)**: 헤더 주석: INT08U nSaveFlag = 0; INT32S nTmpPostion = 0;
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:3201: INT08U Set_Trav_Maintenance_Position_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:4944: if (pCom->Data[3] & cbits[5]) is_save = Set_Trav_Maintenance_Position_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:130: extern INT08U Set_Trav_Maintenance_Position_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pW`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Trav_DecelationRange_Mode

- **종류**: 함수
- **시그니처**: `INT08U Set_Trav_DecelationRange_Mode(ParamTravelDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:3220: INT08U Set_Trav_DecelationRange_Mode(ParamTravelDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:4899: Set_Trav_DecelationRange_Mode(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:132: extern INT08U Set_Trav_DecelationRange_Mode(ParamTravelDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Trav_DecelationRange_Mode_1

- **종류**: 함수
- **시그니처**: `INT08U Set_Trav_DecelationRange_Mode_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pWrBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:3255: INT08U Set_Trav_DecelationRange_Mode_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:4945: if (pCom->Data[3] & cbits[6]) is_save = Set_Trav_DecelationRange_Mode_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:133: extern INT08U Set_Trav_DecelationRange_Mode_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pW`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Trav_SoftLimit_Error

- **종류**: 함수
- **시그니처**: `INT08U Set_Trav_SoftLimit_Error(ParamTravelDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:3294: INT08U Set_Trav_SoftLimit_Error(ParamTravelDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:4900: Set_Trav_SoftLimit_Error(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:135: extern INT08U Set_Trav_SoftLimit_Error(ParamTravelDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Trav_SoftLimit_Error_1

- **종류**: 함수
- **시그니처**: `INT08U Set_Trav_SoftLimit_Error_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pWrBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:3329: INT08U Set_Trav_SoftLimit_Error_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:4946: if (pCom->Data[3] & cbits[7]) is_save = Set_Trav_SoftLimit_Error_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:136: extern INT08U Set_Trav_SoftLimit_Error_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pWrBuf)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Trav_SoftDecelation_Error

- **종류**: 함수
- **시그니처**: `INT08U Set_Trav_SoftDecelation_Error(ParamTravelDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:3368: INT08U Set_Trav_SoftDecelation_Error(ParamTravelDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:4901: Set_Trav_SoftDecelation_Error(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:137: extern INT08U Set_Trav_SoftDecelation_Error(ParamTravelDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Trav_SoftDecelation_Error_1

- **종류**: 함수
- **시그니처**: `INT08U Set_Trav_SoftDecelation_Error_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pWrBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:3397: INT08U Set_Trav_SoftDecelation_Error_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:4950: if (pCom->Data[4] & cbits[0]) is_save = Set_Trav_SoftDecelation_Error_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:138: extern INT08U Set_Trav_SoftDecelation_Error_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pW`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Trav_OriginDog_Postion_Error

- **종류**: 함수
- **시그니처**: `INT08U Set_Trav_OriginDog_Postion_Error(ParamTravelDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:3438: INT08U Set_Trav_OriginDog_Postion_Error(ParamTravelDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:4902: Set_Trav_OriginDog_Postion_Error(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:140: extern INT08U Set_Trav_OriginDog_Postion_Error(ParamTravelDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Trav_OriginDog_Postion_Error_1

- **종류**: 함수
- **시그니처**: `INT08U Set_Trav_OriginDog_Postion_Error_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pWrBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:3467: INT08U Set_Trav_OriginDog_Postion_Error_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pWrBuf`
  - `Core/Src/com_tml.c:4951: if (pCom->Data[4] & cbits[1]) is_save = Set_Trav_OriginDog_Postion_Error_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:141: extern INT08U Set_Trav_OriginDog_Postion_Error_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR*`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Trav_DecelDog_Position_Error

- **종류**: 함수
- **시그니처**: `INT08U Set_Trav_DecelDog_Position_Error(ParamTravelDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:3503: INT08U Set_Trav_DecelDog_Position_Error(ParamTravelDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:4903: Set_Trav_DecelDog_Position_Error(pSetParam);`
  - `Core/Src/dev_SRM.c:52730: Set_Trav_DecelDog_Position_Error(&nTravParam);`
  - `Core/Inc/SRM_Parameter.h:143: extern INT08U Set_Trav_DecelDog_Position_Error(ParamTravelDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Trav_DecelDog_Postion_Error_1

- **종류**: 함수
- **시그니처**: `INT08U Set_Trav_DecelDog_Postion_Error_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pWrBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:3532: INT08U Set_Trav_DecelDog_Postion_Error_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:4952: if (pCom->Data[4] & cbits[2]) is_save = Set_Trav_DecelDog_Postion_Error_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:144: extern INT08U Set_Trav_DecelDog_Postion_Error_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* `

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Trav_Invertor_Param

- **종류**: 함수
- **시그니처**: `void Set_Trav_Invertor_Param(ParamTravelDriveSTR* pSetting) { if (pSetting->InvParam.RefOffset < (-999999))`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:3570: void Set_Trav_Invertor_Param(ParamTravelDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:4904: Set_Trav_Invertor_Param(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:146: extern void Set_Trav_Invertor_Param(ParamTravelDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Trav_Invertor_Param_1

- **종류**: 함수
- **시그니처**: `void Set_Trav_Invertor_Param_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pWrBuf) { if (pSetting->InvParam.RefOffset < (-999999))`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:3598: void Set_Trav_Invertor_Param_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:4954: if (pCom->Data[4] & cbits[3]) Set_Trav_Invertor_Param_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:147: extern void Set_Trav_Invertor_Param_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pWrBuf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Save_Travel_Drive_Param

- **종류**: 함수
- **시그니처**: `void Save_Travel_Drive_Param(ParamTravelDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: Save_*: RAM(m_St) + BKPSRAM 기록. UART 즉시 송신 아님. 지상반 0x30 폴링.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:3623: void Save_Travel_Drive_Param(ParamTravelDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:4978: Save_Travel_Drive_Param(&nWriteParam);`
  - `Core/Src/com_tml.c:4984: Save_Travel_Drive_Param(pSetParam);`
  - `Core/Src/dev_SRM.c:52732: Save_Travel_Drive_Param(&nTravParam);`
  - `Core/Inc/SRM_Parameter.h:149: extern void Save_Travel_Drive_Param(ParamTravelDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

