# Vol.08 — 기타 함수 (28)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

com_tml 등.

---

### MX_USART3_UART_Init

- **종류**: 함수
- **시그니처**: `static void MX_USART3_UART_Init(void)`
- **정의 위치**: `Core/Src/main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/main.c:137: static void MX_USART3_UART_Init(void);`
  - `Core/Src/main.c:791: MX_USART3_UART_Init();`
  - `Core/Src/main.c:1312: static void MX_USART3_UART_Init(void)`
  - `Core/Src/main.c:1654: MX_USART3_UART_Init();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### MX_USART6_UART_Init

- **종류**: 함수
- **시그니처**: `static void MX_USART6_UART_Init(void)`
- **정의 위치**: `Core/Src/main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/main.c:140: static void MX_USART6_UART_Init(void);`
  - `Core/Src/main.c:794: MX_USART6_UART_Init();`
  - `Core/Src/main.c:1349: static void MX_USART6_UART_Init(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### MX_DMA_Init

- **종류**: 함수
- **시그니처**: `static void MX_DMA_Init(void)`
- **정의 위치**: `Core/Src/main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/main.c:135: static void MX_DMA_Init(void);`
  - `Core/Src/main.c:789: MX_DMA_Init();`
  - `Core/Src/main.c:1384: static void MX_DMA_Init(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### MX_FMC_Init

- **종류**: 함수
- **시그니처**: `static void MX_FMC_Init(void)`
- **정의 위치**: `Core/Src/main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/main.c:139: static void MX_FMC_Init(void);`
  - `Core/Src/main.c:793: MX_FMC_Init();`
  - `Core/Src/main.c:1398: static void MX_FMC_Init(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### MX_GPIO_Init

- **종류**: 함수
- **시그니처**: `static void MX_GPIO_Init(void)`
- **정의 위치**: `Core/Src/main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/main.c:134: static void MX_GPIO_Init(void);`
  - `Core/Src/main.c:788: MX_GPIO_Init();`
  - `Core/Src/main.c:1491: static void MX_GPIO_Init(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### HAL_TIM_PeriodElapsedCallback

- **종류**: 함수
- **시그니처**: `void HAL_TIM_PeriodElapsedCallback(TIM_HandleTypeDef * htim)`
- **정의 위치**: `Core/Src/main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/main.c:1625: void HAL_TIM_PeriodElapsedCallback(TIM_HandleTypeDef * htim)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### HAL_UART_ErrorCallback

- **종류**: 함수
- **시그니처**: `void HAL_UART_ErrorCallback(UART_HandleTypeDef * huart) { if (huart->Instance == USART3)`
- **정의 위치**: `Core/Src/main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/main.c:1648: void HAL_UART_ErrorCallback(UART_HandleTypeDef * huart)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Error_Handler

- **종류**: 함수
- **시그니처**: `void Error_Handler(void)`
- **정의 위치**: `Core/Src/main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/can.c:43: Error_Handler();`
  - `Core/Src/can.c:119: Error_Handler();`
  - `Core/Src/can.c:158: Error_Handler();`
  - `Core/Src/flash.c:92: Error_Handler();`
  - `Core/Src/main.c:894: Error_Handler();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### assert_failed

- **종류**: 함수
- **시그니처**: `void assert_failed(uint8_t *file, uint32_t line)`
- **정의 위치**: `Core/Src/main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/main.c:1689: void assert_failed(uint8_t *file, uint32_t line)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Get_STM32_Dev_ID

- **종류**: 함수
- **시그니처**: `INT32U Get_STM32_Dev_ID(void)`
- **정의 위치**: `Core/Src/mcu_util.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/mcu_util.c:53: INT32U Get_STM32_Dev_ID(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Get_STM32_Rev_ID

- **종류**: 함수
- **시그니처**: `INT32U Get_STM32_Rev_ID(void)`
- **정의 위치**: `Core/Src/mcu_util.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/mcu_util.c:73: INT32U Get_STM32_Rev_ID(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Get_STM32_UniqueID

- **종류**: 함수
- **시그니처**: `INT32U Get_STM32_UniqueID(INT08U inx)`
- **정의 위치**: `Core/Src/mcu_util.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/mcu_util.c:97: INT32U Get_STM32_UniqueID(INT08U inx)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Get_STM32_FlashSize

- **종류**: 함수
- **시그니처**: `INT16U Get_STM32_FlashSize(void)`
- **정의 위치**: `Core/Src/mcu_util.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/mcu_util.c:126: INT16U Get_STM32_FlashSize(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### getDoW

- **종류**: 함수
- **시그니처**: `INT32U getDoW(INT32U y, INT32U m, INT32U d) { // Zeller's formula: 0=Sunday if (m < 3)`
- **정의 위치**: `Core/Src/rtc.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/rtc.c:82: INT32U getDoW(INT32U y, INT32U m, INT32U d)`
  - `Core/Src/rtc.c:153: utc_struct.tm_wday = (int)getDoW(`
  - `Core/Src/rtc.c:228: dow = getDoW(year, 3, 1);           // 3월 1일 요일`
  - `Core/Src/rtc.c:238: dow = getDoW(year, 11, 1);          // 11월 1일 요일`
  - `Core/Src/rtc.c:272: dow = getDoW(year, 3, 1); // Get day of week for March 1st`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### isDaylightSavingTime

- **종류**: 함수
- **시그니처**: `INT08U isDaylightSavingTime(INT32U year, INT32U month, INT32U day, INT32U hour)`
- **정의 위치**: `Core/Src/rtc.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/rtc.c:218: INT08U isDaylightSavingTime(INT32U year, INT32U month, INT32U day, INT32U hour)`
  - `Core/Src/rtc.c:427: if (isDaylightSavingTime(local_year, local_month, local_day, local_hour))`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### isDaylightSavingTime_Utc

- **종류**: 함수
- **시그니처**: `INT08U isDaylightSavingTime_Utc(INT32U nUtcTime)`
- **정의 위치**: `Core/Src/rtc.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/rtc.c:248: INT08U isDaylightSavingTime_Utc(INT32U nUtcTime)`
  - `Core/Src/rtc.c:311: if (isDaylightSavingTime_Utc(nUtcTime))`
  - `Core/Src/rtc.c:421: *        UTC 기준 함수(isDaylightSavingTime_Utc)를 쓰면 이중 변환 오류 발생 */`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### setTimeDate_Rev2

- **종류**: 함수
- **시그니처**: `void setTimeDate_Rev2(INT32U nUtcTime)`
- **정의 위치**: `Core/Src/rtc.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:493: setTimeDate_Rev2(pCtrl->SysTime);`
  - `Core/Src/com_tml.c:2026: setTimeDate_Rev2(nSysTime_Utc);`
  - `Core/Src/rtc.c:302: void setTimeDate_Rev2(INT32U nUtcTime)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### getTimeDate_Rev2

- **종류**: 함수
- **시그니처**: `INT32U getTimeDate_Rev2(void)`
- **정의 위치**: `Core/Src/rtc.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:931: m_DevStatus.SysTime = getTimeDate_Rev2();`
  - `Core/Src/com_tml.c:494: m_DevStatus.SysTime = getTimeDate_Rev2();`
  - `Core/Src/com_tml.c:2028: m_DevStatus.SysTime = getTimeDate_Rev2();`
  - `Core/Src/ecat_dc.c:229: mastertime.sec = getTimeDate_Rev2();`
  - `Core/Src/main.c:598: m_DevStatus.SysTime = getTimeDate_Rev2();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### loadBKSramSTR

- **종류**: 함수
- **시그니처**: `void loadBKSramSTR(void)`
- **정의 위치**: `Core/Src/rtc.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/main.c:529: loadBKSramSTR();`
  - `Core/Src/main.c:545: loadBKSramSTR();`
  - `Core/Src/rtc.c:452: void loadBKSramSTR(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### saveBKSramSTR

- **종류**: 함수
- **시그니처**: `void saveBKSramSTR(void)`
- **정의 위치**: `Core/Src/rtc.c`
- **인근 주석**: m_BKSram.ParaCRC = calcParameterCrc(0); m_TestStatus.Ctrl[DEBUG_BUFFER_11] = m_BKSram.ParaCRC;
- **용도(읽는 법)**: 헤더 주석: m_BKSram.ParaCRC = calcParameterCrc(0); m_TestStatus.Ctrl[DEBUG_BUFFER_11] = m_BKSram.ParaCRC;
- **코드 참조 예**:
  - `Core/Src/com_tml.c:5931: saveBKSramSTR();`
  - `Core/Src/rtc.c:467: void saveBKSramSTR(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### loadBKSramBlock

- **종류**: 함수
- **시그니처**: `INT16U loadBKSramBlock(INT08U* ptr, INT16U len)`
- **정의 위치**: `Core/Src/rtc.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/rtc.c:483: INT16U loadBKSramBlock(INT08U* ptr, INT16U len)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### saveBKSramBlock

- **종류**: 함수
- **시그니처**: `INT16U saveBKSramBlock(INT08U* ptr, INT16U len)`
- **정의 위치**: `Core/Src/rtc.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:33: saveBKSramBlock((INT08U*)m_BKSram.InitDeviceCode, sizeof(DeviceSetting) + (sizeof(INT08U) * 4));`
  - `Core/Src/SRM_Parameter.c:239: saveBKSramBlock((INT08U*)m_BKSram.InitDeviceCode, sizeof(DeviceSetting) + (sizeof(INT08U) * 4));`
  - `Core/Src/alarm.c:313: saveBKSramBlock((INT08U*)&m_BKSram.Graph_Type, sizeof(INT08U) * 6);`
  - `Core/Src/alarm.c:352: saveBKSramBlock((INT08U*)&m_BKSram.Graph_Type, sizeof(INT08U) * 6);`
  - `Core/Src/alarm.c:471: saveBKSramBlock((INT08U*)&m_BKSram.AlarmLog, sizeof(AlarmLogSTR));`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### HAL_MspInit

- **종류**: 함수
- **시그니처**: `void HAL_MspInit(void)`
- **정의 위치**: `Core/Src/stm32f7xx_hal_msp.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/stm32f7xx_hal_msp.c:65: void HAL_MspInit(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### HAL_CAN_MspInit

- **종류**: 함수
- **시그니처**: `void HAL_CAN_MspInit(CAN_HandleTypeDef* hcan)`
- **정의 위치**: `Core/Src/stm32f7xx_hal_msp.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/stm32f7xx_hal_msp.c:93: void HAL_CAN_MspInit(CAN_HandleTypeDef* hcan)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### HAL_CAN_MspDeInit

- **종류**: 함수
- **시그니처**: `void HAL_CAN_MspDeInit(CAN_HandleTypeDef* hcan) { if(hcan->Instance==CAN1)`
- **정의 위치**: `Core/Src/stm32f7xx_hal_msp.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/stm32f7xx_hal_msp.c:137: void HAL_CAN_MspDeInit(CAN_HandleTypeDef* hcan)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

