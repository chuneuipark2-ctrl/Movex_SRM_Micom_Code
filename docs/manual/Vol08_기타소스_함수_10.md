# Vol.08 — 기타 함수 (10)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

com_tml 등.

---

### Set_Fork_Manual_Drive

- **종류**: 함수
- **시그니처**: `void Set_Fork_Manual_Drive(ParamForkDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:5504: void Set_Fork_Manual_Drive(ParamForkDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:5225: Set_Fork_Manual_Drive(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:205: extern void Set_Fork_Manual_Drive(ParamForkDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Fork_Manual_Drive_1

- **종류**: 함수
- **시그니처**: `void Set_Fork_Manual_Drive_1(ParamForkDriveSTR* pSetting, ParamForkDriveSTR* pWrBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:5527: void Set_Fork_Manual_Drive_1(ParamForkDriveSTR* pSetting, ParamForkDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:5269: if (pCom->Data[3] & cbits[1]) Set_Fork_Manual_Drive_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:206: extern void Set_Fork_Manual_Drive_1(ParamForkDriveSTR* pSetting, ParamForkDriveSTR* pWrBuf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Fork_Encoder_Setup

- **종류**: 함수
- **시그니처**: `INT08U Set_Fork_Encoder_Setup(ParamForkDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:5552: INT08U Set_Fork_Encoder_Setup(ParamForkDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:5226: Set_Fork_Encoder_Setup(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:207: extern INT08U Set_Fork_Encoder_Setup(ParamForkDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Fork_Encoder_Setup_1

- **종류**: 함수
- **시그니처**: `INT08U Set_Fork_Encoder_Setup_1(ParamForkDriveSTR* pSetting, ParamForkDriveSTR* pWrBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:5594: INT08U Set_Fork_Encoder_Setup_1(ParamForkDriveSTR* pSetting, ParamForkDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:5270: if (pCom->Data[3] & cbits[2]) is_save = Set_Fork_Encoder_Setup_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:208: extern INT08U Set_Fork_Encoder_Setup_1(ParamForkDriveSTR* pSetting, ParamForkDriveSTR* pWrBuf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Fork_Origin_Offset

- **종류**: 함수
- **시그니처**: `void Set_Fork_Origin_Offset(ParamForkDriveSTR* pSetting) { if (pSetting->OriginOffset_FCL > 0) { if (pSetting->OriginOffset_FCL > MAX_FORK_ORIGIN_OFFSET)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:5628: void Set_Fork_Origin_Offset(ParamForkDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:5227: Set_Fork_Origin_Offset(pSetParam);`
  - `Core/Src/dev_SRM.c:53906: Set_Fork_Origin_Offset(&nFork_Param);`
  - `Core/Inc/SRM_Parameter.h:209: extern void Set_Fork_Origin_Offset(ParamForkDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Fork_Origin_Offset_1

- **종류**: 함수
- **시그니처**: `void Set_Fork_Origin_Offset_1(ParamForkDriveSTR* pSetting, ParamForkDriveSTR* pWrBuf) { if (pSetting->OriginOffset_FCL > 0) { if (pSetting->OriginOffset_FCL > MAX_FORK_ORIGIN_OFFSET)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:5666: void Set_Fork_Origin_Offset_1(ParamForkDriveSTR* pSetting, ParamForkDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:5271: if (pCom->Data[3] & cbits[3]) Set_Fork_Origin_Offset_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:210: extern void Set_Fork_Origin_Offset_1(ParamForkDriveSTR* pSetting, ParamForkDriveSTR* pWrBuf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Fork_Decelation_Control

- **종류**: 함수
- **시그니처**: `INT08U Set_Fork_Decelation_Control(ParamForkDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:5708: INT08U Set_Fork_Decelation_Control(ParamForkDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:5230: Set_Fork_Decelation_Control(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:215: extern INT08U Set_Fork_Decelation_Control(ParamForkDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Fork_Decelation_Control_1

- **종류**: 함수
- **시그니처**: `INT08U Set_Fork_Decelation_Control_1(ParamForkDriveSTR* pSetting, ParamForkDriveSTR* pWrBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:5772: INT08U Set_Fork_Decelation_Control_1(ParamForkDriveSTR* pSetting, ParamForkDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:5274: if (pCom->Data[3] & cbits[6]) is_save = Set_Fork_Decelation_Control_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:216: extern INT08U Set_Fork_Decelation_Control_1(ParamForkDriveSTR* pSetting, ParamForkDriveSTR* pWrBuf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Fork_TwinFork_Setup

- **종류**: 함수
- **시그니처**: `void Set_Fork_TwinFork_Setup(ParamForkDriveSTR* pSetting) { } /** * @brief * @param * @retval None */ void Set_Fork_TwinFork_Setup_1(ParamForkDriveSTR* pSetting, ParamForkDriveSTR* pWrBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:5842: void Set_Fork_TwinFork_Setup(ParamForkDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:5231: Set_Fork_TwinFork_Setup(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:217: extern void Set_Fork_TwinFork_Setup(ParamForkDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Fork_Position_UseFlag

- **종류**: 함수
- **시그니처**: `INT08U Set_Fork_Position_UseFlag(ParamForkDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:5862: INT08U Set_Fork_Position_UseFlag(ParamForkDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:5232: Set_Fork_Position_UseFlag(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:219: extern INT08U Set_Fork_Position_UseFlag(ParamForkDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Fork_Position_UseFlag_1

- **종류**: 함수
- **시그니처**: `INT08U Set_Fork_Position_UseFlag_1(ParamForkDriveSTR* pSetting, ParamForkDriveSTR* pWrBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:5892: INT08U Set_Fork_Position_UseFlag_1(ParamForkDriveSTR* pSetting, ParamForkDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:5279: if (pCom->Data[4] & cbits[0]) Set_Fork_Position_UseFlag_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:220: extern INT08U Set_Fork_Position_UseFlag_1(ParamForkDriveSTR* pSetting, ParamForkDriveSTR* pWrBuf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Fork_Position_Depth

- **종류**: 함수
- **시그니처**: `void Set_Fork_Position_Depth(ParamForkDriveSTR* pSetting) { if (m_ExtSEnv.Machine.mType.Fork_Type == FORK_TYPE_SINGLE_DEEP) { //if (m_ExtSEnv2.ForkDrive.ManualDrive.LeftPos > pSetting->ForkPosDepth.mFEL) if (pSetting->ManualDrive.LeftPos > pSetting->ForkPosDepth.mFEL)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:5927: void Set_Fork_Position_Depth(ParamForkDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:5233: Set_Fork_Position_Depth(pSetParam);`
  - `Core/Src/dev_SRM.c:54031: Set_Fork_Position_Depth(&nFork_Param);`
  - `Core/Inc/SRM_Parameter.h:221: extern void Set_Fork_Position_Depth(ParamForkDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Fork_Position_Depth_1

- **종류**: 함수
- **시그니처**: `void Set_Fork_Position_Depth_1(ParamForkDriveSTR* pSetting, ParamForkDriveSTR* pWrBuf) { if (m_ExtSEnv.Machine.mType.Fork_Type == FORK_TYPE_SINGLE_DEEP) { if (m_ExtSEnv2.ForkDrive.ManualDrive.LeftPos > pSetting->ForkPosDepth.mFEL)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:5972: void Set_Fork_Position_Depth_1(ParamForkDriveSTR* pSetting, ParamForkDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:5280: if (pCom->Data[4] & cbits[1]) Set_Fork_Position_Depth_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:222: extern void Set_Fork_Position_Depth_1(ParamForkDriveSTR* pSetting, ParamForkDriveSTR* pWrBuf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Fork_Invertor_Param

- **종류**: 함수
- **시그니처**: `void Set_Fork_Invertor_Param(ParamForkDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:6041: void Set_Fork_Invertor_Param(ParamForkDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:5234: Set_Fork_Invertor_Param(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:223: extern void Set_Fork_Invertor_Param(ParamForkDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Fork_Invertor_Param_1

- **종류**: 함수
- **시그니처**: `void Set_Fork_Invertor_Param_1(ParamForkDriveSTR* pSetting, ParamForkDriveSTR* pWrBuf) { if (pSetting->InvParam.RefOffset < (-999999))`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:6072: void Set_Fork_Invertor_Param_1(ParamForkDriveSTR* pSetting, ParamForkDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:5281: if (pCom->Data[4] & cbits[2]) Set_Fork_Invertor_Param_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:224: extern void Set_Fork_Invertor_Param_1(ParamForkDriveSTR* pSetting, ParamForkDriveSTR* pWrBuf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Save_Fork_Drive_Param

- **종류**: 함수
- **시그니처**: `void Save_Fork_Drive_Param(ParamForkDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: Save_*: RAM(m_St) + BKPSRAM 기록. UART 즉시 송신 아님. 지상반 0x30 폴링.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:6098: void Save_Fork_Drive_Param(ParamForkDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:5305: Save_Fork_Drive_Param(&nWriteParam);`
  - `Core/Src/com_tml.c:5311: Save_Fork_Drive_Param(pSetParam);`
  - `Core/Src/dev_SRM.c:54032: Save_Fork_Drive_Param(&nFork_Param);`
  - `Core/Inc/SRM_Parameter.h:226: extern void Save_Fork_Drive_Param(ParamForkDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Default_Fork_Drive_Param_Init

- **종류**: 함수
- **시그니처**: `void Default_Fork_Drive_Param_Init(void)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:6130: void Default_Fork_Drive_Param_Init(void)`
  - `Core/Src/SRM_Parameter.c:6190: Default_Fork_Drive_Param_Init();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Fork_Drive_Param_Init

- **종류**: 함수
- **시그니처**: `void Fork_Drive_Param_Init()`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:6178: void Fork_Drive_Param_Init()`
  - `Core/Src/main.c:378: Fork_Drive_Param_Init();`
  - `Core/Inc/SRM_Parameter.h:227: extern void Fork_Drive_Param_Init();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Check_Available_Fork_InPostion_Tolerance

- **종류**: 함수
- **시그니처**: `INT08U Check_Available_Fork_InPostion_Tolerance() { if ((m_ExtSEnv2.ForkDrive.InPos_Tolerance < MIN_FORK_IN_POS) || (m_ExtSEnv2.ForkDrive.InPos_Tolerance > MAX_FORK_IN_POS))`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:6241: INT08U Check_Available_Fork_InPostion_Tolerance()`
  - `Core/Src/dev_SRM.c:8759: if (Check_Available_Fork_InPostion_Tolerance())`
  - `Core/Src/dev_SRM.c:9425: if (Check_Available_Fork_InPostion_Tolerance())`
  - `Core/Src/dev_SRM.c:9561: if (Check_Available_Fork_InPostion_Tolerance())`
  - `Core/Src/dev_SRM.c:9790: if (Check_Available_Fork_InPostion_Tolerance())`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Check_Available_Fork_InPostion_Hysteresis

- **종류**: 함수
- **시그니처**: `INT08U Check_Available_Fork_InPostion_Hysteresis() { if ((m_ExtSEnv2.ForkDrive.InPos_Hysteresis < MIN_FORK_HYSTERESIS) || (m_ExtSEnv2.ForkDrive.InPos_Hysteresis > MAX_FORK_HYSTERESIS))`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:6259: INT08U Check_Available_Fork_InPostion_Hysteresis()`
  - `Core/Src/dev_SRM.c:9434: if (Check_Available_Fork_InPostion_Hysteresis())`
  - `Core/Src/dev_SRM.c:9799: if (Check_Available_Fork_InPostion_Hysteresis())`
  - `Core/Src/dev_SRM.c:47772: if (Check_Available_Fork_InPostion_Hysteresis())`
  - `Core/Src/dev_SRM.c:47904: if (Check_Available_Fork_InPostion_Hysteresis())`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Check_Available_Fork_Origin_Offset

- **종류**: 함수
- **시그니처**: `INT08U Check_Available_Fork_Origin_Offset() { if ((m_ExtSEnv2.ForkDrive.OriginOffset_FCL >= MIN_FORK_ORIGIN_OFFSET) && (m_ExtSEnv2.ForkDrive.OriginOffset_FCL <= MAX_FORK_ORIGIN_OFFSET))`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:6277: INT08U Check_Available_Fork_Origin_Offset()`
  - `Core/Src/dev_SRM.c:8768: if (Check_Available_Fork_Origin_Offset())`
  - `Core/Inc/SRM_Parameter.h:232: extern INT08U Check_Available_Fork_Origin_Offset();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_TestStatus

- **종류**: 함수
- **시그니처**: `void Set_TestStatus(INT32S nIndex, INT32S nData) { if ((nIndex >= DEBUG_STR_1) && (nIndex <= DEBUG_STR_32))`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:19: void Set_TestStatus(INT32S nIndex, INT32S nData)`
  - `Core/Src/com_tml.c:1209: Set_TestStatus(DEBUG_STR_52, pCtrl->Value);`
  - `Core/Src/com_tml.c:1210: Set_TestStatus(DEBUG_STR_53, s_TestVal);`
  - `Core/Src/com_tml.c:1919: Set_TestStatus(DEBUG_STR_33, nID);`
  - `Core/Src/com_tml.c:1920: Set_TestStatus(DEBUG_STR_34, nMode);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Clear_TestStatus

- **종류**: 함수
- **시그니처**: `void Clear_TestStatus(INT32S nIndex) { if ((nIndex >= DEBUG_STR_1) && (nIndex <= DEBUG_STR_32))`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:31: void Clear_TestStatus(INT32S nIndex)`
  - `Core/Inc/alarm.h:40: extern void Clear_TestStatus(INT32S nIndex);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### All_Clear_TestStatus

- **종류**: 함수
- **시그니처**: `void All_Clear_TestStatus()`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:43: void All_Clear_TestStatus()`
  - `Core/Src/com_tml.c:1161: All_Clear_TestStatus();`
  - `Core/Inc/alarm.h:41: extern void All_Clear_TestStatus();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_TestControl

- **종류**: 함수
- **시그니처**: `void Set_TestControl(INT32S nIndex, INT32S nData) { if ((nIndex >= DEBUG_CTR_1) && (nIndex <= DEBUG_CTR_16))`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:49: void Set_TestControl(INT32S nIndex, INT32S nData)`
  - `Core/Inc/alarm.h:42: extern void Set_TestControl(INT32S nIndex, INT32S nData);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

