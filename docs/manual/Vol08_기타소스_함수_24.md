# Vol.08 — 기타 함수 (24)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

com_tml 등.

---

### get_FPGA_SSI_Send_Bit

- **종류**: 함수
- **시그니처**: `INT08U get_FPGA_SSI_Send_Bit(void)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:192: INT08U get_FPGA_SSI_Send_Bit(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### set_FPGA_SSI_Send_Bit

- **종류**: 함수
- **시그니처**: `void set_FPGA_SSI_Send_Bit(INT08U bit_cnt)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:601: set_FPGA_SSI_Send_Bit(25);	// Bit 0 ~ 24, Total : 25 bit`
  - `Core/Src/fpga.c:197: void set_FPGA_SSI_Send_Bit(INT08U bit_cnt)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### get_FPGA_SSI_Clk

- **종류**: 함수
- **시그니처**: `INT16U get_FPGA_SSI_Clk(void)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:205: INT16U get_FPGA_SSI_Clk(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### set_FPGA_SSI_Clk

- **종류**: 함수
- **시그니처**: `void set_FPGA_SSI_Clk(INT16U nClk)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:598: set_FPGA_SSI_Clk(nSPI_Divider);`
  - `Core/Src/fpga.c:218: void set_FPGA_SSI_Clk(INT16U nClk)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### get_FPGA_SSI_Read_Data

- **종류**: 함수
- **시그니처**: `INT32U get_FPGA_SSI_Read_Data(void)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:628: nGray_code_pos = get_FPGA_SSI_Read_Data();`
  - `Core/Src/fpga.c:224: INT32U get_FPGA_SSI_Read_Data(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### get_FPGA_PTO_Pulse_Low_Count

- **종류**: 함수
- **시그니처**: `INT32U get_FPGA_PTO_Pulse_Low_Count(void)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:247: INT32U get_FPGA_PTO_Pulse_Low_Count(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### get_FPGA_PTO_Pulse_High_Count

- **종류**: 함수
- **시그니처**: `INT32U get_FPGA_PTO_Pulse_High_Count(void)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:266: INT32U get_FPGA_PTO_Pulse_High_Count(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### set_FPGA_PTO_Pulse_Low_Count

- **종류**: 함수
- **시그니처**: `void set_FPGA_PTO_Pulse_Low_Count(INT32U cnt)`
- **정의 위치**: `Core/Src/fpga.c`
- **인근 주석**: m_FpgaReg->PTO.Pulse_Low_Cnt = cnt; /*
- **용도(읽는 법)**: 헤더 주석: m_FpgaReg->PTO.Pulse_Low_Cnt = cnt; /*
- **코드 참조 예**:
  - `Core/Src/fpga.c:285: void set_FPGA_PTO_Pulse_Low_Count(INT32U cnt)`
  - `Core/Src/fpga.c:343: set_FPGA_PTO_Pulse_Low_Count(cnt);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### set_FPGA_PTO_Pulse_High_Count

- **종류**: 함수
- **시그니처**: `void set_FPGA_PTO_Pulse_High_Count(INT32U cnt)`
- **정의 위치**: `Core/Src/fpga.c`
- **인근 주석**: m_FpgaReg->PTO.Pulse_High_Cnt = cnt; /*
- **용도(읽는 법)**: 헤더 주석: m_FpgaReg->PTO.Pulse_High_Cnt = cnt; /*
- **코드 참조 예**:
  - `Core/Src/fpga.c:296: void set_FPGA_PTO_Pulse_High_Count(INT32U cnt)`
  - `Core/Src/fpga.c:342: set_FPGA_PTO_Pulse_High_Count(cnt);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### set_FPGA_PTO_Pulse_Count_Apply

- **종류**: 함수
- **시그니처**: `void set_FPGA_PTO_Pulse_Count_Apply(INT08U nFlag)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:307: void set_FPGA_PTO_Pulse_Count_Apply(INT08U nFlag)`
  - `Core/Src/fpga.c:313: void set_FPGA_PTO_Pulse_Count_Apply(void)`
  - `Core/Src/fpga.c:340: set_FPGA_PTO_Pulse_Count_Apply(0);`
  - `Core/Src/fpga.c:348: set_FPGA_PTO_Pulse_Count_Apply(1);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### set_FPGA_PTO_Pulse_Freq

- **종류**: 함수
- **시그니처**: `void set_FPGA_PTO_Pulse_Freq(INT32U freq)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:56: set_FPGA_PTO_Pulse_Freq(s_SetPTO_Freq);`
  - `Core/Src/Mcu_Test.c:70: set_FPGA_PTO_Pulse_Freq(s_SetPTO_Freq);`
  - `Core/Src/Mcu_Test.c:786: set_FPGA_PTO_Pulse_Freq(s_Test_Pto_Output);`
  - `Core/Src/Mcu_Test.c:798: set_FPGA_PTO_Pulse_Freq(m_McuTestCtr.PTO_Output);`
  - `Core/Src/fpga.c:319: void set_FPGA_PTO_Pulse_Freq(INT32U freq)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### set_FPGA_EthNet_MAC_Address

- **종류**: 함수
- **시그니처**: `void set_FPGA_EthNet_MAC_Address(INT08U* addr)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/dev_SRM.c:5181: set_FPGA_EthNet_MAC_Address((INT08U*)m_DevStatus.MyMACAddr_1);`
  - `Core/Src/fpga.c:355: void set_FPGA_EthNet_MAC_Address(INT08U* addr)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### set_FPGA_EthNet_IP_Address

- **종류**: 함수
- **시그니처**: `void set_FPGA_EthNet_IP_Address(INT08U* addr)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/dev_SRM.c:5182: set_FPGA_EthNet_IP_Address((INT08U*)m_DevStatus.NetworkInfo_1.MyIP);`
  - `Core/Src/fpga.c:365: void set_FPGA_EthNet_IP_Address(INT08U* addr)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### get_FPGA_EthNet_Read_Buff_Num

- **종류**: 함수
- **시그니처**: `INT08U get_FPGA_EthNet_Read_Buff_Num(void)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:373: INT08U get_FPGA_EthNet_Read_Buff_Num(void)`
  - `Core/Src/fpga.c:432: read_buff_num1 = get_FPGA_EthNet_Read_Buff_Num();`
  - `Core/Src/fpga.c:434: read_buff_num2 = get_FPGA_EthNet_Read_Buff_Num();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### get_FPGA_EthNet_Recv_Buff_Num

- **종류**: 함수
- **시그니처**: `INT08U get_FPGA_EthNet_Recv_Buff_Num(void)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:378: INT08U get_FPGA_EthNet_Recv_Buff_Num(void)`
  - `Core/Src/fpga.c:431: recv_buff_num1 = get_FPGA_EthNet_Recv_Buff_Num();`
  - `Core/Src/fpga.c:433: recv_buff_num2 = get_FPGA_EthNet_Recv_Buff_Num();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### set_FPGA_EthNet_Read_Buff_Num

- **종류**: 함수
- **시그니처**: `void set_FPGA_EthNet_Read_Buff_Num(INT08U num)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:383: void set_FPGA_EthNet_Read_Buff_Num(INT08U num)`
  - `Core/Src/fpga.c:450: set_FPGA_EthNet_Read_Buff_Num(read_buff_num1);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### get_FPGA_EthNet_RxBuff

- **종류**: 함수
- **시그니처**: `INT08U get_FPGA_EthNet_RxBuff(INT16U addr)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:388: INT08U get_FPGA_EthNet_RxBuff(INT16U addr)`
  - `Core/Src/fpga.c:440: len = ((INT16U)get_FPGA_EthNet_RxBuff(2000));`
  - `Core/Src/fpga.c:441: len |= ((INT16U)get_FPGA_EthNet_RxBuff(2001) << 8);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### get_FPGA_EthNet_RxBuff_Len

- **종류**: 함수
- **시그니처**: `void get_FPGA_EthNet_RxBuff_Len(INT16U addr, INT08U* buff, INT16U len)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:393: void get_FPGA_EthNet_RxBuff_Len(INT16U addr, INT08U* buff, INT16U len)`
  - `Core/Src/fpga.c:445: get_FPGA_EthNet_RxBuff_Len(0, (INT08U*)&buff[0], len);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### set_FPGA_EthNet_TxBuff

- **종류**: 함수
- **시그니처**: `void set_FPGA_EthNet_TxBuff(INT16U addr, INT08U data)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:398: void set_FPGA_EthNet_TxBuff(INT16U addr, INT08U data)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### set_FPGA_EthNet_TxBuff_Len

- **종류**: 함수
- **시그니처**: `void set_FPGA_EthNet_TxBuff_Len(INT16U addr, INT08U* buff, INT16U len)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:403: void set_FPGA_EthNet_TxBuff_Len(INT16U addr, INT08U* buff, INT16U len)`
  - `Core/Src/fpga.c:464: set_FPGA_EthNet_TxBuff_Len(0, (INT08U*)&buff[0], buff_size);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### set_FPGA_EthNet_Send_Size

- **종류**: 함수
- **시그니처**: `void set_FPGA_EthNet_Send_Size(INT16U len)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:408: void set_FPGA_EthNet_Send_Size(INT16U len)`
  - `Core/Src/fpga.c:465: set_FPGA_EthNet_Send_Size(buff_size);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### set_FPGA_EthNet_Send_Cmd

- **종류**: 함수
- **시그니처**: `void set_FPGA_EthNet_Send_Cmd(INT08U mode)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:413: void set_FPGA_EthNet_Send_Cmd(INT08U mode)`
  - `Core/Src/fpga.c:466: set_FPGA_EthNet_Send_Cmd(1);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### getFPGAEthernet

- **종류**: 함수
- **시그니처**: `INT16U getFPGAEthernet(INT08U* buff, INT16U buff_size)`
- **정의 위치**: `Core/Src/fpga.c`
- **인근 주석**: INT08U i; INT32U dst_mac; INT32U src_mac; INT32U eth_type;
- **용도(읽는 법)**: 헤더 주석: INT08U i; INT32U dst_mac; INT32U src_mac; INT32U eth_type;
- **코드 참조 예**:
  - `Core/Src/fpga.c:418: INT16U getFPGAEthernet(INT08U* buff, INT16U buff_size)`
  - `Core/Src/fpgatcpipstack.c:478: return getFPGAEthernet((INT08U*)buf, len);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### putFPGAEthernet

- **종류**: 함수
- **시그니처**: `void putFPGAEthernet(INT08U* buff, INT16U buff_size) { if (buff_size <= 1492)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:460: void putFPGAEthernet(INT08U* buff, INT16U buff_size)`
  - `Core/Src/fpgatcpipstack.c:473: putFPGAEthernet((INT08U*)buf, len);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### get_FPGA_EthCat_Status

- **종류**: 함수
- **시그니처**: `INT08U get_FPGA_EthCat_Status(void)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:483: INT08U get_FPGA_EthCat_Status(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

