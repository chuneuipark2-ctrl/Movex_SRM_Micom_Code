# Vol.08 — 기타 함수 (6)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

com_tml 등.

---

### Set_OutputStation_DoubleEntry_AlarmFlag

- **종류**: 함수
- **시그니처**: `INT08U Set_OutputStation_DoubleEntry_AlarmFlag(ParamControlSTR* pSetting) { if (pSetting->Station_DSTLR_Fault_Flag < 2)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **인근 주석**: 0 : not use, 1 : use
- **용도(읽는 법)**: 헤더 주석: 0 : not use, 1 : use
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2197: INT08U Set_OutputStation_DoubleEntry_AlarmFlag(ParamControlSTR* pSetting)`
  - `Core/Src/com_tml.c:4760: if (pCom->Data[2] & cbits[0])	is_save = Set_OutputStation_DoubleEntry_AlarmFlag(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:88: extern INT08U Set_OutputStation_DoubleEntry_AlarmFlag(ParamControlSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Forking_Interlock_AlarmFlag

- **종류**: 함수
- **시그니처**: `INT08U Set_Forking_Interlock_AlarmFlag(ParamControlSTR* pSetting) { if (pSetting->Auto_ForkOut_Interlock_Fault < 2)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **인근 주석**: 0 : not use, 1 : use
- **용도(읽는 법)**: 인터록: 스테이션/CV OK 대기. 타임아웃→ERROR1_STATION_INTERLOCK_TIMEOUT.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2215: INT08U Set_Forking_Interlock_AlarmFlag(ParamControlSTR* pSetting)`
  - `Core/Src/com_tml.c:4761: if (pCom->Data[2] & cbits[1])	is_save = Set_Forking_Interlock_AlarmFlag(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:89: extern INT08U Set_Forking_Interlock_AlarmFlag(ParamControlSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Forking_Interlock_CheckFlag

- **종류**: 함수
- **시그니처**: `INT08U Set_Forking_Interlock_CheckFlag(ParamControlSTR* pSetting) { if (pSetting->Man_ForkOut_Interlock_Fault < 2)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **인근 주석**: 0 : not use, 1 : use
- **용도(읽는 법)**: 인터록: 스테이션/CV OK 대기. 타임아웃→ERROR1_STATION_INTERLOCK_TIMEOUT.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2233: INT08U Set_Forking_Interlock_CheckFlag(ParamControlSTR* pSetting)`
  - `Core/Src/com_tml.c:4762: if (pCom->Data[2] & cbits[2])	is_save = Set_Forking_Interlock_CheckFlag(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:90: extern INT08U Set_Forking_Interlock_CheckFlag(ParamControlSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Forking_lift_BrakeOn_UseFlag

- **종류**: 함수
- **시그니처**: `INT08U Set_Forking_lift_BrakeOn_UseFlag(ParamControlSTR* pSetting) { if (pSetting->ForkOut_LT_BrakeOn_Flag < 2)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **인근 주석**: 0 : not use, 1 : use
- **용도(읽는 법)**: 헤더 주석: 0 : not use, 1 : use
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2251: INT08U Set_Forking_lift_BrakeOn_UseFlag(ParamControlSTR* pSetting)`
  - `Core/Src/com_tml.c:4763: if (pCom->Data[2] & cbits[3])	is_save = Set_Forking_lift_BrakeOn_UseFlag(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:91: extern INT08U Set_Forking_lift_BrakeOn_UseFlag(ParamControlSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Forking_Origin_OperCount

- **종류**: 함수
- **시그니처**: `void Set_Forking_Origin_OperCount(ParamControlSTR* pSetting) { if (pSetting->Fork_Auto_Home_Count > MAX_FORK_AUTO_HOME_CNT)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2269: void Set_Forking_Origin_OperCount(ParamControlSTR* pSetting)`
  - `Core/Src/com_tml.c:4767: Set_Forking_Origin_OperCount(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:92: extern void Set_Forking_Origin_OperCount(ParamControlSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_RetryInPositon

- **종류**: 함수
- **시그니처**: `void Set_RetryInPositon(ParamControlSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2282: void Set_RetryInPositon(ParamControlSTR* pSetting)`
  - `Core/Src/com_tml.c:4770: if (pCom->Data[3] & cbits[0])	Set_RetryInPositon(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:93: extern void Set_RetryInPositon(ParamControlSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Loading_DelayTime

- **종류**: 함수
- **시그니처**: `void Set_Loading_DelayTime(ParamControlSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2301: void Set_Loading_DelayTime(ParamControlSTR* pSetting)`
  - `Core/Src/com_tml.c:4771: if (pCom->Data[3] & cbits[1])	Set_Loading_DelayTime(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:94: extern void Set_Loading_DelayTime(ParamControlSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Unloading_DelayTime

- **종류**: 함수
- **시그니처**: `void Set_Unloading_DelayTime(ParamControlSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2317: void Set_Unloading_DelayTime(ParamControlSTR* pSetting)`
  - `Core/Src/com_tml.c:4772: if (pCom->Data[3] & cbits[2])	Set_Unloading_DelayTime(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:95: extern void Set_Unloading_DelayTime(ParamControlSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Fork_Origin_DelayTime

- **종류**: 함수
- **시그니처**: `void Set_Fork_Origin_DelayTime(ParamControlSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2333: void Set_Fork_Origin_DelayTime(ParamControlSTR* pSetting)`
  - `Core/Src/com_tml.c:4775: Set_Fork_Origin_DelayTime(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:96: extern void Set_Fork_Origin_DelayTime(ParamControlSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Setup_TimeOut

- **종류**: 함수
- **시그니처**: `void Set_Setup_TimeOut(ParamControlSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2344: void Set_Setup_TimeOut(ParamControlSTR* pSetting)`
  - `Core/Src/com_tml.c:4778: if (pCom->Data[3] & cbits[4])	Set_Setup_TimeOut(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:97: extern void Set_Setup_TimeOut(ParamControlSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Drive_Timeout

- **종류**: 함수
- **시그니처**: `void Set_Drive_Timeout(ParamControlSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2360: void Set_Drive_Timeout(ParamControlSTR* pSetting)`
  - `Core/Src/com_tml.c:4779: if (pCom->Data[3] & cbits[5])	Set_Drive_Timeout(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:98: extern void Set_Drive_Timeout(ParamControlSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Auto_Timeout

- **종류**: 함수
- **시그니처**: `void Set_Auto_Timeout(ParamControlSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2376: void Set_Auto_Timeout(ParamControlSTR* pSetting)`
  - `Core/Src/com_tml.c:4780: if (pCom->Data[3] & cbits[6])	Set_Auto_Timeout(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:99: extern void Set_Auto_Timeout(ParamControlSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_ForceMode_ReleaseTime

- **종류**: 함수
- **시그니처**: `void Set_ForceMode_ReleaseTime(ParamControlSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2392: void Set_ForceMode_ReleaseTime(ParamControlSTR* pSetting)`
  - `Core/Src/com_tml.c:4789: Set_ForceMode_ReleaseTime(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:100: extern void Set_ForceMode_ReleaseTime(ParamControlSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Fan_Control

- **종류**: 함수
- **시그니처**: `INT08U Set_Fan_Control(ParamControlSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2403: INT08U Set_Fan_Control(ParamControlSTR* pSetting)`
  - `Core/Src/com_tml.c:4792: if (pCom->Data[4] & cbits[1])	is_save = Set_Fan_Control(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:101: extern INT08U Set_Fan_Control(ParamControlSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Buzzer_Control

- **종류**: 함수
- **시그니처**: `void Set_Buzzer_Control(ParamControlSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2423: void Set_Buzzer_Control(ParamControlSTR* pSetting)`
  - `Core/Src/com_tml.c:4793: if (pCom->Data[4] & cbits[2])	Set_Buzzer_Control(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:102: extern void Set_Buzzer_Control(ParamControlSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_UpperBrakeSetup

- **종류**: 함수
- **시그니처**: `void Set_UpperBrakeSetup(ParamControlSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2446: void Set_UpperBrakeSetup(ParamControlSTR* pSetting)`
  - `Core/Src/com_tml.c:4794: if (pCom->Data[4] & cbits[3])	Set_UpperBrakeSetup(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:103: extern void Set_UpperBrakeSetup(ParamControlSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_AutoFork_PositionSensor_Alarm

- **종류**: 함수
- **시그니처**: `INT08U Set_AutoFork_PositionSensor_Alarm(ParamControlSTR* pSetting) { if (pSetting->Auto_Fork_Pos_Sensor_Fault < 2)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2463: INT08U Set_AutoFork_PositionSensor_Alarm(ParamControlSTR* pSetting)`
  - `Core/Src/com_tml.c:4795: if (pCom->Data[4] & cbits[4])	is_save = Set_AutoFork_PositionSensor_Alarm(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:105: extern INT08U Set_AutoFork_PositionSensor_Alarm(ParamControlSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Carriage_Stabilzation_Time

- **종류**: 함수
- **시그니처**: `INT08U Set_Carriage_Stabilzation_Time(ParamControlSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2480: INT08U Set_Carriage_Stabilzation_Time(ParamControlSTR* pSetting)`
  - `Core/Src/com_tml.c:4796: if (pCom->Data[4] & cbits[5])	is_save = Set_Carriage_Stabilzation_Time(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:106: INT08U Set_Carriage_Stabilzation_Time(ParamControlSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Default_ControlParam_Init

- **종류**: 함수
- **시그니처**: `void Default_ControlParam_Init(void)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2500: void Default_ControlParam_Init(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Save_Control_Param

- **종류**: 함수
- **시그니처**: `void Save_Control_Param(ParamControlSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: Save_*: RAM(m_St) + BKPSRAM 기록. UART 즉시 송신 아님. 지상반 0x30 폴링.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2592: void Save_Control_Param(ParamControlSTR* pSetting)`
  - `Core/Src/com_tml.c:4822: Save_Control_Param(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:107: extern void Save_Control_Param(ParamControlSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Control_Param_Init

- **종류**: 함수
- **시그니처**: `void Control_Param_Init()`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2624: void Control_Param_Init()`
  - `Core/Src/main.c:375: Control_Param_Init();`
  - `Core/Inc/SRM_Parameter.h:108: extern void Control_Param_Init();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Trav_Velocity

- **종류**: 함수
- **시그니처**: `void Set_Trav_Velocity(INT08U nType, UnitVelocitySTR* pVelocity) { switch (nType)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2671: void Set_Trav_Velocity(INT08U nType, UnitVelocitySTR* pVelocity)`
  - `Core/Src/com_tml.c:4884: Set_Trav_Velocity(i, &pSetParam->Vel[i]);`
  - `Core/Src/com_tml.c:4888: Set_Trav_Velocity(i, &pSetParam->Vel[i]);`
  - `Core/Inc/SRM_Parameter.h:110: extern void Set_Trav_Velocity(INT08U nType, UnitVelocitySTR* pVelocity);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Trav_Velocity_1

- **종류**: 함수
- **시그니처**: `void Set_Trav_Velocity_1(INT08U nType, UnitVelocitySTR* pSrcVel, UnitVelocitySTR* pWrVel) { switch (nType)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2802: void Set_Trav_Velocity_1(INT08U nType, UnitVelocitySTR* pSrcVel, UnitVelocitySTR* pWrVel)`
  - `Core/Src/com_tml.c:4919: Set_Trav_Velocity_1(i, &pSetParam->Vel[i], &nWriteParam.Vel[i]);`
  - `Core/Src/com_tml.c:4928: Set_Trav_Velocity_1(i, &pSetParam->Vel[i], &nWriteParam.Vel[i]);`
  - `Core/Inc/SRM_Parameter.h:111: extern void Set_Trav_Velocity_1(INT08U nType, UnitVelocitySTR* pSrcVel, UnitVelocitySTR* pWrVel);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Trav_InPostion_Tolerance

- **종류**: 함수
- **시그니처**: `void Set_Trav_InPostion_Tolerance(ParamTravelDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2916: void Set_Trav_InPostion_Tolerance(ParamTravelDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:4892: Set_Trav_InPostion_Tolerance(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:112: extern void Set_Trav_InPostion_Tolerance(ParamTravelDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Trav_InPostion_Tolerance_1

- **종류**: 함수
- **시그니처**: `void Set_Trav_InPostion_Tolerance_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pWrBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2930: void Set_Trav_InPostion_Tolerance_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:4936: Set_Trav_InPostion_Tolerance_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:113: extern void Set_Trav_InPostion_Tolerance_1(ParamTravelDriveSTR* pSetting, ParamTravelDriveSTR* pWrBu`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

