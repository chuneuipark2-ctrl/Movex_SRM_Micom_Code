# Vol.08 — 기타 함수 (12)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

com_tml 등.

---

### DIManager

- **종류**: 함수
- **시그니처**: `void DIManager(void)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:752: void DIManager(void)`
  - `Core/Src/alarm.c:1942: DIManager();`
  - `Core/Inc/alarm.h:34: extern void           DIManager(void);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### AIManager

- **종류**: 함수
- **시그니처**: `void AIManager(void)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:761: void AIManager(void)`
  - `Core/Src/alarm.c:1943: AIManager();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### controlFND

- **종류**: 함수
- **시그니처**: `void controlFND(INT08U value, INT08U point)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:788: void controlFND(INT08U value, INT08U point)`
  - `Core/Src/alarm.c:817: controlFND(m_McuTestSt.FND_Display, 0);`
  - `Core/Src/alarm.c:902: controlFND(DispData[DispInx++], 0);`
  - `Core/Src/alarm.c:909: controlFND(16, 0);`
  - `Core/Src/alarm.c:913: controlFND(DispData[DispInx++], 0);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### FNDManager

- **종류**: 함수
- **시그니처**: `void FNDManager(void)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:808: void FNDManager(void)`
  - `Core/Src/alarm.c:1944: FNDManager();`
  - `Core/Inc/alarm.h:36: extern void           FNDManager(void);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### getSystemDateTime

- **종류**: 함수
- **시그니처**: `void getSystemDateTime(void)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:922: void getSystemDateTime(void)`
  - `Core/Src/alarm.c:1940: getSystemDateTime();`
  - `Core/Inc/alarm.h:33: extern void           getSystemDateTime(void);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### runningLED

- **종류**: 함수
- **시그니처**: `void runningLED(void)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:940: void runningLED(void)`
  - `Core/Src/alarm.c:1938: runningLED();`
  - `Core/Inc/alarm.h:35: extern void           runningLED(void);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### LED_Bar_Control

- **종류**: 함수
- **시그니처**: `void LED_Bar_Control(INT08U nColor) { if (m_ExtSEnv.Machine.mType.Lamp_Type == 0) { // LED Bar switch (nColor)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:951: void LED_Bar_Control(INT08U nColor)`
  - `Core/Src/alarm.c:1023: LED_Bar_Control(LAMP_RED);`
  - `Core/Src/alarm.c:1029: LED_Bar_Control(LAMP_YELLOW);`
  - `Core/Src/alarm.c:1037: LED_Bar_Control(LAMP_CYAN);`
  - `Core/Src/alarm.c:1048: LED_Bar_Control(LAMP_GREEN);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### LED_Bar_Control_Proc

- **종류**: 함수
- **시그니처**: `void LED_Bar_Control_Proc()`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:1008: void LED_Bar_Control_Proc()`
  - `Core/Src/alarm.c:1326: LED_Bar_Control_Proc();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### TowerLamp_Control_Proc_1

- **종류**: 함수
- **시그니처**: `void TowerLamp_Control_Proc_1()`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:1073: void TowerLamp_Control_Proc_1()`
  - `Core/Src/alarm.c:1337: s_fpPosition = TowerLamp_Control_Proc_1;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### TowerLamp_Control_Proc_2

- **종류**: 함수
- **시그니처**: `void TowerLamp_Control_Proc_2()`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:1187: void TowerLamp_Control_Proc_2()`
  - `Core/Src/alarm.c:1341: s_fpPosition = TowerLamp_Control_Proc_2;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### SignalLampManager

- **종류**: 함수
- **시그니처**: `void SignalLampManager(void)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:1318: void SignalLampManager(void)`
  - `Core/Src/alarm.c:1947: SignalLampManager();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ModeKeyManager

- **종류**: 함수
- **시그니처**: `void ModeKeyManager(void)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:1352: void ModeKeyManager(void)`
  - `Core/Src/alarm.c:1945: ModeKeyManager();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Delete_ForkMoveInfo

- **종류**: 함수
- **시그니처**: `void Delete_ForkMoveInfo(INT08U num) { if (num < 2)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:1457: void Delete_ForkMoveInfo(INT08U num)`
  - `Core/Inc/alarm.h:25: void Delete_ForkMoveInfo(INT08U nNum);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Save_ForkMove_OrderStatus

- **종류**: 함수
- **시그니처**: `void Save_ForkMove_OrderStatus(INT08U num, INT08U st) { // ì ë¬ë°ì ì«ìê° 2ë³´ë¤ ìì¼ë©´ ì¤í if (num < 2) { if (m_St.ForkWork[num].OrderStatus_Move != st)// í¬í¬ê° 1ë²ê³¼ 2ë²ì´ ìëë° í¬í¬ëª¨í°ì ë²í¸ê°ì, í¬í¬ëª¨í° 1ë²ì ììì´ëìíê° ëê²¨ë°ë ìììíì¸ë¯ ---> ì´ì½ëììë í¬í¬ 1ì ìììíê° ì´ëì´ ìëê²½ì°)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: Save_*: RAM(m_St) + BKPSRAM 기록. UART 즉시 송신 아님. 지상반 0x30 폴링.
- **코드 참조 예**:
  - `Core/Src/alarm.c:1469: void Save_ForkMove_OrderStatus(INT08U num, INT08U st)`
  - `Core/Src/dev_SRM.c:58871: Save_ForkMove_OrderStatus(0, WORK_STATUS_DOING); //mcu, ram, ì§ìë°ì ëª¨ë íì¬ì ìíë¥¼`
  - `Core/Src/dev_SRM.c:59524: Save_ForkMove_OrderStatus(0, WORK_STATUS_COMPLETE);`
  - `Core/Src/dev_SRM.c:59532: Save_ForkMove_OrderStatus(0, WORK_STATUS_COMPLETE);`
  - `Core/Src/dev_SRM.c:60459: Save_ForkMove_OrderStatus(0, WORK_STATUS_COMPLETE);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Save_ForkMove_OrderProcess

- **종류**: 함수
- **시그니처**: `void Save_ForkMove_OrderProcess(INT08U num, INT08U st) { if (num < 2) { if (m_St.ForkWork[num].OrderProcess_Move != st)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: Save_*: RAM(m_St) + BKPSRAM 기록. UART 즉시 송신 아님. 지상반 0x30 폴링.
- **코드 참조 예**:
  - `Core/Src/alarm.c:1485: void Save_ForkMove_OrderProcess(INT08U num, INT08U st)`
  - `Core/Src/dev_SRM.c:58872: Save_ForkMove_OrderProcess(0, CMD_MOVE_STEP_MOVING);//mcu, ram, ì§ìë°ì ëª¨ë íì¬ì ìí`
  - `Core/Src/dev_SRM.c:59522: Save_ForkMove_OrderProcess(0, CMD_MOVE_STEP_COMPELTE);`
  - `Core/Src/dev_SRM.c:59530: Save_ForkMove_OrderProcess(0, CMD_MOVE_STEP_COMPELTE);`
  - `Core/Src/dev_SRM.c:59586: Save_ForkMove_OrderProcess(0, CMD_MOVE_STEP_COMPELTE);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Delete_ForkWorkInfo

- **종류**: 함수
- **시그니처**: `void Delete_ForkWorkInfo(INT08U num) { if (num < 2)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:1500: void Delete_ForkWorkInfo(INT08U num)`
  - `Core/Inc/alarm.h:28: void Delete_ForkWorkInfo(INT08U nNum);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Save_ForkWork_OrderStatus

- **종류**: 함수
- **시그니처**: `void Save_ForkWork_OrderStatus(INT08U num, INT08U nStatus) { if (num < 2) { if (m_St.ForkWork[num].OrderStatus_Fork != nStatus)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: Save_*: RAM(m_St) + BKPSRAM 기록. UART 즉시 송신 아님. 지상반 0x30 폴링.
- **코드 참조 예**:
  - `Core/Src/alarm.c:1512: void Save_ForkWork_OrderStatus(INT08U num, INT08U nStatus)`
  - `Core/Src/dev_SRM.c:47441: Save_ForkWork_OrderStatus(0, WORK_STATUS_FAIL);  // 5:ì¤í¨ìë£`
  - `Core/Src/dev_SRM.c:47444: Save_ForkWork_OrderStatus(1, WORK_STATUS_FAIL);  // 5:ì¤í¨ìë£`
  - `Core/Src/dev_SRM.c:61819: Save_ForkWork_OrderStatus(0, WORK_STATUS_COMPLETE);  // 4:ì±ê³µìë£`
  - `Core/Src/dev_SRM.c:62519: Save_ForkWork_OrderStatus(0, WORK_STATUS_COMPLETE);  // 4:ì±ê³µìë£`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Save_ForkWork_OrderProcess

- **종류**: 함수
- **시그니처**: `void Save_ForkWork_OrderProcess(INT08U num, INT08U nProcess) { if (num < 2) { if (m_St.ForkWork[num].OrderProcess_Fork != nProcess)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: Save_*: RAM(m_St) + BKPSRAM 기록. UART 즉시 송신 아님. 지상반 0x30 폴링.
- **코드 참조 예**:
  - `Core/Src/alarm.c:1525: void Save_ForkWork_OrderProcess(INT08U num, INT08U nProcess)`
  - `Core/Src/dev_SRM.c:58878: Save_ForkWork_OrderProcess(0, CMD_FORK_STEP_MOVING_FROM); // mcu, ram, ì§ìë°ì ëª¨ë íì¬ì`
  - `Core/Src/dev_SRM.c:58890: Save_ForkWork_OrderProcess(0, CMD_FORK_STEP_MOVING_FROM); // í¬í¬ ì¤íì´ë ì´ë¼ê³  ë³´ê³`
  - `Core/Src/dev_SRM.c:58898: Save_ForkWork_OrderProcess(0, CMD_FORK_STEP_MOVING_TO); // ì¤íë¬´ë¹í¬ ëªë ¹(ìì´ëì¤ ëªë `
  - `Core/Src/dev_SRM.c:58904: Save_ForkWork_OrderProcess(0, CMD_FORK_STEP_MOVING_TO);// mcu, ram, ì§ìë°ì ëª¨ë íì¬ì ì`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Save_ForkWork_Information

- **종류**: 함수
- **시그니처**: `void Save_ForkWork_Information(INT08U num, INT08U OrderStatus, INT08U OrderProcess) { if (num < 2) { if ((m_St.ForkWork[num].OrderProcess_Fork != OrderProcess) || (m_St.ForkWork[num].OrderStatus_Fork != OrderStatus))`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: Save_*: RAM(m_St) + BKPSRAM 기록. UART 즉시 송신 아님. 지상반 0x30 폴링.
- **코드 참조 예**:
  - `Core/Src/alarm.c:1539: void Save_ForkWork_Information(INT08U num, INT08U OrderStatus, INT08U OrderProcess)`
  - `Core/Inc/alarm.h:31: extern void           Save_ForkWork_Information(INT08U num, INT08U OrderStatus, INT08U OrderProcess)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ComPortLinkCheck

- **종류**: 함수
- **시그니처**: `void ComPortLinkCheck(void)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:1557: void ComPortLinkCheck(void)`
  - `Core/Src/alarm.c:1949: ComPortLinkCheck();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### PDoorCheck

- **종류**: 함수
- **시그니처**: `void PDoorCheck(void) { if (m_ExtSEnv2.CtrParam.DoorSensor_UseFlag == 1) { if (m_pgmEnv.SRM_RunMode == 0) { // éï¿½ï¿½ëìå ï¿½/å ìê°å ì°ë¹³å ï¿½ å ììå ìì å ìï¿½ï¿½ï¿½ï¿½æ¶ï¿½å ìï¿½å ììå ììå ï¿½ éï¿½ï¿½ï¿½å ìï¿½å ï¿½ å ììå ìì. if (Get_DI_State(IN_PDOOR) == 0)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:1601: void PDoorCheck(void)`
  - `Core/Src/alarm.c:1951: PDoorCheck();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Fan_Ctrl_Process

- **종류**: 함수
- **시그니처**: `void Fan_Ctrl_Process(void)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:1620: void Fan_Ctrl_Process(void)`
  - `Core/Src/alarm.c:1953: Fan_Ctrl_Process();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Buzzer_Control_Proc

- **종류**: 함수
- **시그니처**: `void Buzzer_Control_Proc(void)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:1681: void Buzzer_Control_Proc(void)`
  - `Core/Src/alarm.c:1955: Buzzer_Control_Proc();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### AlarmManager

- **종류**: 함수
- **시그니처**: `void AlarmManager(void)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:1936: void AlarmManager(void)`
  - `Core/Src/main.c:821: AlarmManager();`
  - `Core/Inc/alarm.h:37: extern void           AlarmManager(void);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### HAL_CAN_RxFifo0MsgPendingCallback

- **종류**: 함수
- **시그니처**: `void HAL_CAN_RxFifo0MsgPendingCallback(CAN_HandleTypeDef* hcan)`
- **정의 위치**: `Core/Src/can.c`
- **인근 주석**: INT08U nCan1_CheckData[8] = { 1, 2, 3, 4, 5, 6, 7, 8 }; INT08U nCan2_CheckData[8] = { 8, 7, 6, 5, 4, 3, 2, 1 };
- **용도(읽는 법)**: 헤더 주석: INT08U nCan1_CheckData[8] = { 1, 2, 3, 4, 5, 6, 7, 8 }; INT08U nCan2_CheckData[8] = { 8, 7, 6, 5, 4, 3, 2, 1 };
- **코드 참조 예**:
  - `Core/Src/can.c:32: void HAL_CAN_RxFifo0MsgPendingCallback(CAN_HandleTypeDef* hcan)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

