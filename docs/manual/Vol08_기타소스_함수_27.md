# Vol.08 — 기타 함수 (27)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

com_tml 등.

---

### connectFpgaTcp

- **종류**: 함수
- **시그니처**: `void connectFpgaTcp(INT08U* remote_ip, INT16U remote_port, INT16U local_port)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/dev_SRM.c:67977: connectFpgaTcp((INT08U*)modbus_server_ip, 502, 502);`
  - `Core/Src/fpgatcpipstack.c:800: void connectFpgaTcp(INT08U* remote_ip, INT16U remote_port, INT16U local_port)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### connectFpgaTcp_1

- **종류**: 함수
- **시그니처**: `void connectFpgaTcp_1(INT08U nSocket, INT08U* remote_ip, INT16U remote_port, INT16U local_port)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/dev_SRM.c:66029: connectFpgaTcp_1(1, (INT08U*)modbus_server_ip, 502, 502);`
  - `Core/Src/fpgatcpipstack.c:814: void connectFpgaTcp_1(INT08U nSocket, INT08U* remote_ip, INT16U remote_port, INT16U local_port)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### setFpgaTcpIpStackMyMACAddrress

- **종류**: 함수
- **시그니처**: `void setFpgaTcpIpStackMyMACAddrress(INT08U* mac_addr)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/dev_SRM.c:5179: setFpgaTcpIpStackMyMACAddrress((INT08U*)m_DevStatus.MyMACAddr_1);`
  - `Core/Src/fpgatcpipstack.c:835: void setFpgaTcpIpStackMyMACAddrress(INT08U* mac_addr)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### FpgaTcpIpStackInit

- **종류**: 함수
- **시그니처**: `void FpgaTcpIpStackInit(void)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/dev_SRM.c:5177: FpgaTcpIpStackInit();`
  - `Core/Src/fpgatcpipstack.c:840: void FpgaTcpIpStackInit(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### FpgaTcpIpStackProcess

- **종류**: 함수
- **시그니처**: `void FpgaTcpIpStackProcess(void)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/dev_SRM.c:67946: FpgaTcpIpStackProcess();`
  - `Core/Src/fpgatcpipstack.c:854: void FpgaTcpIpStackProcess(void)`
  - `Core/Src/upgrade.c:890: FpgaTcpIpStackProcess();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### _write

- **종류**: 함수
- **시그니처**: `int _write(int32_t file, uint8_t * ptr, int32_t len)`
- **정의 위치**: `Core/Src/main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/main.c:156: extern "C" int _write(int32_t file, uint8_t* ptr, int32_t len) {`
  - `Core/Src/main.c:158: int _write(int32_t file, uint8_t * ptr, int32_t len) {`
  - `Core/Src/syscalls.c:77: __attribute__((weak)) int _write(int file, char *ptr, int len)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### MPU_Config

- **종류**: 함수
- **시그니처**: `static void MPU_Config(void)`
- **정의 위치**: `Core/Src/main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/main.c:181: static void MPU_Config(void)`
  - `Core/Src/main.c:782: MPU_Config();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Check_Flash_DualBank

- **종류**: 함수
- **시그니처**: `void Check_Flash_DualBank()`
- **정의 위치**: `Core/Src/main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/main.c:207: void Check_Flash_DualBank()`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### checkVerStr

- **종류**: 함수
- **시그니처**: `void checkVerStr(INT08U * verStr, INT08U ver)`
- **정의 위치**: `Core/Src/main.c`
- **인근 주석**: __LINE__, __FILE__, __TIME__, __DATE__, __STDC__ __DATE__  4A 75 6C 20 32 33 20 32 30 30 39 00      Jul 23 2009 0  1  2     4  5     7  8  9  10
- **용도(읽는 법)**: 헤더 주석: __LINE__, __FILE__, __TIME__, __DATE__, __STDC__ __DATE__  4A 75 6C 20 32 33 20 32 30 30 39 00      Jul 23 2009 0  1  2     4  5     7  8  9  10
- **코드 참조 예**:
  - `Core/Src/main.c:246: void checkVerStr(INT08U * verStr, INT08U ver)`
  - `Core/Src/main.c:605: checkVerStr((INT08U*)m_DevStatus.PgmVer, FW_VERSION);     // Ver1.1 - 090723   =>  0x10 0x09 0x07 0x`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Load_ExtFlash_Setting

- **종류**: 함수
- **시그니처**: `void Load_ExtFlash_Setting()`
- **정의 위치**: `Core/Src/main.c`
- **인근 주석**: sst_init();
- **용도(읽는 법)**: 헤더 주석: sst_init();
- **코드 참조 예**:
  - `Core/Src/main.c:329: void Load_ExtFlash_Setting()`
  - `Core/Src/main.c:596: Load_ExtFlash_Setting();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Recovery_Work_Info

- **종류**: 함수
- **시그니처**: `void Recovery_Work_Info()`
- **정의 위치**: `Core/Src/main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/main.c:444: void Recovery_Work_Info()`
  - `Core/Src/main.c:569: Recovery_Work_Info();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Load_BackupSRAM

- **종류**: 함수
- **시그니처**: `void Load_BackupSRAM()`
- **정의 위치**: `Core/Src/main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/main.c:537: void Load_BackupSRAM()`
  - `Core/Src/main.c:595: Load_BackupSRAM();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### initValue

- **종류**: 함수
- **시그니처**: `void initValue(void)`
- **정의 위치**: `Core/Src/main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/main.c:580: void initValue(void)`
  - `Core/Src/main.c:813: initValue();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### WatchDog

- **종류**: 함수
- **시그니처**: `void WatchDog(void)`
- **정의 위치**: `Core/Src/main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:1399: WatchDog();`
  - `Core/Src/FlashSST26.c:1450: WatchDog();`
  - `Core/Src/FlashSST26.c:1596: WatchDog();`
  - `Core/Src/SRM_Parameter.c:18: extern void WatchDog(void);`
  - `Core/Src/SRM_Parameter.c:1714: WatchDog();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Reset_Add_AlarmLog

- **종류**: 함수
- **시그니처**: `void Reset_Add_AlarmLog(void)`
- **정의 위치**: `Core/Src/main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/main.c:667: Reset_Add_AlarmLog();`
  - `Core/Src/main.c:735: void Reset_Add_AlarmLog(void)`
  - `Core/Inc/main.h:73: void Reset_Add_AlarmLog(void);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### main

- **종류**: 함수
- **시그니처**: `int main(void)`
- **정의 위치**: `Core/Src/main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:14: #include "main.h"`
  - `Core/Src/alarm.c:8: #include "main.h"`
  - `Core/Src/can.c:8: #include "main.h"`
  - `Core/Src/com_dsp.c:8: #include "main.h"`
  - `Core/Src/com_hmi.c:14: #include "main.h"`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### SystemClock_Config

- **종류**: 함수
- **시그니처**: `void SystemClock_Config(void)`
- **정의 위치**: `Core/Src/main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/main.c:133: void SystemClock_Config(void);`
  - `Core/Src/main.c:779: SystemClock_Config();`
  - `Core/Src/main.c:862: void SystemClock_Config(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### MX_CAN1_Init

- **종류**: 함수
- **시그니처**: `static void MX_CAN1_Init(void)`
- **정의 위치**: `Core/Src/main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/main.c:146: static void MX_CAN1_Init(void);`
  - `Core/Src/main.c:803: MX_CAN1_Init();`
  - `Core/Src/main.c:924: static void MX_CAN1_Init(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### MX_IWDG_Init

- **종류**: 함수
- **시그니처**: `static void MX_IWDG_Init(void)`
- **정의 위치**: `Core/Src/main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/main.c:145: static void MX_IWDG_Init(void);`
  - `Core/Src/main.c:802: MX_IWDG_Init();`
  - `Core/Src/main.c:990: static void MX_IWDG_Init(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### MX_RTC_Init

- **종류**: 함수
- **시그니처**: `static void MX_RTC_Init(void)`
- **정의 위치**: `Core/Src/main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/main.c:136: static void MX_RTC_Init(void);`
  - `Core/Src/main.c:790: MX_RTC_Init();`
  - `Core/Src/main.c:1026: static void MX_RTC_Init(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### MX_SPI2_Init

- **종류**: 함수
- **시그니처**: `static void MX_SPI2_Init(void)`
- **정의 위치**: `Core/Src/main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/main.c:141: static void MX_SPI2_Init(void);`
  - `Core/Src/main.c:795: MX_SPI2_Init();`
  - `Core/Src/main.c:1060: static void MX_SPI2_Init(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### MX_TIM2_Init

- **종류**: 함수
- **시그니처**: `static void MX_TIM2_Init(void)`
- **정의 위치**: `Core/Src/main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/main.c:142: static void MX_TIM2_Init(void);`
  - `Core/Src/main.c:797: MX_TIM2_Init();`
  - `Core/Src/main.c:1135: static void MX_TIM2_Init(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### MX_TIM3_Init

- **종류**: 함수
- **시그니처**: `static void MX_TIM3_Init(void)`
- **정의 위치**: `Core/Src/main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/main.c:138: static void MX_TIM3_Init(void);`
  - `Core/Src/main.c:792: MX_TIM3_Init();`
  - `Core/Src/main.c:1184: static void MX_TIM3_Init(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### MX_TIM4_Init

- **종류**: 함수
- **시그니처**: `static void MX_TIM4_Init(void)`
- **정의 위치**: `Core/Src/main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/main.c:144: static void MX_TIM4_Init(void);`
  - `Core/Src/main.c:801: MX_TIM4_Init();`
  - `Core/Src/main.c:1232: static void MX_TIM4_Init(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### MX_UART4_Init

- **종류**: 함수
- **시그니처**: `static void MX_UART4_Init(void)`
- **정의 위치**: `Core/Src/main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/main.c:143: static void MX_UART4_Init(void);`
  - `Core/Src/main.c:800: MX_UART4_Init();`
  - `Core/Src/main.c:1277: static void MX_UART4_Init(void)`
  - `Core/Src/main.c:1660: MX_UART4_Init();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

