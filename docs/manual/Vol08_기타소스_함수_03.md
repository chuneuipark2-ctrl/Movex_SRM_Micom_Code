# Vol.08 — 기타 함수 (3)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

com_tml 등.

---

### CAN2_Test_Proc

- **종류**: 함수
- **시그니처**: `void CAN2_Test_Proc(INT08U* s_InitFlag)`
- **정의 위치**: `Core/Src/Mcu_Test.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:487: void CAN2_Test_Proc(INT08U* s_InitFlag)`
  - `Core/Src/Mcu_Test.c:979: CAN2_Test_Proc(&s_TestFlag.CAN2_Enable);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### SSI_Test_Proc

- **종류**: 함수
- **시그니처**: `void SSI_Test_Proc(INT08U* s_InitFlag)`
- **정의 위치**: `Core/Src/Mcu_Test.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:529: void SSI_Test_Proc(INT08U* s_InitFlag)`
  - `Core/Src/Mcu_Test.c:986: SSI_Test_Proc(&s_TestFlag.SSI_Enable);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Encoder_Test_Proc

- **종류**: 함수
- **시그니처**: `void Encoder_Test_Proc(INT08U* s_InitFlag)`
- **정의 위치**: `Core/Src/Mcu_Test.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:676: void Encoder_Test_Proc(INT08U* s_InitFlag)`
  - `Core/Src/Mcu_Test.c:948: Encoder_Test_Proc(&s_TestFlag.Encoder_Enable);`
  - `Core/Src/Mcu_Test.c:988: Encoder_Test_Proc(&s_TestFlag.Encoder_Enable);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EtherCAT_Test_Proc

- **종류**: 함수
- **시그니처**: `void EtherCAT_Test_Proc(INT08U* s_InitFlag)`
- **정의 위치**: `Core/Src/Mcu_Test.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:723: void EtherCAT_Test_Proc(INT08U* s_InitFlag)`
  - `Core/Src/Mcu_Test.c:984: EtherCAT_Test_Proc(&s_TestFlag.EtherCAT_Enable);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### DIO_Test_Proc

- **종류**: 함수
- **시그니처**: `void DIO_Test_Proc(INT08U* s_InitFlag)`
- **정의 위치**: `Core/Src/Mcu_Test.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:811: void DIO_Test_Proc(INT08U* s_InitFlag)`
  - `Core/Src/Mcu_Test.c:992: DIO_Test_Proc(&s_TestFlag.DIO_Test_Enable);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### USB_Mem_Test_Proc

- **종류**: 함수
- **시그니처**: `void USB_Mem_Test_Proc(INT08U* s_InitFlag)`
- **정의 위치**: `Core/Src/Mcu_Test.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:862: void USB_Mem_Test_Proc(INT08U* s_InitFlag)`
  - `Core/Src/Mcu_Test.c:994: USB_Mem_Test_Proc(&s_TestFlag.USB_Test_Enable);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### SampleCode_Test_Proc

- **종류**: 함수
- **시그니처**: `void SampleCode_Test_Proc(INT08U* s_InitFlag)`
- **정의 위치**: `Core/Src/Mcu_Test.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:891: void SampleCode_Test_Proc(INT08U* s_InitFlag)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### MCU_Test_Manager

- **종류**: 함수
- **시그니처**: `void MCU_Test_Manager(void)`
- **정의 위치**: `Core/Src/Mcu_Test.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:921: void MCU_Test_Manager(void)`
  - `Core/Src/main.c:844: MCU_Test_Manager();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### BackUp_Device_Parameter

- **종류**: 함수
- **시그니처**: `void BackUp_Device_Parameter(DeviceSetting* pSet)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **인근 주석**: 2024. 8. 29 FW 다운로드 시작 시, extFlash 초기화되는 문제 발생 시 기본정보 삭제. BackUpSRAM에 설정 백업.
- **용도(읽는 법)**: 헤더 주석: 2024. 8. 29 FW 다운로드 시작 시, extFlash 초기화되는 문제 발생 시 기본정보 삭제. BackUpSRAM에 설정 백업.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:25: void BackUp_Device_Parameter(DeviceSetting* pSet)`
  - `Core/Src/SRM_Parameter.c:105: BackUp_Device_Parameter(&m_ExtSEnv.mDevice);`
  - `Core/Src/SRM_Parameter.c:147: BackUp_Device_Parameter(&m_ExtSEnv.mDevice);`
  - `Core/Inc/SRM_Parameter.h:14: void BackUp_Device_Parameter(DeviceSetting* pSet);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Default_Device_Setting

- **종류**: 함수
- **시그니처**: `void Default_Device_Setting()`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:42: void Default_Device_Setting()`
  - `Core/Src/SRM_Parameter.c:183: Default_Device_Setting();`
  - `Core/Src/SRM_Parameter.c:210: Default_Device_Setting();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Device_Parameter

- **종류**: 함수
- **시그니처**: `INT08U Set_Device_Parameter(DeviceSetting* pSet)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:113: INT08U Set_Device_Parameter(DeviceSetting* pSet)`
  - `Core/Src/SRM_Parameter.c:188: Set_Device_Parameter(&m_BKSram.mDevice);`
  - `Core/Src/SRM_Parameter.c:214: Set_Device_Parameter(&m_BKSram.mDevice);`
  - `Core/Src/com_tml.c:543: Set_Device_Parameter(&nSetVlaue);	/*saveFlashSEnvSTR();*/`
  - `Core/Inc/SRM_Parameter.h:15: extern INT08U Set_Device_Parameter(DeviceSetting* pSet);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Device_Setting_Init

- **종류**: 함수
- **시그니처**: `void Device_Setting_Init()`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:168: void Device_Setting_Init()`
  - `Core/Src/main.c:363: Device_Setting_Init();`
  - `Core/Inc/SRM_Parameter.h:16: extern void Device_Setting_Init();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Default_MachineType_Setting

- **종류**: 함수
- **시그니처**: `void Default_MachineType_Setting()`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:276: void Default_MachineType_Setting()`
  - `Core/Src/SRM_Parameter.c:433: Default_MachineType_Setting();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Machine_Parameter

- **종류**: 함수
- **시그니처**: `INT08U Set_Machine_Parameter(SRM_Machine_STR* pSet)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:324: INT08U Set_Machine_Parameter(SRM_Machine_Type* pSet)`
  - `Core/Src/SRM_Parameter.c:354: INT08U Set_Machine_Parameter(SRM_Machine_STR* pSet)`
  - `Core/Src/com_tml.c:596: is_reset = Set_Machine_Parameter((SRM_Machine_STR*)&pCtrl->mType);`
  - `Core/Src/com_tml.c:603: is_reset = Set_Machine_Parameter((SRM_Machine_STR*)&pCtrl->mType);`
  - `Core/Inc/SRM_Parameter.h:20: extern INT08U Set_Machine_Parameter(SRM_Machine_STR* pSet);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_DSP_Parameter

- **종류**: 함수
- **시그니처**: `INT08U Set_DSP_Parameter(MX_DSP_Setting* pSet)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:384: INT08U Set_DSP_Parameter(MX_DSP_Setting* pSet)`
  - `Core/Src/com_tml.c:597: Set_DSP_Parameter((MX_DSP_Setting*)&pCtrl->DSP_Set);`
  - `Core/Src/com_tml.c:608: Set_DSP_Parameter((MX_DSP_Setting*)&pCtrl->DSP_Set);`
  - `Core/Inc/SRM_Parameter.h:21: extern INT08U Set_DSP_Parameter(MX_DSP_Setting* pSet);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Machine_Type_Setting_Init

- **종류**: 함수
- **시그니처**: `void Machine_Type_Setting_Init()`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:422: void Machine_Type_Setting_Init()`
  - `Core/Src/main.c:364: Machine_Type_Setting_Init();`
  - `Core/Inc/SRM_Parameter.h:22: extern void Machine_Type_Setting_Init();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Default_DIO_Setting_Init

- **종류**: 함수
- **시그니처**: `void Default_DIO_Setting_Init()`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:468: void Default_DIO_Setting_Init()`
  - `Core/Src/SRM_Parameter.c:555: Default_DIO_Setting_Init();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Check_DIO_SetParameter

- **종류**: 함수
- **시그니처**: `INT08U Check_DIO_SetParameter() { if (checkFusingString((INT08U*)m_ExtSEnv.InitDIOCode, (INT08U*)DIO_INIT_CODE, 4))`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:494: INT08U Check_DIO_SetParameter()`
  - `Core/Inc/SRM_Parameter.h:24: extern INT08U Check_DIO_SetParameter();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_DIO_Parameter

- **종류**: 함수
- **시그니처**: `void Set_DIO_Parameter(DIO_SetSTR* pDIO_Set)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:486: Set_DIO_Parameter(&nSetting);`
  - `Core/Src/SRM_Parameter.c:511: void Set_DIO_Parameter(DIO_SetSTR* pDIO_Set)`
  - `Core/Src/com_tml.c:3317: Set_DIO_Parameter(pCtrl);`
  - `Core/Inc/SRM_Parameter.h:25: extern void Set_DIO_Parameter(DIO_SetSTR* pDIO_Set);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### DIO_Setting_Init

- **종류**: 함수
- **시그니처**: `void DIO_Setting_Init()`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:544: void DIO_Setting_Init()`
  - `Core/Src/main.c:366: DIO_Setting_Init();`
  - `Core/Inc/SRM_Parameter.h:26: extern void DIO_Setting_Init();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Check_RackEnv_Config

- **종류**: 함수
- **시그니처**: `INT08U Check_RackEnv_Config()`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:592: INT08U Check_RackEnv_Config()`
  - `Core/Src/com_tml.c:4265: if (Check_RackEnv_Config())`
  - `Core/Src/dev_SRM.c:10839: if (!Check_RackEnv_Config())		return;`
  - `Core/Src/dev_SRM.c:15792: if (Check_RackEnv_Config() && Check_CellEnv_Config() && Check_StationEnv_Config())`
  - `Core/Src/dev_SRM.c:16615: if (Check_RackEnv_Config() && Check_CellEnv_Config() && Check_StationEnv_Config())`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Rack_Config

- **종류**: 함수
- **시그니처**: `void Set_Rack_Config(RackConfigSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:628: void Set_Rack_Config(RackConfigSTR* pSetting)`
  - `Core/Src/com_tml.c:3485: Set_Rack_Config(pCtrl);`
  - `Core/Src/com_tml.c:3846: Set_Rack_Config(&nWrite_RackCfg);`
  - `Core/Inc/SRM_Parameter.h:29: extern void Set_Rack_Config(RackConfigSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Clear_Rack_Config

- **종류**: 함수
- **시그니처**: `void Clear_Rack_Config(void)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:655: void Clear_Rack_Config(void)`
  - `Core/Src/SRM_Parameter.c:688: Clear_Rack_Config();`
  - `Core/Src/com_tml.c:4262: Clear_Rack_Config();`
  - `Core/Src/com_tml.c:4267: Clear_Rack_Config();`
  - `Core/Inc/SRM_Parameter.h:30: extern void Clear_Rack_Config(void);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Rack_Config_Init

- **종류**: 함수
- **시그니처**: `void Rack_Config_Init()`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:676: void Rack_Config_Init()`
  - `Core/Src/main.c:368: Rack_Config_Init();`
  - `Core/Inc/SRM_Parameter.h:31: extern void Rack_Config_Init();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Default_Cell_Config_Init

- **종류**: 함수
- **시그니처**: `void Default_Cell_Config_Init(CellConfigSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:723: void Default_Cell_Config_Init(CellConfigSTR* pSetting)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

