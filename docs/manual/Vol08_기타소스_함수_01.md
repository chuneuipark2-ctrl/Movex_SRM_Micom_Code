# Vol.08 — 기타 함수 (1)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

com_tml 등.

---

### init_FlashSst26

- **종류**: 함수
- **시그니처**: `void init_FlashSst26(void)`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:29: void init_FlashSst26(void)`
  - `Core/Src/main.c:1125: init_FlashSst26();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### SPI_SEND

- **종류**: 함수
- **시그니처**: `INT08U SPI_SEND(INT08U cmd)`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:42: INT08U SPI_SEND(INT08U cmd)`
  - `Core/Src/FlashSST26.c:185: SPI_SEND(SST26_WRDI);`
  - `Core/Src/FlashSST26.c:194: SPI_SEND(SST26_WREN);`
  - `Core/Src/FlashSST26.c:206: SPI_SEND(SST26_RDID);`
  - `Core/Src/FlashSST26.c:988: SPI_SEND(SST26_FAST_READ);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### SPI_SNDBLOCK

- **종류**: 함수
- **시그니처**: `INT08U SPI_SNDBLOCK(const INT08U* pWriteData, INT16U nLen)`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:85: INT08U SPI_SNDBLOCK(const INT08U* pWriteData, INT16U nLen)`
  - `Core/Src/FlashSST26.c:266: SPI_SNDBLOCK(cmd, sizeof(cmd));`
  - `Core/Src/FlashSST26.c:299: SPI_SNDBLOCK(cmd, sizeof(cmd));`
  - `Core/Src/FlashSST26.c:331: SPI_SNDBLOCK(cmd, sizeof(cmd));`
  - `Core/Src/FlashSST26.c:385: SPI_SNDBLOCK(cmd, sizeof(cmd));`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### SPI_RECVBLOCK

- **종류**: 함수
- **시그니처**: `INT08U SPI_RECVBLOCK(INT08U* pReadData, INT16U nLen)`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:134: INT08U SPI_RECVBLOCK(INT08U* pReadData, INT16U nLen)`
  - `Core/Src/FlashSST26.c:207: SPI_RECVBLOCK(nRead_Data, sizeof(nRead_Data));`
  - `Core/Src/FlashSST26.c:267: SPI_RECVBLOCK(data, sizeof(data));`
  - `Core/Src/FlashSST26.c:300: SPI_RECVBLOCK(data, sizeof(data));`
  - `Core/Src/FlashSST26.c:332: SPI_RECVBLOCK(data, sizeof(data));`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### disableWrite

- **종류**: 함수
- **시그니처**: `void disableWrite()`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:181: void disableWrite()`
  - `Core/Src/FlashSST26.c:1157: disableWrite();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### enableWrite

- **종류**: 함수
- **시그니처**: `void enableWrite()`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:190: void enableWrite()`
  - `Core/Src/FlashSST26.c:382: enableWrite();`
  - `Core/Src/FlashSST26.c:413: enableWrite();`
  - `Core/Src/FlashSST26.c:436: enableWrite();`
  - `Core/Src/FlashSST26.c:470: enableWrite();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### writeRegisterConfiguration

- **종류**: 함수
- **시그니처**: `INT08U writeRegisterConfiguration(registerConfiguration_t registerConfiguration)`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:355: INT08U writeRegisterConfiguration(registerConfiguration_t registerConfiguration)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### writeRegisterBlockProtection

- **종류**: 함수
- **시그니처**: `INT08U writeRegisterBlockProtection(registerBlockProtection_t registerBlockProtection)`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:401: INT08U writeRegisterBlockProtection(registerBlockProtection_t registerBlockProtection)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### disableGlobalBlockProtection

- **종류**: 함수
- **시그니처**: `INT08U disableGlobalBlockProtection(void)`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:430: INT08U disableGlobalBlockProtection(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### erase4kByte

- **종류**: 함수
- **시그니처**: `INT08U erase4kByte(INT32U addressStart)`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **인근 주석**: Address has to start at begin of sector
- **용도(읽는 법)**: 헤더 주석: Address has to start at begin of sector
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:454: INT08U erase4kByte(INT32U addressStart)`
  - `Core/Src/FlashSST26.c:467: printf("[EXT_SFLASH] erase4kByte. CMD: %x, Address: 0x%04lx\r\n", cmd[0], addressStart);`
  - `Core/Src/FlashSST26.c:1297: erase4kByte(TEST_PAGE_ADDRESS);`
  - `Core/Src/FlashSST26.c:1366: erase4kByte(nAddress + (i * EXT_MEMORY_BLOCK));`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### eraseBlock

- **종류**: 함수
- **시그니처**: `INT08U eraseBlock(INT32U addressStart)`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **인근 주석**: Address has to start at begin of Block
- **용도(읽는 법)**: 헤더 주석: Address has to start at begin of Block
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:494: INT08U eraseBlock(INT32U addressStart)`
  - `Core/Src/FlashSST26.c:507: printf("[EXT_SFLASH] eraseBlock. CMD: %x, Address: %lx\r\n", cmd[0], addressStart);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### eraseBlock_64K

- **종류**: 함수
- **시그니처**: `INT08U eraseBlock_64K(INT32U addressStart)`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **인근 주석**: Address has to start at begin of Block
- **용도(읽는 법)**: 헤더 주석: Address has to start at begin of Block
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:534: INT08U eraseBlock_64K(INT32U addressStart)`
  - `Core/Src/FlashSST26.c:1498: nResult = eraseBlock_64K(nStartAddress + (i * EXT_MEMORY_64K));`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### eraseAll

- **종류**: 함수
- **시그니처**: `INT08U eraseAll(void)`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:574: INT08U eraseAll(void)`
  - `Core/Src/FlashSST26.c:581: printf("[EXT_SFLASH] eraseAll. CMD: %x\r\n", cmd[0]);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### writeByte

- **종류**: 함수
- **시그니처**: `INT08U writeByte(INT32U addressStart, INT08U data[])`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **인근 주석**: Adress has to start at begin of page
- **용도(읽는 법)**: 헤더 주석: Adress has to start at begin of page
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:609: INT08U writeByte(INT32U addressStart, INT08U data[])`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### writePage

- **종류**: 함수
- **시그니처**: `INT08U writePage(INT32U addressStart, INT08U page[])`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **인근 주석**: Address has to start at begin of page
- **용도(읽는 법)**: 헤더 주석: Address has to start at begin of page
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:646: INT08U writePage(INT32U addressStart, INT08U page[])`
  - `Core/Src/FlashSST26.c:723: writePage(addressStart + numBuffer * SIZE_PAGE, buffer);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### writeData_Page

- **종류**: 함수
- **시그니처**: `INT08U writeData_Page(INT32U addressStart, INT08U data[], INT32U len) { //Address has to start at begin of page if ((addressStart % SIZE_PAGE) != 0)`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:685: INT08U writeData_Page(INT32U addressStart, INT08U data[], INT32U len)`
  - `Core/Src/FlashSST26.c:1184: nResult = writeData_Page(TEST_PAGE_ADDRESS, nWriteBuffer, SIZE_PAGE);`
  - `Core/Src/FlashSST26.c:1230: nResult = writeData_Page(TEST_PAGE_ADDRESS, nWriteBuffer, SIZE_PAGE);`
  - `Core/Src/FlashSST26.c:1377: nResult = writeData_Page(addr_now, dataPtr, SIZE_PAGE);`
  - `Core/Src/FlashSST26.c:1573: nResult = writeData_Page(addr_now, dataPtr, nWriteSize);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### readData

- **종류**: 함수
- **시그니처**: `void readData(INT32U addressStart, INT08U data[], INT32U lenData) { //analyze lenData //nothing to read if (lenData == 0)`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:742: void readData(INT32U addressStart, INT08U data[], INT32U lenData)`
  - `Core/Src/FlashSST26.c:869: readData(address + page * 0x100, buffer, 0x100);`
  - `Core/Src/FlashSST26.c:880: readData(address + numPages * 0x100, buffer, modulo);`
  - `Core/Src/SRM_Parameter.c:1670: readData(nReadAddr, (INT08U*)pReadBuf, sizeof(CellOffsetSTR));`
  - `Core/Src/SRM_Parameter.c:1704: readData(nReadAddr, (INT08U*)pReadBuf, sizeof(CellOffsetSTR));`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### readPage

- **종류**: 함수
- **시그니처**: `void readPage(INT32U addressStart, INT08U page[])`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:781: void readPage(INT32U addressStart, INT08U page[])`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### readBytes

- **종류**: 함수
- **시그니처**: `void readBytes(INT32U addressStart, INT08U bytes[], INT16U numBytes)`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **인근 주석**: nothing to read
- **용도(읽는 법)**: 헤더 주석: nothing to read
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:813: void readBytes(INT32U addressStart, INT08U bytes[], INT16U numBytes)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### readCrc32

- **종류**: 함수
- **시그니처**: `INT32U readCrc32(INT32U address, INT32U lenData)`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **인근 주석**: CRC32 value
- **용도(읽는 법)**: 헤더 주석: CRC32 value
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:851: INT32U readCrc32(INT32U address, INT32U lenData)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### reset

- **종류**: 함수
- **시그니처**: `void reset(void) { } //************************************ // Method: isBusy // FullName: isBusy // Access: public // Returns: INT08U // Qualifier: // Parameter: void //************************************ INT08U isBusy(void)`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:895: void reset(void)`
  - `Core/Src/com_udp.c:549: printf("[TCP] AssemBuf overflow, reset. (assem:%d + new:%d)\r\n",`
  - `Core/Src/com_udp.c:577: printf("[TCP] Invalid MBAP Length:%d, reset AssemBuf\r\n", mbap_length);`
  - `Core/Src/com_udp.c:945: /* DP83838 reset disable */`
  - `Core/Src/system_stm32f7xx.c:9: *      - SystemInit(): This function is called at startup just after reset and`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### calculateCrc32

- **종류**: 함수
- **시그니처**: `INT32U calculateCrc32(INT08U data[], INT32U lenData, INT32U previousCrc32)`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:871: crc32 = calculateCrc32(buffer, 0x100, crc32);`
  - `Core/Src/FlashSST26.c:882: crc32 = calculateCrc32(buffer, modulo, crc32);`
  - `Core/Src/FlashSST26.c:929: INT32U calculateCrc32(INT08U data[], INT32U lenData, INT32U previousCrc32)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### sst26_globalunlock

- **종류**: 함수
- **시그니처**: `void sst26_globalunlock()`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:999: void sst26_globalunlock()`
  - `Core/Src/FlashSST26.c:1156: sst26_globalunlock();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### sst26_waitwritecomplete

- **종류**: 함수
- **시그니처**: `void sst26_waitwritecomplete()`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:1006: void sst26_waitwritecomplete()`
  - `Core/Src/FlashSST26.c:1036: sst26_waitwritecomplete();`
  - `Core/Src/FlashSST26.c:1095: sst26_waitwritecomplete();`
  - `Core/Src/FlashSST26.c:1127: sst26_waitwritecomplete();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### sst26_sectorerase

- **종류**: 함수
- **시그니처**: `static void sst26_sectorerase(uint16_t sector, uint8_t type)`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:1083: static void sst26_sectorerase(uint16_t sector, uint8_t type)`
  - `Core/Src/FlashSST26.c:1108: sst26_sectorerase(startblock, SST26_SE);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

