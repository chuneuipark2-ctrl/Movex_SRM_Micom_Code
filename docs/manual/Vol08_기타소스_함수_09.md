# Vol.08 — 기타 함수 (9)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

com_tml 등.

---

### Set_Lift_OriginDog_Postion_Error

- **종류**: 함수
- **시그니처**: `INT08U Set_Lift_OriginDog_Postion_Error(ParamLiftDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4700: INT08U Set_Lift_OriginDog_Postion_Error(ParamLiftDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:5067: Set_Lift_OriginDog_Postion_Error(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:177: extern INT08U Set_Lift_OriginDog_Postion_Error(ParamLiftDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_OriginDog_Postion_Error_1

- **종류**: 함수
- **시그니처**: `INT08U Set_Lift_OriginDog_Postion_Error_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4722: INT08U Set_Lift_OriginDog_Postion_Error_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:5119: if (pCom->Data[4] & cbits[1]) Set_Lift_OriginDog_Postion_Error_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:178: extern INT08U Set_Lift_OriginDog_Postion_Error_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWr`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_DecelDog_Postion_Error

- **종류**: 함수
- **시그니처**: `INT08U Set_Lift_DecelDog_Postion_Error(ParamLiftDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4750: INT08U Set_Lift_DecelDog_Postion_Error(ParamLiftDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:5068: Set_Lift_DecelDog_Postion_Error(pSetParam);`
  - `Core/Src/dev_SRM.c:53163: Set_Lift_DecelDog_Postion_Error(&nLift_Param);`
  - `Core/Inc/SRM_Parameter.h:179: extern INT08U Set_Lift_DecelDog_Postion_Error(ParamLiftDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_DecelDog_Postion_Error_1

- **종류**: 함수
- **시그니처**: `INT08U Set_Lift_DecelDog_Postion_Error_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4772: INT08U Set_Lift_DecelDog_Postion_Error_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:5120: if (pCom->Data[4] & cbits[2]) Set_Lift_DecelDog_Postion_Error_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:180: extern INT08U Set_Lift_DecelDog_Postion_Error_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrB`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_ForkOut_Jerk_Set

- **종류**: 함수
- **시그니처**: `INT08U Set_Lift_ForkOut_Jerk_Set(ParamLiftDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4803: INT08U Set_Lift_ForkOut_Jerk_Set(ParamLiftDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:5069: Set_Lift_ForkOut_Jerk_Set(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:181: extern INT08U Set_Lift_ForkOut_Jerk_Set(ParamLiftDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_ForkOut_Jerk_Set_1

- **종류**: 함수
- **시그니처**: `INT08U Set_Lift_ForkOut_Jerk_Set_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrbuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4826: INT08U Set_Lift_ForkOut_Jerk_Set_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrbuf)`
  - `Core/Src/com_tml.c:5121: if (pCom->Data[4] & cbits[3]) Set_Lift_ForkOut_Jerk_Set_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:182: extern INT08U Set_Lift_ForkOut_Jerk_Set_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrbuf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_Invertor_Param

- **종류**: 함수
- **시그니처**: `void Set_Lift_Invertor_Param(ParamLiftDriveSTR* pSetting) { if (pSetting->InvParam.RefOffset < (-999999))`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4854: void Set_Lift_Invertor_Param(ParamLiftDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:5070: Set_Lift_Invertor_Param(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:184: extern void Set_Lift_Invertor_Param(ParamLiftDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_Invertor_Param_1

- **종류**: 함수
- **시그니처**: `void Set_Lift_Invertor_Param_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf) { if (pSetting->InvParam.RefOffset < (-999999))`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4882: void Set_Lift_Invertor_Param_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:5122: if (pCom->Data[4] & cbits[4]) Set_Lift_Invertor_Param_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:185: extern void Set_Lift_Invertor_Param_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_LevelUpOffset

- **종류**: 함수
- **시그니처**: `void Set_Lift_LevelUpOffset(ParamLiftDriveSTR* pSetting) { if (pSetting->LevelUpOffset > (RACK_SET_LEVEL_UP_OFFSET_MAX))`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4912: void Set_Lift_LevelUpOffset(ParamLiftDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:5071: Set_Lift_LevelUpOffset(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:187: extern void Set_Lift_LevelUpOffset(ParamLiftDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_LevelUpOffset_1

- **종류**: 함수
- **시그니처**: `void Set_Lift_LevelUpOffset_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf) { if (pSetting->LevelUpOffset > (RACK_SET_LEVEL_UP_OFFSET_MAX))`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4929: void Set_Lift_LevelUpOffset_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:5123: if (pCom->Data[3] & cbits[5]) Set_Lift_LevelUpOffset_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:188: extern void Set_Lift_LevelUpOffset_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_LevelDnOffset

- **종류**: 함수
- **시그니처**: `void Set_Lift_LevelDnOffset(ParamLiftDriveSTR* pSetting) { if (pSetting->LevelDnOffset > (RACK_SET_LEVEL_DN_OFFSET_MAX))`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4948: void Set_Lift_LevelDnOffset(ParamLiftDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:5072: Set_Lift_LevelDnOffset(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:189: extern void Set_Lift_LevelDnOffset(ParamLiftDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_LevelDnOffset_1

- **종류**: 함수
- **시그니처**: `void Set_Lift_LevelDnOffset_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf) { if (pSetting->LevelDnOffset > RACK_SET_LEVEL_DN_OFFSET_MAX)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4965: void Set_Lift_LevelDnOffset_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:5124: if (pCom->Data[3] & cbits[6]) Set_Lift_LevelDnOffset_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:190: extern void Set_Lift_LevelDnOffset_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_Veriant_LT_Offset

- **종류**: 함수
- **시그니처**: `void Set_Lift_Veriant_LT_Offset(ParamLiftDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4984: void Set_Lift_Veriant_LT_Offset(ParamLiftDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:5073: Set_Lift_Veriant_LT_Offset(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:191: extern void Set_Lift_Veriant_LT_Offset(ParamLiftDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_Veriant_LT_Offset_1

- **종류**: 함수
- **시그니처**: `void Set_Lift_Veriant_LT_Offset_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4994: void Set_Lift_Veriant_LT_Offset_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:5125: if (pCom->Data[3] & cbits[7]) Set_Lift_Veriant_LT_Offset_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:192: extern void Set_Lift_Veriant_LT_Offset_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Save_Lift_Drive_Param

- **종류**: 함수
- **시그니처**: `void Save_Lift_Drive_Param(ParamLiftDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: Save_*: RAM(m_St) + BKPSRAM 기록. UART 즉시 송신 아님. 지상반 0x30 폴링.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4999: void Save_Lift_Drive_Param(ParamLiftDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:5149: Save_Lift_Drive_Param(&nWriteParam);`
  - `Core/Src/com_tml.c:5155: Save_Lift_Drive_Param(pSetParam);`
  - `Core/Src/dev_SRM.c:53165: Save_Lift_Drive_Param(&nLift_Param);`
  - `Core/Inc/SRM_Parameter.h:193: extern void Save_Lift_Drive_Param(ParamLiftDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Default_Lift_Drive_Param_Init

- **종류**: 함수
- **시그니처**: `void Default_Lift_Drive_Param_Init(void)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:5031: void Default_Lift_Drive_Param_Init(void)`
  - `Core/Src/SRM_Parameter.c:5133: Default_Lift_Drive_Param_Init();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Lift_Drive_Param_Init

- **종류**: 함수
- **시그니처**: `void Lift_Drive_Param_Init()`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:5121: void Lift_Drive_Param_Init()`
  - `Core/Src/main.c:377: Lift_Drive_Param_Init();`
  - `Core/Inc/SRM_Parameter.h:194: extern void Lift_Drive_Param_Init();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Check_Available_Lift_InPostion_Tolerance

- **종류**: 함수
- **시그니처**: `INT08U Check_Available_Lift_InPostion_Tolerance() { if ((m_ExtSEnv2.LiftDrive.InPos_Tolerance < MIN_LIFT_IN_POS) || (m_ExtSEnv2.LiftDrive.InPos_Tolerance > MAX_LIFT_IN_POS))`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:5185: INT08U Check_Available_Lift_InPostion_Tolerance()`
  - `Core/Src/dev_SRM.c:11302: if (Check_Available_Lift_InPostion_Tolerance())`
  - `Core/Src/dev_SRM.c:11335: if (Check_Available_Lift_InPostion_Tolerance())`
  - `Core/Src/dev_SRM.c:11382: if (Check_Available_Lift_InPostion_Tolerance())`
  - `Core/Src/dev_SRM.c:11414: if (Check_Available_Lift_InPostion_Tolerance())`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Check_Available_Lift_InPostion_Hysteresis

- **종류**: 함수
- **시그니처**: `INT08U Check_Available_Lift_InPostion_Hysteresis() { if ((m_ExtSEnv2.LiftDrive.InPos_Hysteresis < MIN_TRAV_HYSTERESIS) || (m_ExtSEnv2.LiftDrive.InPos_Hysteresis > MAX_TRAV_HYSTERESIS))`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:5203: INT08U Check_Available_Lift_InPostion_Hysteresis()`
  - `Core/Src/dev_SRM.c:14975: if (Check_Available_Lift_InPostion_Hysteresis())`
  - `Core/Src/dev_SRM.c:15833: if (Check_Available_Lift_InPostion_Hysteresis())`
  - `Core/Src/dev_SRM.c:48133: if (Check_Available_Lift_InPostion_Hysteresis()) //íì¤íë¦¬ ìì¤ê°ì ì²´í¬íê² ëìì¼ë`
  - `Core/Inc/SRM_Parameter.h:197: extern INT08U Check_Available_Lift_InPostion_Hysteresis();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Fork_Velocity

- **종류**: 함수
- **시그니처**: `void Set_Fork_Velocity(INT08U nType, UnitVelocitySTR* pVelocity) { switch (nType)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:5222: void Set_Fork_Velocity(INT08U nType, UnitVelocitySTR* pVelocity)`
  - `Core/Src/com_tml.c:5214: Set_Fork_Velocity(i, &pSetParam->Vel[i]);`
  - `Core/Src/com_tml.c:5218: Set_Fork_Velocity(i, &pSetParam->Vel[i]);`
  - `Core/Inc/SRM_Parameter.h:199: extern void Set_Fork_Velocity(INT08U nType, UnitVelocitySTR* pVelocity);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Fork_Velocity_1

- **종류**: 함수
- **시그니처**: `void Set_Fork_Velocity_1(INT08U nType, UnitVelocitySTR* pVelocity, UnitVelocitySTR* pWrbuf) { switch (nType)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:5335: void Set_Fork_Velocity_1(INT08U nType, UnitVelocitySTR* pVelocity, UnitVelocitySTR* pWrbuf)`
  - `Core/Src/com_tml.c:5248: Set_Fork_Velocity_1(i, &pSetParam->Vel[i], &nWriteParam.Vel[i]);`
  - `Core/Src/com_tml.c:5256: Set_Fork_Velocity_1(i, &pSetParam->Vel[i], &nWriteParam.Vel[i]);`
  - `Core/Inc/SRM_Parameter.h:200: extern void Set_Fork_Velocity_1(INT08U nType, UnitVelocitySTR* pVelocity, UnitVelocitySTR* pWrbuf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Fork_InPostion_Tolerance

- **종류**: 함수
- **시그니처**: `void Set_Fork_InPostion_Tolerance(ParamForkDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:5443: void Set_Fork_InPostion_Tolerance(ParamForkDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:5223: Set_Fork_InPostion_Tolerance(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:201: extern void Set_Fork_InPostion_Tolerance(ParamForkDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Fork_InPostion_Tolerance_1

- **종류**: 함수
- **시그니처**: `void Set_Fork_InPostion_Tolerance_1(ParamForkDriveSTR* pSetting, ParamForkDriveSTR* pWrBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:5457: void Set_Fork_InPostion_Tolerance_1(ParamForkDriveSTR* pSetting, ParamForkDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:5264: Set_Fork_InPostion_Tolerance_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:202: extern void Set_Fork_InPostion_Tolerance_1(ParamForkDriveSTR* pSetting, ParamForkDriveSTR* pWrBuf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Fork_InPostion_Hysteresis

- **종류**: 함수
- **시그니처**: `void Set_Fork_InPostion_Hysteresis(ParamForkDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:5474: void Set_Fork_InPostion_Hysteresis(ParamForkDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:5224: Set_Fork_InPostion_Hysteresis(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:203: extern void Set_Fork_InPostion_Hysteresis(ParamForkDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Fork_InPostion_Hysteresis_1

- **종류**: 함수
- **시그니처**: `void Set_Fork_InPostion_Hysteresis_1(ParamForkDriveSTR* pSetting, ParamForkDriveSTR* pWrBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:5488: void Set_Fork_InPostion_Hysteresis_1(ParamForkDriveSTR* pSetting, ParamForkDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:5265: Set_Fork_InPostion_Hysteresis_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:204: extern void Set_Fork_InPostion_Hysteresis_1(ParamForkDriveSTR* pSetting, ParamForkDriveSTR* pWrBuf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

