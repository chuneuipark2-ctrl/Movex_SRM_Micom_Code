# Vol.08 — 기타 함수 (15)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

com_tml 등.

---

### rxStatusReq

- **종류**: 함수
- **시그니처**: `void rxStatusReq(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **인근 주석**: static ControlPanelSTR s_Pre_CP_State; static INT08U s_Pre_KeyState = 0;
- **용도(읽는 법)**: 헤더 주석: static ControlPanelSTR s_Pre_CP_State; static INT08U s_Pre_KeyState = 0;
- **코드 참조 예**:
  - `Core/Src/com_tml.c:2073: void rxStatusReq(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6265: rxStatusReq(port, (TMLComPTR)pCom);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxOperationInfo_Req

- **종류**: 함수
- **시그니처**: `void rxOperationInfo_Req(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:2139: void rxOperationInfo_Req(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6271: case TY_OPERATION_INFO_REQ: rxOperationInfo_Req(port, (TMLComPTR)pCom);      break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxInvertorInfo_Req

- **종류**: 함수
- **시그니처**: `void rxInvertorInfo_Req(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:2169: void rxInvertorInfo_Req(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6273: case TY_INVERTOR_INFO_REQ: rxInvertorInfo_Req(port, (TMLComPTR)pCom);      break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxTaskCmdOrder

- **종류**: 함수
- **시그니처**: `void rxTaskCmdOrder(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: Task: TML 0x40 다중작업. 0x41과 분기 다름.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:2192: void rxTaskCmdOrder(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6275: case TY_CMD_TASK_ORDER: rxTaskCmdOrder(port, (TMLComPTR)pCom);      break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxCmdOrder

- **종류**: 함수
- **시그니처**: `void rxCmdOrder(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:2340: void rxCmdOrder(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6277: case TY_CMD_ORDER: rxCmdOrder(port, (TMLComPTR)pCom);      break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxCmdReferenceSearch

- **종류**: 함수
- **시그니처**: `void rxCmdReferenceSearch(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:2696: void rxCmdReferenceSearch(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6279: case TY_CMD_REFERENCE: rxCmdReferenceSearch(port, (TMLComPTR)pCom);      break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxCmdStart

- **종류**: 함수
- **시그니처**: `void rxCmdStart(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:2825: void rxCmdStart(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6281: case TY_CMD_START: rxCmdStart(port, (TMLComPTR)pCom);      break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxCmd_MoveHome

- **종류**: 함수
- **시그니처**: `void rxCmd_MoveHome(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: HOME/RETURN: 원점복귀 vs 에러복귀 RunMode 혼재. grep으로 확인.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:2888: void rxCmd_MoveHome(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6282: case TY_CMD_MOVE_HOME: rxCmd_MoveHome(port, (TMLComPTR)pCom); break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxCmdMoveMaintanence

- **종류**: 함수
- **시그니처**: `void rxCmdMoveMaintanence(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:2921: void rxCmdMoveMaintanence(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6283: case TY_CMD_MOVE_MAINTANENCE: rxCmdMoveMaintanence(port, (TMLComPTR)pCom); break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxCmdErrorReset

- **종류**: 함수
- **시그니처**: `void rxCmdErrorReset(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:3032: void rxCmdErrorReset(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6285: case TY_CMD_ERROR_RESET: rxCmdErrorReset(port, (TMLComPTR)pCom);      break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxCmdOrderDelete

- **종류**: 함수
- **시그니처**: `void rxCmdOrderDelete(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:3046: void rxCmdOrderDelete(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6286: case TY_CMD_ORDER_DELETE: rxCmdOrderDelete(port, (TMLComPTR)pCom);	break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxModeChange

- **종류**: 함수
- **시그니처**: `void rxModeChange(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:3167: void rxModeChange(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6292: case TY_MODE_CHANGE: rxModeChange(port, (TMLComPTR)pCom);      break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxDigital_IO_Form_Req

- **종류**: 함수
- **시그니처**: `void rxDigital_IO_Form_Req(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:3235: void rxDigital_IO_Form_Req(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6357: case TY_DIO_FORM_REQ: rxDigital_IO_Form_Req(port, (TMLComPTR)pCom);      break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxDigital_IO_Form_Control

- **종류**: 함수
- **시그니처**: `void rxDigital_IO_Form_Control(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:3267: void rxDigital_IO_Form_Control(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6358: case TY_DIO_FORM_CONTROL: rxDigital_IO_Form_Control(port, (TMLComPTR)pCom);      break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxRackEnvReq

- **종류**: 함수
- **시그니처**: `void rxRackEnvReq(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **인근 주석**: RackConfigComSTR nSendRackConfig;
- **용도(읽는 법)**: 헤더 주석: RackConfigComSTR nSendRackConfig;
- **코드 참조 예**:
  - `Core/Src/com_tml.c:3328: void rxRackEnvReq(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6299: /*case TY_RACK_ENV_REQ: rxRackEnvReq(port, (TMLComPTR)pCom);      break;*/`
  - `Core/Src/com_tml.c:6366: /*case TY_RACK_ENV_REQ: rxRackEnvReq(port, (TMLComPTR)pCom);      break;*/`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxRackEnvSet

- **종류**: 함수
- **시그니처**: `void rxRackEnvSet(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **인근 주석**: INT08U i = 0;
- **용도(읽는 법)**: 헤더 주석: INT08U i = 0;
- **코드 참조 예**:
  - `Core/Src/com_tml.c:3360: void rxRackEnvSet(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6300: /*case TY_RACK_ENV_SET: rxRackEnvSet(port, (TMLComPTR)pCom);      break;*/`
  - `Core/Src/com_tml.c:6367: /*case TY_RACK_ENV_SET: rxRackEnvSet(port, (TMLComPTR)pCom);      break;*/`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxCellEnvReq

- **종류**: 함수
- **시그니처**: `void rxCellEnvReq(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:3506: void rxCellEnvReq(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6302: case TY_CELL_ENV_REQ: rxCellEnvReq(port, (TMLComPTR)pCom);      break;`
  - `Core/Src/com_tml.c:6369: case TY_CELL_ENV_REQ: rxCellEnvReq(port, (TMLComPTR)pCom);      break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxCellEnvSet

- **종류**: 함수
- **시그니처**: `void rxCellEnvSet(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:3602: void rxCellEnvSet(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6303: case TY_CELL_ENV_SET: rxCellEnvSet(port, (TMLComPTR)pCom);      break;`
  - `Core/Src/com_tml.c:6370: case TY_CELL_ENV_SET: rxCellEnvSet(port, (TMLComPTR)pCom);      break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxStationEnvReq

- **종류**: 함수
- **시그니처**: `void rxStationEnvReq(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:3876: void rxStationEnvReq(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6308: case TY_STATION_ENV_REQ: rxStationEnvReq(port, (TMLComPTR)pCom); break;`
  - `Core/Src/com_tml.c:6375: case TY_STATION_ENV_REQ: rxStationEnvReq(port, (TMLComPTR)pCom); break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxStationEnvSet

- **종류**: 함수
- **시그니처**: `void rxStationEnvSet(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:3904: void rxStationEnvSet(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6309: case TY_STATION_ENV_SET: rxStationEnvSet(port, (TMLComPTR)pCom); break;`
  - `Core/Src/com_tml.c:6376: case TY_STATION_ENV_SET: rxStationEnvSet(port, (TMLComPTR)pCom); break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxProhibitRackEnvReq

- **종류**: 함수
- **시그니처**: `void rxProhibitRackEnvReq(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:4115: void rxProhibitRackEnvReq(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6311: case TY_PROHIBIT_RACK_ENV_REQ: rxProhibitRackEnvReq(port, (TMLComPTR)pCom); break;`
  - `Core/Src/com_tml.c:6378: case TY_PROHIBIT_RACK_ENV_REQ: rxProhibitRackEnvReq(port, (TMLComPTR)pCom); break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxProhibitRackEnvSet

- **종류**: 함수
- **시그니처**: `void rxProhibitRackEnvSet(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:4138: void rxProhibitRackEnvSet(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6312: case TY_PROHIBIT_RACK_ENV_SET: rxProhibitRackEnvSet(port, (TMLComPTR)pCom); break;`
  - `Core/Src/com_tml.c:6379: case TY_PROHIBIT_RACK_ENV_SET: rxProhibitRackEnvSet(port, (TMLComPTR)pCom); break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxSpecialRackEnvReq

- **종류**: 함수
- **시그니처**: `void rxSpecialRackEnvReq(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **인근 주석**: SpecialRackComSTR nSendComData;
- **용도(읽는 법)**: 헤더 주석: SpecialRackComSTR nSendComData;
- **코드 참조 예**:
  - `Core/Src/com_tml.c:4187: void rxSpecialRackEnvReq(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6314: case TY_SPECIAL_RACK_ENV_REQ: rxSpecialRackEnvReq(port, (TMLComPTR)pCom); break;`
  - `Core/Src/com_tml.c:6381: case TY_SPECIAL_RACK_ENV_REQ: rxSpecialRackEnvReq(port, (TMLComPTR)pCom); break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxSpecialRackEnvSet

- **종류**: 함수
- **시그니처**: `void rxSpecialRackEnvSet(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:4210: void rxSpecialRackEnvSet(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6315: case TY_SPECIAL_RACK_ENV_SET: rxSpecialRackEnvSet(port, (TMLComPTR)pCom); break;`
  - `Core/Src/com_tml.c:6382: case TY_SPECIAL_RACK_ENV_SET: rxSpecialRackEnvSet(port, (TMLComPTR)pCom); break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxRackEnvInitializeSet

- **종류**: 함수
- **시그니처**: `void rxRackEnvInitializeSet(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:4248: void rxRackEnvInitializeSet(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6317: case TY_SETUP_INITIALIZE_SET: rxRackEnvInitializeSet(port, (TMLComPTR)pCom); break;`
  - `Core/Src/com_tml.c:6384: case TY_SETUP_INITIALIZE_SET: rxRackEnvInitializeSet(port, (TMLComPTR)pCom); break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

