# Vol.09 — #define (2)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

매크로 상수.

---

### WORK_STATUS_DOING

- **종류**: `#define` 상수
- **값**: `2`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:2238: else if ((m_St.ForkWork[0].OrderStatus_Fork == WORK_STATUS_DOING)`
  - `Core/Src/com_tml.c:2456: if ((m_St.ForkWork[0].OrderStatus_Move == WORK_STATUS_DOING))  accept[0] |= cbits[0];`
  - `Core/Src/com_tml.c:2475: if (m_St.ForkWork[0].OrderStatus_Fork == WORK_STATUS_DOING)`
  - `Core/Src/com_tml.c:2530: if (m_St.ForkWork[0].OrderStatus_Fork == WORK_STATUS_DOING)`
  - `Core/Src/com_tml.c:2984: if ((m_St.ForkWork[0].OrderStatus_Move == WORK_STATUS_DOING)) //이동 작업 진행중일때`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### WORK_STATUS_FAIL

- **종류**: `#define` 상수
- **값**: `3`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:2240: || (m_St.ForkWork[0].OrderStatus_Fork == WORK_STATUS_FAIL))`
  - `Core/Src/com_tml.c:2483: else if (m_St.ForkWork[0].OrderStatus_Fork == WORK_STATUS_FAIL)`
  - `Core/Src/com_tml.c:2589: else if (m_St.ForkWork[0].OrderStatus_Fork == WORK_STATUS_FAIL)`
  - `Core/Src/dev_SRM.c:47131: || (m_St.ForkWork[0].OrderStatus_Fork == WORK_STATUS_FAIL))`
  - `Core/Src/dev_SRM.c:47322: if ((m_St.ForkWork[0].WorkNum_Move != 0) && (m_St.ForkWork[0].OrderStatus_Move == WORK_STATUS_FAIL))`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### WORK_STATUS_COMPLETE

- **종류**: `#define` 상수
- **값**: `4`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:2539: else if (m_St.ForkWork[0].OrderStatus_Fork == WORK_STATUS_COMPLETE)`
  - `Core/Src/com_tml.c:2584: if (m_St.ForkWork[0].OrderStatus_Fork == WORK_STATUS_COMPLETE)`
  - `Core/Src/dev_SRM.c:46877: if ((m_St.ForkWork[0].OrderStatus_Fork == WORK_STATUS_COMPLETE) \`
  - `Core/Src/dev_SRM.c:46878: && (m_St.ForkWork[0].OrderStatus_Move == WORK_STATUS_COMPLETE))`
  - `Core/Src/dev_SRM.c:47305: WORK_STATUS_COMPLETE, CMD_FORK_STEP_UNLODADED);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### WORK_STATUS_STOP

- **종류**: `#define` 상수
- **값**: `5`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:2239: || (m_St.ForkWork[0].OrderStatus_Fork == WORK_STATUS_STOP)`
  - `Core/Src/com_tml.c:2479: else if (m_St.ForkWork[0].OrderStatus_Fork == WORK_STATUS_STOP)`
  - `Core/Src/com_tml.c:2534: else if (m_St.ForkWork[0].OrderStatus_Fork == WORK_STATUS_STOP)`
  - `Core/Src/dev_SRM.c:46088: m_St.ForkWork[POS_FORK_1].OrderStatus_Fork = WORK_STATUS_STOP;`
  - `Core/Src/dev_SRM.c:47130: || (m_St.ForkWork[0].OrderStatus_Fork == WORK_STATUS_STOP)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### WORK_STEP_NONE

- **종류**: `#define` 상수
- **값**: `0`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Inc/User_Define.h:1180: #define WORK_STEP_NONE				0		//`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### WORK_STEP_CMD_RX

- **종류**: `#define` 상수
- **값**: `1`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Inc/User_Define.h:1181: #define WORK_STEP_CMD_RX			1		//`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### WORK_STEP_FROM_MOVE

- **종류**: `#define` 상수
- **값**: `2`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Inc/User_Define.h:1182: #define WORK_STEP_FROM_MOVE			2		//`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### WORK_STEP_STORE

- **종류**: `#define` 상수
- **값**: `3`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Inc/User_Define.h:1183: #define WORK_STEP_STORE				3		//`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### WORK_STEP_TO_MOVE

- **종류**: `#define` 상수
- **값**: `4`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Inc/User_Define.h:1184: #define WORK_STEP_TO_MOVE			4		//`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### WORK_STEP_UNSTORE

- **종류**: `#define` 상수
- **값**: `5`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Inc/User_Define.h:1185: #define WORK_STEP_UNSTORE			5		//`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### WORK_STEP_WRITE_WORKNUM

- **종류**: `#define` 상수
- **값**: `6`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Inc/User_Define.h:1186: #define WORK_STEP_WRITE_WORKNUM		6		//`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### WORK_STEP_TO_MOVING

- **종류**: `#define` 상수
- **값**: `7`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Inc/User_Define.h:1187: #define WORK_STEP_TO_MOVING			7		//`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### WORK_STEP_TO_MOVE_COMPLETE

- **종류**: `#define` 상수
- **값**: `8`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Inc/User_Define.h:1188: #define WORK_STEP_TO_MOVE_COMPLETE	8		//`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ORDER_CODE_NONE

- **종류**: `#define` 상수
- **값**: `0x00`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/dev_SRM.c:46686: Store_Fork_WorkStatus(0, 0, ORDER_CODE_NONE, WORK_STATUS_NONE, CMD_MOVE_STEP_NONE);`
  - `Core/Src/dev_SRM.c:46700: Store_Fork_WorkStatus(0, 0, ORDER_CODE_NONE, WORK_STATUS_NONE, CMD_FORK_STEP_NONE);`
  - `Core/Src/dev_SRM.c:46889: if (m_St.ForkWork[0].OrderCode_Fork != ORDER_CODE_NONE)`
  - `Core/Src/dev_SRM.c:47021: if (m_St.ForkWork[0].OrderCode_Fork != ORDER_CODE_NONE)`
  - `Core/Src/dev_SRM.c:47078: if (m_St.ForkWork[0].OrderCode_Fork != ORDER_CODE_NONE)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ORDER_CODE_MOVE

- **종류**: `#define` 상수
- **값**: `0x01`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:2389: if (pCmd->OrderCode == ORDER_CODE_MOVE)`
  - `Core/Src/com_tml.c:2454: case ORDER_CODE_MOVE:`
  - `Core/Src/dev_SRM.c:44879: if (OrderCode == ORDER_CODE_MOVE)`
  - `Core/Src/dev_SRM.c:44912: if (OrderCode == ORDER_CODE_MOVE)`
  - `Core/Src/dev_SRM.c:45966: if (pCmd->OrderCode == ORDER_CODE_MOVE)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ORDER_CODE_STORE

- **종류**: `#define` 상수
- **값**: `0x12`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:2395: if ((pCmd->OrderCode == ORDER_CODE_STORE)`
  - `Core/Src/com_tml.c:2470: case ORDER_CODE_STORE: // 입고`
  - `Core/Src/dev_SRM.c:46247: else if ((pCmd->OrderCode == ORDER_CODE_STORE) || (pCmd->OrderCode == ORDER_CODE_UNSTORE) \`
  - `Core/Src/dev_SRM.c:46637: else if ((pCmd->OrderCode == ORDER_CODE_STORE) || (pCmd->OrderCode == ORDER_CODE_UNSTORE) \`
  - `Core/Src/dev_SRM.c:46689: else if ((pCmd->OrderCode == ORDER_CODE_STORE) || (pCmd->OrderCode == ORDER_CODE_UNSTORE) \`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ORDER_CODE_UNSTORE

- **종류**: `#define` 상수
- **값**: `0x13`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:2396: || (pCmd->OrderCode == ORDER_CODE_UNSTORE)`
  - `Core/Src/com_tml.c:2471: case ORDER_CODE_UNSTORE: // 출고`
  - `Core/Src/dev_SRM.c:46247: else if ((pCmd->OrderCode == ORDER_CODE_STORE) || (pCmd->OrderCode == ORDER_CODE_UNSTORE) \`
  - `Core/Src/dev_SRM.c:46637: else if ((pCmd->OrderCode == ORDER_CODE_STORE) || (pCmd->OrderCode == ORDER_CODE_UNSTORE) \`
  - `Core/Src/dev_SRM.c:46689: else if ((pCmd->OrderCode == ORDER_CODE_STORE) || (pCmd->OrderCode == ORDER_CODE_UNSTORE) \`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ORDER_CODE_RACK_TO_RACK

- **종류**: `#define` 상수
- **값**: `0x14`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:2397: || (pCmd->OrderCode == ORDER_CODE_RACK_TO_RACK)`
  - `Core/Src/com_tml.c:2472: case ORDER_CODE_RACK_TO_RACK: // 랙간반송`
  - `Core/Src/dev_SRM.c:46248: || (pCmd->OrderCode == ORDER_CODE_RACK_TO_RACK) || (pCmd->OrderCode == ORDER_CODE_STATION_TO_STATION`
  - `Core/Src/dev_SRM.c:46638: || (pCmd->OrderCode == ORDER_CODE_RACK_TO_RACK) || (pCmd->OrderCode == ORDER_CODE_STATION_TO_STATION`
  - `Core/Src/dev_SRM.c:46690: || (pCmd->OrderCode == ORDER_CODE_RACK_TO_RACK) || (pCmd->OrderCode == ORDER_CODE_STATION_TO_STATION`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ORDER_CODE_STATION_TO_STATION

- **종류**: `#define` 상수
- **값**: `0x15`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:2398: || (pCmd->OrderCode == ORDER_CODE_STATION_TO_STATION)`
  - `Core/Src/com_tml.c:2473: case ORDER_CODE_STATION_TO_STATION: //`
  - `Core/Src/dev_SRM.c:46248: || (pCmd->OrderCode == ORDER_CODE_RACK_TO_RACK) || (pCmd->OrderCode == ORDER_CODE_STATION_TO_STATION`
  - `Core/Src/dev_SRM.c:46638: || (pCmd->OrderCode == ORDER_CODE_RACK_TO_RACK) || (pCmd->OrderCode == ORDER_CODE_STATION_TO_STATION`
  - `Core/Src/dev_SRM.c:46690: || (pCmd->OrderCode == ORDER_CODE_RACK_TO_RACK) || (pCmd->OrderCode == ORDER_CODE_STATION_TO_STATION`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ORDER_CODE_RACK_CHG_DST

- **종류**: `#define` 상수
- **값**: `0x16`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:2527: case ORDER_CODE_RACK_CHG_DST: // 목적지변경`
  - `Core/Src/dev_SRM.c:45970: else if ((pCmd->OrderCode == ORDER_CODE_RACK_CHG_DST)`
  - `Core/Src/dev_SRM.c:45988: if (pCmd->OrderCode == ORDER_CODE_RACK_CHG_DST)`
  - `Core/Src/dev_SRM.c:46427: else if ((pCmd->OrderCode == ORDER_CODE_RACK_CHG_DST) || (pCmd->OrderCode == ORDER_CODE_STATION_CHG_`
  - `Core/Src/dev_SRM.c:46651: else if ((pCmd->OrderCode == ORDER_CODE_RACK_CHG_DST) || (pCmd->OrderCode == ORDER_CODE_STATION_CHG_`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ORDER_CODE_STATION_CHG_DST

- **종류**: `#define` 상수
- **값**: `0x17`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:2528: case ORDER_CODE_STATION_CHG_DST: // 목적지변경`
  - `Core/Src/dev_SRM.c:45971: || (pCmd->OrderCode == ORDER_CODE_STATION_CHG_DST))`
  - `Core/Src/dev_SRM.c:46427: else if ((pCmd->OrderCode == ORDER_CODE_RACK_CHG_DST) || (pCmd->OrderCode == ORDER_CODE_STATION_CHG_`
  - `Core/Src/dev_SRM.c:46651: else if ((pCmd->OrderCode == ORDER_CODE_RACK_CHG_DST) || (pCmd->OrderCode == ORDER_CODE_STATION_CHG_`
  - `Core/Src/dev_SRM.c:46678: || (pCmd->OrderCode == ORDER_CODE_STATION_CHG_DST))`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ORDER_CODE_STICKY

- **종류**: `#define` 상수
- **값**: `0x1A`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:2399: || (pCmd->OrderCode == ORDER_CODE_STICKY))`
  - `Core/Src/com_tml.c:2474: case ORDER_CODE_STICKY:				// Sticky`
  - `Core/Src/dev_SRM.c:46524: else if (pCmd->OrderCode == ORDER_CODE_STICKY)`
  - `Core/Src/dev_SRM.c:46661: else if (pCmd->OrderCode == ORDER_CODE_STICKY)`
  - `Core/Src/dev_SRM.c:46691: || (pCmd->OrderCode == ORDER_CODE_STICKY))`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### DEF_PARAM_COUNT

- **종류**: `#define` 상수
- **값**: `8`
- **정의 위치**: `Core/Inc/User_Define.h`
- **원문 주석**: ////////////////////////////////////////////////////////////////////////////////
- **용도(읽는 법)**: 헤더 주석: ////////////////////////////////////////////////////////////////////////////////
- **코드 참조 예**:
  - `Core/Src/dev_SRM.c:37263: INT16U nParam_Addr[DEF_PARAM_COUNT][2] = { {8364, 128},	// ì¶ë ¥ ì ë¥, mA`
  - `Core/Src/dev_SRM.c:37421: if (s_ReadIndex >= DEF_PARAM_COUNT)`
  - `Core/Inc/User_Define.h:1453: #define DEF_PARAM_COUNT		8`
  - `Core/Inc/User_Define.h:1465: INT32S	Param[DEF_PARAM_COUNT];`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### MAX_EVENT_SECTOR

- **종류**: `#define` 상수
- **값**: `4`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Inc/User_Define.h:1554: #define MAX_EVENT_SECTOR            4`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### MAX_EVENT_PER_SECTOR

- **종류**: `#define` 상수
- **값**: `16384`
- **정의 위치**: `Core/Inc/User_Define.h`
- **원문 주석**: 0x3FFF
- **용도(읽는 법)**: 헤더 주석: 0x3FFF
- **코드 참조 예**:
  - `Core/Inc/User_Define.h:1555: #define MAX_EVENT_PER_SECTOR    16384	// 0x3FFF`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### VIEW_EVENT_COUNT

- **종류**: `#define` 상수
- **값**: `5000`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Inc/User_Define.h:1556: #define VIEW_EVENT_COUNT         5000`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### SAVE_EVENT_TYPE

- **종류**: `#define` 상수
- **값**: `0x06`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: Save_*: RAM(m_St) + BKPSRAM 기록. UART 즉시 송신 아님. 지상반 0x30 폴링.
- **코드 참조 예**:
  - `Core/Inc/User_Define.h:1560: #define SAVE_EVENT_TYPE          0x06`
  - `Core/Inc/User_Define.h:1564: #define SAVE_EVENT_TYPE          0x07`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### MAX_DIO_MODULE_CNT

- **종류**: `#define` 상수
- **값**: `10`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:3700: for (INT08U i = 0; i < MAX_DIO_MODULE_CNT; i++)`
  - `Core/Src/ecat_main.c:3982: for (INT08U i = 0; i < MAX_DIO_MODULE_CNT; i++)`
  - `Core/Src/ecat_main.c:4009: for (INT08U i = 0; i < MAX_DIO_MODULE_CNT; i++)`
  - `Core/Inc/User_Define.h:1928: #define MAX_DIO_MODULE_CNT	10`
  - `Core/Inc/User_Define.h:1932: INT08U	SlaveID[MAX_DIO_MODULE_CNT];`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### MAX_VELOCITY_CNT

- **종류**: `#define` 상수
- **값**: `16`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/dev_SRM.c:39723: if (vel_type < MAX_VELOCITY_CNT)`
  - `Core/Src/dev_SRM.c:39770: if (vel_type < MAX_VELOCITY_CNT)`
  - `Core/Inc/User_Define.h:2073: #define MAX_VELOCITY_CNT	16`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### MAX_GRAPH_DATA_COUNT

- **종류**: `#define` 상수
- **값**: `700`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:185: if (m_GraphLog.Index < MAX_GRAPH_DATA_COUNT)`
  - `Core/Src/alarm.c:204: if (++m_GraphLog.Index >= MAX_GRAPH_DATA_COUNT)  m_GraphLog.Index = MAX_GRAPH_DATA_COUNT;`
  - `Core/Src/alarm.c:256: if (m_GraphLog.Index < MAX_GRAPH_DATA_COUNT)`
  - `Core/Src/alarm.c:271: if (++m_GraphLog.Index >= MAX_GRAPH_DATA_COUNT)  m_GraphLog.Index = MAX_GRAPH_DATA_COUNT;`
  - `Core/Src/com_tml.c:1800: nGraph_Start = MAX_GRAPH_DATA_COUNT / 2;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### INVERTOR_INSTALL_COUNT

- **종류**: `#define` 상수
- **값**: `3`
- **정의 위치**: `Core/Inc/dev_SRM.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2286: for (i = 0; i < INVERTOR_INSTALL_COUNT; i++)`
  - `Core/Src/dev_SRM.c:312: static MoviC_RxPDO  m_SRM_RxPDO[INVERTOR_INSTALL_COUNT];`
  - `Core/Src/dev_SRM.c:313: static MoviC_TxPDO  m_SRM_TxPDO[INVERTOR_INSTALL_COUNT];`
  - `Core/Src/dev_SRM.c:315: static MoviC_RxPDO  m_Send_RxPDO[INVERTOR_INSTALL_COUNT];`
  - `Core/Src/dev_SRM.c:316: static MoviC_TxPDO  m_Recv_TxPDO[INVERTOR_INSTALL_COUNT];`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### INV_TRAVEL

- **종류**: `#define` 상수
- **값**: `0`
- **정의 위치**: `Core/Inc/dev_SRM.h`
- **원문 주석**: 80m/min  500mm/s^2  500mm/s^2   500mm/s^2
- **용도(읽는 법)**: 인버터/EtherCAT 상태. m_ExtSEnv와 함께 확인.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2523: nInitParam.InPositon[INV_TRAVEL].RetryCount = 0;`
  - `Core/Src/SRM_Parameter.c:2524: nInitParam.InPositon[INV_TRAVEL].RetryOffset = 100;`
  - `Core/Src/alarm.c:406: m_BKSram.AlarmLog.Log[m_BKSram.AlarmLog.Inx].InvErr.Position = m_St.Inv_St[INV_TRAVEL].Current_Pos;`
  - `Core/Src/alarm.c:421: if (Get_Inverter_Fault_State(INV_TRAVEL))`
  - `Core/Src/alarm.c:423: m_BKSram.AlarmLog.Log[m_BKSram.AlarmLog.Inx].InvErr.Position = Get_Inverter_CurPosition(INV_TRAVEL);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### INV_HOIST

- **종류**: `#define` 상수
- **값**: `1`
- **정의 위치**: `Core/Inc/dev_SRM.h`
- **원문 주석**: 240m/min  600mm/s^2  600mm/s^2   600mm/s^2
- **용도(읽는 법)**: 인버터/EtherCAT 상태. m_ExtSEnv와 함께 확인.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2525: nInitParam.InPositon[INV_HOIST].RetryCount = 0;`
  - `Core/Src/SRM_Parameter.c:2526: nInitParam.InPositon[INV_HOIST].RetryOffset = 100;`
  - `Core/Src/alarm.c:411: m_BKSram.AlarmLog.Log[m_BKSram.AlarmLog.Inx].InvErr.Position = m_St.Inv_St[INV_HOIST].Current_Pos;`
  - `Core/Src/alarm.c:429: else if (Get_Inverter_Fault_State(INV_HOIST))`
  - `Core/Src/alarm.c:431: m_BKSram.AlarmLog.Log[m_BKSram.AlarmLog.Inx].InvErr.Position = Get_Inverter_CurPosition(INV_HOIST);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### INV_FORK_1

- **종류**: `#define` 상수
- **값**: `2`
- **정의 위치**: `Core/Inc/dev_SRM.h`
- **원문 주석**: 80m/min   200mm/s^2   200mm/s^2  200mm/s^2
- **용도(읽는 법)**: 인버터/EtherCAT 상태. m_ExtSEnv와 함께 확인.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:2527: nInitParam.InPositon[INV_FORK_1].RetryCount = 0;`
  - `Core/Src/SRM_Parameter.c:2528: nInitParam.InPositon[INV_FORK_1].RetryOffset = 100;`
  - `Core/Src/alarm.c:416: m_BKSram.AlarmLog.Log[m_BKSram.AlarmLog.Inx].InvErr.Position = m_St.Inv_St[INV_FORK_1].Current_Pos;`
  - `Core/Src/alarm.c:437: else if (Get_Inverter_Fault_State(INV_FORK_1))`
  - `Core/Src/alarm.c:439: m_BKSram.AlarmLog.Log[m_BKSram.AlarmLog.Inx].InvErr.Position = Get_Inverter_CurPosition(INV_FORK_1);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### INV_FORK_2

- **종류**: `#define` 상수
- **값**: `3`
- **정의 위치**: `Core/Inc/dev_SRM.h`
- **원문 주석**: 80m/min   200mm/s^2   200mm/s^2  200mm/s^2
- **용도(읽는 법)**: 인버터/EtherCAT 상태. m_ExtSEnv와 함께 확인.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:3133: m_St.Inv_St[INV_FORK_2].Target_Pos = 0;`
  - `Core/Src/com_tml.c:5498: m_St.Inv_St[INV_FORK_2].Status2.IO_Control = 0;`
  - `Core/Src/com_tml.c:5502: m_St.Inv_St[INV_FORK_2].Status2.IO_Control = 1;`
  - `Core/Src/dev_SRM.c:63680: if (m_St.Inv_St[INV_FORK_2].Status2.Connect == 1)`
  - `Core/Src/dev_SRM.c:63684: m_St.Inv_St[INV_FORK_2].Status2.Com_Control = 0;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### FORK_1

- **종류**: `#define` 상수
- **값**: `0`
- **정의 위치**: `Core/Inc/dev_SRM.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:5533: m_pgmEnv.Fork_IO_Manual_Control[FORK_1] = 1;`
  - `Core/Src/com_tml.c:5537: m_pgmEnv.Fork_IO_Manual_Control[FORK_1] = 0;`
  - `Core/Src/dev_SRM.c:46771: m_TestStatus.St[DEBUG_STR_2] = WorkData[FORK_1].TravPos;`
  - `Core/Src/dev_SRM.c:46772: m_TestStatus.St[DEBUG_STR_3] = WorkData[FORK_1].LiftPos;`
  - `Core/Src/dev_SRM.c:46773: m_TestStatus.St[DEBUG_STR_4] = WorkData[FORK_1].ForkAct;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### FORK_2

- **종류**: `#define` 상수
- **값**: `1`
- **정의 위치**: `Core/Inc/dev_SRM.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:5545: m_pgmEnv.Fork_IO_Manual_Control[FORK_2] = 1;`
  - `Core/Src/com_tml.c:5549: m_pgmEnv.Fork_IO_Manual_Control[FORK_2] = 0;`
  - `Core/Src/dev_SRM.c:63682: if (m_pgmEnv.Fork_IO_Manual_Control[FORK_2] == 1)`
  - `Core/Src/dev_SRM.c:63942: m_pgmEnv.Fork_IO_Manual_Control[FORK_2] = 0;`
  - `Core/Src/dev_SRM.c:64021: m_pgmEnv.Fork_IO_Manual_Control[FORK_2] = 0;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

