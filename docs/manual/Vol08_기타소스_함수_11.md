# Vol.08 — 기타 함수 (11)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

com_tml 등.

---

### Clear_TestControl

- **종류**: 함수
- **시그니처**: `void Clear_TestControl(INT32S nIndex) { if ((nIndex >= DEBUG_CTR_1) && (nIndex <= DEBUG_CTR_16))`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:57: void Clear_TestControl(INT32S nIndex)`
  - `Core/Inc/alarm.h:43: extern void Clear_TestControl(INT32S nIndex);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### All_Clear_TestControl

- **종류**: 함수
- **시그니처**: `void All_Clear_TestControl()`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:65: void All_Clear_TestControl()`
  - `Core/Src/com_tml.c:1165: All_Clear_TestControl();`
  - `Core/Inc/alarm.h:44: extern void All_Clear_TestControl();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### SetTimer1ms

- **종류**: 함수
- **시그니처**: `void SetTimer1ms(INT32U* nTimer)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:70: void SetTimer1ms(INT32U* nTimer)`
  - `Core/Src/alarm.c:638: SetTimer1ms(&s_InputTimer[i]);`
  - `Core/Src/alarm.c:680: SetTimer1ms(&s_InputFilterTimer);`
  - `Core/Src/alarm.c:735: SetTimer1ms(&s_InputFilterTimer);`
  - `Core/Src/can.c:189: SetTimer1ms(&s_StepTimer);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### SetTimer100us

- **종류**: 함수
- **시그니처**: `void SetTimer100us(INT32U* nTimer)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:75: void SetTimer100us(INT32U* nTimer)`
  - `Core/Inc/alarm.h:47: extern void SetTimer100us(INT32U*);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### getCalcTimer100us

- **종류**: 함수
- **시그니처**: `INT32U getCalcTimer100us(INT32U startTime)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:1190: TEST_PAGE_ADDRESS, SIZE_PAGE, getCalcTimer100us(nProcessTime));`
  - `Core/Src/FlashSST26.c:1240: TEST_PAGE_ADDRESS, SIZE_PAGE, getCalcTimer100us(nProcessTime));`
  - `Core/Src/FlashSST26.c:1276: TEST_PAGE_ADDRESS, SIZE_PAGE, getCalcTimer100us(nProcessTime));`
  - `Core/Src/FlashSST26.c:1320: nReadAddress, SIZE_PAGE, getCalcTimer100us(nProcessTime));`
  - `Core/Src/Mcu_Test.c:587: if (getCalcTimer100us(s_chkTimer) >= 100)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### getCalcTimer1ms

- **종류**: 함수
- **시그니처**: `INT32U getCalcTimer1ms(INT32U startTime)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:1299: printf("[EXT_SFLASH] Erase page. Process Time: %ldms\r\n", getCalcTimer1ms(nProcessTime));`
  - `Core/Src/FlashSST26.c:1417: nAddress, size, getCalcTimer1ms(nProcessTime));`
  - `Core/Src/FlashSST26.c:1456: nAddress, size, getCalcTimer1ms(nProcessTime));`
  - `Core/Src/FlashSST26.c:1515: nStartAddress, size, getCalcTimer1ms(nProcessTime));`
  - `Core/Src/FlashSST26.c:1520: nStartAddress, size, getCalcTimer1ms(nProcessTime));`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### wait100us

- **종류**: 함수
- **시그니처**: `void wait100us(INT32U time)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:96: void wait100us(INT32U time)`
  - `Core/Inc/alarm.h:52: extern void wait100us(INT32U);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### wait1ms

- **종류**: 함수
- **시그니처**: `void wait1ms(INT32U time)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:479: wait1ms(DURATION_25_MILLIS);`
  - `Core/Src/FlashSST26.c:519: wait1ms(DURATION_25_MILLIS);`
  - `Core/Src/FlashSST26.c:559: wait1ms(DURATION_25_MILLIS);`
  - `Core/Src/FlashSST26.c:593: wait1ms(DURATION_50_MILLIS);`
  - `Core/Src/FlashSST26.c:668: wait1ms(DURATION_1_MILLIS);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### wait1ms_no_wd

- **종류**: 함수
- **시그니처**: `void wait1ms_no_wd(INT32U time)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:122: void wait1ms_no_wd(INT32U time)`
  - `Core/Inc/alarm.h:54: extern void wait1ms_no_wd(INT32U);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### uSleep_ms

- **종류**: 함수
- **시그니처**: `void uSleep_ms(INT32U time)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:130: void uSleep_ms(INT32U time)`
  - `Core/Src/ecat_main.c:885: uSleep_ms(1000);`
  - `Core/Src/ecat_main.c:927: uSleep_ms(EC_LOCALDELAY);`
  - `Core/Src/ecat_main.c:988: uSleep_ms(EC_LOCALDELAY);`
  - `Core/Src/ecat_main.c:1037: uSleep_ms(EC_LOCALDELAY);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### MCU_Output_Control

- **종류**: 함수
- **시그니처**: `void MCU_Output_Control(INT08U nPin, INT08U nFlag)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:849: MCU_Output_Control(i, 1);`
  - `Core/Src/Mcu_Test.c:853: MCU_Output_Control(i, 0);`
  - `Core/Src/alarm.c:147: void MCU_Output_Control(INT08U nPin, INT08U nFlag)`
  - `Core/Src/dev_SRM.c:7065: MCU_Output_Control(m_DIO_SetInfo.Set.DO_Set[nPort].Pin, nDoFlag);`
  - `Core/Src/dev_SRM.c:7102: MCU_Output_Control(m_DIO_SetInfo.Set.DO_Set2[nPort_Index].Pin, nDoFlag);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Graph_Update

- **종류**: 함수
- **시그니처**: `void Graph_Update(void)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:175: void Graph_Update(void)`
  - `Core/Src/dev_SRM.c:40195: Graph_Update();`
  - `Core/Inc/alarm.h:10: extern void		Graph_Update(void);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Graph_Update_Mode

- **종류**: 함수
- **시그니처**: `void Graph_Update_Mode(void)`
- **정의 위치**: `Core/Src/alarm.c`
- **인근 주석**: Interrpt interval : 10ms
- **용도(읽는 법)**: 헤더 주석: Interrpt interval : 10ms
- **코드 참조 예**:
  - `Core/Src/alarm.c:211: void Graph_Update_Mode(void)`
  - `Core/Src/dev_SRM.c:6901: Graph_Update_Mode();`
  - `Core/Src/dev_SRM.c:6917: Graph_Update_Mode();`
  - `Core/Src/dev_SRM.c:6930: Graph_Update_Mode();`
  - `Core/Src/dev_SRM.c:6943: Graph_Update_Mode();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### clearGraph

- **종류**: 함수
- **시그니처**: `void clearGraph(void)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:239: clearGraph();`
  - `Core/Src/alarm.c:280: void clearGraph(void)`
  - `Core/Src/alarm.c:357: clearGraph();`
  - `Core/Src/dev_SRM.c:30956: clearGraph();`
  - `Core/Src/dev_SRM.c:32060: clearGraph();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Graph_Setup_Init

- **종류**: 함수
- **시그니처**: `void Graph_Setup_Init()`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:291: void Graph_Setup_Init()`
  - `Core/Src/main.c:567: Graph_Setup_Init();`
  - `Core/Inc/alarm.h:13: void			Graph_Setup_Init();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Graph_Setup

- **종류**: 함수
- **시그니처**: `void Graph_Setup(INT08U nGraphType, INT08U nInterval, INT32U nStart_Postion)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:326: void Graph_Setup(INT08U nGraphType, INT08U nInterval, INT32U nStart_Postion)`
  - `Core/Src/com_tml.c:1827: Graph_Setup(nGraph_Type, nGraph_Interval, nGraph_Start_mm);`
  - `Core/Inc/alarm.h:14: void			Graph_Setup(INT08U nGraphType, INT08U nInterval, INT32U nStart_Postion);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### add_alarm_log

- **종류**: 함수
- **시그니처**: `void add_alarm_log(INT08U MainCode, INT08U SubCode, INT08U Value) { // BackUpSRAM if (m_BKSram.AlarmLog.Inx >= MAX_ALARM_LOG_COUNT)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:365: void add_alarm_log(INT08U MainCode, INT08U SubCode, INT08U Value)`
  - `Core/Src/alarm.c:505: add_alarm_log(m_St.MainErrCode, m_St.SubErrCode, error_reason);`
  - `Core/Src/alarm.c:522: add_alarm_log(m_St.MainErrCode, m_St.SubErrCode, error_reason);`
  - `Core/Src/dev_SRM.c:67265: add_alarm_log(0, 2, 0);`
  - `Core/Src/dev_SRM.c:67472: add_alarm_log(0, 3, s_Level - 1);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### save_error_code

- **종류**: 함수
- **시그니처**: `void save_error_code(INT08U main_error_code, INT08U sub_error_code, INT16U error_reason)`
- **정의 위치**: `Core/Src/alarm.c`
- **인근 주석**: SRM_Warning_reset();
- **용도(읽는 법)**: Save_*: RAM(m_St) + BKPSRAM 기록. UART 즉시 송신 아님. 지상반 0x30 폴링.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:182: save_error_code(ERROR1_MCU_PARAM_DATA, 1, 1);`
  - `Core/Src/SRM_Parameter.c:209: save_error_code(ERROR1_MCU_PARAM_DATA, 1, 2);`
  - `Core/Src/SRM_Parameter.c:435: save_error_code(ERROR1_MCU_PARAM_DATA, 2, 1);`
  - `Core/Src/SRM_Parameter.c:451: save_error_code(ERROR1_MCU_PARAM_DATA, 2, 0);`
  - `Core/Src/SRM_Parameter.c:572: save_error_code(ERROR1_MCU_PARAM_DATA, 3, 0);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### save_error_code_Invertor

- **종류**: 함수
- **시그니처**: `void save_error_code_Invertor(INT08U main_error_code, INT08U sub_error_code, INT16U error_reason) { if (m_St.SRM_Mode.Bit.Auto)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: Save_*: RAM(m_St) + BKPSRAM 기록. UART 즉시 송신 아님. 지상반 0x30 폴링.
- **코드 참조 예**:
  - `Core/Src/alarm.c:509: void save_error_code_Invertor(INT08U main_error_code, INT08U sub_error_code, INT16U error_reason)`
  - `Core/Src/dev_SRM.c:48592: save_error_code_Invertor(ERROR1_INVERTER_TRAVEL, 1, 2);`
  - `Core/Src/dev_SRM.c:48596: save_error_code_Invertor(ERROR1_INVERTER_TRAVEL, 1, 1);`
  - `Core/Src/dev_SRM.c:48601: save_error_code_Invertor(ERROR1_INVERTER_TRAVEL, 1, 1);`
  - `Core/Src/dev_SRM.c:48642: save_error_code_Invertor(ERROR1_INVERTER_HOIST, 1, 2);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Event_Warning_Code

- **종류**: 함수
- **시그니처**: `void Event_Warning_Code(INT08U main_error_code, INT08U sub_error_code, INT16U error_reason) { if (m_St.SRM_Status1.Bit.Fault == 0)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:525: void Event_Warning_Code(INT08U main_error_code, INT08U sub_error_code, INT16U error_reason)`
  - `Core/Src/com_tml.c:1124: Event_Warning_Code(m_pgmEnv.Test_Simulator.Test_Fault_MainCode, m_pgmEnv.Test_Simulator.Test_Fault_S`
  - `Core/Src/com_tml.c:2359: Event_Warning_Code(WARNING_CODE_13, 0, 0);`
  - `Core/Src/com_tml.c:5418: Event_Warning_Code(WARNING_CODE_15, 0, 0);`
  - `Core/Src/com_tml.c:5425: Event_Warning_Code(WARNING_CODE_10, 0, 0);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### controlCpuReset

- **종류**: 함수
- **시그니처**: `void controlCpuReset(void)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:541: void controlCpuReset(void)`
  - `Core/Src/com_tml.c:546: if (is_reset)	controlCpuReset();`
  - `Core/Src/com_tml.c:631: if (is_reset)	controlCpuReset();`
  - `Core/Src/com_tml.c:4355: if (is_reset)	controlCpuReset();`
  - `Core/Src/upgrade.c:855: controlCpuReset();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_MCU_Input_FilterTime

- **종류**: 함수
- **시그니처**: `void Set_MCU_Input_FilterTime(void)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:552: void Set_MCU_Input_FilterTime(void)`
  - `Core/Src/alarm.c:613: Set_MCU_Input_FilterTime();`
  - `Core/Src/com_tml.c:3319: Set_MCU_Input_FilterTime();`
  - `Core/Inc/alarm.h:22: extern void			Set_MCU_Input_FilterTime(void);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### get_main_controller_di

- **종류**: 함수
- **시그니처**: `void get_main_controller_di(void)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:589: void get_main_controller_di(void)`
  - `Core/Src/alarm.c:754: get_main_controller_di();`
  - `Core/Inc/alarm.h:23: extern void           get_main_controller_di(void);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### get_main_controller_Mode_SW

- **종류**: 함수
- **시그니처**: `void get_main_controller_Mode_SW(void)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:663: void get_main_controller_Mode_SW(void)`
  - `Core/Src/alarm.c:755: get_main_controller_Mode_SW();`
  - `Core/Inc/alarm.h:24: extern void           get_main_controller_Mode_SW(void);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### get_main_controller_ID_SW

- **종류**: 함수
- **시그니처**: `void get_main_controller_ID_SW(void)`
- **정의 위치**: `Core/Src/alarm.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:709: void get_main_controller_ID_SW(void)`
  - `Core/Src/alarm.c:756: get_main_controller_ID_SW();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

