# Vol.08 — 기타 함수 (13)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

com_tml 등.

---

### CAN1_Tx_Message

- **종류**: 함수
- **시그니처**: `void CAN1_Tx_Message(uint32_t nStdID, uint32_t nLen, uint8_t* pData)`
- **정의 위치**: `Core/Src/can.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/can.c:24: void CAN1_Tx_Message(uint32_t nStdID, uint32_t nLen, uint8_t* pData);`
  - `Core/Src/can.c:75: CAN1_Tx_Message(0x003, 8, (INT08U*)RxData);`
  - `Core/Src/can.c:90: CAN1_Tx_Message(0x004, 8, (INT08U*)RxData);`
  - `Core/Src/can.c:102: void CAN1_Tx_Message(uint32_t nStdID, uint32_t nLen, uint8_t* pData)`
  - `Core/Src/can.c:204: CAN1_Tx_Message(0x010, 4, csend);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### CAN2_Tx_Message

- **종류**: 함수
- **시그니처**: `void CAN2_Tx_Message(uint32_t nStdID, uint32_t nLen, uint8_t* pData)`
- **정의 위치**: `Core/Src/can.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/can.c:141: void CAN2_Tx_Message(uint32_t nStdID, uint32_t nLen, uint8_t* pData)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### CAN_Manager

- **종류**: 함수
- **시그니처**: `void CAN_Manager(void)`
- **정의 위치**: `Core/Src/can.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/can.c:179: void CAN_Manager(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsStartCheck

- **종류**: 함수
- **시그니처**: `static void rsStartCheck(void) { if (rsComData == FRAME_STX) { if (++rsComLen >= FRAME_SYNC_SIZE)`
- **정의 위치**: `Core/Src/com_Debug.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_Debug.c:31: static void rsStartCheck(void)`
  - `Core/Src/com_Debug.c:105: case 0: rsStartCheck();          break;`
  - `Core/Src/com_hmi.c:96: static void rsStartCheck(void)`
  - `Core/Src/com_hmi.c:170: case 0: rsStartCheck();          break;`
  - `Core/Src/com_tml.c:36: static void rsStartCheck(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsHeaderCheck

- **종류**: 함수
- **시그니처**: `static void rsHeaderCheck(void)`
- **정의 위치**: `Core/Src/com_Debug.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_Debug.c:45: static void rsHeaderCheck(void)`
  - `Core/Src/com_Debug.c:106: case 1: rsHeaderCheck();         break;`
  - `Core/Src/com_hmi.c:110: static void rsHeaderCheck(void)`
  - `Core/Src/com_hmi.c:171: case 1: rsHeaderCheck();         break;`
  - `Core/Src/com_tml.c:50: static void rsHeaderCheck(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsDataCheck

- **종류**: 함수
- **시그니처**: `static void rsDataCheck(void)`
- **정의 위치**: `Core/Src/com_Debug.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_Debug.c:63: static void rsDataCheck(void)`
  - `Core/Src/com_Debug.c:107: case 2: rsDataCheck();           break;`
  - `Core/Src/com_hmi.c:128: static void rsDataCheck(void)`
  - `Core/Src/com_hmi.c:172: case 2: rsDataCheck();           break;`
  - `Core/Src/com_tml.c:68: static void rsDataCheck(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsCrcCheck

- **종류**: 함수
- **시그니처**: `static void rsCrcCheck(void)`
- **정의 위치**: `Core/Src/com_Debug.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_Debug.c:75: static void rsCrcCheck(void)`
  - `Core/Src/com_Debug.c:108: case 3: rsCrcCheck();            break;`
  - `Core/Src/com_hmi.c:140: static void rsCrcCheck(void)`
  - `Core/Src/com_hmi.c:173: case 3: rsCrcCheck();            break;`
  - `Core/Src/com_tml.c:80: static void rsCrcCheck(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsEndCheck

- **종류**: 함수
- **시그니처**: `static void rsEndCheck(void) { if ((rsComData == FRAME_ETX) && (rsCRCValue == 0))`
- **정의 위치**: `Core/Src/com_Debug.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_Debug.c:86: static void rsEndCheck(void)`
  - `Core/Src/com_Debug.c:109: case 4: rsEndCheck();            break;`
  - `Core/Src/com_hmi.c:151: static void rsEndCheck(void)`
  - `Core/Src/com_hmi.c:174: case 4: rsEndCheck();            break;`
  - `Core/Src/com_tml.c:91: static void rsEndCheck(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsComHandler_1

- **종류**: 함수
- **시그니처**: `void rsComHandler_1(INT08U rsData)`
- **정의 위치**: `Core/Src/com_Debug.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_Debug.c:99: void rsComHandler_1(INT08U rsData)`
  - `Core/Src/stm32f7xx_it.c:361: rsComHandler_1(nTmp);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### McuComManager

- **종류**: 함수
- **시그니처**: `void McuComManager(void)`
- **정의 위치**: `Core/Src/com_Debug.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_Debug.c:120: void McuComManager(void)`
  - `Core/Src/main.c:825: McuComManager();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Request_DSP_Status

- **종류**: 함수
- **시그니처**: `void Request_DSP_Status(INT08U port, INT08U SlaveNo)`
- **정의 위치**: `Core/Src/com_dsp.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_dsp.c:31: void Request_DSP_Status(INT08U port, INT08U SlaveNo)`
  - `Core/Src/com_dsp.c:471: Request_DSP_Status( s_ComPort, m_Slave_Index);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Request_DSP_Control

- **종류**: 함수
- **시그니처**: `void Request_DSP_Control(INT08U port, INT08U SlaveNo)`
- **정의 위치**: `Core/Src/com_dsp.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_dsp.c:73: void Request_DSP_Control(INT08U port, INT08U SlaveNo)`
  - `Core/Src/com_dsp.c:533: Request_DSP_Control(s_ComPort, m_Slave_Index);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Update_DSP_Screen_Test

- **종류**: 함수
- **시그니처**: `void Update_DSP_Screen_Test(void)`
- **정의 위치**: `Core/Src/com_dsp.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_dsp.c:148: void Update_DSP_Screen_Test(void)`
  - `Core/Src/com_dsp.c:527: Update_DSP_Screen_Test();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Update_DSP_Screen

- **종류**: 함수
- **시그니처**: `void Update_DSP_Screen(void)`
- **정의 위치**: `Core/Src/com_dsp.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_dsp.c:172: void Update_DSP_Screen(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### DSP_ComManager

- **종류**: 함수
- **시그니처**: `void DSP_ComManager(void)`
- **정의 위치**: `Core/Src/com_dsp.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_dsp.c:390: void DSP_ComManager(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### RS485_Mode_Control

- **종류**: 함수
- **시그니처**: `void RS485_Mode_Control(INT08U nFlag)`
- **정의 위치**: `Core/Src/com_hmi.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_dsp.c:455: RS485_Mode_Control(RS485_RX_MODE);`
  - `Core/Src/com_hmi.c:39: void RS485_Mode_Control(INT08U nFlag)`
  - `Core/Src/com_hmi.c:233: RS485_Mode_Control(RS485_TX_MODE);`
  - `Core/Src/com_hmi.c:256: RS485_Mode_Control(RS485_RX_MODE);`
  - `Core/Src/com_hmi.c:308: RS485_Mode_Control(RS485_RX_MODE);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### RS422_Mode_Enable

- **종류**: 함수
- **시그니처**: `void RS422_Mode_Enable(void)`
- **정의 위치**: `Core/Src/com_hmi.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_hmi.c:66: void RS422_Mode_Enable(void)`
  - `Core/Src/com_hmi.c:314: RS422_Mode_Enable();`
  - `Core/Src/com_hmi.c:338: RS422_Mode_Enable();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsStartCheck

- **종류**: 함수
- **시그니처**: `static void rsStartCheck(void) { if (rsComData == FRAME_STX) { if (++rsComLen >= FRAME_SYNC_SIZE)`
- **정의 위치**: `Core/Src/com_hmi.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_Debug.c:31: static void rsStartCheck(void)`
  - `Core/Src/com_Debug.c:105: case 0: rsStartCheck();          break;`
  - `Core/Src/com_hmi.c:96: static void rsStartCheck(void)`
  - `Core/Src/com_hmi.c:170: case 0: rsStartCheck();          break;`
  - `Core/Src/com_tml.c:36: static void rsStartCheck(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsHeaderCheck

- **종류**: 함수
- **시그니처**: `static void rsHeaderCheck(void)`
- **정의 위치**: `Core/Src/com_hmi.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_Debug.c:45: static void rsHeaderCheck(void)`
  - `Core/Src/com_Debug.c:106: case 1: rsHeaderCheck();         break;`
  - `Core/Src/com_hmi.c:110: static void rsHeaderCheck(void)`
  - `Core/Src/com_hmi.c:171: case 1: rsHeaderCheck();         break;`
  - `Core/Src/com_tml.c:50: static void rsHeaderCheck(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsDataCheck

- **종류**: 함수
- **시그니처**: `static void rsDataCheck(void)`
- **정의 위치**: `Core/Src/com_hmi.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_Debug.c:63: static void rsDataCheck(void)`
  - `Core/Src/com_Debug.c:107: case 2: rsDataCheck();           break;`
  - `Core/Src/com_hmi.c:128: static void rsDataCheck(void)`
  - `Core/Src/com_hmi.c:172: case 2: rsDataCheck();           break;`
  - `Core/Src/com_tml.c:68: static void rsDataCheck(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsCrcCheck

- **종류**: 함수
- **시그니처**: `static void rsCrcCheck(void)`
- **정의 위치**: `Core/Src/com_hmi.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_Debug.c:75: static void rsCrcCheck(void)`
  - `Core/Src/com_Debug.c:108: case 3: rsCrcCheck();            break;`
  - `Core/Src/com_hmi.c:140: static void rsCrcCheck(void)`
  - `Core/Src/com_hmi.c:173: case 3: rsCrcCheck();            break;`
  - `Core/Src/com_tml.c:80: static void rsCrcCheck(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsEndCheck

- **종류**: 함수
- **시그니처**: `static void rsEndCheck(void) { if ((rsComData == FRAME_ETX) && (rsCRCValue == 0))`
- **정의 위치**: `Core/Src/com_hmi.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_Debug.c:86: static void rsEndCheck(void)`
  - `Core/Src/com_Debug.c:109: case 4: rsEndCheck();            break;`
  - `Core/Src/com_hmi.c:151: static void rsEndCheck(void)`
  - `Core/Src/com_hmi.c:174: case 4: rsEndCheck();            break;`
  - `Core/Src/com_tml.c:91: static void rsEndCheck(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsComHandler_485

- **종류**: 함수
- **시그니처**: `void rsComHandler_485(INT08U rsData)`
- **정의 위치**: `Core/Src/com_hmi.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_hmi.c:164: void rsComHandler_485(INT08U rsData)`
  - `Core/Src/stm32f7xx_it.c:409: rsComHandler_485(nTmp);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### HMI_Com_Receive_Proc

- **종류**: 함수
- **시그니처**: `void HMI_Com_Receive_Proc()`
- **정의 위치**: `Core/Src/com_hmi.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_dsp.c:402: HMI_Com_Receive_Proc();`
  - `Core/Src/com_hmi.c:185: void HMI_Com_Receive_Proc()`
  - `Core/Src/com_hmi.c:347: HMI_Com_Receive_Proc();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### HMI_Com_Transmit_Proc

- **종류**: 함수
- **시그니처**: `void HMI_Com_Transmit_Proc()`
- **정의 위치**: `Core/Src/com_hmi.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_dsp.c:597: HMI_Com_Transmit_Proc();`
  - `Core/Src/com_hmi.c:221: void HMI_Com_Transmit_Proc()`
  - `Core/Src/com_hmi.c:349: HMI_Com_Transmit_Proc();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

