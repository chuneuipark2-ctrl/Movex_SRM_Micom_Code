# Vol.08 — 기타 함수 (17)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

com_tml 등.

---

### rxDataAnalysis

- **종류**: 함수
- **시그니처**: `void rxDataAnalysis(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **인근 주석**: m_TestStatus.Ctrl[DEBUG_CTR_3] = pCom->Header.DstID.Type; m_TestStatus.Ctrl[DEBUG_CTR_4] = pCom->Header.DstID.Index;
- **용도(읽는 법)**: 헤더 주석: m_TestStatus.Ctrl[DEBUG_CTR_3] = pCom->Header.DstID.Type; m_TestStatus.Ctrl[DEBUG_CTR_4] = pCom->Header.DstID.Index;
- **코드 참조 예**:
  - `Core/Src/com_Debug.c:133: rxDataAnalysis(rsMCU_DEBUG_Port, (TMLComPTR)&DebugCom);`
  - `Core/Src/com_hmi.c:197: rxDataAnalysis(rsRS485_422_Port, (TMLComPTR)&HMICom);`
  - `Core/Src/com_tml.c:6230: void rxDataAnalysis(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6467: rxDataAnalysis(rsTMLPort, (TMLComPTR)&TMLCom);`
  - `Core/Src/com_udp.c:267: rxDataAnalysis(rsETHUDPPort_Uplink, (TMLComSTR*)&pCom.Header);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### TMLComManager

- **종류**: 함수
- **시그니처**: `void TMLComManager(void)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:6448: void TMLComManager(void)`
  - `Core/Src/main.c:827: TMLComManager();`
  - `Core/Inc/com_tml.h:141: extern void TMLComManager(void);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### udp_com_init

- **종류**: 함수
- **시그니처**: `void udp_com_init(void)`
- **정의 위치**: `Core/Src/com_udp.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_udp.c:71: void udp_com_init(void)`
  - `Core/Src/com_udp.c:969: udp_com_init();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### udp_com_init_DSP

- **종류**: 함수
- **시그니처**: `void udp_com_init_DSP()`
- **정의 위치**: `Core/Src/com_udp.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_dsp.c:431: udp_com_init_DSP();`
  - `Core/Src/com_udp.c:100: void udp_com_init_DSP()`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### udp_Check_Fw_Download_IP_Info

- **종류**: 함수
- **시그니처**: `INT08U udp_Check_Fw_Download_IP_Info(const ip_addr_t* pAddr, INT16U nRemotePort) { if (memcmp(pAddr, &m_udp_IPAddr_FW_Download, sizeof(ip_addr_t)))`
- **정의 위치**: `Core/Src/com_udp.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_udp.c:124: INT08U udp_Check_Fw_Download_IP_Info(const ip_addr_t* pAddr, INT16U nRemotePort)`
  - `Core/Src/com_udp.c:262: if (udp_Check_Fw_Download_IP_Info(addr, port))`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### udp_Set_Fw_Download_IP_Info

- **종류**: 함수
- **시그니처**: `void udp_Set_Fw_Download_IP_Info()`
- **정의 위치**: `Core/Src/com_udp.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_udp.c:140: * @brief	udp_Set_Fw_Download_IP_Info`
  - `Core/Src/com_udp.c:144: void udp_Set_Fw_Download_IP_Info()`
  - `Core/Src/upgrade.c:588: udp_Set_Fw_Download_IP_Info();`
  - `Core/Src/upgrade.c:593: udp_Set_Fw_Download_IP_Info();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### udp_Process_echo

- **종류**: 함수
- **시그니처**: `void udp_Process_echo(void* arg, struct udp_pcb* upcb, struct pbuf* p, const ip_addr_t* addr, u16_t port)`
- **정의 위치**: `Core/Src/com_udp.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_udp.c:47: void udp_Process_echo(void* arg, struct udp_pcb* upcb, struct pbuf* p, const ip_addr_t* addr, u16_t `
  - `Core/Src/com_udp.c:162: void udp_Process_echo(void* arg, struct udp_pcb* upcb, struct pbuf* p, const ip_addr_t* addr, u16_t `

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### checkUdpServerDataFrame

- **종류**: 함수
- **시그니처**: `INT08U checkUdpServerDataFrame(UDPComSTR* pCom)`
- **정의 위치**: `Core/Src/com_udp.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_udp.c:183: INT08U checkUdpServerDataFrame(UDPComSTR* pCom)`
  - `Core/Src/com_udp.c:244: rxSt = checkUdpServerDataFrame(&pCom);`
  - `Core/Src/com_udp.c:323: rxSt = checkUdpServerDataFrame(&pCom);`
  - `Core/Src/fpgatcpipstack.c:628: rxSt = checkUdpServerDataFrame(pCom);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### udp_RecvProcess

- **종류**: 함수
- **시그니처**: `void udp_RecvProcess(void* arg, struct udp_pcb* upcb, struct pbuf* p, const ip_addr_t* addr, u16_t port)`
- **정의 위치**: `Core/Src/com_udp.c`
- **인근 주석**: INT08U	ret = 0, rxSt = 0;
- **용도(읽는 법)**: 헤더 주석: INT08U	ret = 0, rxSt = 0;
- **코드 참조 예**:
  - `Core/Src/com_udp.c:48: void udp_RecvProcess(void* arg, struct udp_pcb* upcb, struct pbuf* p, const ip_addr_t* addr, u16_t p`
  - `Core/Src/com_udp.c:88: udp_recv(upcb, udp_RecvProcess, NULL);`
  - `Core/Src/com_udp.c:217: void udp_RecvProcess(void* arg, struct udp_pcb* upcb, struct pbuf* p, const ip_addr_t* addr, u16_t p`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### udp_RecvProcess_1

- **종류**: 함수
- **시그니처**: `void udp_RecvProcess_1(void* arg, struct udp_pcb* upcb, struct pbuf* p, const ip_addr_t* addr, u16_t port)`
- **정의 위치**: `Core/Src/com_udp.c`
- **인근 주석**: INT08U	ret = 0, rxSt = 0;
- **용도(읽는 법)**: 헤더 주석: INT08U	ret = 0, rxSt = 0;
- **코드 참조 예**:
  - `Core/Src/com_udp.c:50: void udp_RecvProcess_1(void* arg, struct udp_pcb* upcb, struct pbuf* p, const ip_addr_t* addr, u16_t`
  - `Core/Src/com_udp.c:117: udp_recv(upcb, udp_RecvProcess_1, NULL);`
  - `Core/Src/com_udp.c:296: void udp_RecvProcess_1(void* arg, struct udp_pcb* upcb, struct pbuf* p, const ip_addr_t* addr, u16_t`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### udp_SendBuf

- **종류**: 함수
- **시그니처**: `void udp_SendBuf()`
- **정의 위치**: `Core/Src/com_udp.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:286: udp_SendBuf();`
  - `Core/Src/com_udp.c:52: void udp_SendBuf();`
  - `Core/Src/com_udp.c:351: void udp_SendBuf()`
  - `Core/Src/com_udp.c:739: * @brief  TCP 데이터 전송 (udp_SendBuf 와 동일한 방식으로 사용)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### udp_SendBuf_DSP

- **종류**: 함수
- **시그니처**: `void udp_SendBuf_DSP(INT32U* IPaddr, INT16U RemotePort)`
- **정의 위치**: `Core/Src/com_udp.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:306: udp_SendBuf_DSP((INT32U*)m_pgmEnv.DSPUdpDstIP, m_pgmEnv.DSPUdpDstPort);`
  - `Core/Src/com_udp.c:405: void udp_SendBuf_DSP(INT32U* IPaddr, INT16U RemotePort)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### tcp_client_close

- **종류**: 함수
- **시그니처**: `static void tcp_client_close(void) { if (m_pTcpPcb != NULL)`
- **정의 위치**: `Core/Src/com_udp.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_udp.c:459: static void tcp_client_close(void)`
  - `Core/Src/com_udp.c:853: tcp_client_close();  /* 기존 PCB 정리 */`
  - `Core/Src/com_udp.c:880: tcp_client_close();`
  - `Core/Src/com_udp.c:897: tcp_client_close();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### tcp_cb_err

- **종류**: 함수
- **시그니처**: `static void tcp_cb_err(void* arg, err_t err)`
- **정의 위치**: `Core/Src/com_udp.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_udp.c:63: static void   tcp_cb_err(void* arg, err_t err);`
  - `Core/Src/com_udp.c:640: static void tcp_cb_err(void* arg, err_t err)`
  - `Core/Src/com_udp.c:868: tcp_err(m_pTcpPcb, tcp_cb_err);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### tcp_client_init

- **종류**: 함수
- **시그니처**: `void tcp_client_init(void)`
- **정의 위치**: `Core/Src/com_udp.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_udp.c:680: void tcp_client_init(void)`
  - `Core/Src/com_udp.c:970: tcp_client_init();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### tcp_client_process

- **종류**: 함수
- **시그니처**: `void tcp_client_process(void) { switch (m_TcpState)`
- **정의 위치**: `Core/Src/com_udp.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_udp.c:707: void tcp_client_process(void)`
  - `Core/Src/com_udp.c:982: tcp_client_process();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### tcp_client_SendBuf

- **종류**: 함수
- **시그니처**: `INT08U tcp_client_SendBuf(INT08U* pData, INT16U nLen)`
- **정의 위치**: `Core/Src/com_udp.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_udp.c:669: /* 예: tcp_client_SendBuf((INT08U*)"HB", 2); */`
  - `Core/Src/com_udp.c:744: INT08U tcp_client_SendBuf(INT08U* pData, INT16U nLen)`
  - `Core/Src/dev_SRM.c:197: tcp_client_SendBuf((INT08U*)&ModbusTxPkt.TransactionID, len + 6);`
  - `Core/Src/dev_SRM.c:240: tcp_client_SendBuf((INT08U*)&ModbusTxPkt.TransactionID, len + 6);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### tcp_client_IsConnected

- **종류**: 함수
- **시그니처**: `INT08U tcp_client_IsConnected(void)`
- **정의 위치**: `Core/Src/com_udp.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_udp.c:808: INT08U tcp_client_IsConnected(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### tcp_client_GetSocketState

- **종류**: 함수
- **시그니처**: `INT08U tcp_client_GetSocketState(void) { switch (m_TcpState)`
- **정의 위치**: `Core/Src/com_udp.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_udp.c:822: INT08U tcp_client_GetSocketState(void)`
  - `Core/Src/dev_SRM.c:65901: s_now_SSR = tcp_client_GetSocketState();`
  - `Core/Src/dev_SRM.c:65937: s_now_SSR = tcp_client_GetSocketState();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### tcp_client_connect

- **종류**: 함수
- **시그니처**: `void tcp_client_connect(void)`
- **정의 위치**: `Core/Src/com_udp.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_udp.c:677: *         변수 초기화만 담당, tcp_client_connect() 는 호출하지 않음`
  - `Core/Src/com_udp.c:692: /* ★ tcp_client_connect() 호출하지 않음`
  - `Core/Src/com_udp.c:702: *   tcp_client_connect() 호출 권한은 호출자(HMI_UpLink_ModbusTCP_Manager 등)에게 있음.`
  - `Core/Src/com_udp.c:712: /* ★ 자동 connect 제거 - 외부에서 tcp_client_connect() 명시적 호출 */`
  - `Core/Src/com_udp.c:849: void tcp_client_connect(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EtherNetManager

- **종류**: 함수
- **시그니처**: `void EtherNetManager(void)`
- **정의 위치**: `Core/Src/com_udp.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_udp.c:676: * @brief  TCP 클라이언트 초기화 - EtherNetManager() case 3 에서 1회만 호출`
  - `Core/Src/com_udp.c:698: *         EtherNetManager() case 4: MX_LWIP_Process(); 다음에 호출`
  - `Core/Src/com_udp.c:917: void EtherNetManager(void)`
  - `Core/Src/main.c:829: EtherNetManager();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_SDO_error

- **종류**: 함수
- **시그니처**: `void EC_SDO_error(EC_Main_t* pEC_Main, uint16 Slave, uint16 Index, uint8 SubIdx, int32 AbortCode)`
- **정의 위치**: `Core/Src/ecat_coe.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_coe.c:13: void EC_SDO_error(EC_Main_t* pEC_Main, uint16 Slave, uint16 Index, uint8 SubIdx, int32 AbortCode)`
  - `Core/Src/ecat_coe.c:181: EC_SDO_error(pEC_Main, slave, index, subindex, etohl(pSDOa->ldata[0]));`
  - `Core/Src/ecat_coe.c:209: EC_SDO_error(pEC_Main, slave, index, subindex, etohl(pSDOa->ldata[0]));`
  - `Core/Src/ecat_coe.c:276: EC_SDO_error(pEC_Main, Slave, Index, SubIndex, etohl(aSDOp->ldata[0]));`
  - `Core/Src/ecat_coe.c:392: EC_SDO_error(pEC_Main, Slave, Index, SubIndex, etohl(aSDOp->ldata[0]));`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_SDO_info_error

- **종류**: 함수
- **시그니처**: `static void EC_SDO_info_error(EC_Main_t* pEC_Main, uint16 Slave, uint16 Index, uint8 SubIdx, int32 AbortCode)`
- **정의 위치**: `Core/Src/ecat_coe.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_coe.c:33: static void EC_SDO_info_error(EC_Main_t* pEC_Main, uint16 Slave, uint16 Index, uint8 SubIdx, int32 A`
  - `Core/Src/ecat_coe.c:868: EC_SDO_info_error(pEC_Main, Slave, 0, 0, 0xf000000);`
  - `Core/Src/ecat_coe.c:895: EC_SDO_info_error(pEC_Main, Slave, 0, 0, etohl(aSDOp->ldata[0]));`
  - `Core/Src/ecat_coe.c:971: EC_SDO_info_error(pEC_Main, Slave, pODlist->Index[Item], 0, etohl(aSDOp->ldata[0]));`
  - `Core/Src/ecat_coe.c:1049: EC_SDO_info_error(pEC_Main, Slave, Index, SubI, etohl(aSDOp->ldata[0]));`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_SDO_Read

- **종류**: 함수
- **시그니처**: `int EC_SDO_Read(EC_Main_t* pEC_Main, uint16 slave, uint16 index, uint8 subindex, boolean CA, int* psize, void* p, int timeout)`
- **정의 위치**: `Core/Src/ecat_coe.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_coe.c:47: int EC_SDO_Read(EC_Main_t* pEC_Main, uint16 slave, uint16 index, uint8 subindex, boolean CA, int* ps`
  - `Core/Src/ecat_coe.c:530: wkc = EC_SDO_Read(pEC_Main, Slave, PDOassign, 0x00, FALSE, &rdl, &rdat, EC_TIMEOUT_RXM);`
  - `Core/Src/ecat_coe.c:540: wkc = EC_SDO_Read(pEC_Main, Slave, PDOassign, (uint8)idxloop, FALSE, &rdl, &rdat, EC_TIMEOUT_RXM);`
  - `Core/Src/ecat_coe.c:545: wkc = EC_SDO_Read(pEC_Main, Slave, idx, 0x00, FALSE, &rdl, &subcnt, EC_TIMEOUT_RXM);`
  - `Core/Src/ecat_coe.c:550: wkc = EC_SDO_Read(pEC_Main, Slave, idx, (uint8)subidxloop, FALSE, &rdl, &rdat2, EC_TIMEOUT_RXM);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EC_SDO_Write

- **종류**: 함수
- **시그니처**: `int EC_SDO_Write(EC_Main_t* pEC_Main, uint16 Slave, uint16 Index, uint8 SubIndex, boolean CA, int psize, void* p, int Timeout)`
- **정의 위치**: `Core/Src/ecat_coe.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_coe.c:222: int EC_SDO_Write(EC_Main_t* pEC_Main, uint16 Slave, uint16 Index, uint8 SubIndex, boolean CA, int ps`
  - `Core/Src/ecat_coe.c:1103: return EC_SDO_Write(&m_EC_Main, Slave, Index, SubIndex, CA, psize, p, Timeout);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

