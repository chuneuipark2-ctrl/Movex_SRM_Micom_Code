# Vol.08 — 기타 함수 (32)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

com_tml 등.

---

### initProgram

- **종류**: 함수
- **시그니처**: `void initProgram(void)`
- **정의 위치**: `Core/Src/upgrade.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/main.c:811: initProgram();`
  - `Core/Src/upgrade.c:294: void initProgram(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### initDownload

- **종류**: 함수
- **시그니처**: `void initDownload(void)`
- **정의 위치**: `Core/Src/upgrade.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/upgrade.c:429: void initDownload(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Firmware_Info

- **종류**: 함수
- **시그니처**: `void Set_Firmware_Info(DownloadInfo_t* pSetting)`
- **정의 위치**: `Core/Src/upgrade.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/upgrade.c:443: void Set_Firmware_Info(DownloadInfo_t* pSetting)`
  - `Core/Src/upgrade.c:832: Set_Firmware_Info(&m_pgmEnv.Fw_Info);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxDownloadStart

- **종류**: 함수
- **시그니처**: `void rxDownloadStart(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/upgrade.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:6351: case TY_DOWNLOAD_START: rxDownloadStart(port, (TMLComPTR)pCom);      break;`
  - `Core/Src/upgrade.c:482: void rxDownloadStart(INT08U port, TMLComPTR pCom)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxDownloadData

- **종류**: 함수
- **시그니처**: `void rxDownloadData(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/upgrade.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:6352: case TY_DOWNLOAD_DATA: rxDownloadData(port, (TMLComPTR)pCom);      break;`
  - `Core/Src/upgrade.c:640: void rxDownloadData(INT08U port, TMLComPTR pCom)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxDownloadFinish

- **종류**: 함수
- **시그니처**: `void rxDownloadFinish(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/upgrade.c`
- **인근 주석**: INT16U fileCrc = 0, calCrc = 0;
- **용도(읽는 법)**: 헤더 주석: INT16U fileCrc = 0, calCrc = 0;
- **코드 참조 예**:
  - `Core/Src/com_tml.c:6353: case TY_DOWNLOAD_END: rxDownloadFinish(port, (TMLComPTR)pCom);      break;`
  - `Core/Src/upgrade.c:784: void rxDownloadFinish(INT08U port, TMLComPTR pCom)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### checkDownload

- **종류**: 함수
- **시그니처**: `void checkDownload(void) { if (m_pgmEnv.downloadMode)`
- **정의 위치**: `Core/Src/upgrade.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/main.c:839: checkDownload();`
  - `Core/Src/upgrade.c:886: void checkDownload(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### OpenFile

- **종류**: 함수
- **시그니처**: `void OpenFile()`
- **정의 위치**: `Core/Src/usb_user.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/usb_user.c:58: void OpenFile()`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### CloseFile

- **종류**: 함수
- **시그니처**: `void CloseFile()`
- **정의 위치**: `Core/Src/usb_user.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/usb_user.c:64: void CloseFile()`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### USB_Test_Proc

- **종류**: 함수
- **시그니처**: `void USB_Test_Proc(void)`
- **정의 위치**: `Core/Src/usb_user.c`
- **인근 주석**: const char FileName[] = "Test.txt"; const char FileName[] = "1:Test.txt";
- **용도(읽는 법)**: 헤더 주석: const char FileName[] = "Test.txt"; const char FileName[] = "1:Test.txt";
- **코드 참조 예**:
  - `Core/Src/usb_user.c:101: void USB_Test_Proc(void)`
  - `Core/Src/usb_user.c:472: USB_Test_Proc();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### USB_Test_Proc_1

- **종류**: 함수
- **시그니처**: `INT08U USB_Test_Proc_1(INT08U nEnable)`
- **정의 위치**: `Core/Src/usb_user.c`
- **인근 주석**: const char FileName[] = "Test.txt"; const char FileName[] = "1:Test.txt";
- **용도(읽는 법)**: 헤더 주석: const char FileName[] = "Test.txt"; const char FileName[] = "1:Test.txt";
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:888: m_McuTestSt.USB_Test = USB_Test_Proc_1(s_Test_Flag);`
  - `Core/Src/usb_user.c:278: INT08U USB_Test_Proc_1(INT08U nEnable)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### User_MSC_Proc

- **종류**: 함수
- **시그니처**: `void User_MSC_Proc(void)`
- **정의 위치**: `Core/Src/usb_user.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/usb_user.c:470: void User_MSC_Proc(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

