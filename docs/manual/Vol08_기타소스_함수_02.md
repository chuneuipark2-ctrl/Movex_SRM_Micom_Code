# Vol.08 — 기타 함수 (2)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

com_tml 등.

---

### sst26_erase

- **종류**: 함수
- **시그니처**: `int sst26_erase(uint16_t startblock, uint16_t nblocks)`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:1099: int sst26_erase(uint16_t startblock, uint16_t nblocks)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### sst26_chiperase

- **종류**: 함수
- **시그니처**: `int sst26_chiperase()`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:1115: int sst26_chiperase()`
  - `Core/Src/com_tml.c:1746: sst26_chiperase();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### sst_init

- **종류**: 함수
- **시그니처**: `void sst_init()`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:1131: void sst_init()`
  - `Core/Src/main.c:809: sst_init();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Test_SFlash_Page_WriteRead

- **종류**: 함수
- **시그니처**: `void Test_SFlash_Page_WriteRead()`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:1166: void Test_SFlash_Page_WriteRead()`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Test_SFlash_Page_WriteRead_1

- **종류**: 함수
- **시그니처**: `void Test_SFlash_Page_WriteRead_1()`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:1215: void Test_SFlash_Page_WriteRead_1()`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Test_SFlash_Page_Read

- **종류**: 함수
- **시그니처**: `void Test_SFlash_Page_Read()`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:1263: void Test_SFlash_Page_Read()`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Test_SFlash_Page_Erase

- **종류**: 함수
- **시그니처**: `void Test_SFlash_Page_Erase()`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:1291: void Test_SFlash_Page_Erase()`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### SFlash_Page_Read

- **종류**: 함수
- **시그니처**: `void SFlash_Page_Read(INT32U nReadAddress)`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:1305: void SFlash_Page_Read(INT32U nReadAddress)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ExtFlash_WriteData

- **종류**: 함수
- **시그니처**: `INT08U ExtFlash_WriteData(INT32U nAddress, INT08U* buff, INT32U size)`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:1339: INT08U ExtFlash_WriteData(INT32U nAddress, INT08U* buff, INT32U size)`
  - `Core/Src/SRM_Parameter.c:102: ExtFlash_WriteData(EXT_DEVICE_SET_ADDR_START, (INT08U*)&m_ExtSEnv, sizeof(ExtFlashSEnv));`
  - `Core/Src/SRM_Parameter.c:145: ExtFlash_WriteData(EXT_DEVICE_SET_ADDR_START, (INT08U*)&m_ExtSEnv, sizeof(ExtFlashSEnv));`
  - `Core/Src/SRM_Parameter.c:259: ExtFlash_WriteData(EXT_DEVICE_SET_ADDR_START, (INT08U*)&m_ExtSEnv, sizeof(ExtFlashSEnv));`
  - `Core/Src/SRM_Parameter.c:266: ExtFlash_WriteData(EXT_DEVICE_SET_ADDR_START, (INT08U*)&m_ExtSEnv, sizeof(ExtFlashSEnv));`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ExtFlash_ReadData

- **종류**: 함수
- **시그니처**: `void ExtFlash_ReadData(INT32U nAddress, INT08U* buff, INT32U size)`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:1431: void ExtFlash_ReadData(INT32U nAddress, INT08U* buff, INT32U size)`
  - `Core/Src/SRM_Parameter.c:75: ExtFlash_ReadData(EXT_DEVICE_SET_ADDR_START, (INT08U*)&m_ExtSEnv, sizeof(ExtFlashSEnv));`
  - `Core/Src/SRM_Parameter.c:117: ExtFlash_ReadData(EXT_DEVICE_SET_ADDR_START, (INT08U*)&m_ExtSEnv, sizeof(ExtFlashSEnv));`
  - `Core/Src/SRM_Parameter.c:172: ExtFlash_ReadData(EXT_DEVICE_SET_ADDR_START, (INT08U*)&m_ExtSEnv, sizeof(ExtFlashSEnv));`
  - `Core/Src/SRM_Parameter.c:285: ExtFlash_ReadData(EXT_DEVICE_SET_ADDR_START, (INT08U*)&m_ExtSEnv, sizeof(ExtFlashSEnv));`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ExtFlash_EraseData

- **종류**: 함수
- **시그니처**: `INT08U ExtFlash_EraseData(INT32U nStartAddress, INT32U size)`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:1466: INT08U ExtFlash_EraseData(INT32U nStartAddress, INT32U size)`
  - `Core/Src/upgrade.c:581: nEraseFlag = ExtFlash_EraseData(startAddr, pgmSize);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ExtFlash_WriteFW

- **종류**: 함수
- **시그니처**: `INT08U ExtFlash_WriteFW(INT32U nAddress, INT08U* buff, INT32U size, INT16U* pCrc)`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:1534: INT08U ExtFlash_WriteFW(INT32U nAddress, INT08U* buff, INT32U size, INT16U* pCrc)`
  - `Core/Src/upgrade.c:726: response = ExtFlash_WriteFW(flashadr, (INT08U*)&pCom->Data[6], download_size, &m_Download_CRC);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Calc_CRC_FlahsData

- **종류**: 함수
- **시그니처**: `INT16U Calc_CRC_FlahsData(INT32U nAddress, INT32U nTotalSize)`
- **정의 위치**: `Core/Src/FlashSST26.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:1622: * @brief	Calc_CRC_FlahsData`
  - `Core/Src/FlashSST26.c:1627: INT16U Calc_CRC_FlahsData(INT32U nAddress, INT32U nTotalSize)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### PTO_Test_Proc

- **종류**: 함수
- **시그니처**: `void PTO_Test_Proc()`
- **정의 위치**: `Core/Src/Mcu_Test.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:38: void PTO_Test_Proc()`
  - `Core/Src/Mcu_Test.c:770: void PTO_Test_Proc(INT08U* s_InitFlag)`
  - `Core/Src/Mcu_Test.c:955: PTO_Test_Proc(&s_TestFlag.PTO_Enable);`
  - `Core/Src/Mcu_Test.c:990: PTO_Test_Proc(&s_TestFlag.PTO_Enable);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Test_MCU_Test_Control

- **종류**: 함수
- **시그니처**: `void Test_MCU_Test_Control()`
- **정의 위치**: `Core/Src/Mcu_Test.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:94: void Test_MCU_Test_Control()`
  - `Core/Src/Mcu_Test.c:967: Test_MCU_Test_Control();	// Mcu Test 제어 테스트 코드. 기능 구현 후 주석 처리.`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Update_Mcu_Test_Status

- **종류**: 함수
- **시그니처**: `void Update_Mcu_Test_Status()`
- **정의 위치**: `Core/Src/Mcu_Test.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:142: void Update_Mcu_Test_Status()`
  - `Core/Src/Mcu_Test.c:965: Update_Mcu_Test_Status();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### FND_Test_Proc

- **종류**: 함수
- **시그니처**: `void FND_Test_Proc(INT08U* s_InitFlag)`
- **정의 위치**: `Core/Src/Mcu_Test.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:156: void FND_Test_Proc(INT08U* s_InitFlag)`
  - `Core/Src/Mcu_Test.c:969: FND_Test_Proc(&s_TestFlag.FND_Display);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### RS232_Test_Proc

- **종류**: 함수
- **시그니처**: `void RS232_Test_Proc(INT08U* s_InitFlag)`
- **정의 위치**: `Core/Src/Mcu_Test.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:183: void RS232_Test_Proc(INT08U* s_InitFlag)`
  - `Core/Src/Mcu_Test.c:971: RS232_Test_Proc(&s_TestFlag.RS232_Enable);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### RS485_Test1_Proc

- **종류**: 함수
- **시그니처**: `void RS485_Test1_Proc(INT08U* s_InitFlag)`
- **정의 위치**: `Core/Src/Mcu_Test.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:217: void RS485_Test1_Proc(INT08U* s_InitFlag)`
  - `Core/Src/Mcu_Test.c:973: RS485_Test1_Proc(&s_TestFlag.RS485_P1_Enable);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### RS485_Test2_Proc

- **종류**: 함수
- **시그니처**: `void RS485_Test2_Proc(INT08U* s_InitFlag)`
- **정의 위치**: `Core/Src/Mcu_Test.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:257: void RS485_Test2_Proc(INT08U* s_InitFlag)`
  - `Core/Src/Mcu_Test.c:974: RS485_Test2_Proc(&s_TestFlag.RS485_P2_Enable);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### RS422_Test1_Proc

- **종류**: 함수
- **시그니처**: `void RS422_Test1_Proc(INT08U* s_InitFlag)`
- **정의 위치**: `Core/Src/Mcu_Test.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:297: void RS422_Test1_Proc(INT08U* s_InitFlag)`
  - `Core/Src/Mcu_Test.c:975: RS422_Test1_Proc(&s_TestFlag.RS422_P1_Enable);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### RS422_Test2_Proc

- **종류**: 함수
- **시그니처**: `void RS422_Test2_Proc(INT08U* s_InitFlag)`
- **정의 위치**: `Core/Src/Mcu_Test.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:337: void RS422_Test2_Proc(INT08U* s_InitFlag)`
  - `Core/Src/Mcu_Test.c:976: RS422_Test2_Proc(&s_TestFlag.RS422_P2_Enable);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Ethernet1_Test_Proc

- **종류**: 함수
- **시그니처**: `void Ethernet1_Test_Proc(INT08U* s_InitFlag)`
- **정의 위치**: `Core/Src/Mcu_Test.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:377: void Ethernet1_Test_Proc(INT08U* s_InitFlag)`
  - `Core/Src/Mcu_Test.c:981: Ethernet1_Test_Proc(&s_TestFlag.EtherNet1_Enable);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Ethernet2_Test_Proc

- **종류**: 함수
- **시그니처**: `void Ethernet2_Test_Proc(INT08U* s_InitFlag)`
- **정의 위치**: `Core/Src/Mcu_Test.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:411: void Ethernet2_Test_Proc(INT08U* s_InitFlag)`
  - `Core/Src/Mcu_Test.c:982: Ethernet2_Test_Proc(&s_TestFlag.EtherNet2_Enable);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### CAN1_Test_Proc

- **종류**: 함수
- **시그니처**: `void CAN1_Test_Proc(INT08U* s_InitFlag)`
- **정의 위치**: `Core/Src/Mcu_Test.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:445: void CAN1_Test_Proc(INT08U* s_InitFlag)`
  - `Core/Src/Mcu_Test.c:978: CAN1_Test_Proc(&s_TestFlag.CAN1_Enable);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

