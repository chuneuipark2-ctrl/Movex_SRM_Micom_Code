# Vol.08 — 기타 함수 (8)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

com_tml 등.

---

### Default_Travel_Drive_Param_Init

- **종류**: 함수
- **시그니처**: `void Default_Travel_Drive_Param_Init(void)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:3655: void Default_Travel_Drive_Param_Init(void)`
  - `Core/Src/SRM_Parameter.c:3754: Default_Travel_Drive_Param_Init();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Travel_Drive_Param_Init

- **종류**: 함수
- **시그니처**: `void Travel_Drive_Param_Init()`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:3742: void Travel_Drive_Param_Init()`
  - `Core/Src/main.c:376: Travel_Drive_Param_Init();`
  - `Core/Inc/SRM_Parameter.h:150: extern void Travel_Drive_Param_Init();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Check_Available_Trav_InPostion_Tolerance

- **종류**: 함수
- **시그니처**: `INT08U Check_Available_Trav_InPostion_Tolerance() { if ((m_ExtSEnv2.TravDrive.InPos_Tolerance < MIN_TRAV_IN_POS) || (m_ExtSEnv2.TravDrive.InPos_Tolerance > MAX_TRAV_IN_POS))`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:3805: INT08U Check_Available_Trav_InPostion_Tolerance()`
  - `Core/Src/dev_SRM.c:11031: if (Check_Available_Trav_InPostion_Tolerance())`
  - `Core/Src/dev_SRM.c:11063: if (Check_Available_Trav_InPostion_Tolerance())`
  - `Core/Src/dev_SRM.c:11107: if (Check_Available_Trav_InPostion_Tolerance())`
  - `Core/Src/dev_SRM.c:11143: if (Check_Available_Trav_InPostion_Tolerance())`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Check_Available_Trav_InPostion_Hysteresis

- **종류**: 함수
- **시그니처**: `INT08U Check_Available_Trav_InPostion_Hysteresis() { if ((m_ExtSEnv2.TravDrive.InPos_Hysteresis < MIN_TRAV_HYSTERESIS) || (m_ExtSEnv2.TravDrive.InPos_Hysteresis > MAX_TRAV_HYSTERESIS))`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:3823: INT08U Check_Available_Trav_InPostion_Hysteresis()`
  - `Core/Src/dev_SRM.c:14966: if (Check_Available_Trav_InPostion_Hysteresis())`
  - `Core/Src/dev_SRM.c:15824: if (Check_Available_Trav_InPostion_Hysteresis())`
  - `Core/Src/dev_SRM.c:22204: if (Check_Available_Trav_InPostion_Hysteresis())`
  - `Core/Src/dev_SRM.c:22638: if (Check_Available_Trav_InPostion_Hysteresis())`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_Velocity

- **종류**: 함수
- **시그니처**: `void Set_Lift_Velocity(INT08U nType, UnitVelocitySTR* pVelocity) { switch (nType)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:3842: void Set_Lift_Velocity(INT08U nType, UnitVelocitySTR* pVelocity)`
  - `Core/Src/com_tml.c:5049: Set_Lift_Velocity(i, &pSetParam->Vel[i]);`
  - `Core/Src/com_tml.c:5053: Set_Lift_Velocity(i, &pSetParam->Vel[i]);`
  - `Core/Inc/SRM_Parameter.h:155: extern void Set_Lift_Velocity(INT08U nType, UnitVelocitySTR* pVelocity);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_Velocity_1

- **종류**: 함수
- **시그니처**: `void Set_Lift_Velocity_1(INT08U nType, UnitVelocitySTR* pSrcVel, UnitVelocitySTR* pWrVel) { switch (nType)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:3976: void Set_Lift_Velocity_1(INT08U nType, UnitVelocitySTR* pSrcVel, UnitVelocitySTR* pWrVel)`
  - `Core/Src/com_tml.c:5087: Set_Lift_Velocity_1(i, &pSetParam->Vel[i], &nWriteParam.Vel[i]);`
  - `Core/Src/com_tml.c:5095: Set_Lift_Velocity_1(i, &pSetParam->Vel[i], &nWriteParam.Vel[i]);`
  - `Core/Inc/SRM_Parameter.h:156: extern void Set_Lift_Velocity_1(INT08U nType, UnitVelocitySTR* pSrcVel, UnitVelocitySTR* pWrVel);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_InPostion_Tolerance

- **종류**: 함수
- **시그니처**: `void Set_Lift_InPostion_Tolerance(ParamLiftDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4085: void Set_Lift_InPostion_Tolerance(ParamLiftDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:5057: Set_Lift_InPostion_Tolerance(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:157: extern void Set_Lift_InPostion_Tolerance(ParamLiftDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_InPostion_Tolerance_1

- **종류**: 함수
- **시그니처**: `void Set_Lift_InPostion_Tolerance_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4099: void Set_Lift_InPostion_Tolerance_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:5103: Set_Lift_InPostion_Tolerance_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:158: extern void Set_Lift_InPostion_Tolerance_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_InPostion_Hysteresis

- **종류**: 함수
- **시그니처**: `void Set_Lift_InPostion_Hysteresis(ParamLiftDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4115: void Set_Lift_InPostion_Hysteresis(ParamLiftDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:5058: Set_Lift_InPostion_Hysteresis(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:159: extern void Set_Lift_InPostion_Hysteresis(ParamLiftDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_InPostion_Hysteresis_1

- **종류**: 함수
- **시그니처**: `void Set_Lift_InPostion_Hysteresis_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4129: void Set_Lift_InPostion_Hysteresis_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:5104: Set_Lift_InPostion_Hysteresis_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:160: extern void Set_Lift_InPostion_Hysteresis_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_Manual_Drive

- **종류**: 함수
- **시그니처**: `void Set_Lift_Manual_Drive(ParamLiftDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4145: void Set_Lift_Manual_Drive(ParamLiftDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:5059: Set_Lift_Manual_Drive(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:161: extern void Set_Lift_Manual_Drive(ParamLiftDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_Manual_Drive_1

- **종류**: 함수
- **시그니처**: `void Set_Lift_Manual_Drive_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4165: void Set_Lift_Manual_Drive_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:5107: if (pCom->Data[3] & cbits[1]) Set_Lift_Manual_Drive_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:162: extern void Set_Lift_Manual_Drive_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_BrakeOn_DelayTime

- **종류**: 함수
- **시그니처**: `void Set_Lift_BrakeOn_DelayTime(ParamLiftDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **인근 주석**: if (pSetting->BrakeOnDelayTime < 1)			pSetting->BrakeOnDelayTime = 1;
- **용도(읽는 법)**: 헤더 주석: if (pSetting->BrakeOnDelayTime < 1)			pSetting->BrakeOnDelayTime = 1;
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4187: void Set_Lift_BrakeOn_DelayTime(ParamLiftDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:5060: Set_Lift_BrakeOn_DelayTime(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:163: extern void Set_Lift_BrakeOn_DelayTime(ParamLiftDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_BrakeOn_DelayTime_1

- **종류**: 함수
- **시그니처**: `void Set_Lift_BrakeOn_DelayTime_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **인근 주석**: if (pSetting->BrakeOnDelayTime < 1)			pSetting->BrakeOnDelayTime = 1;
- **용도(읽는 법)**: 헤더 주석: if (pSetting->BrakeOnDelayTime < 1)			pSetting->BrakeOnDelayTime = 1;
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4198: void Set_Lift_BrakeOn_DelayTime_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:5108: if (pCom->Data[3] & cbits[2]) Set_Lift_BrakeOn_DelayTime_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:164: extern void Set_Lift_BrakeOn_DelayTime_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_Origin_Position

- **종류**: 함수
- **시그니처**: `void Set_Lift_Origin_Position(ParamLiftDriveSTR* pSetting) { } /** * @brief * @param * @retval None */ void Set_Lift_Origin_Position_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4211: void Set_Lift_Origin_Position(ParamLiftDriveSTR* pSetting)`
  - `Core/Src/dev_SRM.c:53154: Set_Lift_Origin_Position(&nLift_Param);`
  - `Core/Inc/SRM_Parameter.h:165: extern void Set_Lift_Origin_Position(ParamLiftDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_Home_Position

- **종류**: 함수
- **시그니처**: `INT08U Set_Lift_Home_Position(ParamLiftDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: HOME/RETURN: 원점복귀 vs 에러복귀 RunMode 혼재. grep으로 확인.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4231: INT08U Set_Lift_Home_Position(ParamLiftDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:5062: Set_Lift_Home_Position(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:167: extern INT08U Set_Lift_Home_Position(ParamLiftDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_Home_Position_1

- **종류**: 함수
- **시그니처**: `INT08U Set_Lift_Home_Position_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: HOME/RETURN: 원점복귀 vs 에러복귀 RunMode 혼재. grep으로 확인.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4307: INT08U Set_Lift_Home_Position_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:5110: if (pCom->Data[3] & cbits[4]) is_save = Set_Lift_Home_Position_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:168: extern INT08U Set_Lift_Home_Position_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_Maintanance_Position

- **종류**: 함수
- **시그니처**: `INT08U Set_Lift_Maintanance_Position(ParamLiftDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4386: INT08U Set_Lift_Maintanance_Position(ParamLiftDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:5063: Set_Lift_Maintanance_Position(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:169: extern INT08U Set_Lift_Maintanance_Position(ParamLiftDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_Maintanance_Position_1

- **종류**: 함수
- **시그니처**: `INT08U Set_Lift_Maintanance_Position_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4450: INT08U Set_Lift_Maintanance_Position_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:5111: if (pCom->Data[3] & cbits[5]) is_save = Set_Lift_Maintanance_Position_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:170: extern INT08U Set_Lift_Maintanance_Position_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_DecelationRange_Mode

- **종류**: 함수
- **시그니처**: `INT08U Set_Lift_DecelationRange_Mode(ParamLiftDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4516: INT08U Set_Lift_DecelationRange_Mode(ParamLiftDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:5064: Set_Lift_DecelationRange_Mode(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:171: extern INT08U Set_Lift_DecelationRange_Mode(ParamLiftDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_DecelationRange_Mode_1

- **종류**: 함수
- **시그니처**: `INT08U Set_Lift_DecelationRange_Mode_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4550: INT08U Set_Lift_DecelationRange_Mode_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:5112: if (pCom->Data[3] & cbits[6]) is_save = Set_Lift_DecelationRange_Mode_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:172: extern INT08U Set_Lift_DecelationRange_Mode_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_SoftLimit_Error

- **종류**: 함수
- **시그니처**: `INT08U Set_Lift_SoftLimit_Error(ParamLiftDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4575: INT08U Set_Lift_SoftLimit_Error(ParamLiftDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:5065: Set_Lift_SoftLimit_Error(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:173: extern INT08U Set_Lift_SoftLimit_Error(ParamLiftDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_SoftLimit_Error_1

- **종류**: 함수
- **시그니처**: `INT08U Set_Lift_SoftLimit_Error_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4609: INT08U Set_Lift_SoftLimit_Error_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:5113: if (pCom->Data[3] & cbits[7]) is_save = Set_Lift_SoftLimit_Error_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:174: extern INT08U Set_Lift_SoftLimit_Error_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_SoftDecelation_Error

- **종류**: 함수
- **시그니처**: `INT08U Set_Lift_SoftDecelation_Error(ParamLiftDriveSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4647: INT08U Set_Lift_SoftDecelation_Error(ParamLiftDriveSTR* pSetting)`
  - `Core/Src/com_tml.c:5066: Set_Lift_SoftDecelation_Error(pSetParam);`
  - `Core/Inc/SRM_Parameter.h:175: extern INT08U Set_Lift_SoftDecelation_Error(ParamLiftDriveSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Lift_SoftDecelation_Error_1

- **종류**: 함수
- **시그니처**: `INT08U Set_Lift_SoftDecelation_Error_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:4675: INT08U Set_Lift_SoftDecelation_Error_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf)`
  - `Core/Src/com_tml.c:5118: if (pCom->Data[4] & cbits[0]) Set_Lift_SoftDecelation_Error_1(pSetParam, &nWriteParam);`
  - `Core/Inc/SRM_Parameter.h:176: extern INT08U Set_Lift_SoftDecelation_Error_1(ParamLiftDriveSTR* pSetting, ParamLiftDriveSTR* pWrBuf`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

