# Vol.08 — 기타 함수 (23)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

com_tml 등.

---

### ec_init

- **종류**: 함수
- **시그니처**: `INT08U ec_init()`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:2744: INT08U ec_init()`
  - `Core/Src/ecat_main.c:4059: if (ec_init())`
  - `Core/Src/ecat_main.c:4652: if (ec_init())`
  - `Core/Src/ecat_main.c:4853: if (ec_init())`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ECAT_Com_Process

- **종류**: 함수
- **시그니처**: `void ECAT_Com_Process()`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:2807: void ECAT_Com_Process()`
  - `Core/Src/main.c:1644: ECAT_Com_Process();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EtherCAT_Input_Test

- **종류**: 함수
- **시그니처**: `void EtherCAT_Input_Test()`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:2847: void EtherCAT_Input_Test()`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EtherCAT_Output_Test

- **종류**: 함수
- **시그니처**: `void EtherCAT_Output_Test()`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:2871: void EtherCAT_Output_Test()`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### External_Encoder_Proc

- **종류**: 함수
- **시그니처**: `void External_Encoder_Proc()`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:2892: void External_Encoder_Proc()`
  - `Core/Src/ecat_main.c:4532: External_Encoder_Proc();		// GX_EC0211`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### MX_Ext_Encoder_Proc_1

- **종류**: 함수
- **시그니처**: `void MX_Ext_Encoder_Proc_1()`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:3131: void MX_Ext_Encoder_Proc_1()`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### MX_EXT_Proc

- **종류**: 함수
- **시그니처**: `void MX_EXT_Proc()`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:3367: void MX_EXT_Proc()`
  - `Core/Src/ecat_main.c:4534: MX_EXT_Proc();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Check_EtherCAT_Slave_Connection

- **종류**: 함수
- **시그니처**: `void Check_EtherCAT_Slave_Connection()`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:3599: void Check_EtherCAT_Slave_Connection()`
  - `Core/Src/ecat_main.c:3941: Check_EtherCAT_Slave_Connection();`
  - `Core/Src/ecat_main.c:4606: Check_EtherCAT_Slave_Connection();`
  - `Core/Src/ecat_main.c:4805: Check_EtherCAT_Slave_Connection();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Check_ECAT_Slave_Name

- **종류**: 함수
- **시그니처**: `void Check_ECAT_Slave_Name()`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:3626: void Check_ECAT_Slave_Name()`
  - `Core/Src/ecat_main.c:4887: Check_ECAT_Slave_Name();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Check_ECAT_Slave_Name_1

- **종류**: 함수
- **시그니처**: `INT08U Check_ECAT_Slave_Name_1()`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:3694: INT08U Check_ECAT_Slave_Name_1()`
  - `Core/Src/ecat_main.c:4100: nErrorEventFlag = Check_ECAT_Slave_Name_1();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EtherCAT_Output_Control_All_Off

- **종류**: 함수
- **시그니처**: `void EtherCAT_Output_Control_All_Off(void)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:3801: void EtherCAT_Output_Control_All_Off(void)`
  - `Core/Src/ecat_main.c:4168: EtherCAT_Output_Control_All_Off();`
  - `Core/Src/ecat_main.c:4184: EtherCAT_Output_Control_All_Off();`
  - `Core/Src/ecat_main.c:4248: EtherCAT_Output_Control_All_Off();`
  - `Core/Src/ecat_main.c:4263: EtherCAT_Output_Control_All_Off();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EtherCAT_Output_Control

- **종류**: 함수
- **시그니처**: `void EtherCAT_Output_Control(void)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:3857: void EtherCAT_Output_Control(void)`
  - `Core/Src/ecat_main.c:4530: EtherCAT_Output_Control();`
  - `Core/Src/ecat_main.c:5065: EtherCAT_Output_Control();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ECAT_Manager

- **종류**: 함수
- **시그니처**: `void ECAT_Manager(void)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **인근 주석**: INT08U nDEF_Error_Count = 10;
- **용도(읽는 법)**: 헤더 주석: INT08U nDEF_Error_Count = 10;
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:3911: void ECAT_Manager(void)`
  - `Core/Src/main.c:831: ECAT_Manager();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ECAT_TestManager

- **종류**: 함수
- **시그니처**: `void ECAT_TestManager(INT08U nRunFlag, INT08U* pTestSeq, INT08U* pResult)`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:756: ECAT_TestManager(m_McuTestSt.EtherCAT_Enable, &m_McuTestSt.EtherCAT_Test, &m_McuTestSt.EtherCAT_Wkc)`
  - `Core/Src/ecat_main.c:4581: void ECAT_TestManager(INT08U nRunFlag, INT08U* pTestSeq, INT08U* pResult)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ECAT_TestManager_1

- **종류**: 함수
- **시그니처**: `void ECAT_TestManager_1()`
- **정의 위치**: `Core/Src/ecat_main.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_main.c:4778: void ECAT_TestManager_1()`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### GetEncoderCounter

- **종류**: 함수
- **시그니처**: `int GetEncoderCounter()`
- **정의 위치**: `Core/Src/encoder.c`
- **인근 주석**: static INT32U s_CheckTimer; static int s_prevCounter = 0;
- **용도(읽는 법)**: 헤더 주석: static INT32U s_CheckTimer; static int s_prevCounter = 0;
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:717: m_McuTestSt.Encoder_In = GetEncoderCounter();`
  - `Core/Src/encoder.c:28: int GetEncoderCounter()`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Encoder_Manager

- **종류**: 함수
- **시그니처**: `void Encoder_Manager(void)`
- **정의 위치**: `Core/Src/encoder.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/encoder.c:74: void Encoder_Manager(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### FLASH_User_WriteData

- **종류**: 함수
- **시그니처**: `INT08U FLASH_User_WriteData(INT32U addr, INT08U* buff, INT32U size)`
- **정의 위치**: `Core/Src/flash.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/flash.c:114: INT08U FLASH_User_WriteData(INT32U addr, INT08U* buff, INT32U size)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### FLASH_User_WriteData_1

- **종류**: 함수
- **시그니처**: `INT08U FLASH_User_WriteData_1(INT32U Flash_Addr, INT32U buf_Addr, INT32U Total_Size)`
- **정의 위치**: `Core/Src/flash.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/flash.c:182: INT08U FLASH_User_WriteData_1(INT32U Flash_Addr, INT32U buf_Addr, INT32U Total_Size)`
  - `Core/Src/upgrade.c:850: FLASH_User_WriteData_1(BOOT_PGM_IN_FLASH_START_SECTOR_ADDR, m_pgmEnv.Fw_Info.Buf_ExecFile.DownloadBu`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### byte2word

- **종류**: 함수
- **시그니처**: `static INT32U byte2word(INT08U* buff)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:155: static INT32U byte2word(INT08U* buff)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### byte2hword

- **종류**: 함수
- **시그니처**: `static INT16U byte2hword(INT08U* buff)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:162: static INT16U byte2hword(INT08U* buff)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### get_FPGA_ID

- **종류**: 함수
- **시그니처**: `INT08U get_FPGA_ID(void)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:168: INT08U get_FPGA_ID(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### get_FPGA_Version

- **종류**: 함수
- **시그니처**: `INT08U get_FPGA_Version(void)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:173: INT08U get_FPGA_Version(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### get_FPGA_SSI_Status

- **종류**: 함수
- **시그니처**: `INT08U get_FPGA_SSI_Status(void)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:619: nSSI_status = get_FPGA_SSI_Status();`
  - `Core/Src/fpga.c:182: INT08U get_FPGA_SSI_Status(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### set_FPGA_SSI_Send_Cmd

- **종류**: 함수
- **시그니처**: `void set_FPGA_SSI_Send_Cmd(INT08U mode)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:610: set_FPGA_SSI_Send_Cmd(1);`
  - `Core/Src/fpga.c:187: void set_FPGA_SSI_Send_Cmd(INT08U mode)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

