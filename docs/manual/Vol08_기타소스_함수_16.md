# Vol.08 — 기타 함수 (16)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

com_tml 등.

---

### rxCell_Offset_EnvReq

- **종류**: 함수
- **시그니처**: `void rxCell_Offset_EnvReq(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:4364: void rxCell_Offset_EnvReq(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6305: case TY_CELL_OFFSET_ENV_REQ: rxCell_Offset_EnvReq(port, (TMLComPTR)pCom);      break;`
  - `Core/Src/com_tml.c:6372: case TY_CELL_OFFSET_ENV_REQ: rxCell_Offset_EnvReq(port, (TMLComPTR)pCom);      break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxCell_Offset_EnvSet

- **종류**: 함수
- **시그니처**: `void rxCell_Offset_EnvSet(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:4490: void rxCell_Offset_EnvSet(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6306: case TY_CELL_OFFSET_ENV_SET: rxCell_Offset_EnvSet(port, (TMLComPTR)pCom);      break;`
  - `Core/Src/com_tml.c:6373: case TY_CELL_OFFSET_ENV_SET: rxCell_Offset_EnvSet(port, (TMLComPTR)pCom);      break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxControlParamterReq

- **종류**: 함수
- **시그니처**: `void rxControlParamterReq(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:4695: void rxControlParamterReq(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6319: case TY_CONTROL_REQ: rxControlParamterReq(port, (TMLComPTR)pCom); break;`
  - `Core/Src/com_tml.c:6386: case TY_CONTROL_REQ: rxControlParamterReq(port, (TMLComPTR)pCom); break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxControlParamterSet

- **종류**: 함수
- **시그니처**: `void rxControlParamterSet(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:4726: void rxControlParamterSet(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6320: case TY_CONTROL_SET: rxControlParamterSet(port, (TMLComPTR)pCom); break;`
  - `Core/Src/com_tml.c:6387: case TY_CONTROL_SET: rxControlParamterSet(port, (TMLComPTR)pCom); break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxTravelDriveParamterReq

- **종류**: 함수
- **시그니처**: `void rxTravelDriveParamterReq(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:4839: void rxTravelDriveParamterReq(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6322: case TY_TRAVEL_DRIVE_REQ: rxTravelDriveParamterReq(port, (TMLComPTR)pCom); break;`
  - `Core/Src/com_tml.c:6389: case TY_TRAVEL_DRIVE_REQ: rxTravelDriveParamterReq(port, (TMLComPTR)pCom); break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxTravelDriveParamterSet

- **종류**: 함수
- **시그니처**: `void rxTravelDriveParamterSet(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:4863: void rxTravelDriveParamterSet(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6323: case TY_TRAVEL_DRIVE_SET: rxTravelDriveParamterSet(port, (TMLComPTR)pCom); break;`
  - `Core/Src/com_tml.c:6390: case TY_TRAVEL_DRIVE_SET: rxTravelDriveParamterSet(port, (TMLComPTR)pCom); break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxLiftDriveParamterReq

- **종류**: 함수
- **시그니처**: `void rxLiftDriveParamterReq(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **인근 주석**: ParamLiftDriveComSTR nSendComData;
- **용도(읽는 법)**: 헤더 주석: ParamLiftDriveComSTR nSendComData;
- **코드 참조 예**:
  - `Core/Src/com_tml.c:5002: void rxLiftDriveParamterReq(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6325: case TY_LIFT_DRIVE_REQ: rxLiftDriveParamterReq(port, (TMLComPTR)pCom); break;`
  - `Core/Src/com_tml.c:6392: case TY_LIFT_DRIVE_REQ: rxLiftDriveParamterReq(port, (TMLComPTR)pCom); break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxLiftDriveParamterSet

- **종류**: 함수
- **시그니처**: `void rxLiftDriveParamterSet(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:5025: void rxLiftDriveParamterSet(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6326: case TY_LIFT_DRIVE_SET: rxLiftDriveParamterSet(port, (TMLComPTR)pCom); break;`
  - `Core/Src/com_tml.c:6393: case TY_LIFT_DRIVE_SET: rxLiftDriveParamterSet(port, (TMLComPTR)pCom); break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rx_Fork_Drive_Paramter_Req

- **종류**: 함수
- **시그니처**: `void rx_Fork_Drive_Paramter_Req(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:5173: void rx_Fork_Drive_Paramter_Req(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6328: case TY_FORK_DRIVE_REQ: rx_Fork_Drive_Paramter_Req(port, (TMLComPTR)pCom); break;`
  - `Core/Src/com_tml.c:6395: case TY_FORK_DRIVE_REQ: rx_Fork_Drive_Paramter_Req(port, (TMLComPTR)pCom); break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rx_Fork_Drive_Paramter_Set

- **종류**: 함수
- **시그니처**: `void rx_Fork_Drive_Paramter_Set(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:5193: void rx_Fork_Drive_Paramter_Set(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6329: case TY_FORK_DRIVE_SET: rx_Fork_Drive_Paramter_Set(port, (TMLComPTR)pCom); break;`
  - `Core/Src/com_tml.c:6396: case TY_FORK_DRIVE_SET: rx_Fork_Drive_Paramter_Set(port, (TMLComPTR)pCom); break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxCmdNormalStop

- **종류**: 함수
- **시그니처**: `void rxCmdNormalStop(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:5323: void rxCmdNormalStop(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6288: case TY_NORMAL_STOP_REQ: rxCmdNormalStop(port, (TMLComPTR)pCom);      break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxCmdEmergencyStop

- **종류**: 함수
- **시그니처**: `void rxCmdEmergencyStop(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:5337: void rxCmdEmergencyStop(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6290: case TY_EMERGENCY_STOP_REQ: rxCmdEmergencyStop(port, (TMLComPTR)pCom);      break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxCmdManual

- **종류**: 함수
- **시그니처**: `void rxCmdManual(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **인근 주석**: ManualCmdSTR* pCtrl; pCtrl = (ManualCmdSTR*)&pCom->Data[2];
- **용도(읽는 법)**: 헤더 주석: ManualCmdSTR* pCtrl; pCtrl = (ManualCmdSTR*)&pCom->Data[2];
- **코드 참조 예**:
  - `Core/Src/com_tml.c:5351: void rxCmdManual(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6294: case TY_CMD_MANUAL:	rxCmdManual(port, (TMLComPTR)pCom);      break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxDemoControl

- **종류**: 함수
- **시그니처**: `void rxDemoControl(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:5570: void rxDemoControl(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6398: case TY_CMD_DEMO_CONTROL:	rxDemoControl(port, (TMLComPTR)pCom);	break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxScan_Position_Sensor_Control

- **종류**: 함수
- **시그니처**: `void rxScan_Position_Sensor_Control(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:5638: void rxScan_Position_Sensor_Control(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6400: case TY_CMD_SCAN_POSITION_SENSOR: rxScan_Position_Sensor_Control(port, (TMLComPTR)pCom); break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### txAlarmLog

- **종류**: 함수
- **시그니처**: `void txAlarmLog(INT08U port, TMLComPTR pCom, INT16U req_inx, INT08U req_type, INT32U start_time, INT32U end_time)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:5778: void txAlarmLog(INT08U port, TMLComPTR pCom, INT16U req_inx, INT08U req_type, INT32U start_time, INT`
  - `Core/Src/com_tml.c:5905: txAlarmLog(port, (TMLComPTR)pCom, req_inx, req_type, start_time, end_time);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxAlarmLogRequest

- **종류**: 함수
- **시그니처**: `void rxAlarmLogRequest(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:5878: void rxAlarmLogRequest(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6268: case TY_ALARMLOG_REQUEST: rxAlarmLogRequest(port, (TMLComPTR)pCom);      break;`
  - `Core/Src/com_tml.c:6402: case TY_ALARMLOG_REQUEST: rxAlarmLogRequest(port, (TMLComPTR)pCom);      break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxAlarmLogDelete

- **종류**: 함수
- **시그니처**: `void rxAlarmLogDelete(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:5913: void rxAlarmLogDelete(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6269: case TY_ALARMLOG_DELETE: rxAlarmLogDelete(port, (TMLComPTR)pCom);      break;`
  - `Core/Src/com_tml.c:6403: case TY_ALARMLOG_DELETE: rxAlarmLogDelete(port, (TMLComPTR)pCom);      break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxDSP_Status

- **종류**: 함수
- **시그니처**: `void rxDSP_Status(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:5946: void rxDSP_Status(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6421: case TY_DSP_STATUS: rxDSP_Status(port, (TMLComPTR)pCom);	break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxDSP_Control

- **종류**: 함수
- **시그니처**: `void rxDSP_Control(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:5966: void rxDSP_Control(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6422: case TY_DSP_DATA: rxDSP_Control(port, (TMLComPTR)pCom);	break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxMcuTestReq

- **종류**: 함수
- **시그니처**: `void rxMcuTestReq(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:5992: void rxMcuTestReq(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6405: case TY_MCU_TEST: rxMcuTestReq(port, (TMLComPTR)pCom);	break;`
  - `Core/Src/com_tml.c:6431: case TY_MCU_TEST: rxMcuTestReq(port, (TMLComPTR)pCom);	break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Send_Read_Invertor_Parameter

- **종류**: 함수
- **시그니처**: `void Send_Read_Invertor_Parameter()`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:6033: void Send_Read_Invertor_Parameter()`
  - `Core/Src/com_tml.c:6156: Send_Read_Invertor_Parameter();		// 통신 테스트 후 삭제`
  - `Core/Src/dev_SRM.c:37477: Send_Read_Invertor_Parameter();`
  - `Core/Inc/com_tml.h:136: extern void Send_Read_Invertor_Parameter();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Send_Write_Invertor_Parameter

- **종류**: 함수
- **시그니처**: `void Send_Write_Invertor_Parameter(INT08U nAck)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:6057: void Send_Write_Invertor_Parameter(INT08U nAck)`
  - `Core/Src/com_tml.c:6201: Send_Write_Invertor_Parameter(0);	// 통신 테스트 후 삭제`
  - `Core/Src/dev_SRM.c:37562: Send_Write_Invertor_Parameter(0);`
  - `Core/Src/dev_SRM.c:37574: Send_Write_Invertor_Parameter(1);`
  - `Core/Inc/com_tml.h:138: extern void Send_Write_Invertor_Parameter(INT08U nAck);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxInvertorParameterReq

- **종류**: 함수
- **시그니처**: `void rxInvertorParameterReq(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:6083: void rxInvertorParameterReq(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6296: case TY_INVERTOR_PARAM_REQ: rxInvertorParameterReq(port, (TMLComPTR)pCom);      break;`
  - `Core/Src/com_tml.c:6363: case TY_INVERTOR_PARAM_REQ: rxInvertorParameterReq(port, (TMLComPTR)pCom);      break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxInvertorParameterSet

- **종류**: 함수
- **시그니처**: `void rxInvertorParameterSet(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:6185: void rxInvertorParameterSet(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6297: case TY_INVERTOR_PARAM_SET: rxInvertorParameterSet(port, (TMLComPTR)pCom);      break;`
  - `Core/Src/com_tml.c:6364: case TY_INVERTOR_PARAM_SET: rxInvertorParameterSet(port, (TMLComPTR)pCom);      break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

