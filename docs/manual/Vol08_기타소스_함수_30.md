# Vol.08 — 기타 함수 (30)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

com_tml 등.

---

### _kill

- **종류**: 함수
- **시그니처**: `int _kill(int pid, int sig)`
- **정의 위치**: `Core/Src/syscalls.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/syscalls.c:53: int _kill(int pid, int sig)`
  - `Core/Src/syscalls.c:61: _kill(status, -1);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### _exit

- **종류**: 함수
- **시그니처**: `void _exit (int status)`
- **정의 위치**: `Core/Src/syscalls.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/syscalls.c:59: void _exit (int status)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### _close

- **종류**: 함수
- **시그니처**: `int _close(int file)`
- **정의 위치**: `Core/Src/syscalls.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/syscalls.c:88: int _close(int file)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### _fstat

- **종류**: 함수
- **시그니처**: `int _fstat(int file, struct stat *st)`
- **정의 위치**: `Core/Src/syscalls.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/syscalls.c:94: int _fstat(int file, struct stat *st)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### _isatty

- **종류**: 함수
- **시그니처**: `int _isatty(int file)`
- **정의 위치**: `Core/Src/syscalls.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/syscalls.c:100: int _isatty(int file)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### _lseek

- **종류**: 함수
- **시그니처**: `int _lseek(int file, int ptr, int dir)`
- **정의 위치**: `Core/Src/syscalls.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/syscalls.c:105: int _lseek(int file, int ptr, int dir)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### _open

- **종류**: 함수
- **시그니처**: `int _open(char *path, int flags, ...)`
- **정의 위치**: `Core/Src/syscalls.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/syscalls.c:110: int _open(char *path, int flags, ...)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### _wait

- **종류**: 함수
- **시그니처**: `int _wait(int *status)`
- **정의 위치**: `Core/Src/syscalls.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/syscalls.c:116: int _wait(int *status)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### _unlink

- **종류**: 함수
- **시그니처**: `int _unlink(char *name)`
- **정의 위치**: `Core/Src/syscalls.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/syscalls.c:122: int _unlink(char *name)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### _times

- **종류**: 함수
- **시그니처**: `int _times(struct tms *buf)`
- **정의 위치**: `Core/Src/syscalls.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/syscalls.c:128: int _times(struct tms *buf)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### _stat

- **종류**: 함수
- **시그니처**: `int _stat(char *file, struct stat *st)`
- **정의 위치**: `Core/Src/syscalls.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/syscalls.c:133: int _stat(char *file, struct stat *st)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### _link

- **종류**: 함수
- **시그니처**: `int _link(char *old, char *new)`
- **정의 위치**: `Core/Src/syscalls.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/syscalls.c:139: int _link(char *old, char *new)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### _fork

- **종류**: 함수
- **시그니처**: `int _fork(void)`
- **정의 위치**: `Core/Src/syscalls.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/syscalls.c:145: int _fork(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### _execve

- **종류**: 함수
- **시그니처**: `int _execve(char *name, char **argv, char **env)`
- **정의 위치**: `Core/Src/syscalls.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/syscalls.c:151: int _execve(char *name, char **argv, char **env)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### _sbrk

- **종류**: 함수
- **시그니처**: `void *_sbrk(ptrdiff_t incr)`
- **정의 위치**: `Core/Src/sysmem.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/sysmem.c:33: * @brief _sbrk() allocates memory to the newlib heap and is used by malloc`
  - `Core/Src/sysmem.c:53: void *_sbrk(ptrdiff_t incr)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### SystemInit

- **종류**: 함수
- **시그니처**: `void SystemInit(void)`
- **정의 위치**: `Core/Src/system_stm32f7xx.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/system_stm32f7xx.c:9: *      - SystemInit(): This function is called at startup just after reset and`
  - `Core/Src/system_stm32f7xx.c:151: void SystemInit(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### SystemCoreClockUpdate

- **종류**: 함수
- **시그니처**: `void SystemCoreClockUpdate(void)`
- **정의 위치**: `Core/Src/system_stm32f7xx.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/system_stm32f7xx.c:17: *      - SystemCoreClockUpdate(): Updates the variable SystemCoreClock and must`
  - `Core/Src/system_stm32f7xx.c:117: 1) by calling CMSIS function SystemCoreClockUpdate()`
  - `Core/Src/system_stm32f7xx.c:200: void SystemCoreClockUpdate(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### CRCWhere

- **종류**: 함수
- **시그니처**: `INT16U CRCWhere(INT16U data, INT16U poly, INT16U accum)`
- **정의 위치**: `Core/Src/uart.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/uart.c:67: INT16U CRCWhere(INT16U data, INT16U poly, INT16U accum)`
  - `Core/Src/uart.c:89: CrcTable[i] = CRCWhere(i, CRCCCITT, 0);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### makeCRCTable

- **종류**: 함수
- **시그니처**: `void makeCRCTable(void)`
- **정의 위치**: `Core/Src/uart.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/uart.c:83: void makeCRCTable(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### crcByteCalc

- **종류**: 함수
- **시그니처**: `INT16U crcByteCalc(INT16U crcValue, INT08U crcCh)`
- **정의 위치**: `Core/Src/uart.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_Debug.c:47: rsCRCValue = crcByteCalc(rsCRCValue, rsComData);`
  - `Core/Src/com_Debug.c:65: rsCRCValue = crcByteCalc(rsCRCValue, rsComData);`
  - `Core/Src/com_Debug.c:77: rsCRCValue = crcByteCalc(rsCRCValue, rsComData);`
  - `Core/Src/com_hmi.c:112: rsCRCValue = crcByteCalc(rsCRCValue, rsComData);`
  - `Core/Src/com_hmi.c:130: rsCRCValue = crcByteCalc(rsCRCValue, rsComData);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### crcInitStrCalc

- **종류**: 함수
- **시그니처**: `INT16U crcInitStrCalc(INT16U crcValue, INT08U* crcptr, INT32U len)`
- **정의 위치**: `Core/Src/uart.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:1593: *pCrc = crcInitStrCalc(*pCrc, nReadBuffer, nWriteSize);`
  - `Core/Src/FlashSST26.c:1653: calc_Crc = crcInitStrCalc(calc_Crc, (INT08U*)(nReadBuf), nReadSize);`
  - `Core/Src/SRM_Parameter.c:100: m_ExtSEnv.CheckCRC_Device = crcInitStrCalc(crc, (INT08U*)&m_ExtSEnv.mDevice, sizeof(DeviceSetting));`
  - `Core/Src/SRM_Parameter.c:143: m_ExtSEnv.CheckCRC_Device = crcInitStrCalc(crc, (INT08U*)&m_ExtSEnv.mDevice, sizeof(DeviceSetting));`
  - `Core/Src/SRM_Parameter.c:198: crc = crcInitStrCalc(crc, (INT08U*)&m_ExtSEnv.mDevice, sizeof(DeviceSetting));`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### HAL_UART_RxCpltCallback

- **종류**: 함수
- **시그니처**: `void HAL_UART_RxCpltCallback(UART_HandleTypeDef* huart) { if (huart->Instance == USART3)`
- **정의 위치**: `Core/Src/uart.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/uart.c:129: void HAL_UART_RxCpltCallback(UART_HandleTypeDef* huart)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### HAL_UART_RxHalfCpltCallback

- **종류**: 함수
- **시그니처**: `void HAL_UART_RxHalfCpltCallback(UART_HandleTypeDef* huart) { if (huart->Instance == USART3)`
- **정의 위치**: `Core/Src/uart.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/uart.c:138: void HAL_UART_RxHalfCpltCallback(UART_HandleTypeDef* huart)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### UART_IDLECallback

- **종류**: 함수
- **시그니처**: `void UART_IDLECallback(UART_HandleTypeDef* huart) { if (huart->Instance == USART3)`
- **정의 위치**: `Core/Src/uart.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/stm32f7xx_it.c:334: UART_IDLECallback(&huart3);`
  - `Core/Src/stm32f7xx_it.c:368: UART_IDLECallback(&huart3);`
  - `Core/Src/stm32f7xx_it.c:416: UART_IDLECallback(&huart6);`
  - `Core/Src/uart.c:148: void UART_IDLECallback(UART_HandleTypeDef* huart)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### getUartRxData

- **종류**: 함수
- **시그니처**: `INT08U getUartRxData(INT08U port, INT08U* rxData) { if (port < MAX_UART_PORT) { if (m_RxUartQ[port].Front != m_RxUartQ[port].Rear)`
- **정의 위치**: `Core/Src/uart.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/uart.c:160: INT08U getUartRxData(INT08U port, INT08U* rxData)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

