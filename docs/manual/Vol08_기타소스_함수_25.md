# Vol.08 — 기타 함수 (25)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

com_tml 등.

---

### set_FPGA_EthCat_MAC_Address

- **종류**: 함수
- **시그니처**: `void set_FPGA_EthCat_MAC_Address(INT08U* addr)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:488: void set_FPGA_EthCat_MAC_Address(INT08U* addr)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### set_FPGA_EthCat_IP_Address

- **종류**: 함수
- **시그니처**: `void set_FPGA_EthCat_IP_Address(INT08U* addr)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:498: void set_FPGA_EthCat_IP_Address(INT08U* addr)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### get_FPGA_EthCat_Read_Buff_Num

- **종류**: 함수
- **시그니처**: `INT08U get_FPGA_EthCat_Read_Buff_Num(void)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:506: INT08U get_FPGA_EthCat_Read_Buff_Num(void)`
  - `Core/Src/fpga.c:566: read_buff_num1 = get_FPGA_EthCat_Read_Buff_Num();`
  - `Core/Src/fpga.c:590: read_buff_num1 = get_FPGA_EthCat_Read_Buff_Num();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### get_FPGA_EthCat_Recv_Buff_Num

- **종류**: 함수
- **시그니처**: `INT08U get_FPGA_EthCat_Recv_Buff_Num(void)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:511: INT08U get_FPGA_EthCat_Recv_Buff_Num(void)`
  - `Core/Src/fpga.c:565: recv_buff_num1 = get_FPGA_EthCat_Recv_Buff_Num();`
  - `Core/Src/fpga.c:589: recv_buff_num1 = get_FPGA_EthCat_Recv_Buff_Num();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### set_FPGA_EthCat_Read_Buff_Num

- **종류**: 함수
- **시그니처**: `void set_FPGA_EthCat_Read_Buff_Num(INT08U num)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:516: void set_FPGA_EthCat_Read_Buff_Num(INT08U num)`
  - `Core/Src/fpga.c:587: set_FPGA_EthCat_Read_Buff_Num(read_buff_num1);`
  - `Core/Src/fpga.c:595: set_FPGA_EthCat_Read_Buff_Num(recv_buff_num1);`
  - `Core/Src/fpga.c:626: set_FPGA_EthCat_Read_Buff_Num(read_buff_num1);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### get_FPGA_EthCat_RxBuff

- **종류**: 함수
- **시그니처**: `INT08U get_FPGA_EthCat_RxBuff(INT16U addr)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:521: INT08U get_FPGA_EthCat_RxBuff(INT16U addr)`
  - `Core/Src/fpga.c:572: len = ((INT16U)get_FPGA_EthCat_RxBuff(2000));`
  - `Core/Src/fpga.c:573: len |= ((INT16U)get_FPGA_EthCat_RxBuff(2001) << 8);`
  - `Core/Src/fpga.c:611: len = ((INT16U)get_FPGA_EthCat_RxBuff(2000));`
  - `Core/Src/fpga.c:612: len |= ((INT16U)get_FPGA_EthCat_RxBuff(2001) << 8);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### get_FPGA_EthCat_RxBuff_Len

- **종류**: 함수
- **시그니처**: `void get_FPGA_EthCat_RxBuff_Len(INT16U addr, INT08U* buff, INT16U len)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:526: void get_FPGA_EthCat_RxBuff_Len(INT16U addr, INT08U* buff, INT16U len)`
  - `Core/Src/fpga.c:577: get_FPGA_EthCat_RxBuff_Len(0, (INT08U*)&buff[0], len);`
  - `Core/Src/fpga.c:616: get_FPGA_EthCat_RxBuff_Len(0, (INT08U*)&buff[0], len);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### set_FPGA_EthCat_TxBuff

- **종류**: 함수
- **시그니처**: `void set_FPGA_EthCat_TxBuff(INT16U addr, INT08U data)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:531: void set_FPGA_EthCat_TxBuff(INT16U addr, INT08U data)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### set_FPGA_EthCat_TxBuff_Len

- **종류**: 함수
- **시그니처**: `void set_FPGA_EthCat_TxBuff_Len(INT16U addr, INT08U* buff, INT16U len)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:536: void set_FPGA_EthCat_TxBuff_Len(INT16U addr, INT08U* buff, INT16U len)`
  - `Core/Src/fpga.c:644: set_FPGA_EthCat_TxBuff_Len(0, (INT08U*)&buff[0], buff_size);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### set_FPGA_EthCat_Send_Size

- **종류**: 함수
- **시그니처**: `void set_FPGA_EthCat_Send_Size(INT16U len)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:541: void set_FPGA_EthCat_Send_Size(INT16U len)`
  - `Core/Src/fpga.c:645: set_FPGA_EthCat_Send_Size(buff_size);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### set_FPGA_EthCat_Send_Cmd

- **종류**: 함수
- **시그니처**: `void set_FPGA_EthCat_Send_Cmd(INT08U mode)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:546: void set_FPGA_EthCat_Send_Cmd(INT08U mode)`
  - `Core/Src/fpga.c:646: set_FPGA_EthCat_Send_Cmd(1);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### getFPGAEthercat

- **종류**: 함수
- **시그니처**: `INT16U getFPGAEthercat(INT08U* buff, INT16U buff_size)`
- **정의 위치**: `Core/Src/fpga.c`
- **인근 주석**: INT08U i = 0; INT32U dst_mac = 0; INT32U src_mac = 0; INT32U eth_type = 0;
- **용도(읽는 법)**: 헤더 주석: INT08U i = 0; INT32U dst_mac = 0; INT32U src_mac = 0; INT32U eth_type = 0;
- **코드 참조 예**:
  - `Core/Src/fpga.c:551: INT16U getFPGAEthercat(INT08U* buff, INT16U buff_size)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### putFPGAEthercat

- **종류**: 함수
- **시그니처**: `void putFPGAEthercat(INT08U* buff, INT16U buff_size) { if (buff_size <= 1492)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:640: void putFPGAEthercat(INT08U* buff, INT16U buff_size)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### testFpgaReadWrite

- **종류**: 함수
- **시그니처**: `void testFpgaReadWrite(void)`
- **정의 위치**: `Core/Src/fpga.c`
- **인근 주석**: INT16U i;  m_TestStatus.St[0]++; m_TestStatus.St[1] = m_FpgaReg->ID; m_TestStatus.St[2] = m_FpgaReg->Version;  m_FpgaReg->FpgaTest[0] = 0x12;
- **용도(읽는 법)**: 헤더 주석: INT16U i;  m_TestStatus.St[0]++; m_TestStatus.St[1] = m_FpgaReg->ID; m_TestStatus.St[2] = m_FpgaReg->Version;  m_FpgaReg->FpgaTest[0] = 0x12;
- **코드 참조 예**:
  - `Core/Src/fpga.c:653: void testFpgaReadWrite(void)`
  - `Core/Src/fpga.c:726: testFpgaReadWrite();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### EthPacketTestProcess

- **종류**: 함수
- **시그니처**: `void EthPacketTestProcess(void)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:717: void EthPacketTestProcess(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### FpgaManager

- **종류**: 함수
- **시그니처**: `void FpgaManager(void)`
- **정의 위치**: `Core/Src/fpga.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:743: void FpgaManager(void)`
  - `Core/Src/main.c:823: FpgaManager();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### setEthernetBuffer

- **종류**: 함수
- **시그니처**: `static void setEthernetBuffer(void)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpgatcpipstack.c:59: static void setEthernetBuffer(void)`
  - `Core/Src/fpgatcpipstack.c:842: setEthernetBuffer();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### setStackHostIPAddr

- **종류**: 함수
- **시그니처**: `static void setStackHostIPAddr(INT08U* val)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpgatcpipstack.c:71: static void setStackHostIPAddr(INT08U* val)`
  - `Core/Src/fpgatcpipstack.c:830: setStackHostIPAddr((INT08U*)net_info->MyIP);`
  - `Core/Src/fpgatcpipstack.c:847: setStackHostIPAddr   ( (INT08U *)m_DevStatus.NetworkInfo_1.MyIP );`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### setStackGatewayIPAddr

- **종류**: 함수
- **시그니처**: `static void setStackGatewayIPAddr(INT08U* val)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpgatcpipstack.c:76: static void setStackGatewayIPAddr(INT08U* val)`
  - `Core/Src/fpgatcpipstack.c:831: setStackGatewayIPAddr((INT08U*)net_info->GatewayIP);`
  - `Core/Src/fpgatcpipstack.c:848: setStackGatewayIPAddr( (INT08U *)m_DevStatus.NetworkInfo_1.GatewayIP );`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### setStackSubnetMask

- **종류**: 함수
- **시그니처**: `static void setStackSubnetMask(INT08U* val)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpgatcpipstack.c:81: static void setStackSubnetMask(INT08U* val)`
  - `Core/Src/fpgatcpipstack.c:832: setStackSubnetMask((INT08U*)net_info->SubnetMask);`
  - `Core/Src/fpgatcpipstack.c:849: setStackSubnetMask   ( (INT08U *)m_DevStatus.NetworkInfo_1.SubnetMask );`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### setStackHostMacAddr

- **종류**: 함수
- **시그니처**: `static void setStackHostMacAddr(INT08U* val)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpgatcpipstack.c:86: static void setStackHostMacAddr(INT08U* val)`
  - `Core/Src/fpgatcpipstack.c:837: setStackHostMacAddr((INT08U*)mac_addr);`
  - `Core/Src/fpgatcpipstack.c:850: setStackHostMacAddr  ( (INT08U *)m_DevStatus.MyMACAddr_1);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### open_udp_socket

- **종류**: 함수
- **시그니처**: `static void open_udp_socket(INT08U s, INT32U ipaddr, INT16U lport, INT16U rport, INT08U chkipaddr, INT08U chkrport) { if (s < MAX_UDP_SOCKET_COUNT)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpgatcpipstack.c:91: static void open_udp_socket(INT08U s, INT32U ipaddr, INT16U lport, INT16U rport, INT08U chkipaddr, I`
  - `Core/Src/fpgatcpipstack.c:108: open_udp_socket(0, 0, 9000, 0, 0, 0);             // socket, ipaddr, localport, remoteport, chkipadd`
  - `Core/Src/fpgatcpipstack.c:109: open_udp_socket(1, 0, 9001, 0, 0, 0);             // socket, ipaddr, localport, remoteport, chkipadd`
  - `Core/Src/fpgatcpipstack.c:110: open_udp_socket(2, 0, UPLINK_UDP_SERVER_PORT, 0, 0, 0);             // socket, ipaddr, localport, re`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### initTcpipSocket

- **종류**: 함수
- **시그니처**: `static void initTcpipSocket(void)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpgatcpipstack.c:103: static void initTcpipSocket(void)`
  - `Core/Src/fpgatcpipstack.c:844: initTcpipSocket();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### arp_init

- **종류**: 함수
- **시그니처**: `static void arp_init(void)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpgatcpipstack.c:115: static void arp_init(void)`
  - `Core/Src/fpgatcpipstack.c:843: arp_init();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### arp_timer

- **종류**: 함수
- **시그니처**: `static void arp_timer(void)`
- **정의 위치**: `Core/Src/fpgatcpipstack.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpgatcpipstack.c:125: static void arp_timer(void)`
  - `Core/Src/fpgatcpipstack.c:894: arp_timer();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

