# Vol.08 — 기타 함수 (4)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

com_tml 등.

---

### Check_CellEnv_Config

- **종류**: 함수
- **시그니처**: `INT08U Check_CellEnv_Config()`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:743: INT08U Check_CellEnv_Config()`
  - `Core/Src/SRM_Parameter.c:4250: if (Check_CellEnv_Config())`
  - `Core/Src/SRM_Parameter.c:4326: if (Check_CellEnv_Config())`
  - `Core/Src/com_tml.c:4278: if (Check_CellEnv_Config())`
  - `Core/Src/dev_SRM.c:10840: if (!Check_CellEnv_Config())		return;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Cell_Config

- **종류**: 함수
- **시그니처**: `void Set_Cell_Config(CellConfigSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:769: void Set_Cell_Config(CellConfigSTR* pSetting)`
  - `Core/Inc/SRM_Parameter.h:34: extern void Set_Cell_Config(CellConfigSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Cell_Config_1

- **종류**: 함수
- **시그니처**: `void Set_Cell_Config_1(CellConfigSTR* pSetting, INT08U nDir)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:801: void Set_Cell_Config_1(CellConfigSTR* pSetting, INT08U nDir)`
  - `Core/Src/com_tml.c:3800: Set_Cell_Config_1(&nWrite_Cell, 0);`
  - `Core/Src/com_tml.c:3810: Set_Cell_Config_1(&nWrite_Cell, 0);`
  - `Core/Src/com_tml.c:3821: Set_Cell_Config_1(&nWrite_Cell, 1);`
  - `Core/Src/com_tml.c:3832: Set_Cell_Config_1(&nWrite_Cell, 1);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Clear_Cell_Config

- **종류**: 함수
- **시그니처**: `void Clear_Cell_Config()`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:858: void Clear_Cell_Config()`
  - `Core/Src/SRM_Parameter.c:893: Clear_Cell_Config();`
  - `Core/Src/com_tml.c:4275: Clear_Cell_Config();`
  - `Core/Src/com_tml.c:4280: Clear_Cell_Config();`
  - `Core/Inc/SRM_Parameter.h:36: extern void Clear_Cell_Config();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Cell_Config_Init

- **종류**: 함수
- **시그니처**: `void Cell_Config_Init()`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:881: void Cell_Config_Init()`
  - `Core/Src/main.c:369: Cell_Config_Init();`
  - `Core/Inc/SRM_Parameter.h:37: extern void Cell_Config_Init();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Check_StationEnv_Config

- **종류**: 함수
- **시그니처**: `INT08U Check_StationEnv_Config()`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:926: INT08U Check_StationEnv_Config()`
  - `Core/Src/SRM_Parameter.c:4255: if (Check_StationEnv_Config())`
  - `Core/Src/SRM_Parameter.c:4331: if (Check_StationEnv_Config())`
  - `Core/Src/com_tml.c:4302: if (Check_StationEnv_Config())`
  - `Core/Src/dev_SRM.c:10841: if (!Check_StationEnv_Config())		return;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Clear_Station_Config

- **종류**: 함수
- **시그니처**: `void Clear_Station_Config()`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:951: void Clear_Station_Config()`
  - `Core/Src/SRM_Parameter.c:1012: Clear_Station_Config();`
  - `Core/Src/com_tml.c:4300: Clear_Station_Config();`
  - `Core/Src/com_tml.c:4304: Clear_Station_Config();`
  - `Core/Inc/SRM_Parameter.h:41: extern void Clear_Station_Config();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_Station_Config

- **종류**: 함수
- **시그니처**: `void Set_Station_Config(StationSetupSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:968: void Set_Station_Config(StationSetupSTR* pSetting)`
  - `Core/Src/com_tml.c:4103: Set_Station_Config(&nWriteBuf);`
  - `Core/Inc/SRM_Parameter.h:40: extern void Set_Station_Config(StationSetupSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Station_Config_Init

- **종류**: 함수
- **시그니처**: `void Station_Config_Init()`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1001: void Station_Config_Init()`
  - `Core/Src/main.c:371: Station_Config_Init();`
  - `Core/Inc/SRM_Parameter.h:42: extern void Station_Config_Init();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Test_Prohibit_Rack_Config_Init

- **종류**: 함수
- **시그니처**: `void Test_Prohibit_Rack_Config_Init(ProhibitRackSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1045: void Test_Prohibit_Rack_Config_Init(ProhibitRackSTR* pSetting)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Clear_Prohibit_RackCfg

- **종류**: 함수
- **시그니처**: `void Clear_Prohibit_RackCfg(ProhibitRackSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1064: void Clear_Prohibit_RackCfg(ProhibitRackSTR* pSetting)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Check_ProhibitRack_Config

- **종류**: 함수
- **시그니처**: `INT08U Check_ProhibitRack_Config() { if (checkFusingString((INT08U*)m_ExtSEnv1.InitProhibitCode, (INT08U*)PROHIBIT_RACK_INIT_CODE, 4))`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1074: INT08U Check_ProhibitRack_Config()`
  - `Core/Src/com_tml.c:4314: if (Check_ProhibitRack_Config())`
  - `Core/Inc/SRM_Parameter.h:44: extern INT08U Check_ProhibitRack_Config();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Clear_ProhibitRack_Config

- **종류**: 함수
- **시그니처**: `void Clear_ProhibitRack_Config()`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1091: void Clear_ProhibitRack_Config()`
  - `Core/Src/SRM_Parameter.c:1173: Clear_ProhibitRack_Config();`
  - `Core/Src/com_tml.c:4312: Clear_ProhibitRack_Config();`
  - `Core/Src/com_tml.c:4316: Clear_ProhibitRack_Config();`
  - `Core/Inc/SRM_Parameter.h:45: extern void Clear_ProhibitRack_Config();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Check_InhibitRack_Data

- **종류**: 함수
- **시그니처**: `INT08U Check_InhibitRack_Data(InhibitRackSet_t* nData) { if ((nData->Row_Apply == 0) && (nData->Bay_No == 0) && (nData->Lv_No == 0))`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1111: INT08U Check_InhibitRack_Data(InhibitRackSet_t* nData)`
  - `Core/Src/com_tml.c:4153: if (Check_InhibitRack_Data(&pCtrl->SetData[i]))`
  - `Core/Inc/SRM_Parameter.h:46: extern INT08U Check_InhibitRack_Data(InhibitRackSet_t* nData);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_ProhibitRack_Config

- **종류**: 함수
- **시그니처**: `void Set_ProhibitRack_Config(ProhibitRackSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1128: void Set_ProhibitRack_Config(ProhibitRackSTR* pSetting)`
  - `Core/Src/com_tml.c:4177: Set_ProhibitRack_Config(pCtrl);`
  - `Core/Inc/SRM_Parameter.h:47: extern void Set_ProhibitRack_Config(ProhibitRackSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### ProhibitRack_Config_Init

- **종류**: 함수
- **시그니처**: `void ProhibitRack_Config_Init()`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1162: void ProhibitRack_Config_Init()`
  - `Core/Src/main.c:372: ProhibitRack_Config_Init();`
  - `Core/Inc/SRM_Parameter.h:48: extern void ProhibitRack_Config_Init();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Test_Special_Rack_Config_Init

- **종류**: 함수
- **시그니처**: `void Test_Special_Rack_Config_Init(SpecialRackSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1205: void Test_Special_Rack_Config_Init(SpecialRackSTR* pSetting)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Clear_Special_RackCfg

- **종류**: 함수
- **시그니처**: `void Clear_Special_RackCfg(SpecialRackSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1226: void Clear_Special_RackCfg(SpecialRackSTR* pSetting)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Check_SpecialRack_Config

- **종류**: 함수
- **시그니처**: `INT08U Check_SpecialRack_Config() { if (checkFusingString((INT08U*)m_ExtSEnv1.InitSpecialCode, (INT08U*)SPECIAL_RACK_INIT_CODE, 4))`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1236: INT08U Check_SpecialRack_Config()`
  - `Core/Src/com_tml.c:4326: if (Check_SpecialRack_Config())`
  - `Core/Inc/SRM_Parameter.h:50: extern INT08U Check_SpecialRack_Config();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Clear_SpecialRack_Config

- **종류**: 함수
- **시그니처**: `void Clear_SpecialRack_Config()`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1253: void Clear_SpecialRack_Config()`
  - `Core/Src/SRM_Parameter.c:1333: Clear_SpecialRack_Config();`
  - `Core/Src/com_tml.c:4324: Clear_SpecialRack_Config();`
  - `Core/Src/com_tml.c:4328: Clear_SpecialRack_Config();`
  - `Core/Inc/SRM_Parameter.h:51: extern void Clear_SpecialRack_Config();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Check_SpecialRack_Data

- **종류**: 함수
- **시그니처**: `INT08U Check_SpecialRack_Data(SpecialRackSet_t* nData) { if ((nData->Row_No == 0) && (nData->Bay_No == 0) && (nData->Lv_No == 0) && (nData->Goods_Type == 0))`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1271: INT08U Check_SpecialRack_Data(SpecialRackSet_t* nData)`
  - `Core/Inc/SRM_Parameter.h:52: extern INT08U Check_SpecialRack_Data(SpecialRackSet_t* nData);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Set_SpecialRack_Config

- **종류**: 함수
- **시그니처**: `void Set_SpecialRack_Config(SpecialRackSTR* pSetting)`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1288: void Set_SpecialRack_Config(SpecialRackSTR* pSetting)`
  - `Core/Src/com_tml.c:4238: Set_SpecialRack_Config(pCtrl);`
  - `Core/Inc/SRM_Parameter.h:53: extern void Set_SpecialRack_Config(SpecialRackSTR* pSetting);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### SpecialRack_Config_Init

- **종류**: 함수
- **시그니처**: `void SpecialRack_Config_Init()`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1322: void SpecialRack_Config_Init()`
  - `Core/Src/main.c:373: SpecialRack_Config_Init();`
  - `Core/Inc/SRM_Parameter.h:54: extern void SpecialRack_Config_Init();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### CalcCRC_Cell_Offset_All

- **종류**: 함수
- **시그니처**: `INT16U CalcCRC_Cell_Offset_All()`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1366: INT16U CalcCRC_Cell_Offset_All()`
  - `Core/Src/SRM_Parameter.c:1548: m_ExtSEnv0.CheckCRC_CellOffset = CalcCRC_Cell_Offset_All();`
  - `Core/Src/SRM_Parameter.c:1616: crc = CalcCRC_Cell_Offset_All();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### Make_Test_CellOffset_Data

- **종류**: 함수
- **시그니처**: `void Make_Test_CellOffset_Data()`
- **정의 위치**: `Core/Src/SRM_Parameter.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1392: void Make_Test_CellOffset_Data()`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

