# Vol.09 — #define (1)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

매크로 상수.

---

### DEBUG_MSG

- **종류**: `#define` 상수
- **값**: `1`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:148: #if DEBUG_MSG`
  - `Core/Src/SRM_Parameter.c:155: #if DEBUG_MSG`
  - `Core/Src/SRM_Parameter.c:179: #if DEBUG_MSG`
  - `Core/Src/SRM_Parameter.c:193: #if DEBUG_MSG`
  - `Core/Src/SRM_Parameter.c:201: #if DEBUG_MSG`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### DEBUG_FLASH_MSG

- **종류**: `#define` 상수
- **값**: `0`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/flash.c:78: #if DEBUG_FLASH_MSG`
  - `Core/Src/flash.c:99: #if DEBUG_FLASH_MSG`
  - `Core/Src/flash.c:123: #if DEBUG_FLASH_MSG`
  - `Core/Src/flash.c:135: #if DEBUG_FLASH_MSG`
  - `Core/Src/flash.c:159: #if DEBUG_FLASH_MSG`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### DEBUG_TML_MSG

- **종류**: `#define` 상수
- **값**: `0`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:6461: #if DEBUG_TML_MSG`
  - `Core/Src/uart.c:190: #if DEBUG_TML_MSG`
  - `Core/Src/uart.c:206: #if DEBUG_TML_MSG`
  - `Core/Inc/User_Define.h:115: #define DEBUG_TML_MSG				0		//`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### DEBUG_MCU_DEBUG_MSG

- **종류**: `#define` 상수
- **값**: `0`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Inc/User_Define.h:116: #define DEBUG_MCU_DEBUG_MSG			0		//`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### DEBUG_UPLINK_MSG

- **종류**: `#define` 상수
- **값**: `0`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_udp.c:225: #if DEBUG_UPLINK_MSG`
  - `Core/Src/com_udp.c:251: #if DEBUG_UPLINK_MSG`
  - `Core/Src/com_udp.c:257: #if DEBUG_UPLINK_MSG`
  - `Core/Src/com_udp.c:372: #if DEBUG_UPLINK_MSG`
  - `Core/Src/com_udp.c:385: #if DEBUG_UPLINK_MSG`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### DEBUG_DSP_UDP_MSG

- **종류**: `#define` 상수
- **값**: `0`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_udp.c:304: #if DEBUG_DSP_UDP_MSG`
  - `Core/Src/com_udp.c:327: #if DEBUG_DSP_UDP_MSG`
  - `Core/Src/com_udp.c:333: #if DEBUG_DSP_UDP_MSG`
  - `Core/Src/com_udp.c:426: #if DEBUG_DSP_UDP_MSG`
  - `Core/Src/com_udp.c:439: #if DEBUG_DSP_UDP_MSG`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### DEBUG_UPGRADE_MSG

- **종류**: `#define` 상수
- **값**: `0`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:1547: #if DEBUG_UPGRADE_MSG`
  - `Core/Src/FlashSST26.c:1581: #if DEBUG_UPGRADE_MSG`
  - `Core/Src/FlashSST26.c:1602: #if DEBUG_UPGRADE_MSG`
  - `Core/Src/FlashSST26.c:1610: #if DEBUG_UPGRADE_MSG`
  - `Core/Src/upgrade.c:90: #if DEBUG_UPGRADE_MSG`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### DEBUG_CAN_MSG

- **종류**: `#define` 상수
- **값**: `0`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/can.c:46: #if DEBUG_CAN_MSG`
  - `Core/Src/can.c:122: #if DEBUG_CAN_MSG`
  - `Core/Src/can.c:161: #if DEBUG_CAN_MSG`
  - `Core/Inc/User_Define.h:120: #define DEBUG_CAN_MSG				0		//`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### DEBUG_HMI_MSG

- **종류**: `#define` 상수
- **값**: `0`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_hmi.c:191: #if DEBUG_HMI_MSG`
  - `Core/Src/com_hmi.c:242: #if DEBUG_HMI_MSG`
  - `Core/Src/com_hmi.c:277: #if DEBUG_HMI_MSG`
  - `Core/Inc/User_Define.h:121: #define DEBUG_HMI_MSG				0		//`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### DEBUG_ENCODER_MSG

- **종류**: `#define` 상수
- **값**: `0`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/encoder.c:55: #if DEBUG_ENCODER_MSG`
  - `Core/Inc/User_Define.h:122: #define DEBUG_ENCODER_MSG			0		//`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### DEBUG_FPGA_MSG

- **종류**: `#define` 상수
- **값**: `0`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/fpga.c:777: #if DEBUG_FPGA_MSG`
  - `Core/Src/fpga.c:789: #if DEBUG_FPGA_MSG`
  - `Core/Inc/User_Define.h:123: #define DEBUG_FPGA_MSG				0		//`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### DEBUG_ETHERCAT_MSG

- **종류**: `#define` 상수
- **값**: `0`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_coe.c:1089: #if DEBUG_ETHERCAT_MSG`
  - `Core/Src/ecat_config.c:23: #if DEBUG_ETHERCAT_MSG`
  - `Core/Src/ecat_config.c:55: #if DEBUG_ETHERCAT_MSG`
  - `Core/Src/ecat_config.c:92: #if DEBUG_ETHERCAT_MSG`
  - `Core/Src/ecat_config.c:142: #if DEBUG_ETHERCAT_MSG`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### DEBUG_EXT_SFLASH_MSG

- **종류**: `#define` 상수
- **값**: `0`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/FlashSST26.c:466: #if DEBUG_EXT_SFLASH_MSG`
  - `Core/Src/FlashSST26.c:506: #if DEBUG_EXT_SFLASH_MSG`
  - `Core/Src/FlashSST26.c:546: #if DEBUG_EXT_SFLASH_MSG`
  - `Core/Src/FlashSST26.c:580: #if DEBUG_EXT_SFLASH_MSG`
  - `Core/Src/FlashSST26.c:690: #if DEBUG_EXT_SFLASH_MSG`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### DEBUG_FILE_MEM_MSG

- **종류**: `#define` 상수
- **값**: `0`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/usb_user.c:126: #if DEBUG_FILE_MEM_MSG`
  - `Core/Src/usb_user.c:143: #if DEBUG_FILE_MEM_MSG`
  - `Core/Src/usb_user.c:153: #if DEBUG_FILE_MEM_MSG`
  - `Core/Src/usb_user.c:183: #if DEBUG_FILE_MEM_MSG`
  - `Core/Src/usb_user.c:192: #if DEBUG_FILE_MEM_MSG`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### DEBUG_RTC

- **종류**: `#define` 상수
- **값**: `0`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/rtc.c:356: #if DEBUG_RTC`
  - `Core/Src/rtc.c:409: #if DEBUG_RTC`
  - `Core/Inc/User_Define.h:127: #define DEBUG_RTC					0		//`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### DEBUG_MODBUSTCP

- **종류**: `#define` 상수
- **값**: `0`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Inc/User_Define.h:128: #define DEBUG_MODBUSTCP				0		//`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### DEBUG_MODBUS_HMI

- **종류**: `#define` 상수
- **값**: `1`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/dev_SRM.c:172: #if DEBUG_MODBUS_HMI`
  - `Core/Src/dev_SRM.c:211: #if DEBUG_MODBUS_HMI`
  - `Core/Src/dev_SRM.c:5969: #if DEBUG_MODBUS_HMI`
  - `Core/Src/dev_SRM.c:6536: #if DEBUG_MODBUS_HMI`
  - `Core/Src/dev_SRM.c:6569: #if DEBUG_MODBUS_HMI`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### DEBUG_MODBUS_SEW

- **종류**: `#define` 상수
- **값**: `0`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/dev_SRM.c:279: #if DEBUG_MODBUS_SEW`
  - `Core/Src/dev_SRM.c:6396: #if DEBUG_MODBUS_SEW`
  - `Core/Src/dev_SRM.c:6418: #if DEBUG_MODBUS_SEW`
  - `Core/Src/dev_SRM.c:67963: #if DEBUG_MODBUS_SEW`
  - `Core/Src/dev_SRM.c:67996: #if DEBUG_MODBUS_SEW`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### DEBUG_ETHERCAT_EC_MODULE

- **종류**: `#define` 상수
- **값**: `0`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/ecat_combase.c:1136: #if DEBUG_ETHERCAT_EC_MODULE`
  - `Core/Src/ecat_combase.c:1189: #if DEBUG_ETHERCAT_EC_MODULE`
  - `Core/Src/ecat_main.c:2930: #if DEBUG_ETHERCAT_EC_MODULE`
  - `Core/Src/ecat_main.c:2937: #if DEBUG_ETHERCAT_EC_MODULE`
  - `Core/Src/ecat_main.c:2963: #if DEBUG_ETHERCAT_EC_MODULE`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### DEBUG_CELL_CONFIG

- **종류**: `#define` 상수
- **값**: `0`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:3572: #if DEBUG_CELL_CONFIG`
  - `Core/Src/com_tml.c:3779: #if DEBUG_CELL_CONFIG`
  - `Core/Inc/User_Define.h:132: #define DEBUG_CELL_CONFIG			0		//`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### DEBUG_CELL_OFFSET

- **종류**: `#define` 상수
- **값**: `0`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/SRM_Parameter.c:1400: #if DEBUG_CELL_OFFSET`
  - `Core/Src/SRM_Parameter.c:1433: #if DEBUG_CELL_OFFSET`
  - `Core/Src/SRM_Parameter.c:1443: #if DEBUG_CELL_OFFSET`
  - `Core/Src/SRM_Parameter.c:1449: #if DEBUG_CELL_OFFSET`
  - `Core/Src/SRM_Parameter.c:1491: #if DEBUG_CELL_OFFSET`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### DEBUG_BACKUPSRAM

- **종류**: `#define` 상수
- **값**: `0`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/rtc.c:454: #if DEBUG_BACKUPSRAM`
  - `Core/Src/rtc.c:471: #if DEBUG_BACKUPSRAM`
  - `Core/Src/rtc.c:507: #if DEBUG_BACKUPSRAM`
  - `Core/Src/rtc.c:544: #if DEBUG_BACKUPSRAM`
  - `Core/Inc/User_Define.h:134: #define DEBUG_BACKUPSRAM			0		//`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### DEBUG_DSP

- **종류**: `#define` 상수
- **값**: `0`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_dsp.c:35: #if DEBUG_DSP`
  - `Core/Src/com_dsp.c:36: printf("[DEBUG_DSP] Request DSP Status Slave :%d Seq:%d\r\n", SlaveNo, m_DSP_Com_Seq);`
  - `Core/Src/com_dsp.c:77: #if DEBUG_DSP`
  - `Core/Src/com_dsp.c:78: printf("[DEBUG_DSP] Request DSP Control Slave :%d\r\n", SlaveNo);`
  - `Core/Src/com_dsp.c:428: #if DEBUG_DSP`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### DEBUG_HT100

- **종류**: `#define` 상수
- **값**: `0`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Inc/User_Define.h:136: #define DEBUG_HT100					0		//`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### DEBUG_TCP_MSG

- **종류**: `#define` 상수
- **값**: `1`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_udp.c:483: #if DEBUG_TCP_MSG`
  - `Core/Src/com_udp.c:494: #if DEBUG_TCP_MSG`
  - `Core/Src/com_udp.c:527: #if DEBUG_TCP_MSG`
  - `Core/Src/com_udp.c:548: #if DEBUG_TCP_MSG`
  - `Core/Src/com_udp.c:576: #if DEBUG_TCP_MSG`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### FW_BLOCK_SIZE

- **종류**: `#define` 상수
- **값**: `1024`
- **정의 위치**: `Core/Inc/User_Define.h`
- **원문 주석**: ìì  FW Data Size
- **용도(읽는 법)**: 헤더 주석: ìì  FW Data Size
- **코드 참조 예**:
  - `Core/Src/upgrade.c:614: txPutwPort(port, FW_BLOCK_SIZE);`
  - `Core/Src/upgrade.c:660: else if (download_pos == (m_pgmEnv.Fw_Info.Buf_ExecFile.SuccessSize - FW_BLOCK_SIZE))`
  - `Core/Src/upgrade.c:673: if (download_size > FW_BLOCK_SIZE)  reason = 0x22;`
  - `Core/Inc/User_Define.h:359: #define FW_BLOCK_SIZE		 1024		// ìì  FW Data Size`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### FRAME_STX

- **종류**: `#define` 상수
- **값**: `0x16`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_Debug.c:33: if (rsComData == FRAME_STX)`
  - `Core/Src/com_dsp.c:61: rsPutmPort(port, FRAME_STX, 4);										// STX : 0x16 0x16 0x16 0x16`
  - `Core/Src/com_dsp.c:102: rsPutmPort(port, FRAME_STX, 4);										// STX : 0x16 0x16 0x16 0x16`
  - `Core/Src/com_hmi.c:98: if (rsComData == FRAME_STX)`
  - `Core/Src/com_tml.c:38: if (rsComData == FRAME_STX)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### FRAME_ETX

- **종류**: `#define` 상수
- **값**: `0xF5`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_Debug.c:88: if ((rsComData == FRAME_ETX) && (rsCRCValue == 0))`
  - `Core/Src/com_dsp.c:66: rsPutmPort(port, FRAME_ETX, 1);										// ETX : 0xF5`
  - `Core/Src/com_dsp.c:107: rsPutmPort(port, FRAME_ETX, 1);										// ETX : 0xF5`
  - `Core/Src/com_hmi.c:153: if ((rsComData == FRAME_ETX) && (rsCRCValue == 0))`
  - `Core/Src/com_tml.c:93: if ((rsComData == FRAME_ETX) && (rsCRCValue == 0))`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### FRAME_SYNC_SIZE

- **종류**: `#define` 상수
- **값**: `4`
- **정의 위치**: `Core/Inc/User_Define.h`
- **원문 주석**: #define FRAME_HEADER_SIZE               10
- **용도(읽는 법)**: 헤더 주석: #define FRAME_HEADER_SIZE               10
- **코드 참조 예**:
  - `Core/Src/com_Debug.c:35: if (++rsComLen >= FRAME_SYNC_SIZE)`
  - `Core/Src/com_hmi.c:100: if (++rsComLen >= FRAME_SYNC_SIZE)`
  - `Core/Src/com_tml.c:40: if (++rsComLen >= FRAME_SYNC_SIZE)`
  - `Core/Inc/User_Define.h:389: #define FRAME_SYNC_SIZE                  4`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### FRAME_HEADER_SIZE

- **종류**: `#define` 상수
- **값**: `11`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_Debug.c:26: static INT08U  rsHeader[FRAME_HEADER_SIZE];`
  - `Core/Src/com_Debug.c:50: if (++rsComLen >= FRAME_HEADER_SIZE)`
  - `Core/Src/com_Debug.c:52: memcpy((INT08U*)&rsHeader, (INT08U*)rsComBuffer, FRAME_HEADER_SIZE);`
  - `Core/Src/com_Debug.c:91: memcpy((INT08U*)&DebugCom.Header, (INT08U*)rsHeader, FRAME_HEADER_SIZE);`
  - `Core/Src/com_hmi.c:91: static INT08U  rsHeader[FRAME_HEADER_SIZE];`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### FRAME_CRC_SIZE

- **종류**: `#define` 상수
- **값**: `2`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_Debug.c:79: if (++rsComLen >= FRAME_CRC_SIZE)`
  - `Core/Src/com_hmi.c:144: if (++rsComLen >= FRAME_CRC_SIZE)`
  - `Core/Src/com_tml.c:84: if (++rsComLen >= FRAME_CRC_SIZE)`
  - `Core/Src/com_udp.c:200: if (crcInitStrCalc(0x0000, (INT08U*)&pCom->Header, (pCom->Header.Len - 1) + FRAME_HEADER_SIZE + FRAM`
  - `Core/Inc/User_Define.h:392: #define FRAME_CRC_SIZE                   2`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### FRAME_MAX_DATA_LEN

- **종류**: `#define` 상수
- **값**: `2048`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_Debug.c:27: static INT08U  rsComBuffer[FRAME_MAX_DATA_LEN];`
  - `Core/Src/com_Debug.c:57: if (rsLen > FRAME_MAX_DATA_LEN)  rsComMode = 0;`
  - `Core/Src/com_hmi.c:92: static INT08U  rsComBuffer[FRAME_MAX_DATA_LEN];`
  - `Core/Src/com_hmi.c:122: if (rsLen > FRAME_MAX_DATA_LEN)  rsComMode = 0;`
  - `Core/Src/com_tml.c:32: static INT08U  rsComBuffer[FRAME_MAX_DATA_LEN];`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### FRAME_DSP_DATA_LEN

- **종류**: `#define` 상수
- **값**: `128`
- **정의 위치**: `Core/Inc/User_Define.h`
- **원문 주석**: #define FRAME_MAX_DATA_LEN            5
- **용도(읽는 법)**: 헤더 주석: #define FRAME_MAX_DATA_LEN            5
- **코드 참조 예**:
  - `Core/Src/uart.c:281: if (++m_pgmEnv.txDSPComLen >= FRAME_DSP_DATA_LEN) m_pgmEnv.txDSPComLen = FRAME_DSP_DATA_LEN - 1;`
  - `Core/Inc/User_Define.h:394: #define FRAME_DSP_DATA_LEN            128`
  - `Core/Inc/User_Define.h:3130: INT08U       txDSPComBuffer[FRAME_DSP_DATA_LEN];`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### FRAME_ETH_MAX_DATA_LEN

- **종류**: `#define` 상수
- **값**: `1560`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/uart.c:261: if (++m_pgmEnv.txEthUdpComLen >= FRAME_ETH_MAX_DATA_LEN) m_pgmEnv.txEthUdpComLen = FRAME_MAX_DATA_LE`
  - `Core/Src/uart.c:271: if (++m_pgmEnv.txInvUdpComLen >= FRAME_ETH_MAX_DATA_LEN) m_pgmEnv.txInvUdpComLen = FRAME_MAX_DATA_LE`
  - `Core/Inc/User_Define.h:398: #define FRAME_ETH_MAX_DATA_LEN        1560`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### FRAME_HHT_HEADER_SIZE

- **종류**: `#define` 상수
- **값**: `6`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Inc/User_Define.h:400: #define FRAME_HHT_HEADER_SIZE           6`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### MAX_UART_PORT

- **종류**: `#define` 상수
- **값**: `6`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_hmi.c:25: extern UartQSTR		m_TxUartQ[MAX_UART_PORT];`
  - `Core/Src/main.c:584: memset((INT08U*)&m_TxUartQ[0], 0x00, sizeof(UartQSTR) * MAX_UART_PORT);`
  - `Core/Src/main.c:585: memset((INT08U*)&m_RxUartQ[0], 0x00, sizeof(UartQSTR) * MAX_UART_PORT);`
  - `Core/Src/uart.c:124: UartQSTR	m_RxUartQ[MAX_UART_PORT];`
  - `Core/Src/uart.c:125: UartQSTR	m_TxUartQ[MAX_UART_PORT];`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### UART_1

- **종류**: `#define` 상수
- **값**: `0`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/uart.c:244: case UART_1:`
  - `Core/Inc/User_Define.h:404: #define UART_1                          0`
  - `Core/Inc/User_Define.h:418: #define rsEXTPort                       UART_1`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### UART_2

- **종류**: `#define` 상수
- **값**: `1`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/uart.c:245: case UART_2:`
  - `Core/Inc/User_Define.h:405: #define UART_2                          1`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### UART_3

- **종류**: `#define` 상수
- **값**: `2`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/uart.c:246: case UART_3:`
  - `Core/Inc/User_Define.h:406: #define UART_3                          2`
  - `Core/Inc/User_Define.h:414: #define rsTMLPort                       UART_3`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### UART_4

- **종류**: `#define` 상수
- **값**: `3`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/uart.c:247: case UART_4:`
  - `Core/Inc/User_Define.h:407: #define UART_4                          3`
  - `Core/Inc/User_Define.h:415: #define rsMCU_DEBUG_Port                UART_4`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### UART_5

- **종류**: `#define` 상수
- **값**: `4`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/uart.c:248: case UART_5:`
  - `Core/Inc/User_Define.h:408: #define UART_5                          4`
  - `Core/Inc/User_Define.h:416: #define rsOPTPort                       UART_5		// RS422`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### UART_6

- **종류**: `#define` 상수
- **값**: `5`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/uart.c:249: case UART_6:`
  - `Core/Inc/User_Define.h:409: #define UART_6                          5`
  - `Core/Inc/User_Define.h:417: #define rsRS485_422_Port                UART_6		// RS485`
  - `Core/Inc/User_Define.h:427: #define rsDSP_RS485_Port                UART_6`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsUSBPort

- **종류**: `#define` 상수
- **값**: `11`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:198: case rsUSBPort:`
  - `Core/Src/com_tml.c:279: case rsUSBPort:`
  - `Core/Src/uart.c:254: case rsUSBPort:`
  - `Core/Inc/User_Define.h:419: #define rsUSBPort                       11`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsETHUDPPort_Uplink

- **종류**: `#define` 상수
- **값**: `12`
- **정의 위치**: `Core/Inc/User_Define.h`
- **원문 주석**: Uplink
- **용도(읽는 법)**: 헤더 주석: Uplink
- **코드 참조 예**:
  - `Core/Src/alarm.c:1582: (m_pgmEnv.PortLinkSt[rsETHUDPPort_Uplink]) ? \`
  - `Core/Src/com_tml.c:202: case rsETHUDPPort_Uplink:`
  - `Core/Src/com_tml.c:285: case rsETHUDPPort_Uplink:`
  - `Core/Src/com_tml.c:436: case rsETHUDPPort_Uplink:`
  - `Core/Src/com_udp.c:267: rxDataAnalysis(rsETHUDPPort_Uplink, (TMLComSTR*)&pCom.Header);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsETHTCPPort

- **종류**: `#define` 상수
- **값**: `13`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:205: case rsETHTCPPort:`
  - `Core/Src/com_tml.c:291: case rsETHTCPPort:`
  - `Core/Src/uart.c:264: case rsETHTCPPort:`
  - `Core/Inc/User_Define.h:421: #define rsETHTCPPort                    13`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsETHUDPPort_DwLink

- **종류**: `#define` 상수
- **값**: `14`
- **정의 위치**: `Core/Inc/User_Define.h`
- **원문 주석**: Downlink
- **용도(읽는 법)**: 헤더 주석: Downlink
- **코드 참조 예**:
  - `Core/Src/alarm.c:1585: (m_pgmEnv.PortLinkSt[rsETHUDPPort_DwLink]) ? \`
  - `Core/Src/com_tml.c:208: case rsETHUDPPort_DwLink:`
  - `Core/Src/com_tml.c:296: case rsETHUDPPort_DwLink:`
  - `Core/Src/com_tml.c:443: case rsETHUDPPort_DwLink:`
  - `Core/Src/dev_SRM.c:6907: m_pgmEnv.PortLinkTimer[rsETHUDPPort_DwLink] = m_pgmEnv.timer1ms;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsINVTCPPort

- **종류**: `#define` 상수
- **값**: `15`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:212: case rsINVTCPPort:`
  - `Core/Src/com_tml.c:302: case rsINVTCPPort:`
  - `Core/Src/uart.c:274: case rsINVTCPPort:`
  - `Core/Inc/User_Define.h:423: #define rsINVTCPPort                    15`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsDSP_UDP_Port

- **종류**: `#define` 상수
- **값**: `16`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_dsp.c:433: s_ComPort = rsDSP_UDP_Port;`
  - `Core/Src/com_tml.c:216: case rsDSP_UDP_Port:`
  - `Core/Src/com_tml.c:305: case rsDSP_UDP_Port:`
  - `Core/Src/com_udp.c:336: rxDataAnalysis(rsDSP_UDP_Port, (TMLComSTR*)&pCom.Header);`
  - `Core/Src/uart.c:279: case rsDSP_UDP_Port:`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsHMI_Port

- **종류**: `#define` 상수
- **값**: `17`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:1594: (m_pgmEnv.PortLinkSt[rsHMI_Port]) ? \`
  - `Core/Src/com_tml.c:2043: if (m_pgmEnv.PortLinkSt[rsHMI_Port])`
  - `Core/Src/com_tml.c:2062: if (m_pgmEnv.PortLinkSt[rsHMI_Port])`
  - `Core/Src/dev_SRM.c:64179: if (m_pgmEnv.PortLinkSt[rsHMI_Port] == 1)`
  - `Core/Src/dev_SRM.c:65806: m_pgmEnv.PortLinkSt[rsHMI_Port] = 0;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsHMI_UP_Port

- **종류**: `#define` 상수
- **값**: `18`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:1597: (m_pgmEnv.PortLinkSt[rsHMI_UP_Port]) ? \`
  - `Core/Src/com_tml.c:2036: if (m_pgmEnv.PortLinkSt[rsHMI_UP_Port])`
  - `Core/Src/com_tml.c:2055: if (m_pgmEnv.PortLinkSt[rsHMI_UP_Port])`
  - `Core/Src/dev_SRM.c:64172: if (m_pgmEnv.PortLinkSt[rsHMI_UP_Port] == 1)`
  - `Core/Src/dev_SRM.c:65807: m_pgmEnv.PortLinkSt[rsHMI_UP_Port] = 0;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### COM_ID_GCP

- **종류**: `#define` 상수
- **값**: `0x00`
- **정의 위치**: `Core/Inc/User_Define.h`
- **원문 주석**: ì§ìë°
- **용도(읽는 법)**: 헤더 주석: ì§ìë°
- **코드 참조 예**:
  - `Core/Src/com_tml.c:2830: if (pCom->Header.SrcID.Type == COM_ID_GCP)`
  - `Core/Src/com_tml.c:3174: if (pCom->Header.SrcID.Type == COM_ID_GCP)`
  - `Core/Src/com_tml.c:6258: if (pCom->Header.SrcID.Type == COM_ID_GCP)`
  - `Core/Src/fpgatcpipstack.c:642: if (pCom->Header.SrcID.Type == COM_ID_GCP)`
  - `Core/Inc/User_Define.h:434: #define COM_ID_GCP   0x00		// ì§ìë°`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### COM_ID_WCS

- **종류**: `#define` 상수
- **값**: `0x01`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Inc/User_Define.h:435: #define COM_ID_WCS          0x01`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### COM_ID_GUI

- **종류**: `#define` 상수
- **값**: `0x02`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Inc/User_Define.h:436: #define COM_ID_GUI          0x02`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### COM_ID_HMI

- **종류**: `#define` 상수
- **값**: `0x03`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Inc/User_Define.h:437: #define COM_ID_HMI          0x03`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### COM_ID_SHUTTLE

- **종류**: `#define` 상수
- **값**: `0x10`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Inc/User_Define.h:438: #define COM_ID_SHUTTLE      0x10`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### COM_ID_LIFTER_OUT

- **종류**: `#define` 상수
- **값**: `0x20`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Inc/User_Define.h:439: #define COM_ID_LIFTER_OUT   0x20`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### COM_ID_LIFTER_IN

- **종류**: `#define` 상수
- **값**: `0x21`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Inc/User_Define.h:440: #define COM_ID_LIFTER_IN    0x21`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### COM_ID_MINILODER

- **종류**: `#define` 상수
- **값**: `0x30`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Inc/User_Define.h:441: #define COM_ID_MINILODER    0x30`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### COM_ID_RTV

- **종류**: `#define` 상수
- **값**: `0x40`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Inc/User_Define.h:442: #define COM_ID_RTV          0x40`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### COM_ID_EMS

- **종류**: `#define` 상수
- **값**: `0x50`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Inc/User_Define.h:443: #define COM_ID_EMS          0x50`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### COM_ID_SRM

- **종류**: `#define` 상수
- **값**: `0x60`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:6245: if (pCom->Header.DstID.Type != COM_ID_SRM)	return;`
  - `Core/Src/main.c:615: m_pgmEnv.DeviceType = COM_ID_SRM;`
  - `Core/Inc/User_Define.h:444: #define COM_ID_SRM          0x60`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### COM_ID_WIFI_CTRL

- **종류**: `#define` 상수
- **값**: `0x80`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Inc/User_Define.h:445: #define COM_ID_WIFI_CTRL    0x80`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### COM_ID_WIFI_AP

- **종류**: `#define` 상수
- **값**: `0x90`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Inc/User_Define.h:446: #define COM_ID_WIFI_AP      0x90`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### COM_ID_WIFI_STA

- **종류**: `#define` 상수
- **값**: `0xA0`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Inc/User_Define.h:447: #define COM_ID_WIFI_STA     0xA0`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### COM_ID_DSP

- **종류**: `#define` 상수
- **값**: `0xE4`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_dsp.c:52: nComData.Header.DstID.Type = COM_ID_DSP;`
  - `Core/Src/com_dsp.c:94: nComData.Header.DstID.Type = COM_ID_DSP;`
  - `Core/Src/com_tml.c:5951: if (pCom->Header.SrcID.Type == COM_ID_DSP)`
  - `Core/Src/com_tml.c:5971: if (pCom->Header.SrcID.Type == COM_ID_DSP)`
  - `Core/Inc/User_Define.h:449: #define COM_ID_DSP			0xE4`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### COM_ID_HT100

- **종류**: `#define` 상수
- **값**: `0xE6`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:6243: if (pCom->Header.SrcID.Type != COM_ID_HT100)`
  - `Core/Inc/User_Define.h:451: #define COM_ID_HT100        0xE6`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### MAX_ALARM_LOG_COUNT

- **종류**: `#define` 상수
- **값**: `50`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/alarm.c:368: if (m_BKSram.AlarmLog.Inx >= MAX_ALARM_LOG_COUNT)`
  - `Core/Src/alarm.c:465: if (++m_BKSram.AlarmLog.Inx >= MAX_ALARM_LOG_COUNT)`
  - `Core/Src/com_tml.c:5793: if (req_inx < MAX_ALARM_LOG_COUNT)`
  - `Core/Src/com_tml.c:5797: tot_cnt = MAX_ALARM_LOG_COUNT;`
  - `Core/Src/com_tml.c:5809: start = (m_BKSram.AlarmLog.Inx + req_inx) % MAX_ALARM_LOG_COUNT;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### MAX_TASK_COUNT

- **종류**: `#define` 상수
- **값**: `20`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: Task: TML 0x40 다중작업. 0x41과 분기 다름.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:2249: for (i = 0; i < MAX_TASK_COUNT; i++)`
  - `Core/Src/com_tml.c:2305: for (i = 0; i < MAX_TASK_COUNT; i++)`
  - `Core/Src/com_tml.c:3097: for (i = 0; i < MAX_TASK_COUNT; i++)`
  - `Core/Inc/User_Define.h:490: #define MAX_TASK_COUNT 20`
  - `Core/Inc/User_Define.h:978: TaskCommand			TaskWork[MAX_TASK_COUNT];`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### WORK_STATUS_NONE

- **종류**: `#define` 상수
- **값**: `0`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/dev_SRM.c:46686: Store_Fork_WorkStatus(0, 0, ORDER_CODE_NONE, WORK_STATUS_NONE, CMD_MOVE_STEP_NONE);`
  - `Core/Src/dev_SRM.c:46700: Store_Fork_WorkStatus(0, 0, ORDER_CODE_NONE, WORK_STATUS_NONE, CMD_FORK_STEP_NONE);`
  - `Core/Src/main.c:490: if ((m_BKSram.WorkInfo.WorkSt[i].OrderStatus_Fork == WORK_STATUS_NONE)`
  - `Core/Src/main.c:510: if ((m_BKSram.WorkInfo.WorkSt[i].OrderStatus_Move == WORK_STATUS_NONE)`
  - `Core/Inc/User_Define.h:1103: #define WORK_STATUS_NONE			0`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### WORK_STATUS_IMPOSSIBLE

- **종류**: `#define` 상수
- **값**: `1`
- **정의 위치**: `Core/Inc/User_Define.h`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Inc/User_Define.h:1104: #define WORK_STATUS_IMPOSSIBLE		1`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

