# Vol.08 — 기타 함수 (14)

> 생성일: 2026-06-16 | MX SRM App Ver 4.4 | 소스 미수정

com_tml 등.

---

### Set_RS485_422_Mode

- **종류**: 함수
- **시그니처**: `void Set_RS485_422_Mode(INT08U nMode) { if (nMode == 0)`
- **정의 위치**: `Core/Src/com_hmi.c`
- **인근 주석**: RS485
- **용도(읽는 법)**: 헤더 주석: RS485
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:245: Set_RS485_422_Mode(0);`
  - `Core/Src/Mcu_Test.c:285: Set_RS485_422_Mode(0);`
  - `Core/Src/Mcu_Test.c:325: Set_RS485_422_Mode(1);`
  - `Core/Src/Mcu_Test.c:365: Set_RS485_422_Mode(1);`
  - `Core/Src/com_hmi.c:303: void Set_RS485_422_Mode(INT08U nMode)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### HMI_Com_Manager

- **종류**: 함수
- **시그니처**: `void HMI_Com_Manager(void)`
- **정의 위치**: `Core/Src/com_hmi.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/Mcu_Test.c:939: HMI_Com_Manager();`
  - `Core/Src/com_hmi.c:324: void HMI_Com_Manager(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsStartCheck

- **종류**: 함수
- **시그니처**: `static void rsStartCheck(void) { if (rsComData == FRAME_STX) { if (++rsComLen >= FRAME_SYNC_SIZE)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_Debug.c:31: static void rsStartCheck(void)`
  - `Core/Src/com_Debug.c:105: case 0: rsStartCheck();          break;`
  - `Core/Src/com_hmi.c:96: static void rsStartCheck(void)`
  - `Core/Src/com_hmi.c:170: case 0: rsStartCheck();          break;`
  - `Core/Src/com_tml.c:36: static void rsStartCheck(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsHeaderCheck

- **종류**: 함수
- **시그니처**: `static void rsHeaderCheck(void)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_Debug.c:45: static void rsHeaderCheck(void)`
  - `Core/Src/com_Debug.c:106: case 1: rsHeaderCheck();         break;`
  - `Core/Src/com_hmi.c:110: static void rsHeaderCheck(void)`
  - `Core/Src/com_hmi.c:171: case 1: rsHeaderCheck();         break;`
  - `Core/Src/com_tml.c:50: static void rsHeaderCheck(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsDataCheck

- **종류**: 함수
- **시그니처**: `static void rsDataCheck(void)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_Debug.c:63: static void rsDataCheck(void)`
  - `Core/Src/com_Debug.c:107: case 2: rsDataCheck();           break;`
  - `Core/Src/com_hmi.c:128: static void rsDataCheck(void)`
  - `Core/Src/com_hmi.c:172: case 2: rsDataCheck();           break;`
  - `Core/Src/com_tml.c:68: static void rsDataCheck(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsCrcCheck

- **종류**: 함수
- **시그니처**: `static void rsCrcCheck(void)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_Debug.c:75: static void rsCrcCheck(void)`
  - `Core/Src/com_Debug.c:108: case 3: rsCrcCheck();            break;`
  - `Core/Src/com_hmi.c:140: static void rsCrcCheck(void)`
  - `Core/Src/com_hmi.c:173: case 3: rsCrcCheck();            break;`
  - `Core/Src/com_tml.c:80: static void rsCrcCheck(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsEndCheck

- **종류**: 함수
- **시그니처**: `static void rsEndCheck(void) { if ((rsComData == FRAME_ETX) && (rsCRCValue == 0))`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_Debug.c:86: static void rsEndCheck(void)`
  - `Core/Src/com_Debug.c:109: case 4: rsEndCheck();            break;`
  - `Core/Src/com_hmi.c:151: static void rsEndCheck(void)`
  - `Core/Src/com_hmi.c:174: case 4: rsEndCheck();            break;`
  - `Core/Src/com_tml.c:91: static void rsEndCheck(void)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsComHandler

- **종류**: 함수
- **시그니처**: `void rsComHandler(INT08U rsData)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:104: void rsComHandler(INT08U rsData)`
  - `Core/Src/stm32f7xx_it.c:327: rsComHandler(nTmp);`
  - `Core/Inc/com_tml.h:126: extern void rsComHandler(INT08U rsData);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsComFrame_Parsing

- **종류**: 함수
- **시그니처**: `void rsComFrame_Parsing(const INT08U* data, INT16U len)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:120: void rsComFrame_Parsing(const INT08U* data, INT16U len)`
  - `Core/Src/com_tml.c:155: rsComFrame_Parsing(&m_RxUartQ[rsTMLPort].Data[old_pos], rcvdLen);`
  - `Core/Src/com_tml.c:159: rsComFrame_Parsing(&m_RxUartQ[rsTMLPort].Data[old_pos], ARRAY_LEN(m_RxUartQ[rsTMLPort].Data) - old_p`
  - `Core/Src/com_tml.c:163: rsComFrame_Parsing(&m_RxUartQ[rsTMLPort].Data[0], pos);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rsComHandlerDMA

- **종류**: 함수
- **시그니처**: `void rsComHandlerDMA()`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:142: void rsComHandlerDMA()`
  - `Core/Src/uart.c:134: rsComHandlerDMA();`
  - `Core/Src/uart.c:143: rsComHandlerDMA();`
  - `Core/Src/uart.c:154: rsComHandlerDMA();`
  - `Core/Inc/com_tml.h:127: extern void rsComHandlerDMA();`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### clearTxBuf

- **종류**: 함수
- **시그니처**: `void clearTxBuf(INT08U port) { switch (port)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_dsp.c:60: clearTxBuf(port);`
  - `Core/Src/com_dsp.c:101: clearTxBuf(port);`
  - `Core/Src/com_tml.c:182: void clearTxBuf(INT08U port)`
  - `Core/Src/com_tml.c:378: clearTxBuf(port);`
  - `Core/Src/com_tml.c:530: clearTxBuf(port);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### enableTXEN

- **종류**: 함수
- **시그니처**: `void enableTXEN(INT08U port) { if (port == rsRS485_422_Port)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:222: void enableTXEN(INT08U port)`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### sendTxBuf

- **종류**: 함수
- **시그니처**: `void sendTxBuf(INT08U port) { switch (port)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_dsp.c:67: sendTxBuf(port);`
  - `Core/Src/com_dsp.c:108: sendTxBuf(port);`
  - `Core/Src/com_tml.c:249: void sendTxBuf(INT08U port)`
  - `Core/Src/com_tml.c:385: sendTxBuf(port);`
  - `Core/Src/com_tml.c:536: sendTxBuf(port);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### txPutHeader

- **종류**: 함수
- **시그니처**: `void txPutHeader(INT08U port, ComHeaderSTR* pRxHeader, INT08U Cmd, INT08U Cmd2, INT16U Len)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:324: void txPutHeader(INT08U port, ComHeaderSTR* pRxHeader, INT08U Cmd, INT08U Cmd2, INT16U Len)`
  - `Core/Src/com_tml.c:381: txPutHeader(port, (ComHeaderSTR*)&pCom->Header, pCom->Header.Cmd | 0x80, pCom->Header.Cmd2, sizeof(D`
  - `Core/Src/com_tml.c:533: txPutHeader(port, (ComHeaderSTR*)&pCom->Header, pCom->Header.Cmd | 0x80, pCom->Header.Cmd2, 0);`
  - `Core/Src/com_tml.c:565: txPutHeader(port, (ComHeaderSTR*)&pCom->Header, pCom->Header.Cmd | 0x80, pCom->Header.Cmd2, sizeof(S`
  - `Core/Src/com_tml.c:616: txPutHeader(port, (ComHeaderSTR*)&pCom->Header, pCom->Header.Cmd | 0x80, pCom->Header.Cmd2, 2);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### txPutHeader_1

- **종류**: 함수
- **시그니처**: `void txPutHeader_1(INT08U port, ComHeaderSTR* pRxHeader, INT16U Len)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_dsp.c:63: txPutHeader_1(port, (ComHeaderSTR*)&nComData.Header, 20);`
  - `Core/Src/com_dsp.c:104: txPutHeader_1(port, (ComHeaderSTR*)&nComData.Header, sizeof(DspDataSTR));`
  - `Core/Src/com_tml.c:349: void txPutHeader_1(INT08U port, ComHeaderSTR* pRxHeader, INT16U Len)`
  - `Core/Inc/com_tml.h:130: extern void txPutHeader_1(INT08U port, ComHeaderSTR* pRxHeader, INT16U Len);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxDeviceStatusReq

- **종류**: 함수
- **시그니처**: `void rxDeviceStatusReq(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:369: void rxDeviceStatusReq(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6344: case TY_DEVICE_STATUS: rxDeviceStatusReq(port, (TMLComPTR)pCom);      break;`
  - `Core/Src/com_tml.c:6429: case TY_DEVICE_STATUS: rxDeviceStatusReq(port, (TMLComPTR)pCom);      break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxDeviceControlReq

- **종류**: 함수
- **시그니처**: `void rxDeviceControlReq(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:459: void rxDeviceControlReq(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6345: case TY_DEVICE_CONTROL: rxDeviceControlReq(port, (TMLComPTR)pCom);      break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxMachine_Type_Req

- **종류**: 함수
- **시그니처**: `void rxMachine_Type_Req(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:555: void rxMachine_Type_Req(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6360: case TY_MACHINE_SPEC_STATUS: rxMachine_Type_Req(port, (TMLComPTR)pCom);      break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxMachine_Type_Set

- **종류**: 함수
- **시그니처**: `void rxMachine_Type_Set(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:582: void rxMachine_Type_Set(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6361: case TY_MACHINE_SPEC_CONTROL: rxMachine_Type_Set(port, (TMLComPTR)pCom);      break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxTestStatusReq

- **종류**: 함수
- **시그니처**: `void rxTestStatusReq(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:634: void rxTestStatusReq(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6346: case TY_TEST_STATUS: rxTestStatusReq(port, (TMLComPTR)pCom);      break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxTestControlReq

- **종류**: 함수
- **시그니처**: `void rxTestControlReq(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:657: void rxTestControlReq(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6347: case TY_TEST_CONTROL: rxTestControlReq(port, (TMLComPTR)pCom);      break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxFuncControlReq

- **종류**: 함수
- **시그니처**: `void rxFuncControlReq(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:701: void rxFuncControlReq(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6355: case TY_FUNC_CONTROL: rxFuncControlReq(port, (TMLComPTR)pCom);      break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxGraphRequest

- **종류**: 함수
- **시그니처**: `void rxGraphRequest(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:1763: void rxGraphRequest(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6348: case TY_GRAPH_REQUEST: rxGraphRequest(port, (TMLComPTR)pCom);      break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### rxManualOutCTR_Request

- **종류**: 함수
- **시그니처**: `void rxManualOutCTR_Request(INT08U port, TMLComPTR pCom)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:1862: void rxManualOutCTR_Request(INT08U port, TMLComPTR pCom)`
  - `Core/Src/com_tml.c:6349: case TY_MANUNAL_OUTPUT_CONTROL: rxManualOutCTR_Request(port, (TMLComPTR)pCom);      break;`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

### CP_Time_Synchronization

- **종류**: 함수
- **시그니처**: `void CP_Time_Synchronization(INT32U nSysTime_Utc)`
- **정의 위치**: `Core/Src/com_tml.c`
- **용도(읽는 법)**: grep(심볼, =1, case)으로 역할 확인. 이름만 믿지 말 것.
- **코드 참조 예**:
  - `Core/Src/com_tml.c:2008: void CP_Time_Synchronization(INT32U nSysTime_Utc)`
  - `Core/Src/com_tml.c:2097: CP_Time_Synchronization(pCmd->SysTime);`

**추적 팁**: F12 실패 시 심볼 → `= 1` → `case` → `Save_*` 순 grep.

---

